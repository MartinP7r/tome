{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Main-window allowlist: core defaults + the read-only commands plus narrow opener / clipboard-manager grants for Phase 26 plan 26-03 (D-07). No tauri-plugin-shell/-fs (T-25-04-EoP mitigation — keep the IPC surface auditable). The watcher remains Rust-side (OQ-3 resolution), so no fs:default widening.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "opener:default",
    "clipboard-manager:allow-write-text"
  ]
}
