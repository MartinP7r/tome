{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Minimal allowlist for the main window: core defaults plus the get_status command only. No tauri-plugin-shell/-fs is added this phase (T-25-04-EoP mitigation — keep the IPC surface auditable).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
