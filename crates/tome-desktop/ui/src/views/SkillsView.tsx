// SkillsView — placeholder shell.
//
// This stub exists so plan 26-02 Task 1 can compile against the shell wiring
// in `App.tsx`. Task 2 of 26-02 replaces this file with the real virtualised
// list + fuzzy search + sort/group toolbar (VIEW-02 / NF-01).

export function SkillsView() {
  return <p>Skills view ships in 26-02 Task 2</p>;
}
