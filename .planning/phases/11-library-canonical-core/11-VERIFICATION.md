---
phase: 11-library-canonical-core
verified: 2026-05-03T00:00:00Z
status: passed
score: 5/5 success criteria verified (5/5 requirements satisfied)
---

# Phase 11: library-canonical-core Verification Report

**Phase Goal:** The library becomes the single source of truth — managed and local skills are stored uniformly as real directories. Source removal no longer deletes content. Existing symlink-based libraries migrate cleanly on first sync.

**Verified:** 2026-05-03
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Success Criteria)

| #   | Truth                                                                                                                                                                                                                          | Status      | Evidence                                                                                                                                                                                                                                                                                                                                                              |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SC1 | After `tome sync` completes, the library contains zero symlinks for managed skills (`find <library_dir> -type l \| wc -l == 0`).                                                                                              | VERIFIED    | `library.rs::consolidate_managed` rewritten to use `copy_dir_recursive` (lines 187, 203). Anchor test `library::tests::consolidate_post_sync_no_symlinks_in_library` (passes) walks library with `walkdir` and asserts 0 symlinks. Top-of-file `unix_fs` import deleted; `create_symlink` helper deleted. CLI integration test `migrate_library_converts_managed_symlinks_to_real_dirs` confirms managed symlinks become real dirs end-to-end. |
| SC2 | Removing a `[directories.*]` entry from `tome.toml` and running `tome sync` preserves library content; integration test removes a directory entry and asserts every skill remains with content_hash unchanged.                | VERIFIED    | `cleanup.rs::cleanup_library` partitions stale entries via `config.directories().contains_key(source)` (line 93); Case 1 sets `entry.source_name = None` and preserves library (lines 116–120). Integration test `sync_preserves_library_when_source_removed_from_config` (passes) edits tome.toml, runs sync, asserts library dir + manifest content_hash unchanged. `tome remove` execute path also explicitly transitions (`remove.rs:362-364`).            |
| SC3 | `Manifest` deserialization accepts both old and new shapes via `#[serde(default)]`; entries with `source_name: None` classify as Unowned.                                                                                     | VERIFIED    | `manifest.rs:113-114`: `#[serde(default, skip_serializing_if = "Option::is_none")] pub source_name: Option<DirectoryName>`. `lockfile.rs:40-41` mirrors. Tests `deserialize_old_shape_with_source_name_string`, `deserialize_new_shape_with_null_source_name`, `deserialize_new_shape_missing_source_name`, `new_unowned_constructor_sets_source_name_none` all pass. Lockfile mirror tests also pass.                                          |
| SC4 | On a v0.9-shape library, `tome sync` refuses with Conflict/Why/Suggestion error pointing at `tome migrate-library`. The command converts symlinks to real-dir copies, exits non-zero on any failure with SAFE-01 grouped summary, idempotent on re-run, broken-source preserved per D-04. **No `migration_v010_acknowledged` flag persisted.** | VERIFIED    | `lib.rs:1031-1039` runs `migration_v010::detect_v09_shape` and bails with "library is in v0.9 shape (one or more managed skills are stored as symlinks)" + "Run `tome migrate-library`". `migration_v010.rs::detect_v09_shape` enforces D-03 (managed && symlink && in manifest). `execute` preserves broken symlinks (D-04, lines 251-263). SAFE-01 banner in `render_result` (line 425+). Idempotency via `execute_idempotent_on_re_run` test. **`rg migration_v010_acknowledged` returned 0 matches across src+tests.** Integration tests `sync_refuses_on_v09_shape_library_with_hint`, `migrate_library_converts_managed_symlinks_to_real_dirs`, `sync_succeeds_after_migrate_library` all pass. |
| SC5 | Cleanup phase no longer auto-deletes orphan library entries; orphans are left for Phase 14 surfacing.                                                                                                                          | VERIFIED    | `cleanup.rs::cleanup_library` Case 1 path (lines 102-121) sets `source_name = None` and increments `transitioned_to_unowned` instead of deleting. Tests `cleanup_transitions_orphaned_to_unowned_when_source_removed_from_config` and `cleanup_already_unowned_entry_is_preserved_and_not_counted` confirm preservation. The new `transitioned_to_unowned` counter on `CleanupResult` provides the data Phase 14 will surface.                |

**Score:** 5/5 success criteria verified.

### Required Artifacts

| Artifact                                  | Expected                                                                                                | Status     | Details                                                                                                                                                                                                                                                                |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `crates/tome/src/manifest.rs`             | `Option<DirectoryName>`, `new_unowned`, `skills_get_mut`                                                | VERIFIED   | Line 114: `pub source_name: Option<DirectoryName>`; line 150: `pub fn new_unowned`; line 95: `pub(crate) fn skills_get_mut`. Module-level + field doc comments updated for LIB-02 "update channel" semantics.                                                          |
| `crates/tome/src/lockfile.rs`             | `LockEntry` with `Option<DirectoryName>`                                                                | VERIFIED   | Line 41: `pub source_name: Option<DirectoryName>` with `#[serde(default, skip_serializing_if = "Option::is_none")]`. `resolved_paths_from_lockfile_cache` updated to `if let Some(source) = &entry.source_name`.                                                       |
| `crates/tome/src/library.rs`              | `consolidate_managed` rewritten to use copy semantics                                                   | VERIFIED   | `fn consolidate_managed` at line 144; `copy_dir_recursive(&skill.path, dest)` invoked at lines 187, 203. Top-of-file `unix_fs` import removed; `create_symlink` helper deleted. Module-level doc comment updated to LIB-01/LIB-02 model.                                |
| `crates/tome/src/cleanup.rs`              | `cleanup_library` partitions by Case 1/Case 2; `transitioned_to_unowned` counter                       | VERIFIED   | Signature now takes `config: &crate::config::Config`. Lines 81-99 implement Case 1/Case 2 partition; lines 102-121 transition Case 1; existing delete branch covers Case 2. `CleanupResult.transitioned_to_unowned: usize` (line 22) consumed by lib.rs:497.            |
| `crates/tome/src/remove.rs`               | `tome remove` transitions owned entries before config removal; FailureKind reduced to 2 variants       | VERIFIED   | `execute` (line 305) transitions skills via `manifest.skills_get_mut(skill_name)` → `entry.source_name = None` (lines 362-364). `RemoveResult.library_entries_transitioned_to_unowned: usize` (line 156). `FailureKind::ALL` reduced to 2 (DistributionSymlink, GitCache). |
| `crates/tome/src/migration_v010.rs`       | New module with detection, plan/render/execute, SAFE-01 aggregation, ≥200 lines                        | VERIFIED   | 724 lines. Module doc contains "transitional", "v0.11+", "D-01". Functions: `pub(crate) fn run_migrate_library` (line 469), `detect_v09_shape` (line 182), `plan` (line 146), `execute` (line 243), `render_plan` (line 195). `MigrationFailureKind::ALL` len=2 with compile-time assert. `copy_dir_recursive_resolving` uses `follow_links(true)`.        |
| `crates/tome/src/cli.rs`                  | `MigrateLibrary { dry_run: bool }` variant with after_help                                              | VERIFIED   | Line 151: `MigrateLibrary` variant present; help output mentions "one-shot" and "--dry-run".                                                                                                                                                                          |
| `crates/tome/src/lib.rs`                  | Dispatch + sync v0.9-shape check + `pub use manifest::hash_directory`                                  | VERIFIED   | `pub(crate) mod migration_v010` at line 42; `pub use manifest::hash_directory` at line 74; `Command::MigrateLibrary` dispatch at line 600 (calls `run_migrate_library` and exits 1 on partial); refuse-with-hint check at line 1031 with the documented error message. |
| `crates/tome/tests/cli.rs`                | 5 integration tests + `build_v09_fixture`                                                              | VERIFIED   | `build_v09_fixture` at line 5640; 5 test functions present at lines 5758, 5836, 5887, 5930, 6002. `tome::hash_directory` re-export used (no parallel SHA-256 helper). All 5 tests pass.                                                                                |

### Key Link Verification

| From                                            | To                                              | Via                                              | Status     | Details                                                                                                                                          |
| ----------------------------------------------- | ----------------------------------------------- | ------------------------------------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| `manifest.rs::SkillEntry`                       | `Option<DirectoryName>` serde defaults          | `#[serde(default, skip_serializing_if = "Option::is_none")]` | WIRED      | Verified at line 113-114; round-trip tests confirm both shapes parse and Unowned omits key on serialize.                                         |
| `lockfile.rs::LockEntry`                        | `Option<DirectoryName>` serde defaults          | `#[serde(default, skip_serializing_if = "Option::is_none")]` | WIRED      | Verified at line 40-41; mirror tests pass.                                                                                                       |
| `library.rs::consolidate_managed`               | `copy_dir_recursive` (existing helper)          | filesystem copy instead of `unix_fs::symlink`    | WIRED      | Verified at lines 187, 203 (Empty + Directory branches both copy). v0.9-shape Symlink branch refuses with `result.skipped += 1` per D-02 boundary defense. |
| `remove.rs::execute`                            | manifest entries → `source_name = None`         | explicit transition before manifest.remove       | WIRED      | Verified at lines 362-364; `library_entries_transitioned_to_unowned` counter increments per skill; library content NOT deleted.                  |
| `cleanup.rs::cleanup_library`                   | `config.directories.contains_key` check         | Case 1 detection during stale-entry processing   | WIRED      | Verified at line 93; partition feeds Case 1 (transition) vs Case 2 (delete) lists.                                                                |
| `lib.rs::sync`                                  | v0.9-shape detection (refuse-with-hint)         | isolated check before consolidate                | WIRED      | Verified at lines 1027-1039; calls `migration_v010::detect_v09_shape` and `anyhow::bail!` with the documented error message.                     |
| `lib.rs::run`                                   | `Command::MigrateLibrary` dispatch              | match arm calling `run_migrate_library`          | WIRED      | Verified at lines 600-606; calls `run_migrate_library` and exits 1 on `is_partial_or_failed()`.                                                  |
| `migration_v010.rs::execute`                    | filesystem-only copy (no manifest mutation)     | remove symlink → `copy_dir_recursive_resolving`  | WIRED      | Verified at lines 308-355: `read_link` for raw target → `canonicalize` → `remove_file` (symlink) → `copy_dir_recursive_resolving` → post-copy hash verify. No manifest mutation per D-06. |
| `tests/cli.rs::v0_10_migration tests`           | tome binary via `assert_cmd`                    | `Command::cargo_bin("tome")`                     | WIRED      | All 5 integration tests use `assert_cmd::Command::cargo_bin("tome")`.                                                                            |
| `tests/cli.rs::build_v09_fixture`               | `tome::hash_directory` (re-exported)            | crate-root re-export                             | WIRED      | `tome::hash_directory` invoked 3 times in fixture (p1, p2, l1). No parallel SHA-256 helper (`fn sha256_dir_simple` returns 0 matches).            |

### Data-Flow Trace (Level 4)

| Artifact                  | Data Variable           | Source                                        | Produces Real Data | Status   |
| ------------------------- | ----------------------- | --------------------------------------------- | ------------------ | -------- |
| `consolidate_managed`     | library dest content    | `manifest::hash_directory(&skill.path)` + `copy_dir_recursive` from real source | Yes — copies bytes from disk; verified by `consolidate_managed_creates_real_dir` reading SKILL.md content | FLOWING  |
| `cleanup_library` Case 1  | manifest `source_name`  | `manifest.skills_get_mut(name).source_name = None` | Yes — mutates real BTreeMap entry; observable via `manifest.get(name).source_name == None` after call | FLOWING  |
| `remove::execute`         | manifest `source_name`  | `manifest.skills_get_mut(skill_name).source_name = None` over `plan.skills` | Yes — mutates per skill in plan; integration test verifies `manifest.get(...).source_name` becomes None  | FLOWING  |
| `migrate_library` execute | library dir bytes       | `read_link(symlink) → canonicalize → copy_dir_recursive_resolving(resolved_source, library_path)` | Yes — `execute_converts_managed_symlink_to_real_dir` reads back SKILL.md and asserts content equals source | FLOWING  |
| `detect_v09_shape`        | bool                    | iterates `manifest.iter()` checking `managed && library_path.is_symlink()` | Yes — verified by sync refuse-with-hint integration test (returns true on fixture) and `detect_v09_shape_returns_false_when_managed_already_real_dir` | FLOWING  |

### Behavioral Spot-Checks

| Behavior                                                            | Command                                                                                          | Result                                                                                                | Status |
| ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------ | ------ |
| `tome migrate-library --help` mentions "one-shot"                   | `./target/debug/tome migrate-library --help \| grep one-shot`                                    | "One-shot migration..."; "This is a one-shot command for migrating from tome v0.9.x to v0.10."        | PASS   |
| `tome migrate-library --help` shows `--dry-run`                     | `./target/debug/tome migrate-library --help \| grep -- --dry-run`                                | `--dry-run` flag listed                                                                                | PASS   |
| Manifest unit tests (Plan 11-01) pass                               | `cargo test --package tome --lib manifest::tests`                                                | 23 passed                                                                                              | PASS   |
| Lockfile unit tests (Plan 11-01) pass                               | `cargo test --package tome --lib lockfile::tests`                                                | 26 passed                                                                                              | PASS   |
| Library unit tests (Plan 11-02) pass — incl. `consolidate_post_sync_no_symlinks_in_library` (SC1 anchor) | `cargo test --package tome --lib library::tests`                                | 34 passed                                                                                              | PASS   |
| Cleanup unit tests (Plan 11-03) pass                                | `cargo test --package tome --lib cleanup::tests`                                                 | 12 passed                                                                                              | PASS   |
| Remove unit tests (Plan 11-03) pass                                 | `cargo test --package tome --lib remove::tests`                                                  | 12 passed                                                                                              | PASS   |
| migration_v010 unit tests (Plan 11-04) pass                         | `cargo test --package tome --lib migration_v010::tests`                                          | 13 passed                                                                                              | PASS   |
| 5 Plan 11-05 integration tests pass                                 | `cargo test --package tome --test cli -- migrate_library_* sync_*`                               | 5 passed                                                                                               | PASS   |
| Resolved deferred test `symlink_chain_managed_skill` (v0.10 shape)  | `cargo test --package tome --test cli -- symlink_chain_managed_skill`                            | 1 passed                                                                                               | PASS   |
| Full integration suite                                              | `cargo test --package tome --test cli`                                                           | 141 passed, 0 failed                                                                                   | PASS   |
| Full unit suite (excluding documented flakes)                       | `cargo test --package tome --lib`                                                                | 556 passed, 2 failed (`backup::tests::diff_shows_changes`, `backup::tests::restore_reverts_changes`) — both pre-existing parallel-test contention / git signing flakes documented in STATE.md as Phase 15 / HARD-14 backlog; pass in isolation | PASS (with documented exception) |

### Requirements Coverage

| Requirement | Source Plan        | Description                                                                                                              | Status      | Evidence                                                                                                                                                                              |
| ----------- | ------------------ | ------------------------------------------------------------------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| LIB-01      | 11-02, 11-05       | All library entries are real directories — managed skills are copies of source content (not symlinks).                   | SATISFIED   | `consolidate_managed` rewritten with `copy_dir_recursive`; SC1 anchor test `consolidate_post_sync_no_symlinks_in_library` passes; integration test `migrate_library_converts_managed_symlinks_to_real_dirs` passes. |
| LIB-02      | 11-02              | `Manifest.managed: bool` semantic shift to "update channel"; documentation updated.                                      | SATISFIED   | Field doc comment in `manifest.rs:121-128` rewritten to "update channel" semantics; `library.rs` module-level doc + `consolidate_managed` doc reflect LIB-01/LIB-02 model.            |
| LIB-03      | 11-01              | `SkillEntry.source_name` becomes `Option<DirectoryName>`; both shapes accepted via `#[serde(default)]`.                  | SATISFIED   | Manifest + lockfile both lifted; old/new/missing-key shapes round-trip per dedicated tests.                                                                                          |
| LIB-04      | 11-03, 11-05       | Source removal preserves library content; manifest entries enter Unowned state.                                          | SATISFIED   | Two trigger points wired (D-10): `tome remove` explicit + cleanup-phase implicit. Integration test `sync_preserves_library_when_source_removed_from_config` exercises end-to-end.    |
| LIB-05      | 11-04, 11-05       | First-sync migration converts existing symlink-based libraries to real-directory copies; idempotent.                     | SATISFIED * | `tome migrate-library` CLI command exists with detection (D-03), broken-symlink preservation (D-04), SAFE-01 aggregation (D-05), idempotent re-run (D-06). **Note: REQUIREMENTS.md text mentions persisting consent in `machine.toml::migration_v010_acknowledged: true`; the ROADMAP Success Criterion 4 explicitly supersedes this with "No flag is persisted — running the command IS the consent" (per CONTEXT.md D-01). The implementation correctly follows the contract (no flag); REQUIREMENTS.md text is stale and could be updated post-phase to match.** |

\* See note on LIB-05 — implementation matches the SC4 contract; REQUIREMENTS.md prose is the older auto-on-first-sync wording superseded by D-01.

### Anti-Patterns Found

| File                                | Line  | Pattern                  | Severity | Impact                                                                                                                                                                                       |
| ----------------------------------- | ----- | ------------------------ | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `crates/tome/src/manifest.rs`       | 150   | dead-code warning on `SkillEntry::new_unowned` | Info     | Documented in `deferred-items.md`; constructor exists for Phase 14 (`tome adopt`/`tome forget`). Causes `make ci` (clippy `-D warnings`) to fail until Phase 14 lands; `cargo test` still works. |

No `TODO`, `FIXME`, `XXX`, `HACK`, or `PLACEHOLDER` comments in any of the modified Phase 11 modules. No empty stubs, no hardcoded empty-array returns disguised as data fetches.

### Human Verification Required

None. All five success criteria have automated coverage at unit + integration levels; the migrate-library `--help` output was verified against the binary; the v0.9 fixture in integration tests directly exercises the user-visible binary surface.

### Gaps Summary

No gaps. All five Success Criteria from ROADMAP.md Phase 11 are satisfied with automated evidence. All five LIB-* requirements are satisfied. The two failing unit tests (`backup::tests::diff_shows_changes`, `backup::tests::restore_reverts_changes`) are pre-existing flakes already documented in STATE.md as Phase 15 / HARD-14 backlog (git-signing-agent contention under parallel execution); both pass in isolation and are not related to Phase 11 work. The single dead-code warning on `SkillEntry::new_unowned` is documented in `deferred-items.md` and will be consumed by Phase 14's `tome adopt`/`tome forget` commands. The REQUIREMENTS.md prose for LIB-05 mentions a `machine.toml::migration_v010_acknowledged` flag that the SC4 contract explicitly supersedes (no flag persisted; CONTEXT.md D-01); the implementation correctly follows the contract.

---

_Verified: 2026-05-03_
_Verifier: Claude (gsd-verifier)_
