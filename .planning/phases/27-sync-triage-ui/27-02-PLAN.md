---
phase: 27-sync-triage-ui
plan: 02
type: execute
wave: 3
depends_on:
  - 27-01b
files_modified:
  - crates/tome-desktop/src/commands.rs
  - crates/tome-desktop/src/lib.rs
  - crates/tome-desktop/ui/src/components/SectionHeader.tsx
  - crates/tome-desktop/ui/src/components/SectionHeader.module.css
  - crates/tome-desktop/ui/src/components/TriagePanel.tsx
  - crates/tome-desktop/ui/src/components/TriagePanel.module.css
  - crates/tome-desktop/ui/src/components/TriageRow.tsx
  - crates/tome-desktop/ui/src/components/TriageRow.module.css
  - crates/tome-desktop/ui/src/components/TriageDetail.tsx
  - crates/tome-desktop/ui/src/components/TriageDetail.module.css
  - crates/tome-desktop/ui/src/views/SyncView.tsx
  - crates/tome-desktop/ui/src/hooks/useLockfileDiff.ts
  - crates/tome-desktop/ui/src/hooks/useSync.ts
  - crates/tome-desktop/ui/src/bindings.ts
  - crates/tome-desktop/tests/a11y/axe.spec.ts
autonomous: true
requirements:
  - SYNC-02
tags:
  - tauri
  - react
  - react-aria
  - gridlist
  - triage
  - section-header

must_haves:
  truths:
    - "After Reconcile completes, the triage panel lists every Added/Changed/Removed skill in three vertical sections (NEW / CHANGED / REMOVED), grouped by source within each section."
    - "Each TriageRow renders a primary skill name, a secondary `${source} · ${managed|local} · synced ${relativeTime}` line, and an inline chip that toggles keep ⇄ disable on click (D-12)."
    - "Selecting a TriageRow renders TriageDetail in the right column with a RadioGroup canonical picker (Keep / Disable on this machine / [git-only] View source) and per-skill diff metadata."
    - "Section-level bulk action `[Disable all new]` and source-group bulk action `[Disable all new from <source>]` apply only to the NEW outer section (D-13)."
    - "TriagePanel uses React Aria GridList + GridListItem + GridListSection (NOT ListBox) because rows contain interactive children (Pitfall 1)."
    - "The Sidebar's Sync NavItem renders a pre-sync badge with count = new + changed + removed while triage decisions are pending (D-05)."
    - "SectionHeader supports level=2 (outer NEW/CHANGED/REMOVED, h2) and level=3 (inner source group, h3); the back-compat single-level Health-view consumer continues to work without changes."
  artifacts:
    - path: "crates/tome-desktop/src/commands.rs"
      provides: "get_lockfile_diff command + LockfileDiff specta projection over update::diff"
      exports: ["get_lockfile_diff"]
    - path: "crates/tome-desktop/ui/src/components/TriagePanel.tsx"
      provides: "Sectioned GridList with two-level SectionHeader, inline chip toggle, bulk actions, [Apply N decisions] anchor"
      min_lines: 140
    - path: "crates/tome-desktop/ui/src/components/TriageRow.tsx"
      provides: "52px row matching SkillListRow rhythm + inline keep/disable chip"
      min_lines: 50
    - path: "crates/tome-desktop/ui/src/components/TriageDetail.tsx"
      provides: "Right-column detail with metadata grid + RadioGroup canonical picker + git-source View source action"
      min_lines: 90
    - path: "crates/tome-desktop/ui/src/components/SectionHeader.tsx"
      provides: "level 2 or 3 prop switching between h2 and h3; bulk-action trailing slot"
    - path: "crates/tome-desktop/ui/src/hooks/useLockfileDiff.ts"
      provides: "Calls get_lockfile_diff once, refetches on lockfileChanged events"
      min_lines: 30
  key_links:
    - from: "crates/tome-desktop/ui/src/views/SyncView.tsx"
      to: "TriagePanel"
      via: "renders TriagePanel diff={diff} decisions={decisions} onDecisionChange onBulkAction onApply in the in-progress + post-Reconcile state"
      pattern: "TriagePanel"
    - from: "crates/tome-desktop/ui/src/components/TriagePanel.tsx"
      to: "react-aria-components GridList"
      via: "GridList aria-label='Triage decisions' selectionMode='single'"
      pattern: "GridList"
    - from: "crates/tome-desktop/ui/src/hooks/useLockfileDiff.ts"
      to: "crates/tome-desktop/ui/src/components/TriagePanel.tsx"
      via: "diff={diff} prop in SyncView in-progress branch"
      pattern: "diff=\\{diff\\}"
    - from: "crates/tome-desktop/ui/src/hooks/useLockfileDiff.ts"
      to: "events.lockfileChanged"
      via: "useTauriEvent subscribes for refresh; gated by isRunningRef from useSync to honor Pitfall 6"
      pattern: "events.lockfileChanged"
---

<objective>
Ship SYNC-02: the lockfile-diff triage panel. Add `get_lockfile_diff` Tauri command (specta projection over the existing `update::diff`); build the sectioned GridList triage UI with two-level SectionHeader (NEW/CHANGED/REMOVED outer, source-group inner); inline `[✓ keep]` chip toggle; bulk actions on NEW only; right-column TriageDetail with RadioGroup canonical picker; the `[Apply N decisions]` button (27-03 wires it to PreviewPopover). SectionHeader's level/trailing prop extension lands here too so 27-02b (which back-fills VIEW-02 closure in SkillsView) has the primitive to consume.

Per the revision split, the VIEW-02 SkillsView carryover closure (Sort=Recent comparator wiring + group-by SectionHeader adoption + REQUIREMENTS.md status flip) has been EXTRACTED to a sibling 27-02b plan to keep file-touch count under the warning threshold; both plans run in Wave 3.

Purpose: replaces `update::present_changes`'s CLI interactive triage with the visual flow; this is the largest plan in Phase 27 by net-new-component count (per RESEARCH §Summary §3).

Output: 1 new Rust command + 1 new IPC type (`LockfileDiff` projection); 3 new React components (TriagePanel, TriageRow, TriageDetail), 1 extended (SectionHeader); 1 new hook (useLockfileDiff); useSync extended with triage state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-sync-triage-ui/27-CONTEXT.md
@.planning/phases/27-sync-triage-ui/27-RESEARCH.md
@.planning/phases/27-sync-triage-ui/27-PATTERNS.md
@.planning/phases/27-sync-triage-ui/27-UI-SPEC.md
@.planning/phases/26-read-only-views-alpha-cut/deferred-items.md
@.planning/phases/27-sync-triage-ui/27-01a-PLAN.md
@.planning/phases/27-sync-triage-ui/27-01b-PLAN.md
@crates/tome/src/update.rs
@crates/tome/src/lockfile.rs
@crates/tome-desktop/src/commands.rs
@crates/tome-desktop/ui/src/components/SectionHeader.tsx
@crates/tome-desktop/ui/src/components/SkillListRow.tsx
@crates/tome-desktop/ui/src/components/DetailHeader.tsx
@crates/tome-desktop/ui/src/components/Button.tsx
@crates/tome-desktop/ui/src/components/Badge.tsx
@crates/tome-desktop/ui/src/views/HealthView.tsx
@crates/tome-desktop/ui/src/hooks/useSkills.ts
@crates/tome-desktop/ui/src/lib/relativeTime.ts

<interfaces>
From crates/tome/src/update.rs (REUSED VERBATIM):
- enum SkillChange { Added(LockEntry), Changed { old, new }, Removed(LockEntry) }
- struct UpdateDiff { changes: BTreeMap<SkillName, SkillChange> }
- fn diff(old: &Lockfile, new: &Lockfile) -> UpdateDiff

From crates/tome/src/lockfile.rs:
- struct LockEntry { source_name: Option<DirectoryName>, previous_source: Option<DirectoryName>, content_hash: ContentHash, registry_id: Option<String>, version: Option<String>, git_commit_sha: Option<String> }

From crates/tome-desktop/ui/src/components/SectionHeader.tsx (extend with level prop):
- Current: interface SectionHeaderProps { label: string; count: number } renders h2 only
- After extension: interface SectionHeaderProps { label: string; count: number; level?: 2 | 3; trailing?: ReactNode }

From crates/tome-desktop/ui/src/components/Button.tsx:
- Button variant primary/secondary/small with onPress and ariaLabel props

From crates/tome-desktop/ui/src/components/Badge.tsx:
- Badge subtype managed/disabled/override/type-git

From react-aria-components ^1.18.0:
- GridList aria-label selectionMode selectedKeys onSelectionChange
- GridListSection + Header for sticky-header sections
- GridListItem id textValue aria-label supports interactive children
- RadioGroup label value onChange + Radio value
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add get_lockfile_diff command + LockfileDiff specta projection; SectionHeader level + trailing props; useLockfileDiff hook (Pitfall 6 isRunningRef gate)</name>
  <files>
    crates/tome-desktop/src/commands.rs,
    crates/tome-desktop/src/lib.rs,
    crates/tome-desktop/ui/src/components/SectionHeader.tsx,
    crates/tome-desktop/ui/src/components/SectionHeader.module.css,
    crates/tome-desktop/ui/src/hooks/useLockfileDiff.ts,
    crates/tome-desktop/ui/src/bindings.ts
  </files>
  <read_first>
    crates/tome/src/update.rs (full file — SkillChange, UpdateDiff, diff signature),
    crates/tome/src/lockfile.rs (lines 1-150 — LockEntry shape + Lockfile::load),
    crates/tome-desktop/src/commands.rs (read after 27-01b lands — load_context pattern + get_lockfile_diff insertion point),
    crates/tome-desktop/src/error.rs (TomeError + From for anyhow::Error),
    crates/tome-desktop/ui/src/components/SectionHeader.tsx (full file — existing shape),
    crates/tome-desktop/ui/src/components/SectionHeader.module.css (existing styles to preserve),
    crates/tome-desktop/ui/src/hooks/useSkills.ts (canonical pattern for useLockfileDiff),
    crates/tome-desktop/ui/src/hooks/useTauriEvent.ts,
    crates/tome-desktop/ui/src/hooks/useSync.ts (post-27-01b — expose isRunning via context or via prop for Pitfall 6 gating),
    .planning/phases/27-sync-triage-ui/27-PATTERNS.md §"crates/tome-desktop/src/commands.rs" + §"ui/src/hooks/useLockfileDiff.ts",
    .planning/phases/27-sync-triage-ui/27-UI-SPEC.md §"TriagePanel" (SectionHeader wiring contract)
  </read_first>
  <behavior>
    - Test (Rust unit): with a tempdir lockfile containing 3 entries, calling get_lockfile_diff returns a LockfileDiff projection over update::diff(old, new) where old is the current lockfile and new is the lockfile that would result from a re-discover. For this task, simulate by exposing a helper `fn lockfile_diff_projection(old: &Lockfile, new: &Lockfile, manifest: &Manifest) -> LockfileDiff` and unit-testing that helper. Verify the projection includes per-skill change_kind ("added" or "changed" or "removed"), source_name, content_hash (old plus new for changed), previous_source (for removed), and synced_at (for changed: from manifest).
    - Test (SectionHeader): rendering SectionHeader label NEW count 8 level 2 produces h2; level 3 produces h3; default (no level) produces h2 (back-compat).
    - Test (SectionHeader trailing): rendering SectionHeader with trailing prop set to a Button includes the button after the count chip and the button is keyboard-focusable.
    - Test (useLockfileDiff): hook fetches once on mount, refetches on lockfileChanged event, exposes diff, err, refetch. When isRunningRef.current is true, the lockfileChanged handler does NOT trigger refetch (Pitfall 6 discipline).
  </behavior>
  <action>
    1. In `crates/tome-desktop/src/commands.rs` add `get_lockfile_diff`. Signature: `pub fn get_lockfile_diff(_app: tauri::AppHandle) -> Result<LockfileDiff, TomeError>` decorated with `#[tauri::command] #[specta::specta]`. Body: call `load_context()` with `.map_err(TomeError::from)`, load the current lockfile via `tome::lockfile::load` against `paths.lockfile_path()` (or whatever canonical loader exists — confirm by reading `crates/tome/src/lockfile.rs`), compute the prospective lockfile by re-running discover plus lockfile build. If no read-only helper exists in `tome::update` yet, extract `pub fn compute_pending_diff(config: &Config, paths: &TomePaths) -> Result<(UpdateDiff, Manifest)>` into `crates/tome/src/update.rs` exposing exactly the read-only path that `lib.rs::sync` uses today; reuse from there. Map the `UpdateDiff` into the `LockfileDiff` projection.
    2. Define `pub struct LockfileDiff` and `pub struct TriageEntry` in `commands.rs` (or in a new `crates/tome-desktop/src/sync_types.rs` module — Claude's choice; keep alongside other IPC types). Derive `serde::Serialize` and `specta::Type`. `LockfileDiff` has fields `added: Vec<TriageEntry>`, `changed: Vec<TriageEntry>`, `removed: Vec<TriageEntry>` ordered alphabetically by SkillName within each Vec (BTreeMap iteration preserves order). `TriageEntry` has fields `name: SkillName`, `source_name: Option<DirectoryName>`, `previous_source: Option<DirectoryName>`, `origin: SkillOrigin`, `content_hash_old: Option<String>`, `content_hash_new: Option<String>`, `registry_id: Option<String>`, `version_old: Option<String>`, `version_new: Option<String>`, `git_commit_sha_old: Option<String>`, `git_commit_sha_new: Option<String>`, `synced_at: Option<String>` (ISO-8601 sourced from the in-memory Manifest by SkillName lookup).
    3. Register `get_lockfile_diff` in `make_builder()` (`crates/tome-desktop/src/lib.rs`). Run `cargo run -p tome-desktop --bin gen-bindings` from the repo root and commit the updated `crates/tome-desktop/ui/src/bindings.ts`.
    4. Extend `SectionHeader.tsx` to accept `level?: 2 | 3` (default 2) and `trailing?: ReactNode` (default undefined). Switch on level to render the heading element (use an inline conditional or a `HeadingTag` helper). Apply CSS class `sectionHeader--level-2` or `sectionHeader--level-3` from the existing module.css. In `SectionHeader.module.css` add `.sectionHeader--level-3 { padding-left: 20px; }` per UI-SPEC §Spacing exceptions (20px inner-section indent); reuse existing typography rules for both levels. PRESERVE the existing API for the Health-view consumer (back-compat: omitting level yields h2 and matches today's render).
    5. Create `crates/tome-desktop/ui/src/hooks/useLockfileDiff.ts` mirroring `useSkills.ts`: `useState<LockfileDiff | null>`, `useState<TomeError | null>`; `refetch` as `useCallback` async that calls `commands.getLockfileDiff()` and narrows via `res.status === "ok"`; `useEffect` calls `refetch` on mount; `useTauriEvent(events.lockfileChanged, refetch)` ONLY for `lockfileChanged`. Apply Pitfall 6 discipline: accept an `isRunningRef: RefObject<boolean>` (passed by SyncView via prop or extracted from a `useSync()` selector) and gate the refetch handler with `if (!isRunningRef.current) refetch();` to avoid stale-closure. Return `{ diff, err, refetch }`.
  </action>
  <verify>
    <automated>cargo test -p tome-desktop --lib commands -- get_lockfile_diff &amp;&amp; cargo run -p tome-desktop --bin gen-bindings &amp;&amp; git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts &amp;&amp; cd crates/tome-desktop/ui &amp;&amp; npm run typecheck &amp;&amp; npm run test -- --run SectionHeader useLockfileDiff</automated>
  </verify>
  <done>
    `get_lockfile_diff` returns a `LockfileDiff` projection over `update::diff` with `TriageEntry` shape; `SectionHeader` accepts `level` and `trailing` props with Phase 26 back-compat preserved; `useLockfileDiff` subscribes only to `lockfileChanged` (gated by `isRunningRef`); `bindings.ts` regenerated and CI freshness gate clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TriagePanel + TriageRow + TriageDetail components using React Aria GridList + sectioned grouping + inline chip + RadioGroup picker; mount in SyncView; extend useSync with triage state</name>
  <files>
    crates/tome-desktop/ui/src/components/TriagePanel.tsx,
    crates/tome-desktop/ui/src/components/TriagePanel.module.css,
    crates/tome-desktop/ui/src/components/TriageRow.tsx,
    crates/tome-desktop/ui/src/components/TriageRow.module.css,
    crates/tome-desktop/ui/src/components/TriageDetail.tsx,
    crates/tome-desktop/ui/src/components/TriageDetail.module.css,
    crates/tome-desktop/ui/src/views/SyncView.tsx,
    crates/tome-desktop/ui/src/hooks/useSync.ts,
    crates/tome-desktop/tests/a11y/axe.spec.ts
  </files>
  <read_first>
    crates/tome-desktop/ui/src/components/SkillListRow.tsx (52px row pattern — TriageRow mirrors this),
    crates/tome-desktop/ui/src/components/DetailHeader.tsx (3-row composition: title+badges, KeyValueRow grid, action triplet),
    crates/tome-desktop/ui/src/views/SkillsView.tsx (lines 144-233 — list-rendering pattern; Pitfall 1 evidence that ListBoxItem has no inline buttons today),
    crates/tome-desktop/ui/src/views/HealthView.tsx (lines 112-141 — SectionHeader-driven grouping),
    crates/tome-desktop/ui/src/components/Button.tsx (Button variants),
    crates/tome-desktop/ui/src/components/Badge.tsx (Badge variants — Badge subtype type-git for New or Changed or Removed),
    crates/tome-desktop/ui/src/lib/relativeTime.ts (formatRelative used in secondary line + TriageDetail SYNCED cell),
    crates/tome-desktop/ui/src/hooks/useSync.ts (from 27-01b — extend for triage decisions),
    .planning/phases/27-sync-triage-ui/27-UI-SPEC.md §"TriagePanel" + §"TriageRow" + §"TriageDetail" + §"Copywriting Contract",
    .planning/phases/27-sync-triage-ui/27-PATTERNS.md §"ui/src/components/TriagePanel.tsx" + §"ui/src/components/TriageRow.tsx" + §"ui/src/components/TriageDetail.tsx" + §"Code Examples — React GridList with sections + inline button",
    .planning/phases/27-sync-triage-ui/27-RESEARCH.md §"Pitfall 1" + §"Don't Hand-Roll"
  </read_first>
  <behavior>
    - Test (TriagePanel render): given a LockfileDiff with 8 added, 3 changed, 1 removed across 2 source groups in NEW, the rendered DOM contains an outer h2 NEW with count 8 plus trailing Disable all new button; two inner h3 PLUGINS and h3 MY-REPO headers each with their own Disable all new from source button; 8 GridListItems with primary skill name + secondary line + chip; collapsed CHANGED 3 and REMOVED 1 sections (Claude's discretion: NEW expanded by default, CHANGED and REMOVED collapsed).
    - Test (chip toggle): clicking a TriageRow's keep chip calls `onDecisionToggle(skill)` and the next render shows `disabled here`. Pressing Space when the chip's parent row has keyboard focus does the same.
    - Test (selection round-trips): clicking a TriageRow updates `selectedSkill` via `onSelect`; the TriageDetail right column re-renders with the selected skill's metadata.
    - Test (TriageDetail picker): for an Added git-sourced skill, the RadioGroup renders three radios (Keep / Disable on this machine / View source); selecting View source fires `commands.openSourceFolder(skill)` and the radio bounces back to the previously-selected decision. For a Removed skill, the RadioGroup is omitted and the verbatim removed-helper copy renders.
    - Test (a11y): axe-core scan of a sample TriagePanel passes with zero violations. VoiceOver labels match UI-SPEC §VoiceOver labels for every Phase 27 entry.
    - Test (bulk action scope): `onBulkAction({ kind: "section", section: "new" }, "disable")` is callable only from the NEW outer SectionHeader's trailing button; the CHANGED and REMOVED outer headers do NOT render trailing buttons (D-13 invariant).
    - Test (useSync extension): useSync now exposes `decisions: Map<SkillName, TriageDecision>`, `pendingDecisionCount` (count where decision is not "keep"), `pendingDiffCount` (added+changed+removed), `onDecisionChange(skill, decision)`, `onBulkAction(scope, decision)`. The Sidebar's syncBadge.count wires to pendingDiffCount while pendingDiffCount > 0.
  </behavior>
  <action>
    1. Build `TriagePanel.tsx` per UI-SPEC §TriagePanel. Props: `diff: LockfileDiff`, `decisions: Map<SkillName, TriageDecision>`, `onDecisionChange(skill, decision)`, `selectedSkill: SkillName | null`, `onSelect(skill)`, `onBulkAction(scope, decision)`, `onApply()`. TriageDecision is the literal union `"keep" | "disable"`. BulkScope is `{ kind: "section", section: "new" } | { kind: "source-group", section: "new", source: DirectoryName }`. Render via React Aria GridList aria-label "Triage decisions" selectionMode single (NOT ListBox per Pitfall 1). For each outer section in order [NEW, CHANGED, REMOVED] render a GridListSection wrapping a Header that contains a SectionHeader at level 2 with the trailing prop set ONLY for NEW when count > 0 (button label "Disable all new" calling onBulkAction with section scope). Inside each outer section nest a GridListSection per source group with inner SectionHeader at level 3, source name in uppercase, count, and the per-source trailing button for NEW only (label "Disable all new from sourceName"). Map TriageRows inside each inner group section. Default expansion (Claude's discretion): NEW expanded, CHANGED and REMOVED collapsed — implement via wrapping each outer GridListSection in a details/summary that contains the SectionHeader visual, keyboard-toggleable as a single region. Source-group sorting: alphabetical with "unowned" group last. Bottom-right `<Button variant="primary">Apply N decisions</Button>` where N = count where decision is not "keep", with `isDisabled` when N is 0, ariaLabel "Apply N triage decisions, preview machine.toml diff". This plan does NOT mount the PreviewPopover — 27-03 wires that; the `onApply` prop is a no-op TODO stub provided by SyncView.
    2. Build `TriageRow.tsx` (52px row matching SkillListRow rhythm). Wrap in a GridListItem with id = skill.name, textValue = skill.name, aria-label per UI-SPEC §VoiceOver labels TriageRow default entry. Primary line: skill.name in text-body 13px / 400, 600 when selected (use data-selected attribute on the parent for CSS-only state — match Phase 26 SkillListRow.module.css). Secondary line: `${sourceLabel} · ${origin.kind === "managed" ? "managed" : "local"} · synced ${formatRelative(skill.synced_at)}` in text-small 12px / 400, label-secondary. When change.kind is added, synced_at is null so render `· synced —`. Trailing chip: a Button variant small with onPress calling onDecisionToggle, ariaLabel "Toggle decision for name between keep and disable on this machine", text content `✓ keep` when decision is keep and `⊘ disabled here` when decision is disable. For REMOVED rows render a static non-interactive span with text `implicit remove`, aria-hidden true, 50% opacity, label-secondary color.
    3. Build `TriageDetail.tsx` mirroring `DetailHeader.tsx` 3-row composition. Props: `entry: TriageEntry | null`, `decision: TriageDecision`, `onDecisionChange(decision)`, `onViewSource()`. When entry is null: render `<p className={styles.placeholder}>Select a change to view details</p>`. Otherwise: Row 1: h2 entry.name + trailing Badge subtype type-git with label New or Changed or Removed. Row 2: metadata grid via dl with KeyValueRow-style cells (read DetailHeader.tsx for the canonical markup) — cells SOURCE (entry.source_name or "unowned", monospace, truncate via existing helpers), CONTENT HASH (for Changed render two truncated monospace hashes with arrow glyph between; for Added only _new; for Removed only _old), SYNCED (formatRelative(entry.synced_at) or "—" for Added). Row 3: canonical action picker. For Added or Changed: RadioGroup label "Decision" value decision onChange onDecisionChange with two Radios (Keep this skill / Disable on this machine) plus an additional Radio "View source (open in Finder)" when entry.origin.kind is managed AND entry.git_commit_sha_new is some — the view-source radio is a pseudo-radio: on selection fire onViewSource() and immediately revert to the previously-selected decision (use a useRef storing the last legitimate decision; intercept in onChange). For Removed: omit the RadioGroup and render `<p className={styles.removedHelper}>This skill will be removed from the lockfile. No action required.</p>`. Row 4 (Changed only): a details/summary with label "Show diff metadata" containing KeyValueRows for old vs new registry_id, version, git_commit_sha. A11y: outer section aria-label "skillName change details"; RadioGroup aria-label "Decision for skillName".
    4. Extend `useSync` (created in 27-01b). Add state: `decisions: Map<SkillName, TriageDecision>` (built from LockfileDiff via useEffect with diff as a dep, seeding only when decisions.size is 0). Add state: `selectedTriageSkill: SkillName | null`. Expose handlers: `onDecisionChange(skill, decision)`, `onBulkAction(scope, decision)`, `selectTriageSkill`. Expose computed: `pendingDecisionCount` (count where decision is not "keep"), `pendingDiffCount` (added.length + changed.length + removed.length from useLockfileDiff().diff). The hook still does NOT subscribe to ManifestChanged or LockfileChanged while isRunning is true (Pitfall 6 carried forward).
    5. In `SyncView.tsx`: rewire the in-progress branch to render TriagePanel and TriageDetail in the split layout. Middle column hosts a placeholder stepper (lands in 27-04) above TriagePanel; right column hosts TriageDetail keyed on selectedTriageSkill. The TriagePanel is hidden until useLockfileDiff().diff is non-empty — for now render whenever diff is non-empty regardless of stage (the Reconcile-gating refines in 27-04 once cancellation states settle). The `onApply` prop passed to TriagePanel is `() => { /* TODO 27-03: open PreviewPopover */ }` with an inline comment documenting the seam.
    6. Wire the Sidebar badge from 27-01b: useSync().pendingDiffCount feeds `syncBadge={{ kind: "pending", count: pendingDiffCount }}` when greater than 0. The badge clears when Apply succeeds (27-03) or the diff empties.
    7. Extend `crates/tome-desktop/tests/a11y/axe.spec.ts` Sync-view block: add an "Sync view triage a11y" sub-test that navigates to Sync with a mocked diff, runs `await new AxeBuilder({ page }).analyze()`, asserts `results.violations === []`.
  </action>
  <verify>
    <automated>cd crates/tome-desktop/ui &amp;&amp; npm run typecheck &amp;&amp; npm run test -- --run TriagePanel TriageRow TriageDetail useSync &amp;&amp; cd ../.. &amp;&amp; npx playwright test tests/a11y/axe.spec.ts -g "Sync view"</automated>
  </verify>
  <done>
    `TriagePanel` renders three vertical sections with two-level SectionHeader + inline chip + bulk actions on NEW only + `[Apply N decisions]` button; `TriageRow` mirrors `SkillListRow` 52px rhythm; `TriageDetail` renders metadata + RadioGroup canonical picker + Removed-helper copy; `useSync` exposes triage decisions + bulk handlers; `SyncView` mounts the panel + detail in the in-progress split layout; axe-core scan clean; VoiceOver labels match UI-SPEC.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React → Tauri IPC (`get_lockfile_diff`) | Webview-originated read-only command crosses into native Rust |
| Domain `tome::update::diff` → in-memory Lockfile + Manifest | Read-only computation; no filesystem writes |
| Tauri response payload → Webview | `LockfileDiff` payload contains skill names, source names, content hashes — user-owned data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | Tampering | get_lockfile_diff command invocation | accept | Read-only; single-user local app; no path or URL arguments cross the boundary (paths resolved server-side via load_context) |
| T-27-02-02 | Information Disclosure | LockfileDiff leaks content hashes/source names to webview console | accept | Same user owns both sides; content hashes are not secrets; D-GUI-08 boundary classification still applies — no PII leaked |
| T-27-02-03 | Tampering | webview malicious code triggers thousands of get_lockfile_diff calls | mitigate | useLockfileDiff debounces via useTauriEvent's late-listen-race guard + the watcher's 200ms debounce; explicit gating with `isRunningRef` prevents bounce during sync (Pitfall 6) |
| T-27-02-04 | Spoofing | TriagePanel chip toggle bypasses TriageDetail RadioGroup | accept | Both surfaces fire the same `onDecisionChange` callback against the same `decisions` Map state; no parallel pathways exist; the chip toggle is documented as the one-click shorthand for the canonical picker |
| T-27-02-05 | EoP | onBulkAction invoked outside the NEW section | mitigate | Type system: `BulkScope` is a tagged-union with `section: "new"` literal; the SectionHeader trailing button only renders for NEW (Task 2 implementation); a future refactor that loosens the type forces a compile error in the BulkScope discriminator |
| T-27-02-SC | Tampering | npm/cargo package installs | accept | This plan adds ZERO new external packages (no Cargo additions, no npm additions per UI-SPEC §Registry Safety). RESEARCH §"Package Legitimacy Audit" applies to 27-03 only |
</threat_model>

<verification>
- `cargo build -p tome-desktop --features bindings` compiles clean.
- `cargo test -p tome-desktop --lib commands -- get_lockfile_diff` passes.
- `cargo run -p tome-desktop --bin gen-bindings && git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts` — bindings regenerated cleanly.
- `cd crates/tome-desktop/ui && npm run typecheck && npm run test` — TypeScript + Vitest unit tests pass for TriagePanel, TriageRow, TriageDetail, SectionHeader, useLockfileDiff, useSync.
- `npx playwright test tests/a11y/axe.spec.ts -g "Sync view"` — axe scan of Sync view triage passes.
- Manual smoke: launch `cargo tauri dev`, navigate to Sync, observe TriagePanel renders with 3 sections + chip toggles + bulk-action buttons on NEW; click a row, see TriageDetail render with RadioGroup.

CLI regression: `cargo test -p tome` continues to pass (no domain changes in this plan beyond the read-only `compute_pending_diff` extraction if it was needed).
</verification>

<success_criteria>
- ROADMAP Phase 27 SC#2 met: lockfile diff produces triage panel listing new/changed/removed skills with per-skill diff metadata (source, hash, timestamp), keep/disable per-skill actions, view-source for git-sourced, and bulk actions on NEW only (section-level + per-source-group).
- `bindings.ts` CI freshness gate passes; Sync view has zero axe-core WCAG-AA violations.
- All existing CLI integration tests in `crates/tome/tests/cli*.rs` continue to pass.
- 27-02b (sibling plan) closes the VIEW-02 carryover work in SkillsView; this plan ships the SectionHeader primitive 27-02b consumes.
</success_criteria>

<output>
Create `.planning/phases/27-sync-triage-ui/27-02-SUMMARY.md` when done.
</output>
