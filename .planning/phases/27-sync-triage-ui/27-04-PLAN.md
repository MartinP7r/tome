---
phase: 27-sync-triage-ui
plan: 04
type: execute
wave: 4
depends_on:
  - 27-02
  - 27-03
files_modified:
  - crates/tome/src/lib.rs
  - crates/tome/src/library.rs
  - crates/tome/src/distribute.rs
  - crates/tome/tests/sync_cancel.rs
  - crates/tome-desktop/src/commands.rs
  - crates/tome-desktop/ui/src/hooks/useSync.ts
  - crates/tome-desktop/ui/src/views/SyncView.tsx
  - crates/tome-desktop/ui/src/components/StageStepper.tsx
  - crates/tome-desktop/ui/src/components/StageStepper.module.css
  - crates/tome-desktop/ui/src/components/StageRow.tsx
  - crates/tome-desktop/ui/src/components/StageRow.module.css
  - crates/tome-desktop/ui/src/components/SyncToast.tsx
  - crates/tome-desktop/ui/src/components/SyncToast.module.css
  - crates/tome-desktop/tests/a11y/axe.spec.ts
autonomous: true
requirements:
  - SYNC-04
tags:
  - rust
  - cancellation
  - integration-tests
  - stepper
  - react
  - toast
  - aria-live

must_haves:
  truths:
    - "Pressing [Cancel sync] during any stage causes the pipeline to bail at the NEXT stage boundary; the library is left in a consistent state (no half-written manifest, no partial lockfile)."
    - "The cancellation invariant is proven by a new integration test crates/tome/tests/sync_cancel.rs that drives tome::sync against a TempDir with a pre-flipped or mid-run CancelToken and asserts (a) sync returns the cancellation error, (b) the manifest file either does not exist or has not been modified after the last good stage, (c) the lockfile is either the pre-sync version or unchanged."
    - "StageStepper renders six rows (Reconcile/Discover/Consolidate/Distribute/Cleanup/Save) per UI-SPEC §StageStepper, with per-stage state pending/active/complete/cancelled and per-stage durations (D-10) measured on event-arrival timestamps in React (no domain-API change)."
    - "On cancellation, the stepper transforms in place (D-18): the stage that was running on cancel renders as cancelled (amber ⊘); not-yet-started stages also render as cancelled; the summary block above renders 'Sync cancelled' heading + 'The library is in a consistent state' sub-line + [Run sync] + [Dismiss] buttons."
    - "Clicking [Dismiss] auto-returns to idle (D-06); 'Sync cancelled' transient toast (SyncToast component) is NOT used for cancellation per UI-SPEC (cancellation surfaces inline in the stepper, no toast)."
    - "D-18 (stepper persists in terminal state) supersedes D-06's 'Sync cancelled toast' phrasing — cancellation surfaces inline via the stepper's terminal branch, not as a toast. The toast affordance ships only for success (and is dismissable via Dismiss button per UI-SPEC §SyncToast)."
    - "On successful sync, the stepper transforms to all-complete + transient 'Sync complete' toast (D-06) via the new SyncToast component (hand-rolled role=status aria-live=polite, 5s setTimeout, Pitfall 2 / RESEARCH §Don't Hand-Roll)."
  artifacts:
    - path: "crates/tome/tests/sync_cancel.rs"
      provides: "Integration test driving tome::sync with a CancelToken and asserting consistent-state-on-abort"
      min_lines: 80
    - path: "crates/tome-desktop/ui/src/components/StageStepper.tsx"
      provides: "Vertical 6-stage stepper with active/complete/failed/cancelled variants + [Cancel sync] above + [Dismiss]/[Retry] below"
      min_lines: 100
    - path: "crates/tome-desktop/ui/src/components/StageRow.tsx"
      provides: "Per-stage row renderer with icon + label + duration + currentItem subtitle + variant-specific layouts"
      min_lines: 90
    - path: "crates/tome-desktop/ui/src/components/SyncToast.tsx"
      provides: "Hand-rolled toast: role=status aria-live=polite with 5s setTimeout fade-out (Pitfall 2)"
      min_lines: 30
  key_links:
    - from: "crates/tome-desktop/ui/src/hooks/useSync.ts"
      to: "commands.cancelSync"
      via: "useSync.cancel() invokes cancelSync IPC command"
      pattern: "commands.cancelSync"
    - from: "crates/tome-desktop/ui/src/components/StageStepper.tsx"
      to: "useSync state"
      via: "stages: Map<SyncStage, StageStatus> from useSync; onCancel/onDismiss/onRetryFromStage handlers"
      pattern: "StageStepper"
    - from: "crates/tome/tests/sync_cancel.rs"
      to: "tome::sync"
      via: "drives sync with a mid-flight CancelToken::cancel and asserts SC#4 invariant"
      pattern: "tome::sync"
---

<objective>
Ship SYNC-04: cancellation that leaves library state consistent. Build the StageStepper + StageRow components per UI-SPEC §StageStepper (live + terminal in-place rendering); wire `useSync().cancel` to the `cancel_sync` command from 27-01; build the SyncToast hand-rolled live-region for the "Sync complete" success toast (Pitfall 2); add the canonical integration test `crates/tome/tests/sync_cancel.rs` that asserts SC#4 ("library is in a consistent state" — no half-written manifest, no partial lockfile). Per UI-SPEC §Terminal state the cancelled rendering includes "Sync cancelled" heading + "The library is in a consistent state" sub-line + [Run sync] + [Dismiss] buttons.

Purpose: prove the cancellation invariant end-to-end. The Rust side already has the CancelToken machinery in place from Phase 25 (verified in RESEARCH); this plan VERIFIES the invariant rather than introducing it, and ships the UI that exposes it.

Output: 1 new integration test + 3 new React components (StageStepper, StageRow, SyncToast) + extended useSync handlers + SyncView terminal-state branches for cancelled/success.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-sync-triage-ui/27-CONTEXT.md
@.planning/phases/27-sync-triage-ui/27-RESEARCH.md
@.planning/phases/27-sync-triage-ui/27-PATTERNS.md
@.planning/phases/27-sync-triage-ui/27-UI-SPEC.md
@.planning/phases/27-sync-triage-ui/27-01a-PLAN.md
@.planning/phases/27-sync-triage-ui/27-01b-PLAN.md
@.planning/phases/27-sync-triage-ui/27-02-PLAN.md
@.planning/phases/27-sync-triage-ui/27-03-PLAN.md
@crates/tome/src/lib.rs
@crates/tome/src/progress.rs
@crates/tome-desktop/src/commands.rs
@crates/tome-desktop/ui/src/hooks/useSync.ts
@crates/tome-desktop/ui/src/views/SyncView.tsx
@crates/tome-desktop/ui/src/components/FindingRow.tsx
@crates/tome-desktop/ui/src/components/Pill.tsx
@crates/tome-desktop/ui/src/components/Button.tsx

<interfaces>
From crates/tome/src/progress.rs (REUSED):
- CancelToken with Clone, cancel(), is_cancelled()
- ProgressEvent enum with SyncStageStarted/SyncStageProgress (with item field from 27-01)/SyncStageFinished/GitCloneProgress/BackupSnapshot
- SyncStage enum (Reconcile, Discover, Consolidate, Distribute, Cleanup, Save)

From crates/tome/src/lib.rs::sync (REUSED):
- pub fn sync(config: &Config, paths: &TomePaths, options: SyncOptions, sink: &dyn ProgressSink, cancel: &CancelToken) -> Result<SyncReport>
- Checks cancel.is_cancelled() at every stage boundary (verified RESEARCH lines 1863, 1935, 1999, 2073, 2138, 2179)

From crates/tome-desktop/src/commands.rs (from 27-01):
- pub fn cancel_sync(state: tauri::State<'_, SyncState>) -> Result<(), TomeError>

From crates/tome-desktop/ui/src/hooks/useSync.ts (from 27-01 + 27-02):
- stages: Map<SyncStage, StageStatus>; isRunning: boolean; outcome: SyncReport | TomeError | null
- start(), cancel(), dismiss(), pendingDiffCount

From crates/tome-desktop/ui/src/components/Pill.tsx (PATTERNS.md reference):
- Uses role="status" aria-live="polite" aria-atomic="true" — the same triplet SyncToast adopts

From crates/tome-desktop/ui/src/components/FindingRow.tsx:
- Inline [ErrorCode] message + Show error chain disclosure (Phase 26 D-11) — StageRow failed variant reuses this shape
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integration test sync_cancel.rs proves SC#4 — library state consistent after mid-flight CancelToken::cancel</name>
  <files>
    crates/tome/tests/sync_cancel.rs,
    crates/tome/src/lib.rs,
    crates/tome/src/library.rs,
    crates/tome/src/distribute.rs
  </files>
  <read_first>
    crates/tome/src/lib.rs (lines 1490-2260 — sync pipeline; identify each cancel.is_cancelled() check and the precise stage boundary; verify that NO partial manifest or lockfile write happens between boundaries),
    crates/tome/src/progress.rs (RecordingSink for tests; CancelToken API),
    crates/tome-desktop/tests/watcher_smoke.rs (canonical integration test pattern with TempDir; mirror the structure),
    crates/tome/src/manifest.rs (Manifest::save signature; this is the file the test asserts on),
    crates/tome/src/lockfile.rs (Lockfile::save signature; the second file the test asserts on),
    crates/tome/src/library.rs (consolidate_managed + consolidate_local — verify these check cancel before each skill operation; if they don't, this task adds the checks),
    crates/tome/src/distribute.rs (distribute_to_targets — verify cancel check before each symlink op; add if missing),
    .planning/phases/27-sync-triage-ui/27-RESEARCH.md §"Code Examples — spawn_blocking" + §"Architecture Pattern §Cooperative cancellation" + §"Phase 27 Architectural Responsibility Map / Cancellation flag" row + §"Architectural Responsibility Map / Sync pipeline orchestration" row,
    .planning/phases/27-sync-triage-ui/27-PATTERNS.md §"crates/tome/tests/sync_smoke.rs",
    .planning/phases/27-sync-triage-ui/27-CONTEXT.md §"Failure + cancellation" (D-17 SC#4 invariant)
  </read_first>
  <behavior>
    - Test (pre-flipped cancel at Reconcile boundary): a cancel.cancel() before tome::sync() returns Err with a cancellation-shaped error; the manifest file at paths.manifest_path() either does not exist (fresh TempDir) or is byte-identical to its pre-sync contents.
    - Test (mid-flight cancel during Consolidate): drive sync on a TempDir with at least 3 skills; from a thread, watch the RecordingSink events and call cancel.cancel() the moment the FIRST SyncStageStarted{Consolidate} arrives; assert that sync returns Err, the manifest file is either absent OR contains at most the entries that existed before sync started (NO partial Consolidate writes), the lockfile is unchanged from its pre-sync state.
    - Test (mid-flight cancel during Distribute): same shape, cancel on first SyncStageStarted{Distribute}; assert no half-written distribution symlinks (the relevant target dirs either contain pre-sync symlinks unchanged OR the symlinks created so far in Distribute remain valid pointing at the library — D-17 invariant is "library state consistent" which permits target-dir partial writes as long as the library + manifest + lockfile are consistent).
    - Test (no cancel = clean run): control test — run tome::sync with a NEVER-tripped CancelToken, assert sync returns Ok(SyncReport), the manifest exists and contains entries for the fixture skills, the lockfile is updated.
    - Test (manifest/lockfile atomicity): verify by inspection of sync source that manifest writes occur AFTER the last cancel-check in the Save stage; if the test reveals a write-before-check ordering bug, this task FIXES it by moving the cancel-check to the appropriate position.
  </behavior>
  <action>
    1. Create `crates/tome/tests/sync_cancel.rs` modeled after `crates/tome-desktop/tests/watcher_smoke.rs` (the canonical Phase 26 integration test). Use `tempfile::TempDir` for filesystem isolation, `assert_fs` for path assertions if convenient.
    2. Build a minimal `Config` with one `[directories.test]` source pointing at a fixture directory with 3 skill subdirs (each with a minimal SKILL.md frontmatter + body). Build a `TomePaths` for the TempDir.
    3. Add the four behavior tests above:
       - `pre_flipped_cancel_at_reconcile_boundary`: `let cancel = CancelToken::new(); cancel.cancel(); let sink = RecordingSink::new(); let result = tome::sync(&config, &paths, opts, &sink, &cancel); assert!(result.is_err()); assert!(!paths.manifest_path().exists() || manifest_unchanged(...));`
       - `mid_flight_cancel_during_consolidate`: spawn a thread that polls `RecordingSink::events()` for `ProgressEvent::SyncStageStarted { stage: SyncStage::Consolidate }`; when seen, call `cancel.cancel()`. Use a `Mutex<Option<JoinHandle>>` or `std::sync::Barrier` for coordination. Run sync in the test thread; on completion, assert (a) result is Err, (b) manifest state is consistent.
       - `mid_flight_cancel_during_distribute`: same pattern at Distribute. Assert library + manifest + lockfile remain in pre-sync OR pre-Distribute state.
       - `no_cancel_clean_run`: control case.
    4. While reading `crates/tome/src/lib.rs::sync`, audit each stage's write ordering against the cancel checks. The current pipeline order is:
       - Reconcile (drift detection — read-only, easily cancelled).
       - Discover (read-only scan).
       - Consolidate — WRITES library + manifest entries. **Verify cancel-check happens BEFORE manifest::save.** If `manifest::save` is called mid-Consolidate per-skill, the test will catch it; fix by deferring manifest write to a single save at end-of-stage (likely already correct — verify by reading lib.rs).
       - Distribute — writes distribution symlinks. Cancel-checks before each symlink op are required for D-17 invariant; if missing in `distribute.rs`, add `if cancel.is_cancelled() { bail!("sync cancelled during distribute") }` at the loop head per skill. **The post-Distribute manifest update MUST happen as a single atomic op AFTER all symlinks are committed OR not at all.** Confirm by reading `lib.rs::sync` Save stage location.
       - Cleanup — file removal; cancel-check before each removal acceptable.
       - Save — final atomic write of manifest + lockfile + machine.toml. Per RESEARCH (line 2173 comment) save runs as a single atomic unit — no mid-save cancel; this is correct and the test for "cancel at Save boundary" asserts the save either completed fully or was not started.
    5. If the audit reveals a missing cancel-check or out-of-order write, fix in this task (`crates/tome/src/lib.rs::sync` or `crates/tome/src/library.rs::consolidate_*` or `crates/tome/src/distribute.rs::distribute_to_targets`). Document any fixes in the task summary.
    6. The new integration test MUST live in `crates/tome/tests/` (not under `crates/tome-desktop/`) because it exercises the domain crate directly. Use `cargo test -p tome --test sync_cancel` to invoke.
  </action>
  <verify>
    <automated>cargo test -p tome --test sync_cancel</automated>
  </verify>
  <done>
    `crates/tome/tests/sync_cancel.rs` exists with 4 tests covering pre-flipped + mid-flight Consolidate cancel + mid-flight Distribute cancel + clean-run control; all 4 tests pass; any audit-discovered cancel-ordering bugs in lib.rs/library.rs/distribute.rs are fixed and noted in the summary; SC#4 invariant verified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StageStepper + StageRow components (live + terminal rendering) + SyncToast hand-rolled live region</name>
  <files>
    crates/tome-desktop/ui/src/components/StageStepper.tsx,
    crates/tome-desktop/ui/src/components/StageStepper.module.css,
    crates/tome-desktop/ui/src/components/StageRow.tsx,
    crates/tome-desktop/ui/src/components/StageRow.module.css,
    crates/tome-desktop/ui/src/components/SyncToast.tsx,
    crates/tome-desktop/ui/src/components/SyncToast.module.css
  </files>
  <read_first>
    crates/tome-desktop/ui/src/components/Pill.tsx (lines 1-26 — the role=status aria-live triplet that SyncToast mirrors),
    crates/tome-desktop/ui/src/components/FindingRow.tsx (lines 53-106 — the [ErrorCode] + disclosure pattern StageRow failed variant adopts),
    crates/tome-desktop/ui/src/components/Button.tsx (Button variants for [Cancel sync], [Dismiss], [Retry from stage] in stepper trailing slots),
    .planning/phases/27-sync-triage-ui/27-UI-SPEC.md §"StageStepper" + §"StageRow" + §"Spacing exceptions" (40px stage-row height, 24px icon column, 2px connector line, 24px stepper outer padding) + §"Color §Phase 27 semantic-token uses" + §"VoiceOver labels" (StageRow entries),
    .planning/phases/27-sync-triage-ui/27-PATTERNS.md §"Stepper outer container (No analog)" + §"ui/src/components/StageRow.tsx" + §"ui/src/components/SyncToast.tsx",
    .planning/phases/27-sync-triage-ui/27-RESEARCH.md §"Pitfall 2" (toast strategy: hand-roll, NOT UNSTABLE_ToastRegion) + §"Don't Hand-Roll" toast row,
    .planning/phases/27-sync-triage-ui/27-CONTEXT.md §"Failure + cancellation" (D-18 stepper transforms in place; cancelled icon = ⊘ amber)
  </read_first>
  <behavior>
    - Test (StageStepper pending state): rendering with all six stages in `{ kind: "pending" }` produces six rows in order [Reconcile, Discover, Consolidate, Distribute, Cleanup, Save], each with outline-circle icon (`--label-secondary`), 13px/400 label `--label-secondary`, right-aligned "—" trailing.
    - Test (StageStepper active state): with Reconcile complete (0.3s) + Discover complete (1.1s) + Consolidate active (current 47 of 120, currentItem "axiom-build"), the active row renders with spinner icon (`--accent`), 13px/600 label, subtitle "axiom-build" + inline bar `47/120 running…`. Above the stepper: `[Cancel sync]` button.
    - Test (StageStepper terminal cancelled): with Reconcile complete + Discover cancelled + the rest cancelled, rows render with ✓ (complete) / ⊘ amber (cancelled) icons; no `[Cancel sync]` button; above the stepper: `[Run sync]` primary + `[Dismiss]` secondary.
    - Test (StageStepper terminal success): all 6 complete + no partial failures + no failures → `[Dismiss]` only, no [Retry] button; the summary above renders "Sync complete" heading transient toast handled by SyncView, NOT by the stepper.
    - Test (StageStepper terminal failed with retry): one stage failed (Consolidate) + others cancelled, `retry_from = "discover"` → `[Retry from Discover]` primary + `[Dismiss]` secondary.
    - Test (StageRow failed variant): renders `[ErrorCode] message` inline + `▶ Show error chain` disclosure expanding to `<ul>` of `context: Vec<String>` (Phase 26 D-11 verbatim).
    - Test (StageRow duration format): 300ms → "0.3s"; 8200ms → "8.2s"; 74000ms → "1m 14s".
    - Test (SyncToast): rendering `<SyncToast message="Sync complete" />` produces `<div role="status" aria-live="polite" aria-atomic="true">Sync complete</div>`; auto-unmounts after 5000ms (use jest fake timers).
    - Test (a11y): axe-core scan of an active stepper passes with zero violations; cancelled-state stepper passes; SyncToast in the DOM tree does not introduce violations.
  </behavior>
  <action>
    1. Build `StageStepper.tsx` per UI-SPEC §StageStepper (lines 213-238 + PATTERNS §"Stepper outer container"):
       - Props:
         ```ts
         interface StageStepperProps {
           stages: StageState[]; // exactly 6 entries; order = SyncStage::ALL = [Reconcile, Discover, Consolidate, Distribute, Cleanup, Save]
           onCancel?: () => void;
           onDismiss?: () => void;
           onRetryFromStage?: (stage: SyncStage) => void;
           onRetryFailedItems?: () => void; // 27-05 wires this; pass undefined here
         }
         type StageStatus =
           | { kind: "pending" }
           | { kind: "active"; currentItem: string | null; current: number; total: number }
           | { kind: "complete"; durationMs: number; partialFailures: PartialFailure[] } // partialFailures always [] until 27-05
           | { kind: "failed"; durationMs: number; error: TomeError }
           | { kind: "cancelled" };
         interface StageState { stage: SyncStage; label: string; tooltip: string; status: StageStatus }
         ```
       - Outer container: `<section role="list" aria-label="Sync pipeline progress">`. Padding `--space-6` (24px). Gap `--space-3` (12px) between rows.
       - Trailing slot ABOVE the stepper: when any stage is active → render right-aligned `<Button variant="secondary" onPress={onCancel} ariaLabel="Cancel sync at next stage boundary">Cancel sync</Button>`. When all stages are non-active (terminal) → render `[Dismiss]` always + `[Retry from stage]` when onRetryFromStage is provided + `[Retry failed items]` when onRetryFailedItems is provided (27-05 wires the latter two for the partial-failure path; this plan only renders [Dismiss] + [Run sync] for cancellation per UI-SPEC §Terminal state cancelled, but the SUMMARY block ABOVE is owned by SyncView, not the stepper — see Action 4).
       - 2px vertical connector line in `--separator` between row icons: implement via a CSS pseudo-element on the container (`::before` positioned 12px from left, height = container height minus first+last row offsets).
       - A11y: live region wrapper `<div role="status" aria-live="polite">` announces state changes via React state side effects (e.g., when a stage transitions to complete, announce `"${stageName} stage complete"`).
    2. Build `StageRow.tsx` per UI-SPEC §StageRow:
       - Props: `interface StageRowProps { stage: SyncStage; label: string; status: StageStatus }`.
       - 40px row height. Three-column layout: icon (24px), label + optional subtitle (flexible), trailing (duration / "—" / "running…" / "cancelled"). When subtitle present (active + currentItem), use a 2-line layout per UI-SPEC §StageRow active variant.
       - Variant rendering:
         - **pending**: 16x16 outline circle icon (`--label-secondary`), label `--text-body` 13px / 400 `--label-secondary`, trailing "—" `--text-small` 12px / 400 `--label-secondary`.
         - **active**: small system spinner icon (`--accent`), label `--text-body` 13px / 600 `--label-primary`, trailing "running…" `--text-small` 12px / 400 `--label-secondary`. Below the label, indented 24px: `currentItem` (`--text-small` 12px / 400 `--label-secondary`, monospace when value starts with `git:` or contains a path separator; truncate-middle with CSS ellipsis) + inline progress bar `current / total` (`--accent` fill on `--bg-subtle` track). When `total === 0`: hide the bar (D-09 git-clone case); when `currentItem === null`: collapse the subtitle line.
         - **complete**: 16x16 ✓ icon (`--success`), label `--text-body` 13px / 400 `--label-primary`, trailing duration text via duration-format helper. If `partialFailures.length > 0` (27-05 will populate; render the chip even with empty array gracefully — render nothing when empty), render an amber `<Badge subtype="override">⚠ {K} issues</Badge>` to the right of duration; below the row's primary line, render the per-item `<FindingRow>` list. This plan's stepper renders the chip when `partialFailures.length > 0` but the array stays empty until 27-05 — design as "render-ready".
         - **failed**: 16x16 ! icon (`--danger`), label `--text-body` 13px / 600 `--danger`, trailing duration. Below the label, indented 24px: inline `[ErrorCode] message` + `▶ Show error chain` disclosure with `<ul>` of context entries (Phase 26 D-11 / FindingRow pattern reuse).
         - **cancelled**: 16x16 ⊘ icon (amber, `--badge-override-fg`), label `--text-body` 13px / 400 `--label-secondary`, trailing "cancelled" `--text-small` 12px / 400 amber.
       - Duration format helper: `formatDuration(ms: number): string` returning "0.3s" for <1000ms (1 decimal), "8.2s" for 1000-60000ms (1 decimal), "1m 14s" for >=60000ms (whole seconds). Place in `crates/tome-desktop/ui/src/lib/formatDuration.ts`.
       - A11y: `<div role="listitem" aria-label="${labelExpansion}">` per UI-SPEC §VoiceOver labels (entries for pending/active/complete/failed/cancelled).
    3. Build `SyncToast.tsx` per UI-SPEC §States §Terminal state + Pitfall 2 + PATTERNS §"ui/src/components/SyncToast.tsx":
       - Props: `interface SyncToastProps { message: string; onDismiss?: () => void; durationMs?: number }` (default durationMs = 5000).
       - Render: `<div role="status" aria-live="polite" aria-atomic="true" className={styles.toast}>{message}</div>`. Position via parent CSS (top-right per UI-SPEC; the toast owns its own visual; styling: semitransparent surface, fade-in 200ms / visible 3s / fade-out 200ms = 3.4s total but the spec mentions 5s timeout — use 5s for the unmount and 200ms transitions inside).
       - Lifecycle: `useEffect(() => { const t = setTimeout(() => { onDismiss?.(); }, durationMs); return () => clearTimeout(t); }, [durationMs, onDismiss]);`.
       - **No UNSTABLE_ToastRegion** — per Pitfall 2, hand-roll only.
       - **Only used for success and cancellation toasts** (D-06). Failures do NOT use SyncToast (D-18: stepper-persistence applies).
    4. Add unit tests for each component using Vitest + @testing-library/react. The duration-format and lifecycle tests use jest/vitest fake timers.
  </action>
  <verify>
    <automated>cd crates/tome-desktop/ui &amp;&amp; npm run typecheck &amp;&amp; npm run test -- --run StageStepper StageRow SyncToast formatDuration</automated>
  </verify>
  <done>
    `StageStepper` renders 6 rows with pending/active/complete/failed/cancelled variants + variant-specific trailing slots; `StageRow` matches UI-SPEC variant layouts including failed-inline + partial-issues badge; `SyncToast` is a hand-rolled role=status live region with 5s setTimeout (Pitfall 2); duration-format helper passes 3 boundary tests; all components pass a11y scans.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire useSync.cancel + useSync.dismiss handlers; mount StageStepper + SyncToast in SyncView (in-progress + terminal-cancelled + terminal-success branches)</name>
  <files>
    crates/tome-desktop/ui/src/hooks/useSync.ts,
    crates/tome-desktop/ui/src/views/SyncView.tsx,
    crates/tome-desktop/ui/src/views/SyncView.module.css,
    crates/tome-desktop/tests/a11y/axe.spec.ts
  </files>
  <read_first>
    crates/tome-desktop/ui/src/hooks/useSync.ts (after 27-02 — stages Map, isRunning, decisions, onDecisionChange),
    crates/tome-desktop/ui/src/views/SyncView.tsx (after 27-02 — idle / in-progress / terminal branches with placeholder stepper),
    crates/tome-desktop/ui/src/components/StageStepper.tsx (from Task 2 of this plan),
    crates/tome-desktop/ui/src/components/SyncToast.tsx (from Task 2),
    crates/tome-desktop/ui/src/bindings.ts (commands.cancelSync from 27-01),
    .planning/phases/27-sync-triage-ui/27-UI-SPEC.md §"Per-view Design — Sync section" (all 5 ASCII mockups: idle, in-progress, terminal partial-failure, terminal failed-with-retry, terminal cancelled) + §"Copywriting Contract" §"Terminal state",
    .planning/phases/27-sync-triage-ui/27-PATTERNS.md §"ui/src/views/SyncView.tsx" (3-state branching shape)
  </read_first>
  <behavior>
    - Test (cancel handler): clicking `[Cancel sync]` calls `useSync().cancel()` which invokes `commands.cancelSync()`; the next render shows the stage that was running as cancelled. (The Rust side flips the bit; the next ProgressEvent received reflects the cancelled state; we don't need to assert IPC round-trip here, just that the handler is bound).
    - Test (dismiss handler): clicking `[Dismiss]` calls `useSync().dismiss()` which resets `stages` to all-pending + `outcome` to null + `isRunning` to false; the view re-renders idle hero.
    - Test (success branch): after a clean SyncOutcome arrives, SyncView renders a `<SyncToast message="Sync complete" />` for 5 seconds AND auto-returns to idle (calls `dismiss()` after toast unmounts).
    - Test (cancelled branch): SyncView renders the stepper with cancelled icons + summary "Sync cancelled" heading + sub-line + [Run sync] + [Dismiss] buttons per UI-SPEC §Terminal cancelled. NO SyncToast (cancellation is rendered inline, not as a transient).
    - Test (a11y): axe scan of in-progress state passes; axe scan of cancelled terminal state passes.
  </behavior>
  <action>
    1. Extend `useSync`:
       - `cancel: useCallback(async () => { await commands.cancelSync(); }, [])` — fire-and-forget; the next sync_progress event from the Rust side will reflect the cancelled state.
       - `dismiss: useCallback(() => { setStages(initialPendingStages()); setOutcome(null); setIsRunning(false); }, [])` — purely client-side reset.
       - Add a derived `terminalKind: "success" | "cancelled" | "failed" | "partial" | null` computed from the stages Map + outcome (success: all stages complete && no partialFailures && outcome.is_ok; cancelled: any stage cancelled; failed: any stage failed; partial: all complete with at least one partialFailures.length > 0; null: still in progress or idle).
       - On successful outcome (terminalKind === "success" and isRunning just transitioned false): set a `showSuccessToast = true` flag in state; after the toast lifecycle completes (5s), call dismiss().
    2. Rewire `SyncView.tsx` terminal-state branches per UI-SPEC §Per-view Design + §Copywriting Contract:
       - **In-progress**: middle column = `<StageStepper stages={stagesArray} onCancel={cancel} />` + (after Reconcile completes) `<TriagePanel ... />`. Right column = `<TriageDetail ... />`. The cancel handler comes from `useSync()`.
       - **Terminal cancelled** (terminalKind === "cancelled"): render the stepper with cancelled variants + an above-stepper summary block: `<h1>Sync cancelled</h1>` + `<p>The library is in a consistent state. You can run sync again at any time.</p>` + `<div className={styles.actions}><Button variant="primary" onPress={start}>Run sync</Button><Button variant="secondary" onPress={dismiss}>Dismiss</Button></div>`. Summary heading uses `--text-subhead` 16px / 600 amber `--badge-override-fg`.
       - **Terminal success** (terminalKind === "success"): render `<SyncToast message="Sync complete" onDismiss={dismiss} />` AND immediately render the idle hero (the toast overlays; the idle hero is the underlying view). The toast handles its own 5s unmount; `dismiss()` resets state when called.
       - **Terminal failed** (terminalKind === "failed"): leave as a placeholder for 27-05 to fully wire. For this plan, render a minimal stepper with failed variant + a placeholder `<h1>Sync failed</h1>` + `<Button variant="secondary" onPress={dismiss}>Dismiss</Button>`. 27-05 will add [Retry from stage].
       - **Terminal partial-failure** (terminalKind === "partial"): also a 27-05 surface; render the stepper with complete + amber-badge variants + a placeholder summary + `<Button variant="secondary" onPress={dismiss}>Dismiss</Button>`.
    3. The Sidebar `syncInProgress` and `syncBadge` props (from 27-01/27-02): `syncInProgress = isRunning`; `syncBadge` = `{ kind: "pending", count: pendingDiffCount }` when pre-sync diff is non-empty; the failure-state badge is 27-05's job (set `syncBadge = { kind: "failures", count: K }` when terminal partial-failure has unresolved K items).
    4. Wire keyboard handlers from 27-01's `useMenuActions`: `⌘R` runs `start` (idle) / `cancel` (in-progress) / OR launches retry handler (terminal failed; 27-05). `⌘.` always calls `cancel` when in-progress (no-op otherwise). The keybindings already exist as global handlers from 27-01; verify they correctly route through useSync's start/cancel.
    5. Extend axe.spec.ts Sync-view block: add sub-tests "Sync in-progress a11y" + "Sync cancelled terminal a11y" that navigate to Sync, click [Run sync] (or programmatically set isRunning), run axe in each state, assert zero violations.
  </action>
  <verify>
    <automated>cd crates/tome-desktop/ui &amp;&amp; npm run typecheck &amp;&amp; npm run test -- --run SyncView useSync &amp;&amp; cd ../.. &amp;&amp; npx playwright test tests/a11y/axe.spec.ts -g "Sync"</automated>
  </verify>
  <done>
    `useSync.cancel` invokes `commands.cancelSync`; `useSync.dismiss` resets client state; `useSync.terminalKind` correctly classifies the outcome; `SyncView` mounts `StageStepper` in the in-progress + terminal-cancelled branches, renders the cancelled summary block per UI-SPEC §Terminal cancelled, renders `SyncToast` for success; axe scans of in-progress + cancelled terminal pass; keyboard `⌘R` and `⌘.` route correctly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React → Tauri IPC (`cancel_sync`) | Sync cancellation crosses webview → Rust |
| Domain `CancelToken::cancel()` → AtomicBool flip | Single-flag cancellation across the worker thread + IPC reactor |
| `tome::sync` stage boundaries → filesystem | Each stage's writes ordered against cancel checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04-01 | Repudiation | cancel_sync invoked maliciously by webview JS | accept | RESEARCH §"Known Threat Patterns": single-user local app; cancellation is by design always recoverable per SC#4; threat-model neutral |
| T-27-04-02 | Tampering | Partial state on abort exploited (e.g., to corrupt manifest mid-write) | mitigate | SC#4 invariant — verified end-to-end by Task 1's sync_cancel.rs integration test. The Save stage runs as a single atomic unit (manifest::save + lockfile::save use atomic temp+rename); cancel checks happen BEFORE any write, never mid-write. If Task 1's audit reveals an out-of-order write, the fix is part of this plan. |
| T-27-04-03 | DoS | Webview spams cancel_sync to interrupt every sync | accept | Sync is recoverable; cancellation is always safe; the user is the only actor with cancel-button access. Cosmetic UX issue at worst. |
| T-27-04-04 | Information Disclosure | StageRow `currentItem` subtitle leaks path or skill names to webview | accept | Same user owns both sides (paths are the user's own filesystem); no PII. Sanitization point is the sink in 27-01 (D-09 fold-in formatting); React renders as-is. |
| T-27-04-05 | DoS | StageStepper renders 6 rows with frequent state churn (1000+ progress events/sec) | mitigate | React batches state updates; the stepper renders only 6 rows (no virtualisation overhead). Phase 26 NF-01 perf budget is 60fps with 2000 skills; 6-row stepper is bounded. Optionally, debounce the per-stage subtitle updates to 30Hz if profiling shows churn. |
| T-27-04-SC | Tampering | npm/cargo new deps | accept | This plan adds ZERO new external packages. All components built from Phase 26 stack + Task 2 of 27-03's `similar` dep already audited and approved. |
</threat_model>

<verification>
- `cargo test -p tome --test sync_cancel` — 4 integration tests pass.
- `cargo test -p tome` — full CLI + domain test suite passes (no regressions from cancel-ordering fixes if any landed in Task 1).
- `cargo clippy -p tome -- -D warnings` clean.
- `cd crates/tome-desktop/ui && npm run typecheck && npm run test` — Vitest unit tests pass for StageStepper, StageRow, SyncToast, formatDuration, useSync, SyncView.
- `npx playwright test tests/a11y/axe.spec.ts -g "Sync"` — axe scans of idle / in-progress / cancelled all pass.
- Manual smoke: launch `cargo tauri dev`, navigate to Sync, click [Run sync], observe stepper advancing through stages with subtitles; click [Cancel sync] during Consolidate, observe stepper transitioning to cancelled state with the summary block; click [Dismiss], observe return to idle.
</verification>

<success_criteria>
- ROADMAP Phase 27 SC#4 met: Sync runs cancellable; cancellation leaves library state consistent — proven by `crates/tome/tests/sync_cancel.rs`.
- StageStepper + StageRow render every variant per UI-SPEC §StageStepper / §StageRow.
- SyncToast is hand-rolled (`role="status" aria-live="polite"` + 5s setTimeout) per Pitfall 2.
- Cancelled-state summary block matches UI-SPEC §Terminal cancelled copywriting verbatim.
- `bindings.ts` unchanged from 27-03 (this plan adds no new commands or events; the cancel_sync command lives in 27-01).
- axe-core scans clean for all three Sync states (idle, in-progress, cancelled).
</success_criteria>

<output>
Create `.planning/phases/27-sync-triage-ui/27-04-SUMMARY.md` when done.
</output>
