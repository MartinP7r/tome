---
phase: 27-sync-triage-ui
plan: 05
type: execute
wave: 4
depends_on:
  - 27-04
files_modified:
  - crates/tome/src/lib.rs
  - crates/tome/src/sync_outcome.rs
  - crates/tome-desktop/src/commands.rs
  - crates/tome-desktop/src/lib.rs
  - crates/tome-desktop/ui/src/hooks/useSync.ts
  - crates/tome-desktop/ui/src/views/SyncView.tsx
  - crates/tome-desktop/ui/src/components/StageStepper.tsx
  - crates/tome-desktop/ui/src/components/StageRow.tsx
  - crates/tome-desktop/ui/src/components/Sidebar.tsx
  - crates/tome-desktop/ui/src/bindings.ts
  - crates/tome-desktop/tests/a11y/axe.spec.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - SYNC-05
tags:
  - rust
  - ipc-bindings
  - retry
  - partial-failure
  - safe-01
  - finding-row

must_haves:
  truths:
    - "start_sync now returns a SyncOutcome wrapping struct: { result: Result<(), TomeError>, retry_from: Option<SyncStage>, partial_failures: Vec<PartialFailure> } (RESEARCH §3 recommendation + UI-SPEC contract)."
    - "On stage failure with a recoverable retry_from, the terminal stepper renders [Retry from <stageName>] primary + [Dismiss] secondary; on stage failure with retry_from = None, only [Dismiss] renders (D-19)."
    - "Partial-failure rendering inside a successful stage: the stage icon stays ✓, an amber [⚠ K issues] badge surfaces the count, the row expands by default to a per-operation FindingRow list with [ErrorCode] message + Show error chain disclosure (D-20)."
    - "Successful-with-partials terminal renders 'Sync complete with K issues' heading + sub-line + [Retry failed items] primary + [Dismiss] secondary."
    - "Failed terminal renders 'Sync failed' heading + sub-line + [Retry from <stage>] (if retry_from is Some) + [Dismiss]."
    - "The Sidebar Sync NavItem renders the post-sync failure badge variant (danger fill) with count = unresolved partial-failures after Dismiss / retry attempts (D-20)."
    - "Retry-from-stage safety: the domain owns the retry_from computation. Distribute failure on partial manifest sets retry_from = Some(Reconcile); Consolidate failure sets retry_from = Some(Discover); non-recoverable sets retry_from = None (RESEARCH §SyncOutcome shape decision)."
  artifacts:
    - path: "crates/tome/src/sync_outcome.rs"
      provides: "SyncOutcome + PartialFailure + retry_from computation"
      contains: "SyncOutcome"
    - path: "crates/tome-desktop/src/commands.rs"
      provides: "start_sync return type changed to SyncOutcome; retry_sync_from command for stage-resumable retry"
      exports: ["start_sync", "retry_sync_from", "retry_failed_items"]
    - path: "crates/tome-desktop/ui/src/components/StageRow.tsx"
      provides: "partialFailures.length > 0 surfacing: amber [⚠ K issues] badge + auto-expanded FindingRow list + [Retry failed items] inline action"
    - path: "crates/tome-desktop/ui/src/components/StageStepper.tsx"
      provides: "Terminal state with [Retry from stage] / [Retry failed items] / [Dismiss] action triplet wired"
  key_links:
    - from: "crates/tome-desktop/src/commands.rs::start_sync"
      to: "crates/tome/src/sync_outcome.rs::SyncOutcome::from_result"
      via: "wraps Result<SyncReport, TomeError> into SyncOutcome with retry_from + partial_failures populated"
      pattern: "SyncOutcome::from_result"
    - from: "crates/tome-desktop/ui/src/views/SyncView.tsx"
      to: "useSync().retryFromStage"
      via: "[Retry from stage] Button onPress calls useSync().retryFromStage(stage) which invokes commands.retrySyncFrom(stage)"
      pattern: "retryFromStage"
    - from: "crates/tome-desktop/ui/src/components/Sidebar.tsx"
      to: "useSync().unresolvedFailureCount"
      via: "syncBadge kind:failures count:K renders danger fill when unresolved >0"
      pattern: "syncBadge"
---

<objective>
Ship SYNC-05: per-stage failure summary + retry. Wrap `start_sync`'s return type in `SyncOutcome { result: Result<(), TomeError>, retry_from: Option<SyncStage>, partial_failures: Vec<PartialFailure> }` (RESEARCH §3 wrapping-struct recommendation; UI-SPEC contract); promote the existing SAFE-01 partial-failure aggregation in `SyncReport` (`distribution_cleanup_failures`, `install_failures` per RESEARCH SYNC-05 row) to the IPC boundary; compute `retry_from` server-side per D-19's safety rule (Distribute-failure-on-partial-manifest = retry_from Reconcile; Consolidate-failure = retry_from Discover; non-recoverable = None); add `retry_sync_from(stage)` and `retry_failed_items(failures)` Tauri commands; wire the terminal-state action triplet ([Retry from stage] / [Retry failed items] / [Dismiss]) in StageStepper + SyncView; render partial-failure aggregation inside completed StageRows with the amber [⚠ K issues] badge + auto-expanded FindingRow list (D-20); wire the post-sync failure-badge Sidebar variant.

Purpose: closes the final SYNC requirement (SC#5). Honors Phase 26 D-11 (failures must never be silently swallowed) and the SAFE-01 K-failures semantics shipped in v0.8.

Output: 1 new domain module (sync_outcome.rs); modified start_sync command (signature change); 2 new Tauri commands (retry_sync_from, retry_failed_items); extended StageRow + StageStepper variants; SyncView terminal-failed + terminal-partial branches fully wired; Sidebar failure-badge variant active.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-sync-triage-ui/27-CONTEXT.md
@.planning/phases/27-sync-triage-ui/27-RESEARCH.md
@.planning/phases/27-sync-triage-ui/27-PATTERNS.md
@.planning/phases/27-sync-triage-ui/27-UI-SPEC.md
@.planning/phases/27-sync-triage-ui/27-01-PLAN.md
@.planning/phases/27-sync-triage-ui/27-02-PLAN.md
@.planning/phases/27-sync-triage-ui/27-03-PLAN.md
@.planning/phases/27-sync-triage-ui/27-04-PLAN.md
@crates/tome/src/lib.rs
@crates/tome/src/progress.rs
@crates/tome-desktop/src/commands.rs
@crates/tome-desktop/src/error.rs
@crates/tome-desktop/ui/src/hooks/useSync.ts
@crates/tome-desktop/ui/src/views/SyncView.tsx
@crates/tome-desktop/ui/src/components/StageStepper.tsx
@crates/tome-desktop/ui/src/components/StageRow.tsx
@crates/tome-desktop/ui/src/components/FindingRow.tsx
@crates/tome-desktop/ui/src/components/Sidebar.tsx

<interfaces>
From crates/tome/src/lib.rs (existing):
- SyncReport struct already contains partial-failure aggregations: distribution_cleanup_failures (Vec), install_failures (Vec) — verified RESEARCH SYNC-05 row lines 1762/2142/2210

From crates/tome-desktop/src/error.rs:
- TomeError with ErrorCode enum (Validation, NotFound, Permission, Conflict, Git, Io, Internal); From<anyhow::Error> classification
- ErrorCode::ALL + sentinel + length-pin trio (POLISH-04)

From crates/tome/src/progress.rs:
- SyncStage::ALL = [Reconcile, Discover, Consolidate, Distribute, Cleanup, Save]

From crates/tome-desktop/ui/src/components/FindingRow.tsx:
- Phase 26 D-11 contract: inline [ErrorCode] message + ▶ Show error chain disclosure (Vec<String> context bullets)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tome::sync_outcome module with SyncOutcome + PartialFailure types + retry_from computation</name>
  <files>
    crates/tome/src/sync_outcome.rs,
    crates/tome/src/lib.rs
  </files>
  <read_first>
    crates/tome/src/lib.rs (lines 1490-2260 — full sync pipeline; identify SyncReport struct + the lines where distribution_cleanup_failures and install_failures get populated; understand WHERE in the pipeline state determines whether retry is safe),
    crates/tome/src/progress.rs (SyncStage enum + ALL constant + POLISH-04 trio),
    crates/tome-desktop/src/error.rs (TomeError + ErrorCode shape — the IPC boundary the wrapping struct surfaces error info through),
    .planning/phases/27-sync-triage-ui/27-CONTEXT.md §"D-19" (retry strategy domain-driven; safety rules in Rust) + §"D-20" (partial-failure rendering: stage icon stays ✓, amber badge, [Retry failed items]),
    .planning/phases/27-sync-triage-ui/27-RESEARCH.md §"Summary §3" (SyncOutcome wrapping struct recommendation) + §"Architectural Responsibility Map / Retry-from-stage gating" row + §"Architectural Responsibility Map / Partial-failure aggregation" row,
    .planning/phases/27-sync-triage-ui/27-PATTERNS.md §"crates/tome-desktop/src/commands.rs::SyncOutcome type (new)" + §"crates/tome-desktop/src/commands.rs::PartialFailure type (new)",
    .planning/phases/27-sync-triage-ui/27-UI-SPEC.md §"Terminal state" (5 variants: all-clear / cancelled / failed-with-retry / failed-no-retry / partial-failure) + §"Open Items §1" (UI renders against wrapping-struct shape)
  </read_first>
  <behavior>
    - Test (SyncOutcome::from_result with Ok and no partials): an Ok(SyncReport) with no distribution_cleanup_failures and no install_failures → SyncOutcome { result: Ok(()), retry_from: None, partial_failures: vec![] }.
    - Test (SyncOutcome::from_result with Ok + partials): an Ok(SyncReport) with 2 distribution_cleanup_failures → SyncOutcome { result: Ok(()), retry_from: None, partial_failures: <2 entries with stage = Distribute or Cleanup depending on origin field> }.
    - Test (SyncOutcome::from_result with Err at Consolidate stage): an Err with the domain context indicating Consolidate failure → SyncOutcome { result: Err(TomeError), retry_from: Some(SyncStage::Discover), partial_failures: vec![] }. (Per D-19 safety rule: Consolidate failure means we need to re-discover.)
    - Test (SyncOutcome::from_result with Err at Distribute stage): Err with Distribute failure → SyncOutcome { result: Err, retry_from: Some(SyncStage::Reconcile), partial_failures: vec![] }. (Per D-19: rerunning Distribute on partial manifest is NOT safe; back up to drift detection.)
    - Test (SyncOutcome::from_result with Err at Save stage): Err with Save failure (e.g., manifest write permission) → SyncOutcome { result: Err, retry_from: None, partial_failures: vec![] }. (Save errors are typically non-recoverable in-place — disk full, permission denied; user must intervene.)
    - Test (PartialFailure shape): each PartialFailure carries `stage: SyncStage`, `skill: Option<SkillName>` (None if it's a non-skill-bound failure), `error: TomeError`, `operation: PartialFailureOp` where PartialFailureOp = Distribution | Cleanup | Install | Other.
  </behavior>
  <action>
    1. Create `crates/tome/src/sync_outcome.rs` and register in `lib.rs` via `pub mod sync_outcome;`.
    2. Define:
       ```rust
       pub struct SyncOutcome {
           pub result: Result<(), TomeErrorWire>,           // public-shape Result; the wire-type is the WIRE alias, see step 3
           pub retry_from: Option<SyncStage>,
           pub partial_failures: Vec<PartialFailure>,
       }

       pub struct PartialFailure {
           pub stage: SyncStage,
           pub skill: Option<SkillName>,
           pub operation: PartialFailureOp,
           pub error: TomeErrorWire,                         // same WIRE alias
       }

       pub enum PartialFailureOp { Distribution, Cleanup, Install, Other }
       ```
       — derive `serde::Serialize` + `cfg_attr(feature = "bindings", derive(specta::Type))` on all three. Add `PartialFailureOp::ALL` + sentinel + length-pin trio per POLISH-04.
       — **TomeErrorWire**: the boundary's `TomeError` lives in `crates/tome-desktop/src/error.rs` and is not visible to `crates/tome`. Two options: (a) define a parallel WIRE type in `sync_outcome.rs` that mirrors the same shape (code: u8 or stringified ErrorCode + message: String + context: Vec<String>); (b) leave the domain-side type as `anyhow::Error` and convert at the boundary (`SyncOutcome` in the domain owns `Result<(), anyhow::Error>`; the boundary's `commands.rs` converts to `SyncOutcomeWire` for IPC). **Recommendation: option (b)** — keep the domain anyhow-shaped; let `crates/tome-desktop/src/commands.rs` define a parallel `SyncOutcomeWire` that converts via `From<&SyncOutcome>` using the existing `TomeError::from(anyhow::Error)` classifier. This keeps `crates/tome` free of the IPC concern.
       — Restructure step 2's types: domain-side has `SyncOutcome { result: Result<(), anyhow::Error>, retry_from, partial_failures: Vec<PartialFailure> }` where `PartialFailure.error: anyhow::Error`; the boundary defines a mirror in `crates/tome-desktop/src/commands.rs` (Task 2).
    3. Add `pub fn from_sync_result(result: Result<SyncReport, anyhow::Error>, failed_stage: Option<SyncStage>) -> SyncOutcome`:
       - On `Ok(report)`: pull `report.distribution_cleanup_failures` + `report.install_failures` into `Vec<PartialFailure>` with appropriate `stage` + `operation` mappings. `retry_from = None`. `result = Ok(())`.
       - On `Err(error)`: compute `retry_from` per D-19's safety rules using `failed_stage`:
         - `failed_stage == Some(Reconcile) | Some(Discover)`: retry from `Some(Reconcile)` (the cheapest restart point).
         - `failed_stage == Some(Consolidate)`: retry from `Some(Discover)` (Discover is read-only so safe; Consolidate may have partially written to library — re-discovery is safe because library content is content-hashed).
         - `failed_stage == Some(Distribute)`: retry from `Some(Reconcile)` — per RESEARCH §SC#5 safety rule: "rerunning distribute on a partial manifest is not OK"; the safe restart point is BEFORE drift detection.
         - `failed_stage == Some(Cleanup)`: retry from `Some(Reconcile)` (cleanup failures often reveal external interference; re-evaluation needed).
         - `failed_stage == Some(Save)` or `failed_stage == None`: `retry_from = None`. Save failures (manifest/lockfile write) are non-recoverable in-place; the user must clear the underlying issue (disk full, permission) before re-running. Unknown-stage failures default to safe (None).
         - `partial_failures = vec![]` on Err — the full failure is in `result.err()`.
    4. Update `tome::sync` (`crates/tome/src/lib.rs`) to track the `failed_stage` during execution. Simplest approach: wrap the body in a state-machine where each stage's call updates a `current_stage: Cell<SyncStage>` (or a plain mutable variable since sync is &mut-free); on bail, capture `failed_stage = Some(current_stage)`. Return a `Result<SyncOutcome, _>` OR have the caller (commands.rs) construct it. **Cleaner**: change `tome::sync`'s return type from `Result<SyncReport>` to `SyncOutcome` directly — this is a CLI-visible change. **Verify CLI doesn't regress**: `crates/tome/src/lib.rs::cmd_sync` (the CLI presenter) must adapt to receive `SyncOutcome` and re-derive `SyncReport`-equivalent counters for its existing text rendering. Read `cmd_sync` first; if the change ripples too widely, alternatively expose `pub fn sync_with_outcome(config, paths, opts, sink, cancel) -> SyncOutcome` as a sibling to the existing `sync(...)`. **Recommendation: add a sibling `sync_with_outcome` that wraps `sync`**; the existing `sync` returns `Result<SyncReport>` unchanged for CLI back-compat; the new `sync_with_outcome` invokes `sync` internally, captures any error, and packages the result via `SyncOutcome::from_sync_result`. This zero-touches the CLI.
       - Implementation: `pub fn sync_with_outcome(config: &Config, paths: &TomePaths, options: SyncOptions, sink: &dyn ProgressSink, cancel: &CancelToken) -> SyncOutcome { let mut failed_stage: Option<SyncStage> = None; let result = sync(config, paths, options, sink, cancel).map_err(|e| { failed_stage = current_failed_stage_from_sink(sink); e }); SyncOutcome::from_sync_result(result, failed_stage) }`. The `failed_stage` derivation reads the RecordingSink-style event log to find the last `SyncStageStarted` without a matching `SyncStageFinished` — wrap `sink` in a thin tracker for this. Alternatively: have `sync()` itself capture and return `failed_stage` via a side-channel (e.g., a `&Cell<Option<SyncStage>>` argument). **Choose the cleaner of the two**; Claude picks the side-channel approach if `sync()`'s signature is amenable; otherwise the sink-wrapper trick. Document the choice.
    5. Add unit tests in `sync_outcome.rs` mod tests covering the 5 retry_from behaviors above + 1 partial-failure surfacing test. Use synthetic SyncReports built directly (not via running sync) so the test is fast and deterministic.
  </action>
  <verify>
    <automated>cargo test -p tome --lib sync_outcome::tests &amp;&amp; cargo test -p tome &amp;&amp; cargo clippy -p tome -- -D warnings</automated>
  </verify>
  <done>
    `crates/tome/src/sync_outcome.rs` exists with `SyncOutcome` + `PartialFailure` + `PartialFailureOp` + `from_sync_result` + POLISH-04 trio for `PartialFailureOp`; `sync_with_outcome` wraps `sync` without breaking the CLI; 6 unit tests pass; full `crates/tome` test suite passes (no CLI regression); clippy clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Change start_sync return to SyncOutcomeWire; add retry_sync_from + retry_failed_items commands; regenerate bindings</name>
  <files>
    crates/tome-desktop/src/commands.rs,
    crates/tome-desktop/src/lib.rs,
    crates/tome-desktop/ui/src/bindings.ts
  </files>
  <read_first>
    crates/tome-desktop/src/commands.rs (after 27-01..27-04 — start_sync currently returns Result<SyncReport, TomeError> from 27-01; change to Result<SyncOutcomeWire, TomeError>),
    crates/tome-desktop/src/error.rs (TomeError shape — used as the wire type for errors in SyncOutcomeWire and PartialFailureWire),
    crates/tome/src/sync_outcome.rs (Task 1 output — domain types to mirror at the boundary),
    crates/tome/src/lib.rs (sync_with_outcome from Task 1 — start_sync now calls this instead of sync),
    .planning/phases/27-sync-triage-ui/27-CONTEXT.md §"Integration Points" (5 new commands list — retry_sync_from optional per UI-SPEC Open Items: planner can fold into start_sync's from: Option<SyncStage> arg OR make it a separate command),
    .planning/phases/27-sync-triage-ui/27-UI-SPEC.md §"Terminal state" + §"Copywriting" §"Primary CTA (terminal: failed with retry)" + §"Primary CTA (terminal: partial failure)"
  </read_first>
  <behavior>
    - Test (start_sync wire shape): start_sync invocation returns a SyncOutcomeWire serialized as { result: { tag: "ok" } | { tag: "err", value: TomeError }, retry_from: SyncStage | null, partial_failures: PartialFailureWire[] } per tagged-Result convention used elsewhere in the codebase (verify against existing bindings — if Phase 25's Result narrowing emits a different tag shape, MATCH IT to keep the React-side S-8 pattern consistent).
    - Test (retry_sync_from accepts stage): commands.retrySyncFrom("discover") returns Ok(SyncOutcomeWire); under the hood calls sync_with_outcome with options that signal "start from Discover" (the domain may implement this by skipping Reconcile when start_stage = Discover; verify with sync_with_outcome's signature).
    - Test (retry_failed_items): commands.retryFailedItems(failures: Vec<PartialFailureWire>) returns Ok(SyncOutcomeWire) where the sync invocation only retries the operations listed; partial failures that don't match the input pass through to the new outcome.
    - Test (bindings regen): cargo run -p tome-desktop --bin gen-bindings produces a clean diff including SyncOutcome, PartialFailure, PartialFailureOp; the start_sync return type is updated.
  </behavior>
  <action>
    1. Define WIRE types in `crates/tome-desktop/src/commands.rs` (or a `sync_types.rs` module if 27-02 created one):
       - `pub struct SyncOutcomeWire { pub result: ResultWire<(), TomeError>, pub retry_from: Option<SyncStage>, pub partial_failures: Vec<PartialFailureWire> }`
       - `pub struct PartialFailureWire { pub stage: SyncStage, pub skill: Option<SkillName>, pub operation: PartialFailureOp, pub error: TomeError }`
       - `ResultWire<T, E>` is whatever the project already uses for cross-IPC results — verify by looking at the existing bindings.ts for the `Result<StatusReport, TomeError>` shape that Phase 25 introduced. If the project uses `tauri-specta`'s built-in result projection, use that.
       — All derive `serde::Serialize + specta::Type`.
    2. Add `impl From<tome::sync_outcome::SyncOutcome> for SyncOutcomeWire` that calls `TomeError::from` for each `anyhow::Error` field. Map `PartialFailure` → `PartialFailureWire` with the same conversion.
    3. Change `start_sync` (from 27-01) to:
       - Return type `Result<SyncOutcomeWire, TomeError>`.
       - Body: replace `tome::sync(...)` with `tome::sync_with_outcome(...)`. The `JoinError` from `spawn_blocking` becomes the outer Err per Pitfall 5; convert the `SyncOutcome` returned from `sync_with_outcome` into `SyncOutcomeWire` via the From impl, return `Ok(wire)`.
       - The existing T-27-01-07 mitigation (double-fire guard) stays: bail with `TomeError { code: Conflict, message: "sync already in progress" }` if the SyncState mutex already has a Some(token).
    4. Add `retry_sync_from(app, state, stage: SyncStage) -> Result<SyncOutcomeWire, TomeError>` as a parallel `async fn` `#[tauri::command] #[specta::specta]`. Body: same as start_sync but the `SyncOptions` builder includes a `start_stage: Option<SyncStage>` field passed to `sync_with_outcome`. The domain side may need a `SyncOptions::with_start_stage(SyncStage)` extension — verify by reading `crates/tome/src/lib.rs::SyncOptions`; if no such field exists, add it as an `Option<SyncStage>` and gate Reconcile/Discover etc. in `tome::sync` to skip stages BEFORE the requested start_stage. This is a small additive domain change; document it in the summary.
    5. Add `retry_failed_items(app, state, failures: Vec<PartialFailureWire>) -> Result<SyncOutcomeWire, TomeError>` as a parallel async command. The retry-failed scope per D-20 (Claude's discretion): re-run only the specific per-operation failures within their stage, not the full pipeline. Implementation: pass `failures` to a new `tome::retry_partial_failures(failures, sink, cancel)` domain helper that loops over each failure and re-invokes the relevant operation (e.g., for `PartialFailureOp::Distribution` it re-symlinks the failed skill; for `Cleanup` it re-attempts the cleanup; for `Install` it re-invokes the adapter). The helper returns a `SyncOutcome` reflecting any remaining failures.
       - **Scope reduction guard**: if implementing the per-operation retry helper would consume >40% of a task's context budget (PATTERNS.md doesn't have an analog for this — it's net-new domain code), STOP and report a PHASE SPLIT recommendation to the planner. The current research suggests this is feasible because Distribute/Cleanup operations are already isolated per-skill (each loop iteration is independent); Install is per-plugin. Plan accordingly.
    6. Register all three commands (start_sync now with new return type, retry_sync_from, retry_failed_items) in `make_builder()`. Run `cargo run -p tome-desktop --bin gen-bindings` and commit the updated bindings.ts.
  </action>
  <verify>
    <automated>cargo test -p tome-desktop --lib commands -- start_sync retry_sync_from retry_failed_items &amp;&amp; cargo run -p tome-desktop --bin gen-bindings &amp;&amp; git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts &amp;&amp; cargo clippy -p tome-desktop --features bindings -- -D warnings</automated>
  </verify>
  <done>
    `start_sync` returns `SyncOutcomeWire`; `retry_sync_from(stage)` + `retry_failed_items(failures)` commands exist; SyncOptions exposes `start_stage`; `tome::retry_partial_failures` helper exists (or PHASE SPLIT recommended if scope exceeds budget); bindings.ts regenerated; clippy clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire useSync terminal-failed + partial-failure paths; render StageRow partial-failure variant; SyncView summary blocks; Sidebar failure-badge variant</name>
  <files>
    crates/tome-desktop/ui/src/hooks/useSync.ts,
    crates/tome-desktop/ui/src/views/SyncView.tsx,
    crates/tome-desktop/ui/src/views/SyncView.module.css,
    crates/tome-desktop/ui/src/components/StageStepper.tsx,
    crates/tome-desktop/ui/src/components/StageRow.tsx,
    crates/tome-desktop/ui/src/components/StageRow.module.css,
    crates/tome-desktop/ui/src/components/Sidebar.tsx,
    crates/tome-desktop/tests/a11y/axe.spec.ts,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    crates/tome-desktop/ui/src/hooks/useSync.ts (after 27-02 + 27-04 — stages Map, dismiss, cancel, terminalKind),
    crates/tome-desktop/ui/src/views/SyncView.tsx (after 27-04 — placeholder terminal-failed + terminal-partial branches need full wiring),
    crates/tome-desktop/ui/src/components/StageStepper.tsx (after 27-04 — accept onRetryFromStage + onRetryFailedItems props; render terminal action buttons),
    crates/tome-desktop/ui/src/components/StageRow.tsx (after 27-04 — partial-failure variant placeholder; wire FindingRow list + [Retry failed items] inline action),
    crates/tome-desktop/ui/src/components/FindingRow.tsx (Phase 26 D-11 — used verbatim for each PartialFailure),
    crates/tome-desktop/ui/src/components/Sidebar.tsx (from 27-01 — syncBadge prop accepts kind: "pending" | "failures" | "none"; failure variant uses danger fill),
    crates/tome-desktop/ui/src/components/Badge.tsx (Badge subtype override = amber [⚠ K issues] chip),
    .planning/phases/27-sync-triage-ui/27-UI-SPEC.md §"Per-view Design — Sync section" (Terminal failed with retry + Terminal partial-failure ASCII mockups) + §"Copywriting Contract" §"Terminal state" + §"Sidebar updated NavItem" §"badge variants",
    .planning/phases/27-sync-triage-ui/27-PATTERNS.md §"ui/src/components/StageRow.tsx" §"complete variant with partialFailures" + §"Sidebar.tsx" §"badge logic two-meaning rendering"
  </read_first>
  <behavior>
    - Test (useSync terminal-failed): given a SyncOutcome { result: Err, retry_from: Some("discover"), partial_failures: [] }, useSync.terminalKind === "failed" + useSync.retryFromStage is callable with "discover".
    - Test (useSync terminal-partial): given a SyncOutcome { result: Ok, retry_from: None, partial_failures: [{stage: Distribute, skill: foo, ...}, ...] }, useSync.terminalKind === "partial"; useSync.retryFailedItems(allFailures) is callable.
    - Test (useSync unresolvedFailureCount): computed as `terminalKind === "partial" ? partial_failures.length : terminalKind === "failed" ? 1 : 0`. After Dismiss, the count remains until a successful retry clears it (i.e., the Sidebar failure badge persists across Dismiss); reset on the next sync_with_outcome's success.
    - Test (StageRow partial-failure variant): given a complete StageStatus with partialFailures.length > 0, the row renders the amber [⚠ K issues] badge AND auto-expands a FindingRow list below the primary line AND renders an inline [Retry failed items] action below the FindingRow list (per UI-SPEC StageRow complete variant).
    - Test (SyncView terminal-failed with retry_from): summary block reads "Sync failed" heading (text-subhead 16px / 600 --danger), sub-line "[ErrorCode]: message" (text-body 13px / 400 --label-secondary), [Retry from Discover] primary + [Dismiss] secondary.
    - Test (SyncView terminal-failed without retry_from): only [Dismiss] secondary renders (D-19 invariant).
    - Test (SyncView terminal-partial): "Sync complete with K issues" heading + "Library and lockfile are saved. K individual operations failed." sub-line + [Retry failed items] + [Dismiss].
    - Test (Sidebar failure-badge): when useSync.unresolvedFailureCount > 0 AND useSync.pendingDiffCount === 0, the Sidebar Sync NavItem renders the danger-fill badge with the count; the aria-label matches UI-SPEC §VoiceOver labels post-sync-failure-badge.
    - Test (a11y): axe scans of all 3 new terminal states (failed-with-retry, failed-no-retry, partial-failure) clean.
    - Test (REQUIREMENTS update): grep for SYNC-05 + SYNC-04 + SYNC-03 + SYNC-02 + SYNC-01 in REQUIREMENTS.md confirms all 5 are marked complete.
  </behavior>
  <action>
    1. Extend `useSync`:
       - Handler `retryFromStage(stage)`: `setIsRunning(true); const res = await commands.retrySyncFrom(stage); setIsRunning(false); setOutcome(res);`
       - Handler `retryFailedItems()`: `const failures = outcome?.partial_failures ?? []; setIsRunning(true); const res = await commands.retryFailedItems(failures); setIsRunning(false); setOutcome(res);`
       - Derived `unresolvedFailureCount`: as described in the behavior test.
       - The `terminalKind` from 27-04 expands: when outcome is a SyncOutcomeWire, classify as "success" (result.tag === "ok" && partial_failures.length === 0), "partial" (result.tag === "ok" && partial_failures.length > 0), "failed" (result.tag === "err"). The 27-04 `cancelled` classification stays — derived from the stages Map (any cancelled stage → terminalKind === "cancelled" regardless of outcome shape).
       - On a successful retry that clears partial_failures, `setOutcome(null); dismiss();` to auto-return to idle.
    2. Wire `SyncView.tsx` terminal-failed + terminal-partial branches per UI-SPEC §Per-view Design + Copywriting:
       - **Terminal failed**: summary block above stepper renders `<h1>Sync failed</h1>` + `<p>[${errorCode}]: ${errorMessage}</p>`. Action triplet: when retry_from is Some, render `<Button variant="primary" onPress={() => retryFromStage(retry_from)}>Retry from {stageLabel(retry_from)}</Button>` + `<Button variant="secondary" onPress={dismiss}>Dismiss</Button>`. When retry_from is None, render only `<Button variant="secondary" onPress={dismiss}>Dismiss</Button>`. Stepper renders failed stage variant + cancelled (downstream) variants.
       - **Terminal partial**: summary block renders `<h1>Sync complete with {K} issues</h1>` + `<p>Library and lockfile are saved. {K} individual operations failed.</p>`. Action triplet: `<Button variant="primary" onPress={retryFailedItems}>Retry failed items</Button>` + `<Button variant="secondary" onPress={dismiss}>Dismiss</Button>`. Stepper renders all stages complete with the affected stage(s) carrying the [⚠ K issues] badge + auto-expanded FindingRow list.
       - Pass the appropriate `onRetryFromStage` / `onRetryFailedItems` / `onDismiss` props down to StageStepper so the stepper's trailing slot also exposes the actions (the summary block above is the user's primary affordance; the stepper's trailing slot is a redundant convenience per UI-SPEC).
    3. Wire `StageRow.tsx` partial-failure variant (the 27-04 placeholder):
       - When `status.kind === "complete" && status.partialFailures.length > 0`: render `<Badge subtype="override">⚠ {K} issues</Badge>` to the right of duration; below the primary line, render `<div className={styles.partialFailures}>{status.partialFailures.map(pf => <FindingRow finding={partialFailureAsFinding(pf)} />)}</div>`. The `partialFailureAsFinding` adapter shapes `PartialFailureWire` to the `Finding` prop FindingRow accepts (title = `${stageName} failed for ${pf.skill ?? "..."}` , description = `${pf.error.code}: ${pf.error.message}`, error chain = `pf.error.context`).
       - Below the FindingRow list, render a single inline `<Button variant="small" onPress={onRetryFailedItems}>Retry failed items</Button>` — only when `onRetryFailedItems` is provided.
       - Add a prop `onRetryFailedItems?: () => void` to `StageRowProps`; thread from StageStepper.
    4. Wire `StageStepper.tsx` terminal action triplet:
       - Above the stepper, render the action buttons based on which props are present:
         - `onCancel` only (active state) → `[Cancel sync]` only.
         - `onDismiss` always (terminal state) → `[Dismiss]`.
         - `onRetryFromStage` (terminal failed with retry_from = Some) → `[Retry from {stageLabel}]` BEFORE [Dismiss].
         - `onRetryFailedItems` (terminal partial-failure) → `[Retry failed items]` BEFORE [Dismiss].
       - The stepper does NOT own the summary heading + sub-line — those live in SyncView (Action 2 above).
    5. Wire `Sidebar.tsx` failure-badge variant (Phase 27-01 added the slot; Phase 27-05 activates the failure side):
       - `syncBadge.kind === "failures"` → render the danger-fill badge with the count; aria-label per UI-SPEC §VoiceOver labels: `Sync, Sync section, ${count} unresolved sync failures`.
       - In `SyncView.tsx`, pass `syncBadge` to Sidebar derived from useSync: when `unresolvedFailureCount > 0 && pendingDiffCount === 0`, `syncBadge = { kind: "failures", count: unresolvedFailureCount }`; when `pendingDiffCount > 0`, `syncBadge = { kind: "pending", count: pendingDiffCount }` (pre-sync takes priority over post-sync because pre-sync clears first per UI-SPEC §Sidebar mutually-exclusive rule); when both zero, `syncBadge = { kind: "none", count: 0 }`.
    6. Flip `.planning/REQUIREMENTS.md` SYNC-01..05 markers to complete (or the project's equivalent done marker). Add a one-line traceability note per requirement pointing at the plan that closed it (`SYNC-01` → 27-01; `SYNC-02` → 27-02; `SYNC-03` → 27-03; `SYNC-04` → 27-04; `SYNC-05` → 27-05).
    7. Extend axe.spec.ts Sync-view block with sub-tests "Sync terminal-failed a11y", "Sync terminal-partial a11y", "Sync terminal-failed-no-retry a11y" navigating to each state (use stubbed useSync state via testing harness if Playwright can't trigger a real failed sync; the axe scan only needs the rendered DOM).
  </action>
  <verify>
    <automated>cd crates/tome-desktop/ui &amp;&amp; npm run typecheck &amp;&amp; npm run test -- --run useSync SyncView StageStepper StageRow Sidebar &amp;&amp; cd ../.. &amp;&amp; npx playwright test tests/a11y/axe.spec.ts -g "Sync terminal" &amp;&amp; rg -q "SYNC-05.*complete" /Users/martin/dev/opensource/tome/.planning/REQUIREMENTS.md &amp;&amp; rg -q "SYNC-01.*complete" /Users/martin/dev/opensource/tome/.planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    `useSync` exposes `retryFromStage` + `retryFailedItems` + `unresolvedFailureCount`; `SyncView` renders all 5 terminal states per UI-SPEC; `StageRow` partial-failure variant renders amber badge + auto-expanded FindingRow list + inline `[Retry failed items]`; `StageStepper` action triplet wired; Sidebar failure-badge variant active; REQUIREMENTS.md SYNC-01..05 all marked complete with traceability notes; axe scans of all terminal states clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React → Tauri IPC (`retry_sync_from`, `retry_failed_items`) | Retry intents cross from webview to Rust |
| Domain `tome::retry_partial_failures` → filesystem | Per-operation retry mutates distribution symlinks, cleanup paths, etc. |
| `SyncOutcomeWire.partial_failures` payload | Failure metadata flows webview-wards including skill names + error chains |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05-01 | Tampering | retry_sync_from invoked with an invalid SyncStage variant | mitigate | SyncStage is a typed enum across the IPC boundary (specta + tauri-specta); the webview can only send valid string-tagged variants. The POLISH-04 trio + ALL constant guards prevent silent variant addition. |
| T-27-05-02 | Replay | retry_failed_items invoked with a stale or fabricated PartialFailureWire list | mitigate | The domain re-derives current state on every retry call (re-reads manifest, lockfile, library); the input failure list only narrows the scope of operations to attempt. A fabricated entry referencing a non-existent skill is a no-op (Distribute/Cleanup operations check existence before mutating). Per RESEARCH §"Cancellation flag" row this matches the design's recoverable semantics. |
| T-27-05-03 | Tampering | retry_from logic exploits Distribute-on-partial-manifest to corrupt state | mitigate | D-19 safety rule is implemented domain-side in `SyncOutcome::from_sync_result`: Distribute failure ALWAYS maps to retry_from = Some(Reconcile), never Some(Distribute). The CLI flow inherits the same safety (sync_with_outcome wraps the same domain). Verified by Task 1 unit tests. |
| T-27-05-04 | Information Disclosure | partial_failures error chains leak path / skill info to webview | accept | Same user owns both sides; paths are user-owned; no PII. Phase 26 D-11 FindingRow already exposes this shape for Doctor findings. |
| T-27-05-05 | DoS | retry_failed_items invoked in a loop creating CPU churn | accept | Single-user local app; the retry handler is recoverable; cosmetic UX issue at worst. |
| T-27-05-06 | EoP | A retry that succeeds writes manifest without prior user consent | accept | Retry is explicitly initiated by user click on [Retry failed items] / [Retry from stage]; the action button IS the consent. No auto-fire. The retry runs the same atomic Save stage as the original sync, so the safety invariant is identical. |
| T-27-05-SC | Tampering | npm/cargo new deps | accept | This plan adds ZERO new external packages. All work reuses Phase 26 stack + Phase 27 components already audited (similar in 27-03; everything else inherited). |
</threat_model>

<verification>
- `cargo test -p tome --lib sync_outcome::tests` — 6 unit tests pass.
- `cargo test -p tome` — full domain test suite (including the sync_cancel.rs integration test from 27-04) passes.
- `cargo test -p tome-desktop --lib commands -- start_sync retry_sync_from retry_failed_items` — 3+ new command tests pass.
- `cargo run -p tome-desktop --bin gen-bindings && git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts` — bindings include SyncOutcomeWire + PartialFailureWire + PartialFailureOp + retry_sync_from + retry_failed_items.
- `cargo clippy --all-targets --features bindings -- -D warnings` clean.
- `cd crates/tome-desktop/ui && npm run typecheck && npm run test` — unit tests pass for useSync + SyncView + StageStepper + StageRow + Sidebar.
- `npx playwright test tests/a11y/axe.spec.ts -g "Sync"` — all Sync-view axe scans clean (idle / in-progress / terminal-success / terminal-cancelled / terminal-failed-with-retry / terminal-failed-no-retry / terminal-partial-failure).
- `rg "SYNC-0[1-5].*complete" .planning/REQUIREMENTS.md` matches 5 lines.
- Manual smoke: trigger a partial-failure scenario (e.g., set up a distribution target dir with a permission-denied symlink target; run sync; observe Distribute stage completes with amber [⚠ 1 issues] badge; click [Retry failed items]; observe retry attempts the failing op).
</verification>

<success_criteria>
- ROADMAP Phase 27 SC#5 met: failed sync surfaces per-stage failure summary (SAFE-01 K-failures semantics) with retry-from-failed-stage handler.
- SyncOutcome wrapping struct (RESEARCH §3 recommendation) implemented; retry_from computed domain-side per D-19 safety rules.
- Partial-failure D-20 rendering complete: amber badge + auto-expanded FindingRow list + [Retry failed items] inline action.
- All Phase 27 SYNC-01..05 marked complete in REQUIREMENTS.md.
- All Phase 27 axe-core scans clean across 7 visual states.
- CLI regression suite (`cargo test -p tome`) continues to pass.
</success_criteria>

<output>
Create `.planning/phases/27-sync-triage-ui/27-05-SUMMARY.md` when done.
</output>
