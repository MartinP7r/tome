---
phase: 27-sync-triage-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/tome/src/progress.rs
  - crates/tome/src/discover.rs
  - crates/tome/src/list.rs
  - crates/tome/src/lib.rs
  - crates/tome-desktop/src/sink.rs
  - crates/tome-desktop/src/commands.rs
  - crates/tome-desktop/src/menu.rs
  - crates/tome-desktop/src/lib.rs
  - crates/tome-desktop/ui/src/stores/router.ts
  - crates/tome-desktop/ui/src/App.tsx
  - crates/tome-desktop/ui/src/components/Sidebar.tsx
  - crates/tome-desktop/ui/src/hooks/useMenuActions.ts
  - crates/tome-desktop/ui/src/views/SyncView.tsx
  - crates/tome-desktop/ui/src/hooks/useSync.ts
  - crates/tome-desktop/ui/src/bindings.ts
  - crates/tome-desktop/tests/a11y/axe.spec.ts
autonomous: true
requirements:
  - SYNC-01
tags:
  - tauri
  - react
  - ipc-bindings
  - progress
  - manifest
  - menu-bar
  - sidebar
  - keyboard-shortcuts

must_haves:
  truths:
    - "The Sync section is reachable via the sidebar, the Library menu, and the ⌘3 keyboard shortcut."
    - "Clicking [Run sync] (or pressing ⌘R) starts the real tome::sync pipeline through the Tauri boundary without blocking the IPC reactor."
    - "Each of the six pipeline stages emits a typed SyncProgress event with an optional current-item subtitle (D-08)."
    - "GitCloneProgress and BackupSnapshot events fold into the active stage's item subtitle (D-09) and the sink-side test asserts that SyncStageStarted{Reconcile} precedes the first GitCloneProgress."
    - "DiscoveredSkill carries a synced_at: Option<String> ISO-8601 field sourced from the manifest, surfaced through ListReport, and present in bindings.ts (closes Phase 26 VIEW-02 carryover #2)."
    - "While a sync is running, useSync ignores ManifestChanged/LockfileChanged events to prevent a watcher-feedback loop (Pitfall 6)."
    - "Phase 26's ⌘3 → Health binding is re-anchored to ⌘3 → Sync, with Health moving to ⌘4 (Pitfall 7); the Library menu Sync item ships with the ⌘R accelerator (D-02)."
  artifacts:
    - path: "crates/tome/src/progress.rs"
      provides: "ProgressEvent::SyncStageProgress.item field (D-08)"
      contains: "item: Option<String>"
    - path: "crates/tome/src/discover.rs"
      provides: "DiscoveredSkill.synced_at field (D-16)"
      contains: "synced_at: Option<String>"
    - path: "crates/tome-desktop/src/sink.rs"
      provides: "SyncProgress mirror with item field + D-09 fold-in formatting"
      contains: "item: Option<String>"
    - path: "crates/tome-desktop/src/commands.rs"
      provides: "start_sync + cancel_sync registered as async/sync commands"
      exports: ["start_sync", "cancel_sync"]
    - path: "crates/tome-desktop/src/menu.rs"
      provides: "MenuAction::JumpSync variant; ⌘3 re-anchored to Sync; ⌘4 to Health; Library→Sync ⌘R item"
      contains: "JumpSync"
    - path: "crates/tome-desktop/src/lib.rs"
      provides: "make_builder() registers start_sync, cancel_sync, MenuAction::JumpSync"
      contains: "start_sync"
    - path: "crates/tome-desktop/ui/src/views/SyncView.tsx"
      provides: "Skeleton Sync view (idle hero + Run sync button + stage-state subscription)"
      min_lines: 60
    - path: "crates/tome-desktop/ui/src/hooks/useSync.ts"
      provides: "useSync hook owning stage Map<SyncStage,StageStatus>, start/cancel handlers, watcher-feedback guard"
      min_lines: 80
    - path: "crates/tome-desktop/ui/src/bindings.ts"
      provides: "Regenerated TS bindings (SyncProgress.item, DiscoveredSkill.synced_at, start_sync, cancel_sync, MenuAction::JumpSync)"
  key_links:
    - from: "crates/tome-desktop/src/commands.rs::start_sync"
      to: "crates/tome/src/lib.rs::sync"
      via: "tokio::task::spawn_blocking wrapping tome::sync(&config, &paths, opts, &sink, &cancel)"
      pattern: "spawn_blocking"
    - from: "crates/tome-desktop/src/sink.rs::TauriEventSink::emit"
      to: "SyncProgress event"
      via: "passes ProgressEvent::SyncStageProgress.item through; folds GitCloneProgress into Some(format!(\"git: {dir} ({size})\")"
      pattern: "ProgressEvent::SyncStageProgress \\{ .*item.*\\}"
    - from: "crates/tome-desktop/ui/src/components/Sidebar.tsx"
      to: "router setView('sync')"
      via: "4th NavItem id='sync' between Skills and Health"
      pattern: "id: \"sync\""
    - from: "crates/tome-desktop/src/menu.rs::install"
      to: "MenuAction::JumpSync emission"
      via: "Library menu Sync item with CmdOrCtrl+R + ⌘3 accelerator on the View-menu Jump-to-Sync item"
      pattern: "JumpSync"
---

<objective>
Land the SYNC-01 substrate: extend `ProgressEvent::SyncStageProgress` with an `item: Option<String>` field (D-08); fold `GitCloneProgress`/`BackupSnapshot` into the active stage's subtitle (D-09); plumb `DiscoveredSkill.synced_at` (D-16, closes Phase 26 VIEW-02 carryover #2); register the `start_sync` (async via `spawn_blocking`) + `cancel_sync` (sync) Tauri commands; re-anchor `⌘1..⌘4` to the new 4-row sidebar (Pitfall 7); add the `Sync` sidebar `NavItem`, the `SyncView` skeleton (idle hero + `Run sync` button), the `useSync` hook (with watcher-feedback guard per Pitfall 6), and regenerate `bindings.ts`.

Purpose: unblocks 27-02/27-03 (which need the typed event shape and the view skeleton) and 27-04 (which needs `cancel_sync` wired). Without this plan, no downstream UI work can compile against the right contract.

Output: extended domain types, four new IPC surfaces (2 commands + 1 menu variant + 1 mirror struct field), one new view, one new hook, regenerated `bindings.ts`, a11y axe scan covering the new view.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-sync-triage-ui/27-CONTEXT.md
@.planning/phases/27-sync-triage-ui/27-RESEARCH.md
@.planning/phases/27-sync-triage-ui/27-PATTERNS.md
@.planning/phases/27-sync-triage-ui/27-UI-SPEC.md
@.planning/phases/26-read-only-views-alpha-cut/deferred-items.md
@crates/tome/src/progress.rs
@crates/tome/src/discover.rs
@crates/tome/src/list.rs
@crates/tome-desktop/src/sink.rs
@crates/tome-desktop/src/commands.rs
@crates/tome-desktop/src/menu.rs
@crates/tome-desktop/src/lib.rs
@crates/tome-desktop/ui/src/stores/router.ts
@crates/tome-desktop/ui/src/components/Sidebar.tsx
@crates/tome-desktop/ui/src/hooks/useMenuActions.ts
@crates/tome-desktop/ui/src/hooks/useStatus.ts
@crates/tome-desktop/ui/src/hooks/useTauriEvent.ts

<interfaces>
<!-- Pre-extracted contracts so the executor does not re-explore. -->

From crates/tome/src/progress.rs (existing; D-08 adds `item` to SyncStageProgress):
- `pub enum SyncStage { Reconcile, Discover, Consolidate, Distribute, Cleanup, Save }`
- `pub const ALL: [SyncStage; 6]` + `_ensure_sync_stage_exhaustive` const-fn + length-pin guard (POLISH-04 trio)
- `pub enum ProgressEvent { SyncStageStarted { stage }, SyncStageProgress { stage, current, total }, SyncStageFinished { stage }, GitCloneProgress { directory, received }, BackupSnapshot { message } }`
- `pub trait ProgressSink { fn emit(&self, event: ProgressEvent); }`
- `pub struct CancelToken(Arc<AtomicBool>)` with `new()`, `cancel()`, `is_cancelled()`, `Clone`
- `pub struct RecordingSink { events: Mutex<Vec<ProgressEvent>> }` for tests

From crates/tome/src/discover.rs (existing; D-16 adds `synced_at`):
- `pub struct DiscoveredSkill { pub name: SkillName, pub path: PathBuf, pub source_name: DirectoryName, pub origin: SkillOrigin, #[serde(skip)] pub frontmatter: Option<SkillFrontmatter> }`
- `pub fn discover_all(config: &Config, paths: &TomePaths) -> Result<(Vec<DiscoveredSkill>, Vec<String>)>`

From crates/tome/src/manifest.rs (read-only — synced_at already exists):
- `pub struct SkillEntry { ..., pub synced_at: String, ... }` (ISO-8601, stamped only on material change per library.rs:176-201)
- `pub fn load(path: &Path) -> Result<Manifest>`

From crates/tome-desktop/src/sink.rs (existing — D-08/D-09 extension target):
- `pub struct SyncProgress { stage: SyncStage, current: u64, total: u64 }` (mirror; needs `item: Option<String>`)
- `impl ProgressSink for TauriEventSink { fn emit(&self, event: ProgressEvent) { … match arms … } }`
- helper `saturate_usize` / `saturate_u64`

From crates/tome-desktop/src/commands.rs (existing pattern):
- Every command: `#[tauri::command] #[specta::specta]` + `load_context().map_err(TomeError::from)?` + `.map_err(TomeError::from)`
- `pub fn set_skill_disabled(_app: tauri::AppHandle, name: SkillName, disabled: bool) -> Result<(), TomeError>`

From crates/tome-desktop/src/menu.rs (existing — extend MenuAction):
- `#[derive(Clone, Debug, serde::Serialize, specta::Type, tauri_specta::Event)] #[serde(tag = "kind")] pub enum MenuAction { JumpStatus, JumpSkills, JumpHealth, FocusSearch }`
- `impl MenuAction { pub const ALL: [&'static str; 4] = [...]; }` with exhaustiveness sentinel
- Accelerators: `CmdOrCtrl+1/2/3` on jump items; `CmdOrCtrl+R` currently bound to disabled View→Reload (RESEARCH Pitfall 7)

From crates/tome-desktop/ui/src/stores/router.ts:
- `export type View = "status" | "skills" | "health";` — extend to add `"sync"` between `"skills"` and `"health"`

From crates/tome-desktop/ui/src/hooks/useTauriEvent.ts:
- `export function useTauriEvent<T>(event: EventListener<T>, handler: () => void): void` — late-listen-race guard + cleanup
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ProgressEvent + DiscoveredSkill domain types, regenerate sink mirror, verify Reconcile-stage ordering</name>
  <files>
    crates/tome/src/progress.rs,
    crates/tome/src/discover.rs,
    crates/tome/src/list.rs,
    crates/tome/src/lib.rs,
    crates/tome-desktop/src/sink.rs
  </files>
  <read_first>
    crates/tome/src/progress.rs (full file — read once, extract enum ALL + RecordingSink shape),
    crates/tome/src/discover.rs (lines 1-260 — read once, extract DiscoveredSkill + SkillProvenance pattern + discover_all signature),
    crates/tome/src/list.rs (full file — ListReport::collect shape),
    crates/tome/src/lib.rs (lines 1490-2260 — sync() pipeline; locate the six SyncStageStarted/Progress/Finished emission sites and the resolve_git_directories call that triggers GitCloneProgress),
    crates/tome/src/library.rs (lines 170-330 — confirm synced_at material-change semantics; do not modify),
    crates/tome/src/manifest.rs (lines 160-220 — SkillEntry.synced_at field already present, ISO-8601 string),
    crates/tome-desktop/src/sink.rs (full file — TauriEventSink::emit match arms + SyncProgress mirror struct),
    .planning/phases/27-sync-triage-ui/27-RESEARCH.md §"Code Examples — Domain-side typed event emission" and §"Pitfall 4",
    .planning/phases/27-sync-triage-ui/27-PATTERNS.md §"crates/tome/src/progress.rs" and §"crates/tome-desktop/src/sink.rs"
  </read_first>
  <behavior>
    - Test (RecordingSink): emitting `ProgressEvent::SyncStageProgress { stage: Discover, current: 5, total: 10, item: Some("axiom-build".into()) }` and reading it back via `RecordingSink::events()` returns the value verbatim including `item`.
    - Test (RecordingSink): emitting `ProgressEvent::SyncStageStarted { stage: Reconcile }` followed by `ProgressEvent::GitCloneProgress { directory: "my-repo".into(), received: 4_200_000 }` produces an event sequence where the first event has `stage == Reconcile` (RESEARCH Pitfall 4 / Assumption A4 verification — confirms `resolve_git_directories` emits Reconcile-started BEFORE the first git-clone event).
    - Test (TauriEventSink fold-in, D-09): a fake `AppHandle`-equivalent sink receiving `GitCloneProgress { directory: "my-repo", received: 4_200_000 }` produces a `SyncProgress { stage: Reconcile, current: 4_200_000, total: 0, item: Some("git: my-repo (4.0 MiB)") }` (or matching MB depending on the existing saturating-cast format; assert the prefix `"git: my-repo"` and that `item.is_some()`).
    - Test (TauriEventSink fold-in, D-09): emitting `BackupSnapshot { message: "writing snapshot".into() }` produces `SyncProgress { stage: Save, current: 0, total: 0, item: Some("writing snapshot".into()) }`.
    - Test (synced_at plumbing): `discover_all` returns `DiscoveredSkill { synced_at: Some(_), .. }` for every skill that already has an entry in the manifest (using a fixture manifest under TempDir); skills with no manifest entry have `synced_at: None`.
    - Test (list report): `ListReport::collect` surfaces `synced_at` on each entry; round-trip through `serde_json::to_string` includes the field.
  </behavior>
  <action>
    1. Extend `ProgressEvent::SyncStageProgress` in `crates/tome/src/progress.rs` to add `item: Option<String>` after `total`. Keep the `#[cfg_attr(feature = "bindings", derive(specta::Type))]` derive on the enum. Update every existing emission site in `crates/tome/src/lib.rs::sync` (the six `SyncStageProgress { stage, current, total }` constructors at the lines documented in PATTERNS.md / RESEARCH.md) to carry an `item` — for now pass through the natural per-stage subtitle: Discover = `Some(directory_name.to_string())`, Consolidate = `Some(skill_name.to_string())`, Distribute = `Some(skill_name.to_string())`, Cleanup = `Some(path.display().to_string())`, Save = `Some(filename.to_string())`, Reconcile = `None` (git-clone folds in via the sink instead per D-09). Update `RecordingSink` test fixtures in `progress.rs:269-308` to pass `item: None` (or `Some(...)`) per case.
    2. Extend `DiscoveredSkill` in `crates/tome/src/discover.rs` with `pub synced_at: Option<String>` (ISO-8601, mirrors `SkillEntry::synced_at`). Read the manifest at the discover-call boundary in `lib.rs::sync` (the manifest is already loaded earlier in the pipeline — pass it as an additional argument to whatever helper builds `DiscoveredSkill` records, OR populate `synced_at` in a post-discover pass over the returned `Vec<DiscoveredSkill>` using the already-loaded `Manifest`). If neither path is clean, add `synced_at: None` at the original discover sites and stamp it in `lib.rs::sync` immediately after `discover_all` returns and the manifest is in scope. The field MUST end up populated for known skills before the result is handed to `ListReport::collect`.
    3. Surface `synced_at` on `ListReport`'s per-skill records in `crates/tome/src/list.rs` so the Skills view's "Recent" sort comparator (Phase 26 carryover #2) can use it. Mirror the existing serde derives.
    4. Extend the `SyncProgress` mirror struct in `crates/tome-desktop/src/sink.rs` with `pub item: Option<String>` after `total`. Update every match arm of `impl ProgressSink for TauriEventSink::emit` to set the field. For the `SyncStageProgress` arm: pass `item` through verbatim. For the `GitCloneProgress` arm (D-09): emit `stage: SyncStage::Reconcile`, `current: saturate_u64(received)`, `total: 0`, `item: Some(format!("git: {directory} ({})", format_bytes(received)))` where `format_bytes` is a sink-private helper rendering `MiB`/`GiB` with 1 decimal per the existing saturating-cast format. For the `BackupSnapshot` arm: `stage: SyncStage::Save`, `current: 0`, `total: 0`, `item: Some(message)`. For `SyncStageStarted`/`SyncStageFinished`: `item: None`.
    5. Add three RecordingSink-based tests in `crates/tome/src/progress.rs` mod tests: (a) `item` field round-trips through `SyncStageProgress`; (b) `RecordingSink` ordering — `SyncStageStarted { Reconcile }` precedes a synthetic `GitCloneProgress` event (Pitfall 4 / A4 anchor); (c) sink-side fold-in tests in `crates/tome-desktop/src/sink.rs` mod tests use a `MockTauriEmitter` (or whatever Phase 25/26 used — read the existing sink.rs test pattern; if no mock exists, assert the fold-in by constructing the SyncProgress value directly from the helper functions and comparing to expected).
    6. `cargo run -p tome-desktop --bin gen-bindings` is NOT run here (it ships in Task 3 after all events are registered). Do not commit `bindings.ts` from this task.
  </action>
  <verify>
    <automated>cargo test -p tome --lib progress::tests:: -- --nocapture &amp;&amp; cargo test -p tome --lib discover::tests::synced_at -- --nocapture &amp;&amp; cargo test -p tome-desktop --lib sink::tests:: -- --nocapture &amp;&amp; cargo build -p tome-desktop --features bindings</automated>
  </verify>
  <done>
    `ProgressEvent::SyncStageProgress` carries `item: Option<String>`; every emission site in `lib.rs::sync` passes a value; `SyncProgress` mirror carries the field; the D-09 fold-in formatters render `"git: <dir> (<size>)"` and `BackupSnapshot.message` verbatim; `DiscoveredSkill.synced_at` is populated from the manifest before `ListReport::collect` runs; the new RecordingSink ordering test passes (Pitfall 4 / A4 anchor); no clippy warnings; bindings.ts NOT regenerated yet (Task 3 owns the commit).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire start_sync (async + spawn_blocking) + cancel_sync Tauri commands and SyncState app-state, register MenuAction::JumpSync</name>
  <files>
    crates/tome-desktop/src/commands.rs,
    crates/tome-desktop/src/lib.rs,
    crates/tome-desktop/src/menu.rs
  </files>
  <read_first>
    crates/tome-desktop/src/commands.rs (full file — read once, extract load_context() + the `.map_err(TomeError::from)` discipline at every command edge),
    crates/tome-desktop/src/lib.rs (full file — make_builder() registry pattern + SyncState placement; setup state),
    crates/tome-desktop/src/menu.rs (full file — MenuAction enum + ALL + sentinel; current ⌘1/⌘2/⌘3 accelerator registrations; the disabled View→Reload ⌘R item; Library menu Sync item that is currently `.enabled(false)`),
    crates/tome-desktop/src/error.rs (full file — TomeError boundary; ErrorCode::ALL trio; From<anyhow::Error> classifier),
    crates/tome-desktop/src/sink.rs (Task 1 output — TauriEventSink::new(app) constructor),
    crates/tome/src/progress.rs (CancelToken API — Clone + cancel() + is_cancelled()),
    crates/tome/src/lib.rs::sync (signature confirmation only: `pub fn sync(config: &Config, paths: &TomePaths, options: SyncOptions, sink: &dyn ProgressSink, cancel: &CancelToken) -> Result<SyncReport>`),
    .planning/phases/27-sync-triage-ui/27-RESEARCH.md §"Pitfall 5" + §"Code Examples — spawn_blocking for the sync command (lines 549-577)",
    .planning/phases/27-sync-triage-ui/27-RESEARCH.md §"Pitfall 7" + §"Code Examples — Domain-side typed event emission",
    .planning/phases/27-sync-triage-ui/27-PATTERNS.md §"crates/tome-desktop/src/menu.rs" + §"crates/tome-desktop/src/commands.rs"
  </read_first>
  <behavior>
    - Test (compile): `cargo build -p tome-desktop --features bindings` succeeds with the new `start_sync` + `cancel_sync` commands and `MenuAction::JumpSync` variant.
    - Test (MenuAction exhaustiveness): the existing `_menu_action_exhaustiveness_sentinel` const-fn covers `JumpSync` (compile fails if missed); `MenuAction::ALL.len() == 5`.
    - Test (cancel_sync idempotent): calling `cancel_sync` while no token is set returns `Ok(())`; calling it twice while a token IS set is idempotent (the second call is a no-op against an already-cancelled `Arc<AtomicBool>`).
    - Test (start_sync does not block IPC reactor — per Pitfall 5): a minimal integration test in `crates/tome-desktop/tests/start_sync_smoke.rs` (NEW) using `tauri::test::mock_app` (or whatever existing test harness Phase 26 used — check the `tests/` directory for a Mockable setup) launches `start_sync` against a TempDir config with at least one empty `[directories.X]` source, asserts the function returns `Ok(SyncReport)` (or the bare `Ok(())` that Phase 27's SyncOutcome wrapping in 27-04/27-05 will replace) within 10 seconds. If `tauri::test` is not present, skip the IPC harness test and instead unit-test the inner closure by extracting a `fn run_sync_blocking(config, paths, sink, cancel) -> Result<SyncReport>` and calling that synchronously.
    - Test (menu re-anchoring): no test file exists that asserts menu accelerator strings programmatically (the menu is built imperatively against `tauri::menu::MenuBuilder`); instead `cargo test -p tome-desktop` must compile clean after the changes.
  </behavior>
  <action>
    1. In `crates/tome-desktop/src/lib.rs`: define `pub struct SyncState { pub cancel: Mutex<Option<CancelToken>> }` (or place it in a new `src/sync_state.rs` module — planner's choice; keep it next to `make_builder` if possible). Register it as managed state in the Tauri builder setup (where Phase 25/26 set up app state — read existing code and follow precedent; if no state-setup exists yet, add `app.manage(SyncState::default())` inside the `setup` closure of `Builder::default()`).
    2. In `crates/tome-desktop/src/commands.rs`: add two new commands.
       - `start_sync(app: tauri::AppHandle, state: tauri::State<'_, SyncState>) -> Result<tome::SyncReport, TomeError>` as `async fn` with `#[tauri::command] #[specta::specta]`. Body: (a) create `let cancel = CancelToken::new();`, (b) store `*state.cancel.lock().expect("cancel mutex poisoned") = Some(cancel.clone());`, (c) `let (config, paths) = load_context().map_err(TomeError::from)?;`, (d) load machine prefs via `tome::default_machine_path()` + `tome::machine::load(...)`, build a `tome::SyncOptions` (use the same defaults the CLI uses for `tome sync` — read `crates/tome/src/lib.rs::cmd_sync` for the canonical options builder), (e) `let sink = TauriEventSink::new(app.clone());`, (f) `let result = tokio::task::spawn_blocking(move || tome::sync(&config, &paths, opts, &sink, &cancel)).await.map_err(|join_err| TomeError::from(anyhow::anyhow!("sync task panicked: {join_err}")))?;`, (g) `*state.cancel.lock().expect("cancel mutex poisoned") = None;`, (h) return `result.map_err(TomeError::from)`. **NOTE**: The return type stays `Result<SyncReport, TomeError>` in this plan — 27-04/27-05 will wrap it in `SyncOutcome { result, retry_from, partial_failures }`. Do NOT define `SyncOutcome` here; the React side will treat the result as the partial shape until 27-05 lands.
       - `cancel_sync(state: tauri::State<'_, SyncState>) -> Result<(), TomeError>` as a synchronous `#[tauri::command] #[specta::specta]`. Body: `if let Some(token) = state.cancel.lock().expect("cancel mutex poisoned").as_ref() { token.cancel(); } Ok(())`. Idempotent.
    3. In `crates/tome-desktop/src/menu.rs`:
       - Add `JumpSync` variant to `MenuAction` (place between `JumpSkills` and `JumpHealth` to match sidebar order).
       - Update `pub const ALL: [&'static str; 5] = ["JumpStatus", "JumpSkills", "JumpSync", "JumpHealth", "FocusSearch"];` and extend the `_menu_action_exhaustiveness_sentinel` const-fn match.
       - In `install` (or whatever fn builds the menu): add a `jump-sync` `MenuItemBuilder` with `.accelerator("CmdOrCtrl+3")`; change the existing `jump-health` `MenuItemBuilder` from `CmdOrCtrl+3` to `CmdOrCtrl+4`; add a `"jump-sync" => MenuAction::JumpSync` arm to the click-dispatch match.
       - Re-purpose the existing Library menu `Sync` item that is currently `.enabled(false)`: change to `.enabled(true).accelerator("CmdOrCtrl+R")` and dispatch a `MenuAction::JumpSync` (or, per UI-SPEC Open Item 1, the planner MAY define a sibling `SyncNow` event — but for this plan, fold the Sync menu item into `JumpSync` and let `useMenuActions` route the action to setView('sync') + an in-view `[Run sync]` trigger; the dedicated "run now" intent rides on the `⌘R` keybinding handled by `useSync` in Task 4).
       - REMOVE the disabled View → Reload `CmdOrCtrl+R` `.enabled(false)` item (its `⌘R` slot is reclaimed by Library → Sync per D-02 and RESEARCH Pitfall 7).
    4. In `crates/tome-desktop/src/lib.rs::make_builder`: append `commands::start_sync`, `commands::cancel_sync` to `collect_commands![]`. Confirm `events![]` already contains `sink::SyncProgress` (it does from Phase 25/26 — Task 1 only extended its struct shape, not added a new event variant).
    5. Add `crates/tome-desktop/tests/start_sync_smoke.rs` only if a `tauri::test::mock_app` (or equivalent) test harness exists in the repo (check `tests/watcher_smoke.rs` for the pattern). If no harness exists, skip the test file in this task and rely on Task 5's a11y integration scan + Task 1's unit tests for coverage; document the gap in the task summary.
  </action>
  <verify>
    <automated>cargo build -p tome-desktop --features bindings &amp;&amp; cargo test -p tome-desktop --lib menu::tests 2&gt;&amp;1 || cargo test -p tome-desktop --lib menu 2&gt;&amp;1; cargo clippy -p tome-desktop --features bindings -- -D warnings</automated>
  </verify>
  <done>
    `start_sync` is async + wraps `tome::sync` in `spawn_blocking` (Pitfall 5); `cancel_sync` is sync + idempotent; `SyncState` is managed app state; `MenuAction::JumpSync` is registered with `⌘3` accelerator; Health is on `⌘4`; the disabled View→Reload `⌘R` item is removed and reclaimed by Library→Sync; exhaustiveness sentinel covers all 5 variants; `cargo build -p tome-desktop --features bindings` succeeds; `cargo clippy -p tome-desktop --features bindings -- -D warnings` is clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SyncView skeleton + useSync hook + Sidebar 4th NavItem + router/menu/a11y wiring + bindings.ts regeneration</name>
  <files>
    crates/tome-desktop/ui/src/stores/router.ts,
    crates/tome-desktop/ui/src/App.tsx,
    crates/tome-desktop/ui/src/components/Sidebar.tsx,
    crates/tome-desktop/ui/src/hooks/useMenuActions.ts,
    crates/tome-desktop/ui/src/views/SyncView.tsx,
    crates/tome-desktop/ui/src/hooks/useSync.ts,
    crates/tome-desktop/ui/src/bindings.ts,
    crates/tome-desktop/tests/a11y/axe.spec.ts
  </files>
  <read_first>
    crates/tome-desktop/ui/src/stores/router.ts (full file — View union literal-extension target; line 15),
    crates/tome-desktop/ui/src/App.tsx (full file — read once, locate the switch arm for current views; add the `"sync"` arm),
    crates/tome-desktop/ui/src/components/Sidebar.tsx (full file — current SECTIONS array, ListBox NavItem rendering, badge slot, aria-label template),
    crates/tome-desktop/ui/src/hooks/useMenuActions.ts (full file — switch on MenuAction.kind; current handlers for JumpStatus/JumpSkills/JumpHealth/FocusSearch),
    crates/tome-desktop/ui/src/hooks/useStatus.ts (full file — useTauriEvent subscription discipline; the pattern useSync mirrors),
    crates/tome-desktop/ui/src/hooks/useTauriEvent.ts (cleanup-on-unmount + late-listen-race guard; canonical hook),
    crates/tome-desktop/ui/src/views/SkillsView.tsx (lines 1-80 — `isTextInputFocused()` guard pattern for global keyboard handlers),
    crates/tome-desktop/ui/src/views/HealthView.tsx (variant-branching shape; SyncView's idle/in-progress branches mirror it),
    crates/tome-desktop/ui/src/bindings.ts (current contents — confirm SyncProgress export name + event registration),
    crates/tome-desktop/tests/a11y/axe.spec.ts (existing pattern — page.goto navigation + axe scan),
    .planning/phases/27-sync-triage-ui/27-UI-SPEC.md §"Idle state" + §"In-progress state" + §"Keyboard Map" + §"VoiceOver labels",
    .planning/phases/27-sync-triage-ui/27-PATTERNS.md §"ui/src/components/Sidebar.tsx" + §"ui/src/views/SyncView.tsx" + §"ui/src/hooks/useSync.ts" + §"ui/src/hooks/useMenuActions.ts",
    .planning/phases/27-sync-triage-ui/27-RESEARCH.md §"Pitfall 6" (watcher feedback loop discipline) + §"Pitfall 7"
  </read_first>
  <behavior>
    - Test (axe a11y): after navigating to the new Sync view via `⌘3` (or sidebar click), `@axe-core/playwright` reports zero violations of any WCAG-AA rule. The Sync `NavItem` is keyboard-focusable, the [Run sync] button has an aria-label, the idle hero heading is `<h1>`.
    - Test (router): `router.ts` exports the literal-union `View = "status" | "skills" | "sync" | "health"`; `setView("sync")` round-trips through the store.
    - Test (Sidebar order): the rendered DOM order of NavItems is Status → Skills → Sync → Health.
    - Test (menu route): pressing `⌘3` (via the menu action emission) calls `setView("sync")`; `useMenuActions` switch handles `JumpSync` case.
    - Test (useSync watcher guard, Pitfall 6): a unit test using `@testing-library/react` mounts a component wrapping `useSync`, simulates `events.manifestChanged.emit()` while `isRunning === true`, and asserts that the hook's `refetch`/state-reset side effects DO NOT fire. After `isRunning` transitions to `false`, a subsequent manifestChanged DOES fire (or, equivalently, simply assert that the hook does not subscribe to manifestChanged/lockfileChanged at all — it only subscribes to `syncProgress`; the idle-state hooks (useStatus, useSkills) keep their own subscriptions for post-run refresh).
  </action>
  <action>
    1. In `crates/tome-desktop/ui/src/stores/router.ts`: extend the `View` literal-union to `"status" | "skills" | "sync" | "health"`. Update any switch/match callers if the file enumerates them (typically just App.tsx).
    2. In `crates/tome-desktop/ui/src/components/Sidebar.tsx`: insert `{ id: "sync", label: "Sync" }` into the `SECTIONS` array between `skills` and `health`. Update `SidebarProps` to add `syncInProgress?: boolean` and `syncBadge?: { kind: "pending" | "failures" | "none"; count: number }` (per UI-SPEC §Sidebar lines 192-200). Render the spinner inline when `syncInProgress === true` (small inline `<svg>` spinner replacing the row icon — Claude's discretion, system spinner per CONTEXT.md). Implement the dual-meaning badge (managed-blue vs danger fill) per UI-SPEC §Sidebar updated NavItem. Update `aria-label` template per UI-SPEC §VoiceOver labels (4 entries for the Sync NavItem). For this plan, wire `syncBadge` count from `useSync().pendingDecisions` (which is `0` until 27-02 lands the triage panel — defer the actual count to 27-02) and `syncInProgress` from `useSync().isRunning` (true while the pipeline is active).
    3. In `crates/tome-desktop/ui/src/hooks/useMenuActions.ts`: add `case "JumpSync": setView("sync"); break;` to the switch (mirroring the existing JumpStatus/JumpSkills/JumpHealth pattern). Add a parallel `useEffect` that binds global `⌘R` (to call `useSync().start()` when idle, `useSync().cancel()` when running, `useSync().retry*()` when terminal — for this plan, only wire idle→start and running→cancel; retry comes in 27-05) and `⌘.` (to call `useSync().cancel()` when running, no-op otherwise). Use a window-level `keydown` listener with the existing `isTextInputFocused()`-style guard pattern from `SkillsView.tsx:47-55` — extract that helper into `crates/tome-desktop/ui/src/lib/textInputFocus.ts` IF it isn't already shared; otherwise import it. Note: `⌘R` fires globally per UI-SPEC (Sync is not an Edit action; no input-focus gate).
    4. Create `crates/tome-desktop/ui/src/hooks/useSync.ts`:
       - State: `stages: Map<SyncStage, StageStatus>` (initialised with all 6 stages as `{ kind: "pending" }`), `outcome: SyncReport | TomeError | null` (or `null` while running), `isRunning: boolean`, `err: TomeError | null`, `startedAt: number | null`, `pendingDecisions: number` (always 0 in this plan; 27-02 will populate).
       - Subscribe ONLY to `events.syncProgress` via `useTauriEvent`. Handler accumulates per-stage state: on `SyncStageStarted { stage }` mark stage as `{ kind: "active", currentItem: null, current: 0, total: 0 }` and record `stageStartAt[stage] = Date.now()`; on `SyncStageProgress { stage, current, total, item }` update the active stage's `currentItem`/`current`/`total`; on `SyncStageFinished { stage }` mark complete with `durationMs = Date.now() - stageStartAt[stage]`, `partialFailures: []` (populated in 27-05).
       - Handlers: `start: async () => { setIsRunning(true); const res = await commands.startSync(); setIsRunning(false); /* set outcome per result */ }`, `cancel: () => commands.cancelSync()`, `dismiss: () => { reset stages to pending; clear outcome; }`.
       - **Pitfall 6 discipline**: This hook does NOT subscribe to `events.manifestChanged`, `events.lockfileChanged`, `events.libraryChanged`, or `events.machinePrefsChanged`. The idle-state hooks (useStatus, useSkills) retain those subscriptions to refresh AFTER a run completes — useSync stays isolated to `syncProgress`.
       - Result narrowing per Phase 25 S-8 pattern: `if (res.status === "ok") setOutcome(res.data); else setErr(res.error);`. No try/catch around the discriminated union.
    5. Create `crates/tome-desktop/ui/src/views/SyncView.tsx` as a skeleton:
       - Branch on `useSync()`: if `isRunning === false && outcome === null` → render the **idle hero** per UI-SPEC §Idle state (centred composition: `↺` glyph, `<h1>` heading "You haven't synced yet." OR "Last synced ${relativeTime}" using `lib/relativeTime.ts`, sub-line `${new} new · ${changed} changed · ${removed} removed since last sync` populated from a `useStatus()` call OR omit when never synced, large `<Button variant="primary">Run sync</Button>` wired to `useSync().start()`). The "Recent changes" disclosure is a stub (`<details>…</details>` placeholder with copy "No changes recorded in the previous sync.") — 27-02 will populate it.
       - If `isRunning === true` → render a placeholder `<div role="region" aria-busy="true" aria-label="Sync pipeline">Sync running…</div>` (the StageStepper component lands in 27-05 / split into earlier plans; for now this skeleton just proves the route + state plumbing works). Include a `<Button variant="secondary">Cancel sync</Button>` wired to `useSync().cancel()`.
       - If `outcome !== null && !isRunning` → render a placeholder summary `<div role="status">Sync complete</div>` with a `<Button variant="secondary">Dismiss</Button>` calling `useSync().dismiss()`. Stepper terminal rendering lands in 27-05.
       - A11y: outer `<section role="status" aria-label="Sync status">` wraps idle; `<h1>` for the idle heading; `aria-live="polite"` for the running region. Match UI-SPEC §VoiceOver labels for `[Run sync]`, `[Cancel sync]`, `[Dismiss]`.
    6. In `crates/tome-desktop/ui/src/App.tsx`: add `case "sync": return <SyncView />;` to the route switch; update the titlebar title-label switch to include `"sync": "Sync"`.
    7. Regenerate bindings: run `cargo run -p tome-desktop --bin gen-bindings` from the repo root; commit the resulting diff to `crates/tome-desktop/ui/src/bindings.ts`. The generated file MUST include the new `start_sync` + `cancel_sync` commands, the `item: string | null` field on `SyncProgress`, the `synced_at: string | null` field on `DiscoveredSkill` records in `ListReport`, and the `JumpSync` variant on `MenuAction`.
    8. Extend `crates/tome-desktop/tests/a11y/axe.spec.ts` to add a new spec block "Sync view a11y" that: navigates to the Sync route (either by simulating `⌘3` or by clicking the sidebar Sync NavItem), runs `await new AxeBuilder({ page }).analyze()`, asserts `results.violations === []`. Follow the existing pattern verbatim.
  </action>
  <verify>
    <automated>cargo run -p tome-desktop --bin gen-bindings &amp;&amp; git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts &amp;&amp; cd crates/tome-desktop/ui &amp;&amp; npm run typecheck &amp;&amp; npm run test -- --run useSync 2&gt;&amp;1; cd ../../.. &amp;&amp; cd crates/tome-desktop &amp;&amp; npx playwright test tests/a11y/axe.spec.ts -g "Sync view"</automated>
  </verify>
  <done>
    `View` type extended; `Sidebar` renders 4 NavItems in order Status/Skills/Sync/Health with spinner + dual-meaning badge slots; `useMenuActions` routes `JumpSync` + binds `⌘R`/`⌘.` global handlers; `useSync` hook subscribes ONLY to `syncProgress` (Pitfall 6 discipline); `SyncView` renders idle hero with `[Run sync]` button calling `useSync().start()`; `App.tsx` routes `"sync"` → `<SyncView />`; `bindings.ts` regenerated and committed (CI freshness gate passes); axe-core scan of the Sync view passes with zero WCAG-AA violations; `cargo run -p tome-desktop --bin gen-bindings` followed by `git diff --exit-code` is clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React → Tauri IPC (`start_sync`, `cancel_sync`) | Webview-originated command invocations cross into native Rust |
| Domain `tome::sync` → filesystem | sync mutates library, manifest, lockfile, distribution symlinks |
| Domain → `MachinePrefs` (read for SyncOptions) | sync reads machine.toml (this plan only reads; SYNC-03 writes) |
| Tauri event channel → Webview | `SyncProgress` payloads flow webview-wards; payloads include user paths/skill names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | Spoofing | start_sync command invocation | accept | Single-user local app; webview is trusted; Tauri capability surface unchanged from Phase 25/26 (no `fs:default`, no `shell:default`); the command takes no path or URL arguments — all paths resolved server-side via `load_context()` and `default_machine_path()` |
| T-27-01-02 | Tampering | start_sync runs sync without user consent | mitigate | UI flow requires explicit `[Run sync]` button click or `⌘R` keypress; no auto-fire from watcher events (per Deferred Ideas — real-time auto-sync is out of v1.0 scope) |
| T-27-01-03 | DoS | Webview spams cancel_sync to disrupt user's run | accept | Sync is locally invoked + always recoverable per SC#4 invariant; cancellation by design always safe; threat-model neutral. Listed for completeness per RESEARCH §"Known Threat Patterns" |
| T-27-01-04 | DoS | Watcher event flood causes UI thrashing mid-sync | mitigate | useSync ignores ManifestChanged/LockfileChanged while isRunning (Pitfall 6 / Assumption A5); Phase 26 watcher already debounced at 200ms (notify-debouncer-full) |
| T-27-01-05 | Information Disclosure | SyncProgress.item leaks paths into webview console / logs | accept | Paths are user-owned (skill names, directory names, the user's own machine layout) — disclosure to the webview that the same user controls is non-issue; no PII; D-09 sink-side fold-in is the canonical sanitization point |
| T-27-01-06 | DoS | spawn_blocking on Tauri main runtime starves IPC handler | mitigate | Pitfall 5 — `tome::sync` runs via `tokio::task::spawn_blocking` so it does NOT block the IPC reactor; cancel_sync stays a fast sync command flipping the `Arc<AtomicBool>`; verified by behavior test in Task 2 |
| T-27-01-07 | Tampering | start_sync called concurrently (double-fire) | mitigate | SyncState.cancel is `Mutex<Option<CancelToken>>` — second concurrent invocation observes `Some(token)` and could overwrite it; add a guard: bail with `TomeError { code: Conflict, message: "sync already in progress" }` if the mutex's Option is already `Some` at start. Implement in Task 2. |
| T-27-01-SC | Tampering | `cargo add similar` (deferred to 27-03) — no new deps in this plan | accept | This plan adds zero new external Rust or npm packages. Package legitimacy audit not required (RESEARCH §"Package Legitimacy Audit" applies to 27-03 where `similar` is introduced) |
</threat_model>

<verification>
- `cargo build -p tome --features bindings && cargo build -p tome-desktop --features bindings` — both compile clean.
- `cargo test -p tome --lib progress::tests:: && cargo test -p tome --lib discover::tests::synced_at` — new domain unit tests pass.
- `cargo test -p tome-desktop --lib sink::tests::` — TauriEventSink fold-in tests pass.
- `cargo run -p tome-desktop --bin gen-bindings && git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts` — bindings regenerated cleanly and committed (CI freshness gate).
- `cargo clippy -p tome --all-targets -- -D warnings && cargo clippy -p tome-desktop --all-targets --features bindings -- -D warnings` — zero warnings.
- `cd crates/tome-desktop/ui && npm run typecheck` — TypeScript clean.
- `cd crates/tome-desktop && npx playwright test tests/a11y/axe.spec.ts -g "Sync view"` — axe scan passes.
- Manual smoke: launch `cargo tauri dev`, navigate to Sync via `⌘3`, observe idle hero; click `[Run sync]`, observe stage-running placeholder; click `[Cancel sync]`, observe return to idle.

The full CLI regression suite (`cargo test -p tome`) MUST also pass — the only domain changes are additive (`item: Option<String>`, `synced_at: Option<String>`) so no CLI behavior should regress; if any snapshot test fails it indicates `synced_at` is leaking into a CLI output it shouldn't (e.g., `tome list --json` adds the field; this is INTENTIONAL per the additive-field policy, but the snapshot needs accepting via `cargo insta review`).
</verification>

<success_criteria>
- ROADMAP Phase 27 SC#1 (per-stage progress + current-directory indicator) is **substrate-ready**: the event stream now carries `item: Option<String>` end-to-end; the actual stepper rendering lands in 27-05 (or earlier — split across the wave's downstream plans).
- ROADMAP Phase 27 SC#6 (VIEW-02 "Recent" sort closure) is **plumbing-complete**: `DiscoveredSkill.synced_at` reaches `bindings.ts`; the SkillsView comparator wiring is the small follow-up (Skills view changes belong here in scope but can defer to a 27-02 task — verify the existing SkillsView already supports a `sort: "recent"` mode; if so, update its comparator to use the new field).
- Sync section reachable via sidebar, Library menu, `⌘3`; `[Run sync]` fires `start_sync`; `[Cancel sync]` fires `cancel_sync`; both round-trip through `tokio::task::spawn_blocking` without blocking the IPC reactor (Pitfall 5).
- `bindings.ts` CI freshness gate passes; the Sync view has zero axe-core WCAG-AA violations.
- All existing CLI integration tests in `crates/tome/tests/cli*.rs` continue to pass (additive domain changes only).
</success_criteria>

<output>
Create `.planning/phases/27-sync-triage-ui/27-01-SUMMARY.md` when done.
</output>
