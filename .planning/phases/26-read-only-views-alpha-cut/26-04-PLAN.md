---
phase: 26-read-only-views-alpha-cut
plan: 04
type: execute
wave: 2
depends_on: [26-03]
files_modified:
  - crates/tome-desktop/ui/package.json
  - crates/tome-desktop/ui/src/components/MarkdownBody.tsx
  - crates/tome-desktop/ui/src/components/MarkdownBody.module.css
  - crates/tome-desktop/ui/src/views/SkillsView.tsx
  - crates/tome-desktop/ui/src/components/__tests__/MarkdownBody.test.tsx
  - crates/tome-desktop/ui/vitest.config.ts
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [VIEW-04]
user_setup: []
must_haves:
  truths:
    - "[D-05] Selecting a skill renders its SKILL.md body (post-frontmatter) below the DetailHeader as a scrollable markdown article (D-05's scrolling-markdown-body half; metadata header half delivered by 26-03)"
    - "Markdown subset enforced via react-markdown's `allowedElements`: h1/h2/h3, p, strong, em, code, ul/ol/li, a, pre — verbatim per UI-SPEC §MarkdownBody"
    - "Tables, images, blockquotes, task lists, HTML passthrough, footnotes are STRIPPED by the allow-list (Pitfall 3 — visual smoke test verifies fixture content renders correctly)"
    - "Links open in the system browser via @tauri-apps/plugin-opener; javascript:/data: URLs are explicitly rejected (Security domain mitigation)"
    - "react-markdown 10.1.0 + remark-gfm 4.0.1 used; smoke-test renders without React 19 console warnings (Pitfall 7)"
    - "REQUIREMENTS.md VIEW-04 wording updated to point at ROADMAP SC#4 (supersedes literal `browse/markdown.rs` reference per D-08 / Open Item 4)"
  artifacts:
    - path: "crates/tome-desktop/ui/src/components/MarkdownBody.tsx"
      provides: "react-markdown wrapper enforcing the SC#4 subset"
      contains: "allowedElements"
    - path: "crates/tome-desktop/ui/src/components/__tests__/MarkdownBody.test.tsx"
      provides: "Vitest snapshot test against a fixture SKILL.md exercising every allowed element"
  key_links:
    - from: "crates/tome-desktop/ui/src/components/MarkdownBody.tsx"
      to: "react-markdown allowedElements + remark-gfm"
      via: "Allow-list enforcement (D-08 / Pitfall 3)"
      pattern: "allowedElements.*h1.*h2.*h3"
    - from: "MarkdownBody link onClick"
      to: "@tauri-apps/plugin-opener openUrl"
      via: "Scheme guard rejects javascript:/data:"
      pattern: "openUrl|/\\^https?:/"
---

<objective>
Add the `MarkdownBody` React component that renders the SKILL.md body returned by `commands.getSkillDetail` (plan 26-03) as a scrollable article in the detail column. Enforce the SC#4 markdown subset (headings H1–H3, lists, links, code blocks, inline bold/italic/code) via `react-markdown`'s `allowedElements` allow-list. Add a Vitest snapshot test against a fixture SKILL.md to catch silent-strip regressions (Pitfall 3). Update REQUIREMENTS.md VIEW-04 wording per D-08 / UI-SPEC Open Item 4.

Purpose: VIEW-04 acceptance + closes the docs-consistency flag from D-08 (TUI's `browse/markdown.rs` is too thin to share; SC#4 is the real target).
Output: Click a skill → detail header + scrolling markdown body below; tables/images/HTML stripped; external links open via Tauri opener.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-read-only-views-alpha-cut/26-CONTEXT.md
@.planning/phases/26-read-only-views-alpha-cut/26-RESEARCH.md
@.planning/phases/26-read-only-views-alpha-cut/26-PATTERNS.md
@.planning/phases/26-read-only-views-alpha-cut/26-UI-SPEC.md
@.planning/phases/26-read-only-views-alpha-cut/26-03-PLAN.md
@.planning/REQUIREMENTS.md
@crates/tome/src/browse/markdown.rs
@crates/tome-desktop/ui/src/views/SkillsView.tsx
@crates/tome-desktop/ui/package.json

<interfaces>
<!-- Cross-boundary types and patterns this plan adds. -->

NO new Tauri commands (the body is already returned by `commands.getSkillDetail` from plan 26-03).

NEW npm packages:
- `react-markdown@^10.1.0` (RESEARCH §"Standard Stack — Markdown" — confirmed peerDeps say React >=18; Pitfall 7 calls for smoke test on React 19)
- `remark-gfm@^4.0.1` (same RESEARCH section)
- DEV: `vitest@^4.1.7`, `@testing-library/react@^16.3.2`, `@testing-library/jest-dom@^6`, `jsdom@^25` (for component-level snapshot tests; harness also reused by 26-05 + 26-07)

NEW component — `MarkdownBody.tsx`:
- Props `{ body: string; skillName: string }`.
- Wraps `<article aria-label={`${skillName} documentation`}>` then `<ReactMarkdown allowedElements={ALLOWED} remarkPlugins={[remarkGfm]} components={{...}}>{body}</ReactMarkdown>`.
- `ALLOWED = ['h1','h2','h3','p','strong','em','code','ul','ol','li','a','pre']` (12 elements verbatim per UI-SPEC §MarkdownBody — DO NOT add tables/images/blockquote/task-list/HTML).
- Link component override: intercept `onClick`, `preventDefault`, then `if (/^https?:/.test(href))` call `openUrl(href)` from `@tauri-apps/plugin-opener`. Reject other schemes silently (no-op + console.warn).

REQUIREMENTS.md VIEW-04 cleanup:
- Current text: "Markdown preview pane renders SKILL.md body (post-frontmatter) with the same Markdown subset the existing `browse/markdown.rs` supports."
- New text: "Markdown preview pane renders SKILL.md body (post-frontmatter) with the SC#4 markdown subset: headings (H1–H3), paragraphs, inline emphasis (bold/italic/inline code), lists (ordered + unordered), links (open in system browser), and fenced code blocks. **Supersedes** the original `browse/markdown.rs` reference: that hand-rolled ratatui-only renderer covers only headers + horizontal rules + inline bold/italic/code and cannot be reused in a webview (D-08 / UI-SPEC Open Item 4)."
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Package legitimacy gate — markdown + test deps</name>
  <what-built>NEW deps (all `[ASSUMED]`):
    - `react-markdown@^10.1.0` (remarkjs org)
    - `remark-gfm@^4.0.1` (remarkjs org)
    - DEV: `vitest@^4.1.7`, `@testing-library/react@^16.3.2`, `@testing-library/jest-dom@^6`, `jsdom@^25`
  </what-built>
  <how-to-verify>
```bash
npm view react-markdown version time.modified license repository.url peerDependencies.react
npm view remark-gfm version repository.url license
npm view vitest version repository.url license
npm view @testing-library/react version repository.url license peerDependencies.react
npm view jsdom version repository.url license
```

Expected: `react-markdown` repo `github.com/remarkjs/react-markdown` MIT; `remark-gfm` `github.com/remarkjs/remark-gfm` MIT; `vitest` `github.com/vitest-dev/vitest` MIT; `@testing-library/react` `github.com/testing-library/react-testing-library` MIT (peerDeps React >=18 acceptable per Assumption A2 — Task 2 includes the smoke test); `jsdom` `github.com/jsdom/jsdom` MIT.
  </how-to-verify>
  <resume-signal>Type "approved" with version + repo URL pairs, or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install markdown deps + add MarkdownBody + wire into SkillsView + update REQUIREMENTS.md</name>
  <files>crates/tome-desktop/ui/package.json, crates/tome-desktop/ui/src/components/MarkdownBody.tsx, crates/tome-desktop/ui/src/components/MarkdownBody.module.css, crates/tome-desktop/ui/src/views/SkillsView.tsx, .planning/REQUIREMENTS.md</files>
  <read_first>
    - crates/tome-desktop/ui/package.json (current deps post-26-03)
    - crates/tome-desktop/ui/src/views/SkillsView.tsx (current detail column shape; should have `<DetailHeader>` + empty-selection placeholder after 26-03)
    - .planning/phases/26-read-only-views-alpha-cut/26-UI-SPEC.md §"Component Contract — MarkdownBody" (verbatim allow-list, heading token bindings H1/H2/H3, code styling, link colour, scrolling behavior) + §"Per-view Design — Skills" (DetailHeader fixed, MarkdownBody scrolls)
    - .planning/phases/26-read-only-views-alpha-cut/26-RESEARCH.md §"Standard Stack — Markdown" (lines 233-267) + §"Code Examples — Markdown body" (lines 1205-1233) + Pitfall 3 (allow-list silently strips) + Pitfall 7 (React 19 compatibility smoke test) + §"Security Domain" (no rehype-raw; reject javascript:/data:)
    - .planning/phases/26-read-only-views-alpha-cut/26-PATTERNS.md §"MarkdownBody.tsx" entry
    - .planning/REQUIREMENTS.md (current VIEW-04 text to replace)
    - crates/tome/src/browse/markdown.rs (just to confirm it's the thin ratatui renderer — the REQUIREMENTS.md update notes it's "not reused")
  </read_first>
  <action>
(a) **Install deps.** `cd crates/tome-desktop/ui && npm install react-markdown@^10.1.0 remark-gfm@^4.0.1` then `npm install --save-dev vitest@^4.1.7 @testing-library/react@^16.3.2 @testing-library/jest-dom@^6 jsdom@^25`. (The dev deps are also used by plan 26-05 and 26-07; installing here makes them available for the snapshot test in Task 2.)

(b) **Create `components/MarkdownBody.tsx`** per UI-SPEC §MarkdownBody + RESEARCH §"Code Examples":

```tsx
import ReactMarkdown from 'react-markdown';
import remarkGfm from 'remark-gfm';
import { openUrl } from '@tauri-apps/plugin-opener';
import styles from './MarkdownBody.module.css';

const ALLOWED = ['h1','h2','h3','p','strong','em','code','ul','ol','li','a','pre'] as const;

export function MarkdownBody({ body, skillName }: { body: string; skillName: string }) {
  return (
    <article aria-label={`${skillName} documentation`} className={styles.body}>
      <ReactMarkdown
        allowedElements={ALLOWED as unknown as string[]}
        remarkPlugins={[remarkGfm]}
        components={{
          a: ({ href, children }) => (
            <a href={href} onClick={async (e) => {
              e.preventDefault();
              if (href && /^https?:/.test(href)) {
                try { await openUrl(href); } catch (err) { console.warn('openUrl failed', err); }
              } else {
                console.warn('blocked non-http(s) URL scheme:', href);
              }
            }}>{children}</a>
          ),
        }}
      >{body}</ReactMarkdown>
    </article>
  );
}
```

Use the EXACT 12-element allow-list above (do not add `h4`/`h5`/`h6`/`blockquote`/`table`/`img`/`hr` — UI-SPEC §MarkdownBody is explicit). The scheme guard `/^https?:/` is the security mitigation per RESEARCH §"Security Domain" — `javascript:` and `data:` URLs are rejected with a console.warn.

(c) **Create `components/MarkdownBody.module.css`** with token bindings per UI-SPEC §MarkdownBody:
- `.body` — scrollable container; `overflow-y: auto`; padding `var(--space-5) var(--space-6)`; max-width readable measure ~720px.
- `.body h1` — `font: var(--text-title)`, weight 600, line-height 1.2, margin-top `var(--space-6)`, margin-bottom `var(--space-4)`.
- `.body h2` — `--text-subhead` 16px / 600 / 1.3.
- `.body h3` — `--text-body` 13px / 600 / 1.3 (NO raw 14px — UI-SPEC §Typography is explicit).
- `.body p` — `--text-body` 13px / 400 / 1.5.
- `.body ul, .body ol` — padding-left `var(--space-4)`.
- `.body li` — `--text-body` 13px / 400.
- `.body a` — color `var(--accent)`; text-decoration none; hover underline.
- `.body code` (inline) — font: `--text-small` mono 12px / 400; background `var(--bg-subtle)`; padding `1px 4px`; border-radius `var(--radius-xs)`.
- `.body pre` (fenced block) — background `var(--bg-subtle)`; border-radius `var(--radius-md)`; padding `var(--space-3)`; overflow-x auto. Inner `pre code` resets the inline code background.
- `.body strong` — weight 600.
- `.body em` — italic, weight 400.

(d) **Wire into SkillsView.** Edit `views/SkillsView.tsx`'s `<DetailColumn>` (or equivalent internal component from plan 26-03) so when `detail` is populated:
```
<>
  <DetailHeader detail={detail} ... />
  <MarkdownBody body={detail.body} skillName={detail.name} />
</>
```
The DetailHeader stays fixed at top of the detail column; MarkdownBody scrolls. Adjust the CSS Grid / Flexbox of `<DetailColumn>` so MarkdownBody gets the remaining height with `overflow-y: auto`.

(e) **Update REQUIREMENTS.md VIEW-04.** Find the line under `### Read-only views (VIEW)`:
> **VIEW-04**: Markdown preview pane renders SKILL.md body (post-frontmatter) with the same Markdown subset the existing `browse/markdown.rs` supports.

Replace with:
> **VIEW-04**: Markdown preview pane renders SKILL.md body (post-frontmatter) with the SC#4 markdown subset: headings (H1–H3), paragraphs, inline emphasis (bold/italic/inline code), lists (ordered + unordered), links (open in system browser), and fenced code blocks. **Supersedes** the original `browse/markdown.rs` reference — that hand-rolled ratatui-only renderer covers only headers + horizontal rules + inline bold/italic/code and cannot be reused for a webview (D-08 / Phase 26 UI-SPEC Open Item 4).

Commit this in the same commit as MarkdownBody.tsx so the requirement and implementation land together.
  </action>
  <verify>
    <automated>cd crates/tome-desktop/ui && npx tsc --noEmit && cd ../../.. && cargo check -p tome-desktop</automated>
    <human-check>cargo tauri dev — open a skill with a rich SKILL.md (one that contains H1, H2, H3, paragraphs, bold/italic, inline code, fenced code block, bulleted list, numbered list, link). Verify: (1) Every allowed element renders; (2) Tables/images/blockquotes (if present in the source) are STRIPPED entirely (no leftover bare text); (3) Clicking a link opens the system browser (e.g. Safari); (4) DevTools console shows no React 19 deprecation warnings from react-markdown (Pitfall 7); (5) DetailHeader stays fixed; markdown body scrolls below it.</human-check>
  </verify>
  <done>
    - `MarkdownBody.tsx` + `.module.css` exist with the 12-element allow-list and `openUrl` scheme-guarded link handler.
    - SkillsView's detail column renders DetailHeader (fixed) + MarkdownBody (scrolls).
    - REQUIREMENTS.md VIEW-04 updated to point at SC#4 (D-08 / Open Item 4 closed).
    - `npx tsc --noEmit` exits 0.
    - Manual smoke confirms no React 19 warnings (Assumption A2 holds).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vitest snapshot test for MarkdownBody allow-list</name>
  <files>crates/tome-desktop/ui/vitest.config.ts, crates/tome-desktop/ui/src/components/__tests__/MarkdownBody.test.tsx</files>
  <read_first>
    - crates/tome-desktop/ui/package.json (confirm vitest + testing-library installed)
    - crates/tome-desktop/ui/vite.config.ts (vitest config inherits Vite resolve)
    - .planning/phases/26-read-only-views-alpha-cut/26-RESEARCH.md Pitfall 3 (allow-list silently strips — snapshot test required) + §"Standard Stack — Supporting (dev / testing)" (vitest + testing-library versions)
    - .planning/phases/26-read-only-views-alpha-cut/26-UI-SPEC.md §"Component Contract — MarkdownBody" (the exact allow-list)
  </read_first>
  <behavior>
- Test 1: A fixture containing every allowed element (H1, H2, H3, paragraph, **bold**, *italic*, `inline code`, fenced code block, unordered list, ordered list, `[link](https://example.com)`) renders without losing any element's text content.
- Test 2: A fixture containing disallowed elements (table, image `![](...)`, blockquote `> ...`, raw HTML `<script>alert(1)</script>`) has those elements STRIPPED — their text is NOT present in the rendered DOM (Pitfall 3 catches silent-strip-of-needed-elements; this is the inverse — confirm silent-strip-of-unwanted-elements).
- Test 3: A link with a `javascript:` href, when clicked, does NOT call `openUrl` (mocked) — only `console.warn` is invoked. (Security mitigation per §"Security Domain".)
- Test 4: A link with an `https://example.com` href, when clicked, DOES call `openUrl` exactly once with that exact href (mocked).
  </behavior>
  <action>
(a) **Create `vitest.config.ts`** in `crates/tome-desktop/ui/`:
```ts
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/test-setup.ts'],
  },
});
```

(b) **Create `src/test-setup.ts`** with `import '@testing-library/jest-dom';`.

(c) **Create `src/components/__tests__/MarkdownBody.test.tsx`** implementing Tests 1-4 above. Mock `@tauri-apps/plugin-opener` using `vi.mock('@tauri-apps/plugin-opener', () => ({ openUrl: vi.fn() }))`. Use `@testing-library/react`'s `render` + `screen` to query the DOM; assert `getByText`/`queryByText` for presence/absence; assert `mockedOpenUrl.mock.calls` for the link-click tests. For Test 1, snapshot the entire `<article>` HTML and store under `src/components/__tests__/__snapshots__/`.

(d) **Add npm test script** in `crates/tome-desktop/ui/package.json` `scripts`:
```
"test": "vitest run",
"test:watch": "vitest"
```

Run `npm test` and confirm all 4 tests pass. Commit the snapshot file.
  </action>
  <verify>
    <automated>cd crates/tome-desktop/ui && npm test</automated>
  </verify>
  <done>
    - `vitest.config.ts` + `test-setup.ts` exist.
    - `MarkdownBody.test.tsx` has 4 tests covering allow-list + scheme-guard behavior.
    - `npm test` in `crates/tome-desktop/ui` exits 0; snapshot committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted SKILL.md content → webview | The user clones third-party git repos; SKILL.md body is attacker-controlled (worst case). |
| Markdown link href → OS browser | Scheme-restricted to `https?://` via Rust opener call; `javascript:`/`data:`/`file:` rejected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04-01 | EoP (XSS) | Malicious SKILL.md rendered as raw HTML | **mitigate (HIGH severity)** | `react-markdown` is safe-by-default (no `dangerouslySetInnerHTML`, no `rehype-raw`); `allowedElements` further restricts to 12 elements. Vitest test (Task 2 Test 2) asserts `<script>` is stripped. NEVER add `rehype-raw` to the remarkPlugins. |
| T-26-04-02 | EoP | Markdown link with `javascript:` / `data:` URI | mitigate | `onClick` handler regex `/^https?:/` rejects non-http(s) schemes; Vitest test (Task 2 Test 3) verifies `openUrl` is not called. |
| T-26-04-03 | DoS | Massive SKILL.md body crashes renderer | mitigate | Body capped at 1 MB Rust-side (plan 26-03's `collect_detail`); React renders truncated marker for over-cap files. |
| T-26-04-04 | Tampering | `react-markdown` API breakage on React 19 (Pitfall 7) | mitigate | Manual smoke test in Task 1 verify-block + Vitest test runs against React 19 (Task 2 catches any runtime exceptions). Fallback: `marked` + `DOMPurify` per Assumption A2. |
| T-26-04-SC | Tampering | npm installs (react-markdown, remark-gfm, vitest, testing-library, jsdom) | mitigate | Task 0 blocking-human checkpoint. |
</threat_model>

<verification>
- `cd crates/tome-desktop/ui && npm test` exits 0 (4 tests pass).
- `npx tsc --noEmit` exits 0.
- `cargo check -p tome-desktop` exits 0 (no Rust-side changes; verify the build still links).
- REQUIREMENTS.md VIEW-04 text reflects SC#4 wording.
- Manual: rich SKILL.md renders all allowed elements; tables/HTML stripped; https links open in system browser; no React 19 console warnings.
</verification>

<success_criteria>
- VIEW-04 satisfied at the **SC#4 subset level** (supersedes the literal `browse/markdown.rs` wording per D-08).
- Allow-list enforces the 12-element subset — both directions covered by tests (allowed elements render; disallowed are stripped).
- Security: no raw HTML passthrough; `javascript:`/`data:` URIs rejected at the link click handler.
- REQUIREMENTS.md updated in the same commit that ships the implementation (closes UI-SPEC Open Item 4).
</success_criteria>

<output>
Create `.planning/phases/26-read-only-views-alpha-cut/26-04-SUMMARY.md` when done.
</output>
</content>
</invoke>