---
phase: 26-read-only-views-alpha-cut
plan: 03
type: execute
wave: 2
depends_on: [26-01, 26-02]
files_modified:
  - crates/tome/src/actions.rs
  - crates/tome/src/lib.rs
  - crates/tome/src/browse/app.rs
  - crates/tome/src/skill.rs
  - crates/tome-desktop/Cargo.toml
  - crates/tome-desktop/capabilities/main.json
  - crates/tome-desktop/src/commands.rs
  - crates/tome-desktop/src/lib.rs
  - crates/tome-desktop/src/main.rs
  - crates/tome-desktop/ui/package.json
  - crates/tome-desktop/ui/src/bindings.ts
  - crates/tome-desktop/ui/src/views/SkillsView.tsx
  - crates/tome-desktop/ui/src/components/DetailHeader.tsx
  - crates/tome-desktop/ui/src/components/DetailHeader.module.css
  - crates/tome-desktop/ui/src/components/Button.tsx
  - crates/tome-desktop/ui/src/components/Button.module.css
  - crates/tome-desktop/ui/src/components/SkillContextMenu.tsx
  - crates/tome-desktop/ui/src/hooks/useSkillDetail.ts
  - crates/tome-desktop/ui/src/lib/ariaLabels.ts
autonomous: false
requirements: [VIEW-03]
user_setup: []
must_haves:
  truths:
    - "Selecting a skill row populates the detail column with frontmatter (parsed by lint.rs), source path (mono ellipsised), content hash (truncated sha256:...), last sync (relative time), managed/local badge, and disabled-on-this-machine badge"
    - "Three actions are available from BOTH the detail-header (primary buttons) AND right-click context menu on list rows: Open source folder, Copy path, Disable on this machine (D-07)"
    - "Open source folder calls @tauri-apps/plugin-opener's revealItemInDir against the resolved source path (Finder reveal on macOS)"
    - "Copy path calls @tauri-apps/plugin-clipboard-manager's writeText with the path string returned by the copy_path Tauri command"
    - "Disable on this machine writes machine.toml via tome::actions::set_skill_disabled (atomic temp+rename); the badge state updates after the file watcher fires (silent refresh in plan 26-06; in this plan, manual refetch suffices)"
    - "tome::actions module provides resolve_source_path, copy_path, set_skill_disabled shared between browse TUI and GUI; browse TUI's clipboard/opener glue is preserved (different OS-call shape)"
    - "Every new Tauri command goes through .map_err(TomeError::from) at the boundary; bindings.ts is regenerated and freshness gate passes"
  artifacts:
    - path: "crates/tome/src/actions.rs"
      provides: "Shared TUI+GUI handlers: resolve_source_path, set_skill_disabled (atomic write via machine::save)"
      contains: "pub fn resolve_source_path"
      contains_2: "pub fn set_skill_disabled"
    - path: "crates/tome-desktop/capabilities/main.json"
      provides: "Tauri capability allow-list for opener (revealItemInDir) + clipboard-manager (allow-write-text)"
    - path: "crates/tome-desktop/ui/src/components/DetailHeader.tsx"
      provides: "Skill detail header with name + badges + metadata grid + 3 action buttons per UI-SPEC §DetailHeader"
  key_links:
    - from: "crates/tome-desktop/src/commands.rs::set_skill_disabled"
      to: "tome::actions::set_skill_disabled"
      via: ".map_err(TomeError::from) boundary"
      pattern: "tome::actions::set_skill_disabled"
    - from: "crates/tome-desktop/ui/src/components/DetailHeader.tsx"
      to: "@tauri-apps/plugin-clipboard-manager writeText"
      via: "copy_path command returns String, JS calls writeText"
      pattern: "clipboard.*writeText|writeText.*clipboard"
    - from: "crates/tome/src/browse/app.rs"
      to: "tome::actions::set_skill_disabled"
      via: "Refactor — TUI App::execute_action calls actions:: instead of inlining"
      pattern: "actions::set_skill_disabled"
---

<objective>
Add the `tome::actions` module shared between the browse TUI and the GUI for path computation + machine.toml mutation; wire 4 new Tauri commands (`get_skill_detail`, `set_skill_disabled`, `open_source_folder`, `copy_path`); add the Tauri opener + clipboard-manager plugins with capability permissions; ship the DetailHeader React component with the three actions per D-05/D-06/D-07. This is the lone Phase-26 mutation (D-06: "Disable on this machine") plus the read-only frontmatter + metadata surface.

Purpose: VIEW-03 — Detail pane parity with browse TUI; exercises the write → file-watcher → silent-refresh loop early (de-risks Phase 27+); establishes the `tome::actions` cross-surface pattern Phase 27+ will extend for sync/remove/reassign.
Output: Click a skill → detail pane renders metadata + actions; click Disable → machine.toml updates atomically; right-click any list row → context menu with the same three actions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-read-only-views-alpha-cut/26-CONTEXT.md
@.planning/phases/26-read-only-views-alpha-cut/26-RESEARCH.md
@.planning/phases/26-read-only-views-alpha-cut/26-PATTERNS.md
@.planning/phases/26-read-only-views-alpha-cut/26-UI-SPEC.md
@.planning/phases/26-read-only-views-alpha-cut/26-02-PLAN.md
@crates/tome/src/list.rs
@crates/tome/src/machine.rs
@crates/tome/src/manifest.rs
@crates/tome/src/skill.rs
@crates/tome/src/lint.rs
@crates/tome/src/browse/app.rs
@crates/tome-desktop/src/commands.rs
@crates/tome-desktop/src/lib.rs

<interfaces>
<!-- The shape this plan adds and the constraints it must honor. -->

NEW Rust module — `crates/tome/src/actions.rs`:
- `pub fn resolve_source_path(name: &SkillName, config: &Config, paths: &TomePaths) -> Result<PathBuf>` — reads the manifest entry for `name`, returns the source path (the original disk location, not the library copy). For Unowned skills, falls back to the library path (`library_dir/<name>`). Error on missing skill.
- `pub fn set_skill_disabled(name: &SkillName, disabled: bool, machine_path: &Path) -> Result<()>` — loads `MachinePrefs` from `machine_path`, calls `toggle_global_disabled(name.clone(), disabled)` (already `pub(crate)` per `machine.rs:204-210` — promote to `pub` if needed for cross-module use), saves via `machine::save` (atomic temp+rename per `machine.rs:262-280`). Used by both TUI (browse/app.rs refactor) and GUI (set_skill_disabled command).

NEW shape — `pub struct SkillDetail` (lives in `crates/tome/src/skill.rs` or a new sub-module):
- `pub struct SkillDetail { pub name: SkillName, pub source_path: PathBuf, pub content_hash: ContentHash, pub last_sync: Option<DateTime<Utc>>, pub managed: bool, pub disabled: bool, pub frontmatter: SkillFrontmatter, pub body: String }`
- Specta-gated. Body is the post-frontmatter markdown text; capped at ~1MB (RESEARCH Security §"Markdown body size") via `if body.len() > 1_048_576 { body.truncate(1_048_576); body.push_str("\n\n[... truncated ...]") }`.

NEW Tauri commands (all in `crates/tome-desktop/src/commands.rs`, all follow PATTERNS.md §commands.rs template):
- `pub fn get_skill_detail(_app: AppHandle, name: SkillName) -> Result<SkillDetail, TomeError>`
- `pub fn set_skill_disabled(_app: AppHandle, name: SkillName, disabled: bool) -> Result<(), TomeError>`
- `pub fn open_source_folder(app: AppHandle, name: SkillName) -> Result<(), TomeError>` — uses `tauri-plugin-opener`'s `reveal_item_in_dir`.
- `pub fn copy_path(_app: AppHandle, name: SkillName) -> Result<String, TomeError>` — returns the resolved source path string; React side calls `@tauri-apps/plugin-clipboard-manager::writeText`.

Capability JSON (`crates/tome-desktop/capabilities/main.json`) — add permissions:
- `"opener:default"` (covers revealItemInDir)
- `"clipboard-manager:allow-write-text"` (covers writeText)

NEW npm packages:
- `@tauri-apps/plugin-opener@^2.5.4`
- `@tauri-apps/plugin-clipboard-manager@^2.3.2`

NEW cargo deps in `crates/tome-desktop/Cargo.toml`:
- `tauri-plugin-opener = "2"`
- `tauri-plugin-clipboard-manager = "2"`

Browse TUI refactor (`crates/tome/src/browse/app.rs`):
- Find the existing `execute_action` arms that toggle disabled state and resolve source paths. Replace inline `machine::load + toggle + machine::save` with `tome::actions::set_skill_disabled(&name, disabled, &machine_path)?`. Replace inline path-resolution with `tome::actions::resolve_source_path(&name, config, paths)?`. Keep the TUI's clipboard (`arboard`) + opener (`Command::new("open")`/`xdg-open`) glue unchanged — those are different OS-call shapes per RESEARCH §"Pattern 3 — Action handler refactor". Result: TUI tests still pass; same behavior; smaller `browse/app.rs` execute_action bodies.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Package legitimacy gate — Tauri plugin additions</name>
  <what-built>This plan introduces 4 NEW Tauri plugin deps (all `[ASSUMED]` per RESEARCH §"Package Legitimacy Audit"):
    - npm: `@tauri-apps/plugin-opener@^2.5.4`, `@tauri-apps/plugin-clipboard-manager@^2.3.2`
    - cargo: `tauri-plugin-opener = "2"`, `tauri-plugin-clipboard-manager = "2"`
  All four are official @tauri-apps org packages.
  </what-built>
  <how-to-verify>
```bash
npm view @tauri-apps/plugin-opener version repository.url license
npm view @tauri-apps/plugin-clipboard-manager version repository.url license
cargo info tauri-plugin-opener
cargo info tauri-plugin-clipboard-manager
```

Expected for all: repo `github.com/tauri-apps/plugins-workspace`, license `MIT OR Apache-2.0` (or similar Tauri-standard), versions matching the constraint.
  </how-to-verify>
  <resume-signal>Type "approved" with version + repo URL pairs, or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 1: tome::actions module + browse TUI refactor + SkillDetail shape</name>
  <files>crates/tome/src/actions.rs, crates/tome/src/lib.rs, crates/tome/src/browse/app.rs, crates/tome/src/skill.rs, crates/tome/src/machine.rs</files>
  <read_first>
    - crates/tome/src/lib.rs (module declarations — find where to insert `pub mod actions;`)
    - crates/tome/src/machine.rs (lines 1-330 — `MachinePrefs`, `load`, `save`, `toggle_global_disabled`, `default_machine_path`)
    - crates/tome/src/manifest.rs (find the manifest entry shape; identify how to resolve the source path for a SkillName)
    - crates/tome/src/skill.rs (existing `parse`, `SkillFrontmatter`, `Skill` types)
    - crates/tome/src/lint.rs (frontmatter parsing reused for detail — see what's exported)
    - crates/tome/src/list.rs (CORE-01 collect-shape template the new actions module should mirror)
    - crates/tome/src/browse/app.rs (find the disable/enable + open-source-dir arms in `execute_action` or equivalent; these are what gets refactored to call `actions::`)
    - .planning/phases/26-read-only-views-alpha-cut/26-PATTERNS.md §"crates/tome/src/actions.rs (NEW)" (the analog templates: list.rs file shape + machine.rs::save atomic-write site) + §"Pattern 3 — Action handler refactor" + §S-4 (atomic temp+rename)
    - .planning/phases/26-read-only-views-alpha-cut/26-RESEARCH.md §"Pattern 3" (lines 539-565)
  </read_first>
  <action>
(a) **Create `crates/tome/src/actions.rs`** with leading module doc comment per PATTERNS.md §actions.rs template:
```
//! Cross-surface skill actions (TUI + GUI).
//!
//! Pure-Rust helpers shared between the browse TUI (`browse::app`) and the
//! Tauri command surface (`tome-desktop::commands`). These functions own the
//! "what" of an action — compute the path, mutate machine.toml — but not the
//! "how" of presenting the result (clipboard, opener, focus management).
```

Two functions (per the §interfaces shapes above):
- `pub fn resolve_source_path(name: &SkillName, config: &Config, paths: &TomePaths) -> anyhow::Result<PathBuf>` — opens the manifest from `paths.config_dir()` (use the existing `tome::manifest::load_or_default` or equivalent — read manifest.rs to find the right call), looks up the entry by SkillName. Returns `entry.source_path()` for Owned skills, `paths.library_dir().join(name.as_str())` for Unowned. Error with `anyhow::anyhow!("skill {} not found in manifest", name)` if no entry exists.
- `pub fn set_skill_disabled(name: &SkillName, disabled: bool, machine_path: &Path) -> anyhow::Result<()>` — `let mut prefs = machine::load(machine_path)?; prefs.toggle_global_disabled(name.clone(), disabled); machine::save(&prefs, machine_path)`. Use `.context("failed to load machine prefs")` and `.context("failed to save machine prefs")` per S-1.

If `toggle_global_disabled` is currently `pub(crate)` (it is per `machine.rs:204-210`), either keep `pub(crate)` and make `actions::set_skill_disabled` live in the `tome` crate root (which is the same crate — `pub(crate)` works) — verify with `cargo check` after the change.

(b) **Wire in `lib.rs`** — add `pub mod actions;` in the correct location (alphabetical or grouped with `list`/`status`/`doctor`).

(c) **Add `SkillDetail` struct.** Place in `crates/tome/src/skill.rs` (or a new `skill_detail.rs` if cleaner — pick the cleanest home, document decision in commit). Shape per §interfaces:
```
#[derive(Debug, Clone, serde::Serialize)]
#[cfg_attr(feature = "bindings", derive(specta::Type))]
pub struct SkillDetail {
    pub name: SkillName,
    pub source_path: PathBuf,
    pub content_hash: Option<ContentHash>,
    pub last_sync: Option<DateTime<Utc>>,
    pub managed: bool,
    pub disabled: bool,
    pub frontmatter: SkillFrontmatter,
    pub body: String,
}
```
Add a `pub fn collect_detail(name: &SkillName, config: &Config, paths: &TomePaths) -> anyhow::Result<SkillDetail>` function that:
   1. Resolves the manifest entry to extract `content_hash`, `last_sync` (synced_at), `managed` (= `source_name.is_some()` AND source's directory has role Managed), and `source_path` via `actions::resolve_source_path`.
   2. Reads the SKILL.md file at `library_dir/<name>/SKILL.md` (the library-canonical copy per v0.10 model).
   3. Calls `lint::parse_frontmatter` (or whatever the current parser export is) to split frontmatter from body.
   4. Looks up disabled state by calling `machine::load(default_machine_path()?)?.is_disabled(name)` (or `prefs.disabled.contains(name)`).
   5. Caps body length: `if body.len() > 1_048_576 { body.truncate(1_048_576); body.push_str("\n\n[... truncated ...]"); }` per RESEARCH Security §"Markdown body size".

Ensure `SkillFrontmatter` and `ContentHash` are also specta-gated (verify in `lint.rs` / `validation.rs`; if not, gate them).

(d) **Refactor browse TUI.** In `crates/tome/src/browse/app.rs`, find every place that:
- Calls `machine::load` + `toggle_global_disabled` + `machine::save` inline → replace with `actions::set_skill_disabled(&name, disabled, &machine_path)?`.
- Resolves a skill's source path inline → replace with `actions::resolve_source_path(&name, config, paths)?`.

KEEP clipboard (`arboard`) and opener (`Command::new("open")` / `xdg-open`) call sites in the TUI — they are TUI-specific (different OS-call shape than GUI Finder reveal). The refactor is about path/machine-toml computation only, NOT about presentation.

(e) **Test surface.** Add unit tests under `#[cfg(test)] mod tests` in `actions.rs`:
   - `set_skill_disabled_writes_machine_toml` — create a `TempDir`, write a minimal `machine.toml`, call `set_skill_disabled(&name, true, &path)`, read back and assert `prefs.disabled.contains(&name)`. Then call with `false`, assert removed.
   - `resolve_source_path_returns_manifest_source_for_owned` — set up a tempdir with a manifest entry, assert the returned path matches.
   - `resolve_source_path_falls_back_to_library_for_unowned` — manifest entry with `source_name: None`, assert library_dir/<name>.

Run `cargo test -p tome`. The existing TUI tests (`browse::app::tests::*`) must still pass — if a test was asserting the inlined call shape, update it to assert the refactored behavior.
  </action>
  <verify>
    <automated>cargo test -p tome --lib actions:: && cargo test -p tome --lib browse:: && cargo test -p tome --test cli && cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <done>
    - `crates/tome/src/actions.rs` exists with `resolve_source_path` + `set_skill_disabled` + unit tests.
    - `SkillDetail` struct + `collect_detail()` function exist; specta-gated.
    - `browse/app.rs` refactored to call `actions::*`; browse tests pass.
    - `cargo test -p tome` + `cargo test -p tome --test cli` exit 0.
    - `cargo clippy --all-targets -- -D warnings` exit 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tauri commands + plugins + capabilities + DetailHeader UI + context menu</name>
  <files>crates/tome-desktop/Cargo.toml, crates/tome-desktop/capabilities/main.json, crates/tome-desktop/src/commands.rs, crates/tome-desktop/src/lib.rs, crates/tome-desktop/src/main.rs, crates/tome-desktop/ui/package.json, crates/tome-desktop/ui/src/bindings.ts, crates/tome-desktop/ui/src/views/SkillsView.tsx, crates/tome-desktop/ui/src/components/DetailHeader.tsx, crates/tome-desktop/ui/src/components/DetailHeader.module.css, crates/tome-desktop/ui/src/components/Button.tsx, crates/tome-desktop/ui/src/components/Button.module.css, crates/tome-desktop/ui/src/components/SkillContextMenu.tsx, crates/tome-desktop/ui/src/hooks/useSkillDetail.ts, crates/tome-desktop/ui/src/lib/ariaLabels.ts</files>
  <read_first>
    - crates/tome-desktop/Cargo.toml (current deps; how Phase 25 declared `tauri` etc.)
    - crates/tome-desktop/capabilities/main.json (current permissions; baseline shape)
    - crates/tome-desktop/src/main.rs (Phase 25 setup closure; where to add `.plugin(tauri_plugin_opener::init())` and `.plugin(tauri_plugin_clipboard_manager::init())`)
    - crates/tome-desktop/src/commands.rs + lib.rs (post-26-01 + 26-02 state with `get_status`, `list_skills`)
    - crates/tome-desktop/ui/package.json
    - .planning/phases/26-read-only-views-alpha-cut/26-UI-SPEC.md §"Component Contract — DetailHeader" (3-row layout: name + badges; metadata grid Source/Hash/Sync; action buttons), §"Component Contract — Button" (primary/secondary/small-fix variants), §"Keyboard Map" (⌘C/⌘O/⌘D bound to copy/open/disable when a skill is selected), §"VoiceOver labels — explicit contracts" (aria-label templates verbatim)
    - .planning/phases/26-read-only-views-alpha-cut/26-RESEARCH.md §"Pitfall 4 — Tauri opener / clipboard plugin permissions" (capabilities JSON additions) + §"Standard Stack — Core (NEW)" (plugin versions)
    - .planning/phases/26-read-only-views-alpha-cut/26-PATTERNS.md §"crates/tome-desktop/src/commands.rs (MODIFY)" template + §S-2 (TomeError boundary) + §S-7 (single make_builder)
  </read_first>
  <action>
(a) **Add Rust plugin deps.** Edit `crates/tome-desktop/Cargo.toml` `[dependencies]`:
```
tauri-plugin-opener = "2"
tauri-plugin-clipboard-manager = "2"
```

(b) **Install plugins in main.rs.** Edit `crates/tome-desktop/src/main.rs`'s `tauri::Builder::default()` chain — add `.plugin(tauri_plugin_opener::init())` and `.plugin(tauri_plugin_clipboard_manager::init())` before `.invoke_handler(...)`.

(c) **Capability JSON.** Edit `crates/tome-desktop/capabilities/main.json`:
- Ensure the `permissions` array contains: `"core:default"`, `"core:event:default"` (Phase 25 baseline), plus NEW: `"opener:default"` and `"clipboard-manager:allow-write-text"`. The exact permission names match the Tauri 2 v2.5.4 plugin schemas (verify via plugin docs if unclear).
- Do NOT add `"fs:default"` — file watcher is Rust-side (plan 26-06, OQ-3 resolution).

(d) **Add 4 new Tauri commands** in `crates/tome-desktop/src/commands.rs`, each following the PATTERNS.md §commands.rs template (load_context + map_err):

```
#[tauri::command] #[specta::specta]
pub fn get_skill_detail(_app: tauri::AppHandle, name: SkillName) -> Result<tome::skill::SkillDetail, TomeError> {
    let (config, paths) = load_context().map_err(TomeError::from)?;
    tome::skill::collect_detail(&name, &config, &paths).map_err(TomeError::from)
}

#[tauri::command] #[specta::specta]
pub fn set_skill_disabled(_app: tauri::AppHandle, name: SkillName, disabled: bool) -> Result<(), TomeError> {
    let machine_path = tome::machine::default_machine_path().map_err(|e| TomeError::from(anyhow::Error::from(e)))?;
    tome::actions::set_skill_disabled(&name, disabled, &machine_path).map_err(TomeError::from)
}

#[tauri::command] #[specta::specta]
pub fn open_source_folder(app: tauri::AppHandle, name: SkillName) -> Result<(), TomeError> {
    use tauri_plugin_opener::OpenerExt;
    let (config, paths) = load_context().map_err(TomeError::from)?;
    let src = tome::actions::resolve_source_path(&name, &config, &paths).map_err(TomeError::from)?;
    app.opener()
        .reveal_item_in_dir(&src)
        .map_err(|e| TomeError::from(anyhow::anyhow!(e.to_string())))
}

#[tauri::command] #[specta::specta]
pub fn copy_path(_app: tauri::AppHandle, name: SkillName) -> Result<String, TomeError> {
    let (config, paths) = load_context().map_err(TomeError::from)?;
    let src = tome::actions::resolve_source_path(&name, &config, &paths).map_err(TomeError::from)?;
    Ok(src.display().to_string())
}
```

If `SkillName` does not derive `serde::Deserialize` (it should; tauri-specta needs it for input args), verify in `discover.rs` and add if missing.

(e) **Register in make_builder.** Edit `crates/tome-desktop/src/lib.rs::make_builder()`'s `collect_commands![...]`:
```
commands::get_status,
commands::list_skills,
commands::get_skill_detail,         // NEW
commands::set_skill_disabled,       // NEW
commands::open_source_folder,       // NEW
commands::copy_path,                // NEW
```

(f) **Regenerate bindings.** `cargo run -p tome-desktop --bin gen-bindings`. Commit `bindings.ts`. Confirm `commands.getSkillDetail`, `commands.setSkillDisabled`, `commands.openSourceFolder`, `commands.copyPath` (or snake_case equivalents) appear.

(g) **Install JS plugins.** `cd crates/tome-desktop/ui && npm install @tauri-apps/plugin-opener@^2.5.4 @tauri-apps/plugin-clipboard-manager@^2.3.2`.

(h) **Add UI atoms.** Create:
   - `components/Button.tsx` + `.module.css` — props `{ variant: 'primary' | 'secondary' | 'small-fix'; children; onClick?; ariaLabel?; disabled? }`. Wraps React Aria `<Button>`. Token bindings per UI-SPEC §Button: primary `--accent`/`--accent-on`/weight 600; secondary `--bg-window`/`--label-primary`/weight 400 + 1px `--separator` border; small-fix smaller padding + `--text-small` weight 600.
   - `lib/ariaLabels.ts` — pure functions per UI-SPEC §"VoiceOver labels": `export function openSourceLabel(name: string) { return \`Open source folder for ${name} in Finder\`; }`, similar for `copyPathLabel`, `disableSkillLabel`, `skillRowLabel({name, sourceName, managed, disabled})`. Pure helpers; no JS business logic.
   - `components/DetailHeader.tsx` + `.module.css` — props `{ detail: SkillDetail; onOpenSource: () => void; onCopyPath: () => void; onDisableToggle: () => void; copyState: 'idle' | 'copied' }`. Three rows per UI-SPEC:
     1. Skill name (`--text-title` 22px / 600) + trailing badges `<Badge subtype="managed">Managed</Badge>` if detail.managed, `<Badge subtype="disabled">Disabled</Badge>` if detail.disabled.
     2. Metadata grid (3 cells: SOURCE / HASH / SYNC) — each label `--text-small` 12px / 600 uppercase + value `--text-small` 12px / 400 (mono for path + hash, regular for relative time). Truncate hash to `sha256:abc12345…` (first 14 chars after prefix). Truncate path middle-ellipsis if longer than column width.
     3. Action buttons row: `[Open source folder]` (secondary) `[Copy path]` (secondary; label changes to "Copied" for 2s after click) `[Disable on this machine]` (primary; label flips to "Enable on this machine" when detail.disabled). Each button's `ariaLabel` from `ariaLabels.ts`. Order matches UI-SPEC.
   - `components/SkillContextMenu.tsx` — wraps React Aria `<MenuTrigger trigger="longPress">` or a manual `onContextMenu` that opens a React Aria `<Menu>`. Three items: "Open source folder", "Copy path", "Disable on this machine" / "Enable on this machine". Each `onAction` dispatches the same command as the DetailHeader buttons (D-07). Mount this menu around each `<SkillListRow>` in `SkillsView.tsx`.

(i) **useSkillDetail hook.** `hooks/useSkillDetail.ts` — `export function useSkillDetail(name: string | null)`. When `name === null` returns `{ detail: null, err: null }`. When non-null, calls `commands.getSkillDetail(name)` via S-8 Result-narrowing on every `name` change (`useEffect` dep). No event subscriptions (lands in 26-06).

(j) **Wire DetailHeader into SkillsView.** Replace the "Detail pane ships in 26-03" placeholder in `views/SkillsView.tsx` with:
```
{selected && <DetailColumn name={selected} />}
{!selected && <EmptySelectionPlaceholder />}
```
where `<DetailColumn>` is a new internal component (or `<DetailContainer>`) that calls `useSkillDetail(name)`, then renders `<DetailHeader detail={detail} onOpenSource={...} onCopyPath={...} onDisableToggle={...} />`. The action handlers:
- `onOpenSource`: `await commands.openSourceFolder(name)` (Result-narrow; on error, surface via `<ErrorBanner>`).
- `onCopyPath`: `const res = await commands.copyPath(name); if (res.status === 'ok') { await writeText(res.data); setCopyState('copied'); setTimeout(() => setCopyState('idle'), 2000); } else { setErr(res.error); }` — `writeText` imported from `@tauri-apps/plugin-clipboard-manager`.
- `onDisableToggle`: `await commands.setSkillDisabled(name, !detail.disabled)` then `refetch()` (in-this-plan; in 26-06 the watcher auto-refreshes).

(k) **Wire right-click context menu.** In `SkillsView.tsx`, wrap each `<SkillListRow>` with `<SkillContextMenu name={skill.name} disabled={skill.disabled}>...</SkillContextMenu>` so right-click on any row presents the same three actions (D-07). Actions dispatch the same commands as DetailHeader (extract handlers into a `useSkillActions(name)` custom hook to avoid duplication).

(l) **Keyboard shortcuts** (UI-SPEC §Keyboard Map): in `SkillsView.tsx`, bind global keydown listener — when a skill is selected, `⌘C` triggers copy path, `⌘O` triggers open source, `⌘D` triggers disable toggle. Use `event.metaKey && event.key === 'c'` etc. Plan 26-07 audits these for HIG conflicts (Pitfall 9).

Run `cargo run -p tome-desktop --bin gen-bindings && git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts` and `cd crates/tome-desktop/ui && npx tsc --noEmit`.
  </action>
  <verify>
    <automated>cargo clippy --all-targets -- -D warnings && cargo run -p tome-desktop --bin gen-bindings && git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts && cd crates/tome-desktop/ui && npx tsc --noEmit</automated>
    <human-check>cargo tauri dev — confirm: (1) Click a skill in Skills list → detail header populates with name, managed/local badge, source path (mono), hash (truncated), last sync; (2) Click "Open source folder" → Finder reveals the source dir; (3) Click "Copy path" → button label flips to "Copied" for 2s and the clipboard contains the path (paste into Terminal to verify); (4) Click "Disable on this machine" → button label flips to "Enable on this machine" and the Disabled badge appears (after a manual refetch — silent refresh lands in 26-06); (5) Right-click a list row → context menu shows the three actions and they work; (6) Keyboard: ⌘C copies, ⌘O opens, ⌘D toggles disable; (7) Check `~/.config/tome/machine.toml` content updated after Disable click.</human-check>
  </verify>
  <done>
    - 4 new Tauri commands registered + bindings fresh.
    - capabilities/main.json grants opener + clipboard-manager permissions.
    - DetailHeader renders all 3 rows per UI-SPEC; action buttons fire commands; aria-labels match UI-SPEC verbatim.
    - SkillContextMenu provides right-click access to the same actions.
    - Keyboard shortcuts ⌘C/⌘O/⌘D wired (HIG audit deferred to 26-07).
    - `machine.toml` updates atomically on Disable click; browse TUI tests still pass.
    - Freshness gate passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview ↔ Tauri command IPC | New surface: 4 commands, all behind capability allow-list |
| machine.toml file write | Atomic temp+rename via `machine::save`; no partial-write state |
| SkillName input from JS | Validated by `SkillName::new`/`Deserialize` at the boundary (rejects `.`, `..`, path separators per Phase 25) |
| Source-path opener | Resolved Rust-side via `actions::resolve_source_path`; OS-call (Finder reveal) happens with that path only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03-01 | EoP | SkillName injection via Tauri command (`../../etc/passwd`) | mitigate | `SkillName` newtype `Deserialize` rejects `.`, `..`, path separators at construction (Phase 25 validation). Verified in unit test for `actions::resolve_source_path`. |
| T-26-03-02 | Tampering | Concurrent `machine.toml` write from CLI while GUI writes | mitigate | Atomic temp+rename (`machine::save` lines 262-280). Watcher in 26-06 reconciles on external change. Worst case is last-writer-wins, no partial reads. |
| T-26-03-03 | EoP | Tauri capability surface expansion (opener + clipboard) | mitigate | Capabilities allow-list explicitly grants only `opener:default` + `clipboard-manager:allow-write-text` — no broader fs permissions. Documented in capability JSON. |
| T-26-03-04 | Tampering | Browse TUI behavior regression after refactor | mitigate | `cargo test -p tome --lib browse::` + `cargo test -p tome --test cli` continue to pass; refactor is mechanical (extract helpers, not reshape). |
| T-26-03-05 | InformationDisclosure | Clipboard write exposes paths to all OS clipboard consumers | accept | User-initiated action; same trust posture as the CLI's existing `tome browse` "copy path" feature. |
| T-26-03-06 | DoS | SKILL.md body sent to webview unbounded | mitigate | `collect_detail` caps body at 1 MB; truncated marker appended. |
| T-26-03-SC | Tampering | Tauri plugin installs (opener + clipboard-manager) | mitigate | Task 0 blocking-human checkpoint verifies official @tauri-apps repo before install. |
</threat_model>

<verification>
- `cargo test -p tome --lib actions::` + `cargo test -p tome --lib browse::` + `cargo test -p tome --test cli` all exit 0.
- `cargo clippy --all-targets -- -D warnings` exits 0.
- `cargo run -p tome-desktop --bin gen-bindings && git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts` exits 0.
- `npx tsc --noEmit` in `crates/tome-desktop/ui` exits 0.
- Manual smoke (checkpoint): Disable click writes `machine.toml`, Open reveals in Finder, Copy puts path on clipboard.
</verification>

<success_criteria>
- VIEW-03 satisfied: detail pane renders frontmatter, source path, content hash, last sync, managed/local badge, disabled state; all 3 actions wired to the same Rust handlers the browse TUI calls (via `tome::actions`).
- D-06: Disable-on-this-machine ships as a live `machine.toml` write — atomic, single-action, exercises the write→watcher→refresh loop (silent refresh in 26-06).
- D-07: Same 3 actions accessible from BOTH detail-header primary buttons AND right-click context menu on list rows.
- Browse TUI is refactored to call `actions::*` without behavior change (TUI tests pass).
</success_criteria>

<output>
Create `.planning/phases/26-read-only-views-alpha-cut/26-03-SUMMARY.md` when done.
</output>
</content>
</invoke>