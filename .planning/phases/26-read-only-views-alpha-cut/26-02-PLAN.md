---
phase: 26-read-only-views-alpha-cut
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - crates/tome-desktop/Cargo.toml
  - crates/tome-desktop/tauri.conf.json
  - crates/tome-desktop/src/commands.rs
  - crates/tome-desktop/src/lib.rs
  - crates/tome-desktop/ui/package.json
  - crates/tome-desktop/ui/src/bindings.ts
  - crates/tome-desktop/ui/src/App.tsx
  - crates/tome-desktop/ui/src/tokens.css
  - crates/tome-desktop/ui/src/styles.css
  - crates/tome-desktop/ui/src/shell/Window.tsx
  - crates/tome-desktop/ui/src/shell/Window.module.css
  - crates/tome-desktop/ui/src/shell/Titlebar.tsx
  - crates/tome-desktop/ui/src/shell/Titlebar.module.css
  - crates/tome-desktop/ui/src/shell/Sidebar.tsx
  - crates/tome-desktop/ui/src/shell/Sidebar.module.css
  - crates/tome-desktop/ui/src/shell/ContentPane.tsx
  - crates/tome-desktop/ui/src/shell/ContentPane.module.css
  - crates/tome-desktop/ui/src/views/SkillsView.tsx
  - crates/tome-desktop/ui/src/views/SkillsView.module.css
  - crates/tome-desktop/ui/src/components/SearchField.tsx
  - crates/tome-desktop/ui/src/components/SearchField.module.css
  - crates/tome-desktop/ui/src/components/PopupMenu.tsx
  - crates/tome-desktop/ui/src/components/PopupMenu.module.css
  - crates/tome-desktop/ui/src/components/SkillListRow.tsx
  - crates/tome-desktop/ui/src/components/SkillListRow.module.css
  - crates/tome-desktop/ui/src/hooks/useSkills.ts
  - crates/tome-desktop/ui/src/hooks/useFuzzySearch.ts
  - crates/tome-desktop/ui/src/stores/router.ts
autonomous: false
requirements: [VIEW-02, NF-01]
user_setup: []
must_haves:
  truths:
    - "User sees a 3-column NavigationSplitView shell: vibrancy sidebar (Status/Skills/Health) + middle list column + right detail column"
    - "App lands on Status on launch (D-02); clicking Skills in the sidebar switches the content pane to the Skills view"
    - "Skills view renders an always-on SearchField pinned at the top of the list column with Sort (Name/Source/Recent — default Name) and Group (None/Source/Role — default None) PopupMenus"
    - "Virtualised SkillListRow list renders 2000+ skills without dropping frames; arrow-key + Home/End/PgUp/PgDn navigation works via React Aria ListBox"
    - "Typing in SearchField filters the list as-you-type via JS-side fuzzy matching (no per-keystroke Tauri command); ⌘F focuses the search field"
    - "list_skills Tauri command returns ListReport via tome::list::collect; bindings.ts is regenerated and committed"
  artifacts:
    - path: "crates/tome-desktop/ui/src/tokens.css"
      provides: "CSS custom-property design tokens driven by prefers-color-scheme (per UI-SPEC §Color light + provisional dark)"
    - path: "crates/tome-desktop/ui/src/shell/Window.tsx"
      provides: "3-column NavigationSplitView shell"
    - path: "crates/tome-desktop/ui/src/views/SkillsView.tsx"
      provides: "Virtualised skill list with fuzzy search, sort, group-by toolbar"
    - path: "crates/tome-desktop/src/commands.rs"
      provides: "list_skills Tauri command wrapping tome::list::collect"
  key_links:
    - from: "crates/tome-desktop/ui/src/views/SkillsView.tsx"
      to: "react-aria-components Virtualizer + ListBox"
      via: "OQ-1 path A — React Aria native Virtualizer (zero extra dep, free a11y)"
      pattern: "Virtualizer|ListLayout"
    - from: "crates/tome-desktop/ui/src/hooks/useFuzzySearch.ts"
      to: "fuse.js"
      via: "JS-side fuzzy filter — avoids per-keystroke IPC (60fps budget)"
      pattern: "fuse|Fuse"
---

<objective>
Stand up the 3-column NavigationSplitView shell + design tokens, register the `list_skills` Tauri command, and ship the Skills view with a virtualised list, JS-side fuzzy search, and sort/group toolbar. After this plan the app's structural skeleton (Window/Titlebar/Sidebar/ContentPane) is in place — every later view (Health, future Sync/Config/Backup) drops into the same shell without re-layout.

Purpose: VIEW-02 + NF-01 (2000 skills @ 60fps); also establishes the design-token system + Sidebar/Titlebar/ContentPane atoms inherited by all later phases.
Output: Skills view renders against the user's real skill list at `cargo tauri dev`; ⌘F focuses search; 2000-skill bench (Plan 26-08) will verify the perf budget.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-read-only-views-alpha-cut/26-CONTEXT.md
@.planning/phases/26-read-only-views-alpha-cut/26-RESEARCH.md
@.planning/phases/26-read-only-views-alpha-cut/26-PATTERNS.md
@.planning/phases/26-read-only-views-alpha-cut/26-UI-SPEC.md
@.planning/phases/26-read-only-views-alpha-cut/26-DESIGN-EXTRACT.md
@.planning/phases/26-read-only-views-alpha-cut/26-01-PLAN.md
@crates/tome/src/list.rs
@crates/tome-desktop/src/commands.rs
@crates/tome-desktop/src/lib.rs
@crates/tome-desktop/ui/src/App.tsx
@crates/tome-desktop/ui/src/bindings.ts

<interfaces>
<!-- Cross-boundary types and patterns this plan adds. -->

From crates/tome/src/list.rs:
- `pub struct ListReport { pub skills: Vec<DiscoveredSkill>, pub warnings: Vec<String> }`
- `pub fn collect(config: &Config) -> Result<ListReport>`
- `DiscoveredSkill` already has `name: SkillName`, `source_name: Option<DirectoryName>`, `origin: SkillOrigin` (Owned/Unowned per CORE-01).
- Both types must be specta-gated (verify via grep of `specta::Type`); if not, add `#[cfg_attr(feature = "bindings", derive(specta::Type))]` in this plan.

New Tauri command (this plan adds):
- `pub fn list_skills(_app: tauri::AppHandle) -> Result<tome::list::ListReport, TomeError>` — copies the get_status template (PATTERNS.md §commands.rs).

OQ-1 RESOLUTION: Use React Aria's native `<Virtualizer>` (path A from RESEARCH §"Standard Stack — Virtualisation"). Rationale: zero extra dep (already in `react-aria-components`), free a11y semantics, fixed 52px rows (UI-SPEC anchor) avoid the measurement complexity. Fallback to `@tanstack/react-virtual` if 26-08 bench fails 60fps. **Flag for human review: UI-SPEC §Design System currently names "TanStack Virtual" — file a small UI-SPEC amendment commit during this plan noting the path-A pick.**
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Package legitimacy gate — npm + cargo additions</name>
  <what-built>This plan introduces 6 NEW dependencies (all `[ASSUMED]` per RESEARCH §"Package Legitimacy Audit" since slopcheck was unavailable):
    - npm: `react-aria-components@^1.17.0`, `fuse.js@^7.x` (verify exact version)
    - cargo: none new on the Rust side in THIS plan (clipboard/opener plugins land in 26-03; notify in 26-06)
  </what-built>
  <how-to-verify>
Run these checks and paste results into the resume signal:

```bash
# JS packages
npm view react-aria-components version time.modified license repository.url
npm view fuse.js version time.modified license repository.url
```

Expected:
- `react-aria-components` published by `@adobe` org, repo `github.com/adobe/react-spectrum`, license Apache-2.0, version `1.17.0` (or later 1.17.x).
- `fuse.js` repo `github.com/krisk/Fuse`, license Apache-2.0, version `^7.x`. **fuse.js is the elevated-risk package** (popular name = slopsquat target per RESEARCH Pitfall on package legitimacy); confirm the repo URL points to `krisk/Fuse` exactly.

Approve only if both pass the human eyeball check.
  </how-to-verify>
  <resume-signal>Type "approved" with the verified version + repo URL pairs, or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Design tokens + Window/Titlebar/Sidebar/ContentPane shell</name>
  <files>crates/tome-desktop/tauri.conf.json, crates/tome-desktop/ui/package.json, crates/tome-desktop/ui/src/tokens.css, crates/tome-desktop/ui/src/styles.css, crates/tome-desktop/ui/src/App.tsx, crates/tome-desktop/ui/src/shell/Window.tsx, crates/tome-desktop/ui/src/shell/Window.module.css, crates/tome-desktop/ui/src/shell/Titlebar.tsx, crates/tome-desktop/ui/src/shell/Titlebar.module.css, crates/tome-desktop/ui/src/shell/Sidebar.tsx, crates/tome-desktop/ui/src/shell/Sidebar.module.css, crates/tome-desktop/ui/src/shell/ContentPane.tsx, crates/tome-desktop/ui/src/shell/ContentPane.module.css, crates/tome-desktop/ui/src/stores/router.ts</files>
  <read_first>
    - crates/tome-desktop/tauri.conf.json (current shape — Phase 25 baseline)
    - crates/tome-desktop/ui/package.json
    - crates/tome-desktop/ui/src/App.tsx (post-26-01 — renders `<StatusView />`)
    - crates/tome-desktop/ui/src/styles.css
    - .planning/phases/26-read-only-views-alpha-cut/26-UI-SPEC.md §"Design System", §"Spacing Scale", §"Typography", §"Color" (full token tables), §"Component Contract — Shell" (Window/Titlebar/Sidebar/NavItem/ContentPane), §"Keyboard Map" (⌘1/⌘2/⌘3 mapping)
    - .planning/phases/26-read-only-views-alpha-cut/26-DESIGN-EXTRACT.md (sidebar width 210px, titlebar 44px, nav-item 26px, list-row 52px anchors)
    - .planning/phases/26-read-only-views-alpha-cut/26-RESEARCH.md §"Standard Stack — Native menu bar" (NOT installed in this plan; deferred to 26-07) + §"Pattern 7" preview only
    - .planning/phases/26-read-only-views-alpha-cut/26-PATTERNS.md §"Shell" entry (no in-repo analog; reference UI-SPEC verbatim)
  </read_first>
  <action>
(a) **Install `react-aria-components`.** Run `cd crates/tome-desktop/ui && npm install react-aria-components@^1.17.0` (use the version approved in Task 0). Commit `package.json` + `package-lock.json`.

(b) **Tauri config — unified titlebar + vibrancy.** Edit `crates/tome-desktop/tauri.conf.json` to set the main window's `titleBarStyle` to `"Overlay"` (Tauri 2 unified titlebar with traffic lights overlaid), enable macOS window vibrancy on the sidebar via the appropriate Tauri 2 macOS-specific option (verify in `https://v2.tauri.app/reference/config/` — set `macOSPrivateApi` if required, and apply NSVisualEffectView material `Sidebar`). Document each option's purpose in a JSON comment if the format allows, or in a single-line trailing summary in the commit message. Default window size: `{ width: 1100, height: 740, minWidth: 900, minHeight: 600 }`.

(c) **Design tokens.** Create `ui/src/tokens.css` containing the verbatim token sets from UI-SPEC §Color light + provisional dark, §Spacing (`--space-1` 4px through `--space-12` 48px, plus `--radius-xs` 3px, `--radius-md` 6px, `--radius-pill` 9999px), and §Typography (declare `--text-small`, `--text-body`, `--text-subhead`, `--text-title` as full font shorthand or split into `--text-small-size`/`--text-small-line` if cleaner). Dark tokens live under `@media (prefers-color-scheme: dark)`. Reduced-transparency fallback under `@media (prefers-reduced-transparency: reduce)` swaps `--sidebar-material` to the solid value. Import this file from `main.tsx` (or extend `styles.css` to `@import "./tokens.css"` at the top).

(d) **Shell components.** Create:
   - `shell/Window.tsx` + `.module.css` — root container, CSS Grid `grid-template-columns: 210px minmax(280px, 380px) 1fr` (when in split mode) or `grid-template-columns: 210px 1fr` (single-pane). Accept `mode: 'split' | 'single'` prop and switch grid template accordingly. Background `var(--bg-app)`. Renders `<main>` landmark wrapping children.
   - `shell/Titlebar.tsx` + `.module.css` — fixed 44px height, `role="banner"`, `aria-label="tome ${section}"`, centred title `tome — ${section}` in `--text-body` weight 600. Traffic lights are OS-owned (rendered by Tauri unified titlebar); no JS code for them.
   - `shell/Sidebar.tsx` + `.module.css` — 210px width, `--sidebar-material` background, `prefers-reduced-transparency` solid fallback. Renders `LIBRARY` caption (uppercase, weight 600, `--label-secondary`) + a React Aria `<ListBox aria-label="Sections">` containing three `<ListBoxItem>`s (Status, Skills, Health). Selected item gets the `--accent` capsule fill + `--accent-on` text + weight 600. Footer "tome · {N} skills" at `--text-small` 400 `--label-secondary` reads the count from `useStatus`'s `library_count` (Sidebar imports `useStatus`). Health NavItem accepts a `badgeCount` prop — when > 0, shows a trailing red circle (`Badge--health-count` shape: `--danger` fill, `--accent-on` text, `--text-small` 600). In this plan, hardcode `badgeCount={0}` (real wiring lands in plan 26-05).
   - `shell/ContentPane.tsx` + `.module.css` — header with view title (`--text-title` 22px weight 600) + optional trailing meta slot + scrolling body. Props `{ title: string; trailing?: ReactNode; children: ReactNode }`. `<main aria-label={title}>` landmark.

(e) **Router store.** Create `ui/src/stores/router.ts` exporting a tiny `useRouter()` hook using `useSyncExternalStore` (or a 10-line custom subscribable) with state `{ view: 'status' | 'skills' | 'health' }` defaulting to `'status'` (D-02 "lands on Status"). Export `setView(view)`. No Redux/Zustand (RESEARCH §Anti-Patterns).

(f) **Wire App.tsx.** Replace `<StatusView />` direct render with:
```
<Window mode={view === 'skills' ? 'split' : 'single'}>
  <Titlebar section={sectionLabel(view)} />
  <Sidebar selected={view} onChange={setView} />
  <ContentPane title={sectionLabel(view)}>
    {view === 'status' && <StatusView />}
    {view === 'skills' && <SkillsView />}
    {view === 'health' && <HealthPlaceholder />}
  </ContentPane>
</Window>
```
`<HealthPlaceholder />` is a 3-line stub component returning `<p>Health view ships in 26-05</p>` so the sidebar nav doesn't break. Keyboard: bind ⌘1 / ⌘2 / ⌘3 to `setView('status' | 'skills' | 'health')` via a `useEffect` `keydown` listener that detects `event.metaKey && event.key === '1'` etc.

Run `cd crates/tome-desktop/ui && npx tsc --noEmit`.
  </action>
  <verify>
    <automated>cd crates/tome-desktop/ui && npx tsc --noEmit && cd ../../.. && cargo check -p tome-desktop</automated>
    <human-check>cargo tauri dev — confirm: (1) Window opens with unified titlebar + vibrancy sidebar; (2) ⌘1/⌘2/⌘3 switch between Status/Skills/Health; (3) Sidebar Selected nav-item shows accent capsule + weight 600 label; (4) Status view still renders; (5) Skills view shows placeholder OR live list (after Task 2); (6) Light/dark mode follows system (toggle System Settings → Appearance).</human-check>
  </verify>
  <done>
    - `tokens.css` exists with light + dark tokens + reduced-transparency fallback.
    - Window/Titlebar/Sidebar/ContentPane components exist with their `.module.css` siblings.
    - App boots into Status (D-02); ⌘1/⌘2/⌘3 navigation works.
    - Tauri config has unified titlebar + vibrancy.
    - `npx tsc --noEmit` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: list_skills command + virtualised SkillsView with fuzzy search</name>
  <files>crates/tome-desktop/src/commands.rs, crates/tome-desktop/src/lib.rs, crates/tome-desktop/ui/package.json, crates/tome-desktop/ui/src/bindings.ts, crates/tome-desktop/ui/src/views/SkillsView.tsx, crates/tome-desktop/ui/src/views/SkillsView.module.css, crates/tome-desktop/ui/src/components/SearchField.tsx, crates/tome-desktop/ui/src/components/SearchField.module.css, crates/tome-desktop/ui/src/components/PopupMenu.tsx, crates/tome-desktop/ui/src/components/PopupMenu.module.css, crates/tome-desktop/ui/src/components/SkillListRow.tsx, crates/tome-desktop/ui/src/components/SkillListRow.module.css, crates/tome-desktop/ui/src/hooks/useSkills.ts, crates/tome-desktop/ui/src/hooks/useFuzzySearch.ts</files>
  <read_first>
    - crates/tome/src/list.rs (full — confirm `ListReport`, `DiscoveredSkill` shape; verify specta-gating on cross-boundary types; if missing, add `#[cfg_attr(feature = "bindings", derive(specta::Type))]`)
    - crates/tome/src/discover.rs (find `DiscoveredSkill`, `SkillOrigin`, `SkillProvenance` — confirm fields the UI needs: name, source_name, origin → managed-or-local classification)
    - crates/tome-desktop/src/commands.rs (Phase 25 `get_status` template at lines 33-41)
    - crates/tome-desktop/src/lib.rs (`make_builder` collect_commands registry)
    - .planning/phases/26-read-only-views-alpha-cut/26-UI-SPEC.md §"Per-view Design — Skills" + §"Component Contract" entries for SearchField / PopupMenu / SkillListRow + §"Keyboard Map" (⌘F focus search; arrow nav; PgUp/PgDn)
    - .planning/phases/26-read-only-views-alpha-cut/26-RESEARCH.md §"Standard Stack — Virtualisation" (OQ-1 path A — React Aria native Virtualizer) + §"Standard Stack — Fuzzy search" (fuse.js JS-side) + §"Code Examples — Virtualised skill list" (lines 1130-1167) + Pitfall 2 (TanStack ↔ React Aria friction — moot since we use native) + Pitfall 8 (variable row height; clip with `text-overflow: ellipsis`)
    - .planning/phases/26-read-only-views-alpha-cut/26-PATTERNS.md §"Tauri command template" + §S-3 (specta gate) + §S-7 (single make_builder) + §S-8 (Result-narrowing) + §S-10 (display-only computation)
  </read_first>
  <action>
(a) **Specta-gate ListReport + DiscoveredSkill if not already gated.** Read `crates/tome/src/list.rs` and `crates/tome/src/discover.rs`; if `ListReport`, `DiscoveredSkill`, `SkillOrigin`, `SkillProvenance` do not have `#[cfg_attr(feature = "bindings", derive(specta::Type))]`, add it. Any newtype (`SkillName`, `DirectoryName`) crossing the boundary must also be gated; verify they already are (Phase 25 should have handled this).

(b) **Add `list_skills` Tauri command.** In `crates/tome-desktop/src/commands.rs`:
```
#[tauri::command]
#[specta::specta]
pub fn list_skills(_app: tauri::AppHandle) -> Result<tome::list::ListReport, TomeError> {
    let (config, _paths) = load_context().map_err(TomeError::from)?;
    tome::list::collect(&config).map_err(TomeError::from)
}
```
Register in `crates/tome-desktop/src/lib.rs::make_builder()`'s `collect_commands![...]` macro (after `commands::get_status,`). Do not add a separate `pub fn` outside the macro — single registry per S-7.

(c) **Regenerate bindings.** `cargo run -p tome-desktop --bin gen-bindings` from repo root. Confirm `commands.listSkills` (or `commands.list_skills` depending on tauri-specta naming) appears in `crates/tome-desktop/ui/src/bindings.ts`. Commit the regen.

(d) **Install fuse.js.** `cd crates/tome-desktop/ui && npm install fuse.js@^7` (use Task 0 approved version). Commit package.json + lock.

(e) **Add atoms.** Create:
   - `SearchField.tsx` + `.module.css` — wraps React Aria `<SearchField>`. Props `{ value: string; onChange: (v: string) => void; placeholder?: string }`. Default placeholder `"Search skills"`. Background `var(--bg-subtle)`, `--radius-md`, `--text-body` 13px / 400. Implicit `role="searchbox"` + `aria-label="Search skills"`. Inline magnifier SVG left; X-clear button right when non-empty (React Aria provides this automatically).
   - `PopupMenu.tsx` + `.module.css` — wraps React Aria `<MenuTrigger><Button><Popover><Menu>`. Props `{ label: string; ariaLabel: string; items: { id: string; label: string }[]; selectedId: string; onChange: (id: string) => void }`. Closed state shows button with current value + chevron-down glyph. `--text-body` 13px / 400.
   - `SkillListRow.tsx` + `.module.css` — props `{ skill: DiscoveredSkill; selected: boolean; disabled: boolean }`. Two-line: primary `skill.name` (`--text-body` 13px / 400, weight 600 when selected and recoloured `--accent-on`) + secondary `${source_display} · ${managed ? 'managed' : 'local'}` (`--text-small` 12px / 400, `--label-secondary`, **`text-overflow: ellipsis; white-space: nowrap; overflow: hidden`** to enforce fixed 52px row height per Pitfall 8). When `selected`, capsule `--accent` background. When `disabled`, trailing `<Badge subtype="disabled">Disabled</Badge>`. Height fixed at 52px.

(f) **Hooks.** Create:
   - `hooks/useSkills.ts` — fetches via `commands.listSkills()` once on mount; stores `skills: DiscoveredSkill[] | null`, `err: TomeError | null`, `warnings: string[]`. Uses S-8 Result-narrowing. No event subscriptions (lands in 26-06).
   - `hooks/useFuzzySearch.ts` — `export function useFuzzySearch<T>(items: T[], query: string, options: { keys: (keyof T)[]; threshold?: number }): T[]`. Wraps `fuse.js` — memoize a `Fuse` instance on `items` change (`useMemo`); when `query === ''` return `items` unchanged; otherwise return `fuse.search(query).map(r => r.item)`. Cap result count at `items.length` (no slicing). Document the "not parity with CLI's nucleo" tradeoff in a leading JSDoc comment per RESEARCH §"Standard Stack — Fuzzy search" + Assumption A3.

(g) **SkillsView.** Create `views/SkillsView.tsx` + `SkillsView.module.css` per UI-SPEC §"Per-view Design — Skills" and RESEARCH §"Code Examples — Virtualised skill list":
   - Top-level: CSS Grid with two columns — list column (280-380px) + detail column (1fr). List column contains pinned `<SearchField>`, toolbar row with two `<PopupMenu>`s (Sort and Group), and the virtualised list.
   - Sort items: `[{id:'name',label:'Name'}, {id:'source',label:'Source'}, {id:'recent',label:'Recent'}]` default `'name'`. Group items: `[{id:'none',label:'None'}, {id:'source',label:'Source'}, {id:'role',label:'Role'}]` default `'none'`. Sort/group are display-only computations (D-GUI-08 allowed exceptions per S-10).
   - Virtualised list: import `Virtualizer`, `ListBox`, `ListBoxItem`, `ListLayout` from `react-aria-components`. Layout:
     `<Virtualizer layout={ListLayout} layoutOptions={{ rowHeight: 52, gap: 0, padding: 0 }}>`
       `<ListBox aria-label="Skills" items={filtered} selectionMode="single" selectedKeys={selectedKeys} onSelectionChange={...}>`
         `{(skill) => <ListBoxItem id={skill.name} textValue={skill.name}><SkillListRow ... /></ListBoxItem>}`
       `</ListBox>`
     `</Virtualizer>`
   - Selection state via `useState<string | null>`. Pass `selectedKeys={selected ? new Set([selected]) : new Set()}`.
   - Detail column: when `selected === null` show empty placeholder `<p>Select a skill to view details</p>` per UI-SPEC §"Empty selection" copy. When selected, show `<p>Detail pane ships in 26-03</p>` — the real DetailHeader + MarkdownBody land in plans 26-03 / 26-04.
   - Sort logic (display-only): `name` = `localeCompare` on `skill.name`; `source` = `localeCompare` on `source_display`, then by name; `recent` = sort by `skill.synced_at` (verify `DiscoveredSkill` has this; if not, fall back to `name` with a code comment flagging the missing field). Group is a no-op renderer in this plan (Section headers for grouped mode can be a follow-up); document the no-op in the code.
   - Keyboard: ⌘F focuses the SearchField — bind via a global keydown listener inside SkillsView that calls `searchFieldRef.current?.focus()`. Esc with focused search clears the query (React Aria SearchField default).

Run `cargo run -p tome-desktop --bin gen-bindings && git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts` and `cd crates/tome-desktop/ui && npx tsc --noEmit`.

(h) **Flag UI-SPEC amendment.** During this plan, append a one-paragraph note to `.planning/phases/26-read-only-views-alpha-cut/26-UI-SPEC.md` under §Revision Log: "26-02 deviates from §Design System — uses React Aria native `<Virtualizer>` (1.17+) instead of `@tanstack/react-virtual` per RESEARCH OQ-1 (zero extra dep, free a11y); fallback to TanStack if 26-08 bench fails 60fps." This is per the planner directive in the deep_work_rules — surface the deviation, don't hide it.
  </action>
  <verify>
    <automated>cargo run -p tome-desktop --bin gen-bindings && git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts && cargo clippy --all-targets -- -D warnings && cd crates/tome-desktop/ui && npx tsc --noEmit</automated>
    <human-check>cargo tauri dev — confirm: (1) Switch to Skills (⌘2); (2) SearchField is focused on ⌘F; (3) Typing filters the list as-you-type; (4) Arrow ↑/↓ moves selection; Home/End jump to first/last; PgUp/PgDn scroll a page; (5) Sort PopupMenu switches Name/Source/Recent ordering; (6) Selected row shows accent capsule + weight-600 white text; (7) No console errors in DevTools (Cmd+Opt+I).</human-check>
  </verify>
  <done>
    - `commands.listSkills` registered in `make_builder()` and exported from `bindings.ts`; freshness gate passes.
    - SkillsView renders a virtualised list of 2000+ skills (verified manually with user's real library or synthetic data; perf-budget verification deferred to plan 26-08).
    - Fuzzy search filters as-you-type via `fuse.js` (no IPC per keystroke).
    - Sort PopupMenu functional (Name/Source/Recent); Group menu present (no-op render with code comment).
    - ⌘F focuses search; arrow + Home/End/PgUp/PgDn nav works via React Aria.
    - `cargo clippy --all-targets -- -D warnings` exits 0.
    - UI-SPEC §Revision Log updated with OQ-1 deviation note.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview ↔ Tauri IPC | `list_skills` returns potentially large `DiscoveredSkill[]` — same trust as `tome list` JSON |
| External SKILL.md source paths | Untrusted (user clones third-party git repos); rendered display-only as strings (no execution) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02-01 | DoS | `list_skills` payload size at 2000+ skills | mitigate | List is fetched once; virtualised list renders ~20 rows at a time; fuse.js filtering runs JS-side without re-fetching. Plan 26-08 bench verifies 60fps; if fails, fall back to TanStack Virtual. |
| T-26-02-02 | EoP | New `list_skills` cross-boundary type not specta-gated | mitigate | Task 2 verifies `ListReport`/`DiscoveredSkill` carry `#[cfg_attr(feature = "bindings", derive(specta::Type))]`; CI freshness gate catches drift. |
| T-26-02-03 | Tampering | Fuzzy-search ranking divergence (fuse.js vs nucleo) | accept | Documented in code + Assumption A3; ranking is display-only (D-GUI-08 allowed exception); revisit if beta feedback complains. |
| T-26-02-04 | InformationDisclosure | SkillListRow renders user paths verbatim | accept | Same trust boundary as `tome list`; the user owns these paths. |
| T-26-02-SC | Tampering | npm/cargo installs (react-aria-components, fuse.js) | mitigate | Task 0 blocking-human checkpoint verifies registry + repo URL before install. fuse.js explicitly flagged as elevated slopsquat risk. |
</threat_model>

<verification>
- `cargo run -p tome-desktop --bin gen-bindings && git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts` exits 0.
- `cargo clippy --all-targets -- -D warnings` exits 0.
- `cd crates/tome-desktop/ui && npx tsc --noEmit` exits 0.
- Manual: `cargo tauri dev` boots; Status loads on launch (D-02); ⌘2 switches to Skills; live skill list renders against the user's real library; fuzzy search filters live; sort/group toolbar present.
</verification>

<success_criteria>
- 3-column NavigationSplitView shell (Window/Titlebar/Sidebar/ContentPane) exists and is inherited by every later view.
- VIEW-02 satisfied: virtualised skill list (≥2000 skills handled — perf budget confirmed in plan 26-08), fuzzy search matching `nucleo`-style ranking ("close enough" per A3), sort (name/source/recent), group-by toolbar (none/source/role; render is no-op in alpha with a code comment).
- NF-01 setup in place: React Aria native Virtualizer with fixed 52px rows (no measurement complexity); bench harness lands in 26-08.
- Sidebar nav-item + ⌘1/⌘2/⌘3 routing work.
</success_criteria>

<output>
Create `.planning/phases/26-read-only-views-alpha-cut/26-02-SUMMARY.md` when done.
</output>
</content>
</invoke>