---
phase: 12-marketplace-adapter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/tome/src/marketplace.rs
  - crates/tome/src/lib.rs
autonomous: true
requirements:
  - ADP-01
must_haves:
  truths:
    - "marketplace.rs module exists and is declared in lib.rs"
    - "MarketplaceAdapter trait exists with the six locked method signatures (id, current_version, install, update, list_installed, available)"
    - "InstalledPlugin struct exists with the four locked fields (id, version, install_path, errors)"
    - "MockMarketplaceAdapter test double exists under #[cfg(test)] and exercises the trait shape"
    - "Trait-shape tests demonstrate adapter polymorphism (call all six methods on a dyn MarketplaceAdapter)"
  artifacts:
    - path: "crates/tome/src/marketplace.rs"
      provides: "Trait + InstalledPlugin + MockMarketplaceAdapter (cfg-test)"
      contains: "pub trait MarketplaceAdapter"
    - path: "crates/tome/src/lib.rs"
      provides: "Module declaration `pub(crate) mod marketplace;`"
      contains: "pub(crate) mod marketplace;"
  key_links:
    - from: "crates/tome/src/lib.rs"
      to: "crates/tome/src/marketplace.rs"
      via: "module declaration between `library` and `lint`"
      pattern: "pub\\(crate\\) mod marketplace;"
    - from: "trait MarketplaceAdapter"
      to: "InstalledPlugin"
      via: "list_installed() return type"
      pattern: "fn list_installed\\(&self\\) -> Result<Vec<InstalledPlugin>>"
---

<objective>
Create the foundational `marketplace.rs` module with the locked `MarketplaceAdapter` trait surface, the `InstalledPlugin` data type that adapters return, and a `MockMarketplaceAdapter` test double for unit testing trait callers.

Purpose: Establishes the contract that ClaudeMarketplaceAdapter (Plan 12-04) and GitAdapter (Plan 12-03) will implement, and that Phase 13's sync flow will call. The mock unblocks testing of the failure-aggregation renderer (Plan 12-02) and any future trait consumers without requiring real subprocess invocation.

Output: New file `crates/tome/src/marketplace.rs` with the trait, `InstalledPlugin`, and a `#[cfg(test)]` mock + tests that demonstrate the trait shape compiles and is usable via `dyn MarketplaceAdapter`. Module declaration added to `lib.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-marketplace-adapter/12-CONTEXT.md
@.planning/phases/12-marketplace-adapter/12-RESEARCH.md
@crates/tome/src/lib.rs
@crates/tome/src/remove.rs

<interfaces>
<!-- Locked trait surface from CONTEXT.md D-08 (verbatim — DO NOT paraphrase). -->

```rust
pub trait MarketplaceAdapter {
    fn id(&self) -> &str;
    fn current_version(&self, plugin_id: &str) -> Result<Option<String>>;
    fn install(&self, plugin_id: &str) -> Result<()>;
    fn update(&self, plugin_id: &str) -> Result<()>;
    fn list_installed(&self) -> Result<Vec<InstalledPlugin>>;
    fn available(&self, plugin_id: &str) -> Result<bool>;
}
```

```rust
pub struct InstalledPlugin {
    pub id: String,             // e.g. "axiom@axiom-marketplace" or git URL
    pub version: String,        // semver string for claude, HEAD SHA for git
    pub install_path: PathBuf,  // claude: cache path; git: repos cache path
    pub errors: Vec<String>,    // claude: from `errors[]` JSON field; git: usually empty
}
```

`plugin_id` is `&str` (not a newtype) because marketplace ids carry an `@marketplace` suffix incompatible with `SkillName` validation (e.g. `axiom@axiom-marketplace`). All methods return `anyhow::Result`.

<!-- Module declaration ordering in lib.rs (lib.rs:25-51, alphabetical sibling order). -->
<!-- Insertion point: between `library` (line 37) and `lint` (line 38). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create marketplace.rs with trait, InstalledPlugin, and module declaration</name>
  <read_first>
    - crates/tome/src/lib.rs (lines 25-51 — sibling module declarations and ordering)
    - crates/tome/src/git.rs (entire file — `pub(crate)` convention, anyhow::Result usage, module-doc style)
    - .planning/phases/12-marketplace-adapter/12-CONTEXT.md §decisions D-08, D-10
    - .planning/phases/12-marketplace-adapter/12-RESEARCH.md §"File Layout Recommendation" and §"Module declaration in lib.rs"
  </read_first>
  <files>crates/tome/src/marketplace.rs, crates/tome/src/lib.rs</files>
  <behavior>
    - The file `crates/tome/src/marketplace.rs` exists and is declared in `lib.rs` between the `library` and `lint` declarations (alphabetical order).
    - `pub trait MarketplaceAdapter` exists with EXACTLY these six methods, in this order, with these exact signatures (per D-08, verbatim):
      ```rust
      fn id(&self) -> &str;
      fn current_version(&self, plugin_id: &str) -> Result<Option<String>>;
      fn install(&self, plugin_id: &str) -> Result<()>;
      fn update(&self, plugin_id: &str) -> Result<()>;
      fn list_installed(&self) -> Result<Vec<InstalledPlugin>>;
      fn available(&self, plugin_id: &str) -> Result<bool>;
      ```
    - `pub struct InstalledPlugin` exists with EXACTLY these four public fields (per D-08, verbatim):
      ```rust
      pub id: String,
      pub version: String,
      pub install_path: PathBuf,
      pub errors: Vec<String>,
      ```
    - `InstalledPlugin` derives at minimum `Debug` and `Clone` (Clone needed because Phase 13 will pass entries around; both `String` and `Vec<String>` are Clone-friendly).
    - `cargo check -p tome` passes (compiles cleanly with no marketplace usage yet — trait + struct only).
  </behavior>
  <action>
    Create `crates/tome/src/marketplace.rs` with:

    1. Module-level doc comment `//! Marketplace adapter trait and shared types.`. One-liner explaining the layer (per CLAUDE.md: "anyhow::Result everywhere"; per RESEARCH §File Layout: single-file module).

    2. Imports: `use anyhow::Result; use std::path::PathBuf;` (no `serde` import yet — JSON parsing belongs to Plan 12-04's adapter, not the trait module).

    3. `InstalledPlugin` struct EXACTLY as in `<interfaces>` block above. Add `#[derive(Debug, Clone)]`. Add a doc comment per field explaining the canonical content per D-08 (claude vs git semantics). Quote from CONTEXT.md D-08:
       > "id: e.g. \"axiom@axiom-marketplace\" or git URL"
       > "version: semver string for claude, HEAD SHA for git"
       > "install_path: claude: cache path; git: repos cache path"
       > "errors: claude: from `errors[]` JSON field; git: usually empty"

    4. `pub trait MarketplaceAdapter` with the SIX methods EXACTLY as in `<interfaces>` block. Per D-08 each method returns `anyhow::Result`. Add doc comments per method:
       - `id()` — "Stable identifier for this adapter instance (e.g. git URL, or `\"claude-plugins\"`)."
       - `current_version(plugin_id)` — "Display-only version string for human-readable diffs. Per Phase 11 D-08, drift detection uses content_hash, not version. `Ok(None)` when the plugin isn't locally installed."
       - `install(plugin_id)` — "Install the plugin. Caller invalidates cache via `refresh()` if applicable; ClaudeMarketplaceAdapter's cache auto-invalidates on Ok per D-04."
       - `update(plugin_id)` — "Update the plugin to the marketplace's latest version (no version pinning per D-09 / LIB-FUTURE-01)."
       - `list_installed()` — "Snapshot of installed plugins from this marketplace. May be cached internally per adapter (D-04)."
       - `available(plugin_id)` — "Returns false when the plugin is no longer obtainable from the marketplace (RECON-04 vanished signal)."

    5. Then edit `crates/tome/src/lib.rs`: insert `pub(crate) mod marketplace;` between line 37 (`pub(crate) mod library;`) and line 38 (`pub(crate) mod lint;`) to preserve alphabetical sibling order. Per RESEARCH §"Module declaration in lib.rs". The module is `pub(crate)` (not `pub`) — Phase 13 wires the dispatch and may need to widen visibility then; Phase 12 keeps the surface minimal.

    6. Run `cargo check -p tome` to verify compilation. Run `cargo fmt`. Run `cargo clippy -p tome --all-targets -- -D warnings` (this will fail because the trait/struct are unused; the lint failures will be addressed by Task 2 adding the mock + tests, OR by adding `#[allow(dead_code)]` where needed if clippy complains about unused trait methods on a non-mock-impl path — prefer adding the mock in Task 2 and not silencing).
  </action>
  <verify>
    <automated>cargo check -p tome 2>&1 | tail -20 && grep -q "pub trait MarketplaceAdapter" crates/tome/src/marketplace.rs && grep -q "fn id(&self) -> &str;" crates/tome/src/marketplace.rs && grep -q "fn current_version(&self, plugin_id: &str) -> Result<Option<String>>;" crates/tome/src/marketplace.rs && grep -q "fn install(&self, plugin_id: &str) -> Result<()>;" crates/tome/src/marketplace.rs && grep -q "fn update(&self, plugin_id: &str) -> Result<()>;" crates/tome/src/marketplace.rs && grep -q "fn list_installed(&self) -> Result<Vec<InstalledPlugin>>;" crates/tome/src/marketplace.rs && grep -q "fn available(&self, plugin_id: &str) -> Result<bool>;" crates/tome/src/marketplace.rs && grep -q "pub struct InstalledPlugin" crates/tome/src/marketplace.rs && grep -q "pub id: String," crates/tome/src/marketplace.rs && grep -q "pub version: String," crates/tome/src/marketplace.rs && grep -q "pub install_path: PathBuf," crates/tome/src/marketplace.rs && grep -q "pub errors: Vec<String>," crates/tome/src/marketplace.rs && grep -q "pub(crate) mod marketplace;" crates/tome/src/lib.rs</automated>
  </verify>
  <acceptance_criteria>
    - `crates/tome/src/marketplace.rs` exists.
    - `grep -q "pub trait MarketplaceAdapter" crates/tome/src/marketplace.rs` succeeds.
    - All six method signatures present verbatim (verified by individual greps in `<verify>`).
    - All four `InstalledPlugin` fields present verbatim (verified by individual greps in `<verify>`).
    - `grep -q "pub(crate) mod marketplace;" crates/tome/src/lib.rs` succeeds.
    - `awk '/pub\(crate\) mod (library|marketplace|lint);/' crates/tome/src/lib.rs` shows the order `library`, `marketplace`, `lint` (alphabetical).
    - `cargo check -p tome` exits 0.
    - `cargo fmt --check` exits 0.
  </acceptance_criteria>
  <done>
    The trait, struct, and module declaration exist exactly as locked in CONTEXT.md D-08. The crate compiles. Plan 12-03 and Plan 12-04 can now write `impl MarketplaceAdapter for ...` blocks against this contract.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add MockMarketplaceAdapter test double + trait-shape tests</name>
  <read_first>
    - crates/tome/src/marketplace.rs (current state from Task 1)
    - crates/tome/src/remove.rs (lines 381-461 — `#[cfg(test)] mod tests` style, `tempfile::TempDir` usage, `make_test_setup` builder pattern)
    - .planning/phases/12-marketplace-adapter/12-CONTEXT.md §decisions D-10
    - .planning/phases/12-marketplace-adapter/12-RESEARCH.md §"`MockMarketplaceAdapter` shape recommendation"
  </read_first>
  <files>crates/tome/src/marketplace.rs</files>
  <behavior>
    - Mock supports static fixtures (canned `installed: Vec<InstalledPlugin>`, canned `available: HashSet<String>` of plugin_ids that return `Ok(true)`).
    - Mock supports failure injection: `fail_install: HashSet<String>`, `fail_update: HashSet<String>` of plugin_ids that should return `Err(...)`.
    - At least three tests exercise the trait via `&dyn MarketplaceAdapter` (polymorphic call site) — proves the trait is object-safe and usable as a trait object (Phase 13 will store these in collections).
    - Test 1: A mock with one installed plugin returns it from `list_installed()`; `current_version("known")` returns `Some(version)`; `current_version("unknown")` returns `Ok(None)`.
    - Test 2: `available("vanished")` returns `Ok(false)` for a plugin not in the available set; `Ok(true)` for one that is.
    - Test 3: `install("doomed")` with `fail_install` containing `"doomed"` returns `Err`; `install("happy")` not in `fail_install` returns `Ok(())`. Same shape for `update`.
    - `cargo test -p tome marketplace::tests --quiet` exits 0 with at least the three tests above passing.
  </behavior>
  <action>
    Append to `crates/tome/src/marketplace.rs`:

    1. `#[cfg(test)] mod tests { ... }` block. Inside it:

    2. Define `pub(super) struct MockMarketplaceAdapter` (per D-10: lives `#[cfg(test)]` in `marketplace.rs`; `pub(super)` so the test module's helpers are accessible to nested test fns):
       ```rust
       pub(super) struct MockMarketplaceAdapter {
           pub id: String,
           pub installed: Vec<InstalledPlugin>,
           pub available: std::collections::HashSet<String>,
           pub fail_install: std::collections::HashSet<String>,
           pub fail_update: std::collections::HashSet<String>,
       }
       ```
       (Per RESEARCH §"`MockMarketplaceAdapter` shape recommendation": static fixtures + failure injection knobs — both modes are needed.)

    3. Implement `MarketplaceAdapter` for the mock:
       - `id() -> &str` — returns `&self.id`.
       - `current_version(plugin_id) -> Result<Option<String>>` — search `self.installed` for matching `id`; return `Ok(Some(entry.version.clone()))` if found, else `Ok(None)`.
       - `install(plugin_id) -> Result<()>` — if `self.fail_install.contains(plugin_id)`, return `Err(anyhow::anyhow!("mock: install failed for {plugin_id}"))`; else `Ok(())`.
       - `update(plugin_id) -> Result<()>` — same shape with `fail_update`.
       - `list_installed() -> Result<Vec<InstalledPlugin>>` — return `Ok(self.installed.clone())`.
       - `available(plugin_id) -> Result<bool>` — return `Ok(self.available.contains(plugin_id))`.

    4. Add a `fn make_mock(...)` test builder for ergonomics (mirrors `make_test_setup` pattern from `remove.rs:396`). Optional — inline literal struct construction also fine.

    5. Tests (each test creates a `&dyn MarketplaceAdapter` to prove object-safety):
       - `mock_lists_installed_and_resolves_versions` — assert `list_installed()` shape, `current_version("known") == Some(v)`, `current_version("unknown") == None`.
       - `mock_available_returns_set_membership` — assert `available("vanished") == Ok(false)`, `available("present") == Ok(true)`.
       - `mock_install_and_update_failure_injection` — assert `install("doomed").is_err()`, `install("happy").is_ok()`; same for `update`.
       - (Bonus) `trait_is_object_safe` — `let _: Box<dyn MarketplaceAdapter> = Box::new(...)` line proves object-safety at compile time.

    6. Run `cargo test -p tome marketplace::tests --quiet` and confirm pass.

    7. Run `cargo clippy -p tome --all-targets -- -D warnings`. Address any warnings about unused trait items by ensuring the mock fully implements the trait (which Task 2 does). If non-test paths have unused trait warnings, that's fine — the trait is a contract for Plan 12-03/12-04; no `#[allow(dead_code)]` needed because implementing the trait satisfies clippy's dead-code analysis for trait methods.
  </action>
  <verify>
    <automated>cargo test -p tome marketplace::tests --quiet 2>&1 | tail -15 && grep -q "struct MockMarketplaceAdapter" crates/tome/src/marketplace.rs && grep -q "impl MarketplaceAdapter for MockMarketplaceAdapter" crates/tome/src/marketplace.rs && grep -q "Box<dyn MarketplaceAdapter>" crates/tome/src/marketplace.rs && cargo clippy -p tome --all-targets -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "struct MockMarketplaceAdapter" crates/tome/src/marketplace.rs` succeeds.
    - `grep -q "impl MarketplaceAdapter for MockMarketplaceAdapter" crates/tome/src/marketplace.rs` succeeds.
    - `grep -q "fail_install" crates/tome/src/marketplace.rs` succeeds (failure injection knob present).
    - `grep -q "Box<dyn MarketplaceAdapter>" crates/tome/src/marketplace.rs` OR `grep -q "&dyn MarketplaceAdapter" crates/tome/src/marketplace.rs` succeeds (object-safety exercised).
    - `cargo test -p tome marketplace::tests --quiet` exits 0 with at least 3 passing tests.
    - `cargo clippy -p tome --all-targets -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>
    The trait is exercised via a mock under `#[cfg(test)]`. Object-safety is proven (Phase 13 can store `Box<dyn MarketplaceAdapter>` in collections). The mock is reusable by Plan 12-02's renderer tests (since `marketplace::tests::MockMarketplaceAdapter` is `pub(super)` it's reachable from nested test modules in the same file).
  </done>
</task>

</tasks>

<verification>
- `cargo check -p tome` passes.
- `cargo test -p tome marketplace::tests` passes (at least 3 tests).
- `cargo clippy -p tome --all-targets -- -D warnings` passes.
- `cargo fmt --check` passes.
- The trait surface in `crates/tome/src/marketplace.rs` exactly matches D-08 (six methods, four-field `InstalledPlugin`).
- No changes outside `crates/tome/src/marketplace.rs` and the single-line module declaration in `crates/tome/src/lib.rs`.
- No changes to `lib.rs::sync()` flow (out of scope per CONTEXT.md — Phase 13 wires the dispatch).
- No changes to `cli.rs` or `tome.toml` schema (out of scope).
</verification>

<success_criteria>
The contract that Plan 12-03 (GitAdapter), Plan 12-04 (ClaudeMarketplaceAdapter), and Plan 12-02 (failure renderer tests) build against is in place. ADP-01 is satisfied: trait + InstalledPlugin + mock + module wiring all present and tested. Crate compiles cleanly with strict clippy.
</success_criteria>

<output>
After completion, create `.planning/phases/12-marketplace-adapter/12-01-SUMMARY.md`.
</output>
