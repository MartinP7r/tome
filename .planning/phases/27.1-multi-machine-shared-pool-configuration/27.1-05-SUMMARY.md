---
phase: 27.1-multi-machine-shared-pool-configuration
plan: "05"
subsystem: cli-status
tags: [rust, cli, git, porcelain-v2, serde, status]

requires:
  - phase: 27.1-04
    provides: Safe shared-pool Git coordination and porcelain-v2 probe foundation
provides:
  - Selected profile and typed Git health in the CLI status report
  - Read-only repository snapshot rendered consistently as text and JSON
affects: [phase-28-configuration-ui, tome-desktop]

actuals:
  tokens: 6643
  tasks: 2
  commits: 1

tech-stack:
  added: []
  patterns: [Typed Git availability states, one porcelain-v2 snapshot per status report]

key-files:
  created: [.planning/phases/27.1-multi-machine-shared-pool-configuration/27.1-05-SUMMARY.md]
  modified:
    - crates/tome/src/lib.rs
    - crates/tome/src/repo_sync.rs
    - crates/tome/src/status.rs
    - crates/tome/src/skill.rs
    - crates/tome/tests/cli_status.rs
    - crates/tome/tests/snapshots/cli_status__status_empty_library.snap

key-decisions:
  - "Status projects repo_sync porcelain-v2 data into tagged profile, upstream, and Git-health types instead of parsing display text."
  - "Unavailable repository, remote, upstream, Git command, and commit timestamp states remain explicit rather than fabricated."

patterns-established:
  - "Read-only Git health: use --no-optional-locks porcelain-v2, then expose typed fields to every renderer."

requirements-completed: [POOL-08]

coverage:
  - id: D1
    description: Selected profile and clean Git health render consistently in text and JSON status output.
    requirement: POOL-08
    verification:
      - kind: integration
        ref: crates/tome/tests/cli_status.rs#profile_and_clean_git_health
        status: pass
    human_judgment: false
  - id: D2
    description: Dirty, topology, unavailable, and read-only Git health states are typed and regression tested.
    requirement: POOL-08
    verification:
      - kind: integration
        ref: crates/tome/tests/cli_status.rs#status_reports_dirty_health_without_mutating_repository_state
        status: pass
      - kind: unit
        ref: crates/tome/src/repo_sync.rs#porcelain_parser_projects_dirty_and_topology_states
        status: pass
      - kind: other
        ref: make ci
        status: pass
    human_judgment: false

duration: 28min
completed: 2026-08-21
status: complete
---

# Phase 27.1 Plan 05: Shared-Pool Status Health Summary

**CLI status now reports the selected machine profile and one read-only, typed Git-health snapshot in both text and JSON.**

## Performance

- **Duration:** 28 min
- **Completed:** 2026-08-21
- **Tasks:** 2/2
- **Files modified:** 6

## Accomplishments

- Added bindings-ready profile, upstream, change-summary, and Git-health report types to `StatusReport`.
- Projects one `git --no-optional-locks status --porcelain=v2` snapshot into clean/dirty, staged/unstaged, topology, operation, remote, and commit-age fields.
- Renders detailed Git status in text and stable tagged values in JSON without changing worktree bytes or index state.
- Retained scalar and sequence support for `allowed-tools` frontmatter after its focused regression tests passed.

## Task Commits

1. **Task 1: Return profile and clean repository health in CLI status** — `872eab5` (feat)
2. **Task 2: Cover dirty, topology, and unavailable Git status states** — `872eab5` (feat; implementation was present uncommitted at execution start and completed atomically with Task 1)

## Files Created/Modified

- `crates/tome/src/lib.rs` — forwards the active profile to status rendering.
- `crates/tome/src/repo_sync.rs` — parses porcelain-v2 into a reusable typed health snapshot.
- `crates/tome/src/status.rs` — adds typed profile/Git fields and detailed text output.
- `crates/tome/tests/cli_status.rs` — covers clean and dirty read-only status behavior.
- `crates/tome/tests/snapshots/cli_status__status_empty_library.snap` — captures explicit non-repository health.
- `crates/tome/src/skill.rs` — preserves verified scalar-or-sequence `allowed-tools` parsing.

## Decisions Made

- Reused `repo_sync` for the sole status probe so text and JSON project the same data rather than independently querying Git.
- Reported unavailable conditions with tagged domain types and `Option` fields rather than display-string fallbacks.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected the text renderer's styled string type mismatch**
- **Found during:** Task 2 verification
- **Issue:** Clean and dirty branches returned different `StyledObject` payload types, preventing compilation.
- **Fix:** Styled a single owned status string after selecting clean or dirty text.
- **Files modified:** `crates/tome/src/status.rs`
- **Verification:** `make ci`
- **Committed in:** `872eab5`

**Total deviations:** 1 auto-fixed (Rule 1)
**Impact on plan:** Required for compilation; no scope expansion.

## TDD Task Compliance

The required red phase could not be reproduced because the plan implementation was already present as uncommitted work when execution began. New dirty-state and parser regression tests were added and passed against the preserved implementation.

## Issues Encountered

`requirements.mark-complete POOL-08` could not update `.planning/REQUIREMENTS.md` because that ID is absent from its checkbox and traceability surfaces. The plan's frontmatter still records POOL-08; implementation verification is unaffected.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 28 and the desktop layer can consume typed active-profile and Git-health data from `StatusReport`.
- No blockers.

## Self-Check: PASSED

- Confirmed `872eab5` exists in Git history.
- Confirmed all six implementation files and this summary exist.

---
*Phase: 27.1-multi-machine-shared-pool-configuration*
*Completed: 2026-08-21*
