---
phase: 27.1-multi-machine-shared-pool-configuration
plan: 06
type: execute
wave: 6
depends_on: [27.1-01, 27.1-03, 27.1-04, 27.1-05]
files_modified:
  - crates/tome-desktop/src/commands.rs
  - crates/tome-desktop/src/watcher.rs
  - crates/tome-desktop/src/main.rs
  - crates/tome-desktop/src/bindings.rs
  - crates/tome-desktop/tests/watcher_smoke.rs
autonomous: true
requirements: [POOL-01, POOL-06, POOL-08]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Desktop commands load exactly the same effective profile context and structured Git outcomes as the CLI."
    - "Desktop watchers reload configuration changes across pool policy, profiles, and local settings."
  artifacts:
    - path: crates/tome-desktop/src/commands.rs
      provides: Shared effective-context desktop IPC boundary
    - path: crates/tome-desktop/src/watcher.rs
      provides: New layer artifact watch events
  key_links:
    - from: crates/tome-desktop/src/commands.rs
      to: crates/tome/src/profiles.rs
      via: shared load_effective_context, not desktop-local Config loading
    - from: crates/tome-desktop/src/watcher.rs
      to: crates/tome-desktop/ui/src/bindings.ts
      via: generated typed event/binding contract
---

<objective>
Finish CLI/desktop parity for layered profiles, pool synchronization outcomes, and status.

Purpose: Keep Rust authoritative while preventing desktop configuration/state drift from the CLI.
Output: Shared loader IPC integration, watcher expansion, regenerated bindings, and desktop smoke coverage.
</objective>
<execution_context>@/Users/martin/.config/opencode/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/27-sync-triage-ui/27-01b-SUMMARY.md
@.planning/phases/27.1-multi-machine-shared-pool-configuration/27.1-RESEARCH.md
@crates/tome-desktop/src/commands.rs
@crates/tome-desktop/src/watcher.rs
@crates/tome-desktop/src/main.rs
@crates/tome/src/profiles.rs
@crates/tome/src/repo_sync.rs
@crates/tome/src/status.rs
</context>
<tasks>
<task type="tracer">
  <name>Task 1: Use the Rust effective context in one desktop status/sync path</name>
  <files>crates/tome-desktop/src/commands.rs, crates/tome-desktop/src/main.rs, crates/tome-desktop/src/bindings.rs, crates/tome-desktop/tests/watcher_smoke.rs</files>
  <action>Replace desktop-local `load_context` with the Plan 01 shared effective-context loader and map typed domain validation/conflict/Git failures only at the Tauri boundary. Thread active profile and repo Git outcomes through `get_status` and GUI sync setup so the GUI observes the same policy, safe local-only result, and status contract as CLI. Derive specta types for new cross-boundary report/outcome types, register commands/events through the existing builder, regenerate committed bindings, and add a desktop-side smoke assertion for selected-profile parity. This implements POOL-01, POOL-06, and POOL-08 without JavaScript validation or side-effect policy.</action>
  <verify><automated>cargo test -p tome-desktop --test watcher_smoke && cargo run -p tome-desktop --bin gen-bindings && git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts</automated></verify>
  <done>Desktop status and sync setup use the identical Rust effective context and generated typed contracts as the CLI.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Watch every layered configuration artifact and emit typed refresh events</name>
  <files>crates/tome-desktop/src/watcher.rs, crates/tome-desktop/src/main.rs, crates/tome-desktop/src/bindings.rs, crates/tome-desktop/tests/watcher_smoke.rs</files>
  <behavior>
    - Atomic writes to pool policy, selected profile, or local settings emit typed configuration-change events after debounce.
    - Existing manifest, catalog, library, and preference notifications continue to work.
    - Parent-directory watching handles profile creation/deletion and atomic rename without JS filesystem permissions.
  </behavior>
  <action>Extend `WatcherPaths`, canonical parent filtering, event enum, registration, and test sink for pool policy, `machines/` directory/profile files, and local `settings.toml`. Preserve the existing parent-watch plus debounced atomic-rename pattern, avoid watcher-triggered policy work, and refresh only through typed Rust-to-webview events. Regenerate bindings after event changes and cover each new artifact with temporary-directory writes, per POOL-01 and POOL-08.</action>
  <verify><automated>cargo test -p tome-desktop --test watcher_smoke && cargo clippy -p tome-desktop --all-targets -- -D warnings</automated></verify>
  <done>The desktop receives generated, typed change events for all layered configuration and retains existing shared-state watcher behavior.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| webview → Tauri commands | IPC requests must not choose paths, validate policy, or run Git independently. |
| filesystem events → webview refresh | External atomic writes can request UI refresh but cannot execute actions. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-27.1-13 | Elevation | commands.rs | high | mitigate | Use shared Rust loader and domain results; do not accept profile paths or Git policy decisions from JavaScript. |
| T-27.1-14 | Denial of service | watcher.rs | medium | mitigate | Debounce parent-directory events, filter canonical paths, and emit refresh-only typed events. |
| T-27.1-SC | Tampering | package installs | high | mitigate | No package installation is planned. |
</threat_model>
<verification>Run desktop watcher tests, bindings freshness, workspace lint, and `make ci` at phase end.</verification>
<success_criteria>CLI and desktop share configuration/Git/status authority, and desktop refreshes for every new persisted layer.</success_criteria>
<output>Create `.planning/phases/27.1-multi-machine-shared-pool-configuration/27.1-06-SUMMARY.md` when done</output>
