---
phase: 27.1-multi-machine-shared-pool-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/tome/src/profiles.rs
  - crates/tome/src/config/mod.rs
  - crates/tome/src/cli.rs
  - crates/tome/src/lib.rs
  - crates/tome/tests/cli_profiles.rs
autonomous: true
requirements: [POOL-01]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A valid pool policy, selected named profile, and local settings produce one validated effective Config for CLI use."
    - "Missing, empty, invalid, or absent profile selection stops before filesystem-mutating sync work and gives recovery guidance without guessing a profile."
  artifacts:
    - path: crates/tome/src/profiles.rs
      provides: Layer-specific persisted schemas and the effective-context loader
    - path: crates/tome/tests/cli_profiles.rs
      provides: End-to-end profile-selection regression fixtures
  key_links:
    - from: crates/tome/src/lib.rs
      to: crates/tome/src/profiles.rs
      via: normal command setup uses load_effective_context before dispatch
    - from: crates/tome/src/profiles.rs
      to: crates/tome/src/config/mod.rs
      via: validated layer projection preserves Config path expansion and validation
---

<objective>
Replace implicit machine preferences with explicit, layered profile selection for normal CLI operations.

Purpose: Establish the validated configuration contract every later pool, Git, status, and desktop path consumes.
Output: Layer-specific TOML schemas, effective-context loader, profile command surface, and CLI regression coverage for POOL-01.
</objective>

<execution_context>
@/Users/martin/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/martin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27.1-multi-machine-shared-pool-configuration/27.1-CONTEXT.md
@.planning/phases/27.1-multi-machine-shared-pool-configuration/27.1-SPEC.md
@.planning/phases/27.1-multi-machine-shared-pool-configuration/27.1-RESEARCH.md
@crates/tome/src/config/mod.rs
@crates/tome/src/machine.rs
@crates/tome/src/cli.rs
@crates/tome/src/lib.rs
<interfaces>
Existing `Config::validate`, `Config::expand_tildes`, `config::resolve_config_dir`, `DirectoryName`, and `TomePaths::new` remain the effective-model/path-validation contracts. `MachinePrefs` currently supplies disabled sets and directory filters; preserve those behaviors by projecting profile fields into the effective context rather than making downstream pipeline modules parse new TOML files.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Load one selected profile through the normal CLI path</name>
  <files>crates/tome/src/profiles.rs, crates/tome/src/config/mod.rs, crates/tome/src/cli.rs, crates/tome/src/lib.rs, crates/tome/tests/cli_profiles.rs</files>
  <behavior>
    - A settings file selecting an existing valid profile combines pool library/exclude policy, profile directories and filters, and local runtime policy into the Config used by `tome status`.
    - Missing settings, blank selection, invalid identifier, and nonexistent profile exit before sync or status reaches filesystem mutation and name available profiles plus `tome profile select &lt;name&gt;` or `tome init`.
    - One-profile installations still require an explicit selection.
  </behavior>
  <action>Create `profiles.rs` with separate deny-unknown-fields pool-policy, complete machine-profile, and local-settings structs plus `EffectiveContext` and one public loader. Use `DirectoryName` construction for the profile name, resolve `<config-dir>/machines/&lt;profile&gt;.toml` only after settings validation, merge into a fresh existing `Config`, preserve tilde expansion and `Config::validate`, and attach Validation domain errors at input boundaries. Replace normal non-bootstrap command setup in `lib.rs` with this loader so no command maintains a second layer merge. Add the smallest `profile select` command needed to atomically write the selected local setting and use it in recovery text. This implements the locked separate pool/profile/local boundaries and explicit-selection rule for POOL-01.</action>
  <verify><automated>cargo test -p tome --test cli_profiles profile_selection && cargo clippy -p tome --all-targets -- -D warnings</automated></verify>
  <done>A real selected profile drives a normal CLI command; invalid selection states fail deterministically before side effects and no profile is inferred.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete profile lifecycle and legacy override compatibility</name>
  <files>crates/tome/src/profiles.rs, crates/tome/src/config/mod.rs, crates/tome/src/cli.rs, crates/tome/src/lib.rs, crates/tome/tests/cli_profiles.rs</files>
  <behavior>
    - Profile list and create/select operations serialize TOML atomically and reject invalid or duplicate profile names.
    - Repeated loads of unchanged three-layer files yield byte-equivalent effective values.
    - Legacy `directory_overrides` still applies for legacy input while reporting its deprecation; profile writers do not emit it.
  </behavior>
  <action>Add profile list/create/select routing and checked atomic writers using the project TOML temp-and-rename discipline. Move global and per-directory filtering from the legacy machine model into complete profiles while retaining a legacy reader path that emits one deprecation warning when overrides are consumed. Keep local runtime policy in settings, never in committed profile TOML. Extend fixtures to check deterministic repeated projection, validation, writer shape, and profile recovery guidance, per POOL-01 and the locked TOML/deprecation decisions.</action>
  <verify><automated>cargo test -p tome --test cli_profiles && cargo test -p tome profiles::tests</automated></verify>
  <done>Profile management creates only the three intended layer formats, preserves supported legacy overrides during reads, and has deterministic CLI coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| TOML files → effective context | User-controlled paths, identifiers, and policy reach filesystem operations. |
| CLI → profile selection | A command-selected name determines a committed profile file. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-27.1-01 | Tampering | profiles.rs loader | high | mitigate | Validate identifiers before path construction, deny unknown persisted fields, and validate the projected Config before return. |
| T-27.1-02 | Elevation | lib.rs setup | medium | mitigate | Require selected profile before dispatch, rather than accepting default or inferred topology. |
| T-27.1-SC | Tampering | package installs | high | mitigate | No package installation is planned; retain the package legitimacy control as not applicable. |
</threat_model>

<verification>
Run the focused profile integration and unit suites, then `make ci` before phase completion.
</verification>

<success_criteria>
POOL-01 is satisfied with a single shared Rust loader used by normal CLI paths, explicit recovery for invalid selection, and deterministic layer projection.
</success_criteria>

<output>Create `.planning/phases/27.1-multi-machine-shared-pool-configuration/27.1-01-SUMMARY.md` when done</output>
