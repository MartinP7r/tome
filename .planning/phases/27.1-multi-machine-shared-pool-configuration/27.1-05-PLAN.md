---
phase: 27.1-multi-machine-shared-pool-configuration
plan: 05
type: execute
wave: 5
depends_on: [27.1-01, 27.1-04]
files_modified:
  - crates/tome/src/status.rs
  - crates/tome/src/repo_sync.rs
  - crates/tome/src/lib.rs
  - crates/tome/tests/cli_status.rs
autonomous: true
requirements: [POOL-08]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Text and JSON status report selected profile and one internally consistent Git health snapshot."
    - "Unavailable Git states are explicit and status never changes repository bytes or index state."
  artifacts:
    - path: crates/tome/src/status.rs
      provides: Profile/Git health report and renderers
  key_links:
    - from: crates/tome/src/status.rs
      to: crates/tome/src/repo_sync.rs
      via: gather projects one porcelain-v2 snapshot into status report
---

<objective>
Expose selected-profile and safe repository health through the shared CLI status model.

Purpose: Give users and the later desktop layer a reliable explanation of shared-pool/Git state.
Output: Structured Git health model, text/JSON rendering, and state-matrix regression fixtures.
</objective>
<execution_context>@/Users/martin/.config/opencode/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/27.1-multi-machine-shared-pool-configuration/27.1-SPEC.md
@crates/tome/src/status.rs
@crates/tome/src/repo_sync.rs
@crates/tome/src/lib.rs
@crates/tome/tests/cli_status.rs
</context>
<tasks>
<task type="tracer">
  <name>Task 1: Return profile and clean repository health in CLI status</name>
  <files>crates/tome/src/status.rs, crates/tome/src/repo_sync.rs, crates/tome/src/lib.rs, crates/tome/tests/cli_status.rs</files>
  <action>Define serializable, bindings-ready profile and Git-health report types and project the selected profile plus one `repo_sync` porcelain-v2 snapshot into `StatusReport`. Include clean/dirty availability, staged/unstaged totals, added/modified/deleted counts, upstream availability, ahead/behind, divergence, operation-in-progress, and latest commit age. Render the same report through existing text and JSON paths; represent missing repository, remote, upstream, Git executable, and timestamp as explicit unavailable values rather than fabricated data. Keep collection read-only through `--no-optional-locks`, per POOL-08.</action>
  <verify><automated>cargo test -p tome --test cli_status profile_and_clean_git_health</automated></verify>
  <done>A selected-profile repository produces matching text and JSON health values from one structured snapshot without index mutation.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Cover dirty, topology, and unavailable Git status states</name>
  <files>crates/tome/src/status.rs, crates/tome/src/repo_sync.rs, crates/tome/src/lib.rs, crates/tome/tests/cli_status.rs</files>
  <behavior>
    - Clean, staged, unstaged, mixed, ahead, behind, diverged, detached, no-remote, no-upstream, merge/rebase, and non-repository fixtures map to stable structured/rendered fields.
    - Repeated status calls preserve worktree bytes and index state.
  </behavior>
  <action>Add porcelain parser and renderer tests with isolated repositories for the complete POOL-08 matrix. Assert status snapshots are internally consistent, maintain deterministic field ordering, and perform before/after byte/index comparisons for read-only operation. Keep failure classification in Rust domain types rather than string parsing at future IPC boundaries.</action>
  <verify><automated>cargo test -p tome --test cli_status && cargo test -p tome status::tests</automated></verify>
  <done>Status faithfully distinguishes all supported Git states and has regression evidence that it is read-only.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Git repository → status parser | Git output describes untrusted repository state and must remain read-only. |
| domain report → CLI/desktop consumer | Structured state drives recovery messages in two presentation layers. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-27.1-11 | Tampering | Git status probe | high | mitigate | Use sanitized `git --no-optional-locks status --porcelain=v2` and verify index/worktree immutability in fixtures. |
| T-27.1-12 | Spoofing | status parser | medium | mitigate | Parse machine-readable porcelain fields into tagged availability/state types, not display text. |
| T-27.1-SC | Tampering | package installs | high | mitigate | No package installation is planned. |
</threat_model>
<verification>Run status matrix tests and `make lint`.</verification>
<success_criteria>POOL-08 supplies complete, reliable CLI text/JSON profile and Git health.</success_criteria>
<output>Create `.planning/phases/27.1-multi-machine-shared-pool-configuration/27.1-05-SUMMARY.md` when done</output>
