---
phase: 27.1-multi-machine-shared-pool-configuration
plan: 04
type: execute
wave: 4
depends_on: [27.1-01, 27.1-03]
files_modified:
  - crates/tome/src/repo_sync.rs
  - crates/tome/src/cli.rs
  - crates/tome/src/lib.rs
  - crates/tome/tests/cli_pool.rs
autonomous: true
requirements: [POOL-06, POOL-07]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Git policy and one-run override govern pull and publication while noninteractive ask stays local-only."
    - "Tome-owned publishing cannot stage or alter unrelated existing index/worktree state."
  artifacts:
    - path: crates/tome/src/repo_sync.rs
      provides: Read-only health preflight, lock lifecycle, fast-forward pull, and owned-path publication
  key_links:
    - from: crates/tome/src/lib.rs
      to: crates/tome/src/repo_sync.rs
      via: sync loads settings-only Git policy, preflights/pulls, then loads effective pool/profile once for reconciliation and publishes after success
---

<objective>
Add machine-consented, repository-safe Git coordination around pool synchronization.

Purpose: Synchronize shared content without consuming unrelated dotfiles or modifying the user's existing index.
Output: Git policy/override CLI surface, safe preflight/pull/publish service, lock file, and mixed-repository integration coverage.
</objective>
<execution_context>@/Users/martin/.config/opencode/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/27.1-multi-machine-shared-pool-configuration/27.1-CONTEXT.md
@.planning/phases/27.1-multi-machine-shared-pool-configuration/27.1-RESEARCH.md
@crates/tome/src/git.rs
@crates/tome/src/backup.rs
@crates/tome/src/lib.rs
@crates/tome/src/profiles.rs
</context>
<tasks>
<task type="tracer">
  <name>Task 1: Run a consented fast-forward pull before one pool sync</name>
  <files>crates/tome/src/repo_sync.rs, crates/tome/src/cli.rs, crates/tome/src/lib.rs, crates/tome/tests/cli_pool.rs</files>
  <action>Create `repo_sync.rs` around sanitized Git subprocesses that remove `GIT_DIR`, `GIT_WORK_TREE`, and `GIT_INDEX_FILE`. Add local `git_sync` ask/always/never and one-run sync override parsing. Make bootstrap ordering explicit in the sync entry: read only local settings and the invocation override to obtain Git consent; acquire the config-directory lock; preflight and, when consent allows, fetch plus confirmed `--ff-only` pull; then call `load_effective_context` exactly once against the post-pull checkout for pool policy/profile/runtime projection and reconciliation. Do not load pool policy or a profile before pull. For eligible policy, take one `git --no-optional-locks status --porcelain=v2 --branch --show-stash` snapshot and prompt before a remote-summary pull for interactive ask. In non-TTY ask, warn and perform local-only reconciliation. Add a fixture where the pull changes the selected profile or pool config and assert the same invocation uses changed topology, per POOL-06.</action>
  <verify><automated>cargo test -p tome --test cli_pool git_policy_matrix && cargo test -p tome repo_sync::tests</automated></verify>
  <done>Pull behavior follows persisted/one-run policy; settings-only consent precedes pull; effective pool/profile loading occurs once after pull; pulled configuration affects the same invocation; and concurrent syncs serialize safely.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Publish only reviewed Tome-owned paths without index repair</name>
  <files>crates/tome/src/repo_sync.rs, crates/tome/src/cli.rs, crates/tome/src/lib.rs, crates/tome/tests/cli_pool.rs</files>
  <behavior>
    - Existing staged/unmerged, behind/diverged, merge, and rebase states block managed Git work with recovery guidance.
    - A publish preview has stable owned-path order and includes prior Tome-owned edits but excludes unrelated paths and caches.
    - No-change reconciliation makes no commit; a failed push retains the local commit for later push.
  </behavior>
  <action>Implement bounded post-reconcile publication from the same safe Git layer. Reject unsafe status before mutation; calculate deduplicated repository-relative owned pathspecs for library, pool policy, catalog, profiles, and exclusions; reject publishing when any required owned path lies outside the repository worktree; and stage only those changed paths. Never repair existing index/worktree state. Render exact pool-change and owned-path summary before interactive ask commit/push; retain local commits when push fails and offer ahead commits next run. Add mixed-repository fixtures asserting unrelated staged, unstaged, and cache content remain byte-identical for POOL-06 and POOL-07.</action>
  <verify><automated>cargo test -p tome --test cli_pool git_owned_path_staging && cargo clippy -p tome --all-targets -- -D warnings</automated></verify>
  <done>Git coordination has consent and bounded paths, blocks unsafe state, and leaves unrelated repository/index state untouched.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| local policy → Git subprocess | Local settings authorize network and repository mutations. |
| repository worktree/index → Tome publisher | Repository can contain unrelated files and staged user work. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-27.1-08 | Tampering | repo_sync subprocesses | high | mitigate | Sanitize Git environment and use read-only porcelain-v2 preflight before mutation. |
| T-27.1-09 | Tampering | owned-path staging | high | mitigate | Block pre-existing staged/unmerged state and stage sorted explicit repository-relative owned pathspecs only. |
| T-27.1-10 | Denial of service | sync lock | medium | mitigate | Create-new PID/timestamp lock fails concurrent runs without unsafe stale cleanup. |
| T-27.1-SC | Tampering | package installs | high | mitigate | No package installation is planned. |
</threat_model>
<verification>Run policy, lock, and mixed-index fixtures plus workspace lint before phase completion.</verification>
<success_criteria>POOL-06 and POOL-07 supply consent-driven fast-forward synchronization and surgical owned-path publication.</success_criteria>
<output>Create `.planning/phases/27.1-multi-machine-shared-pool-configuration/27.1-04-SUMMARY.md` when done</output>
