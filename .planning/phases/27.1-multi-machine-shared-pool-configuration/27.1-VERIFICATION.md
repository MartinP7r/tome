---
phase: 27.1-multi-machine-shared-pool-configuration
verified: 2026-08-23T00:00:00Z
status: verified
score: 8/8 must-haves verified
behavior_unverified: 0
overrides_applied: 0
gaps: []
behavior_unverified_items: []
---

# Phase 27.1: Multi-Machine Shared Pool Configuration Verification Report

**Phase Goal:** One Git-backed Tome pool accumulates skills and provenance from independently configured machine profiles, distributes the pool according to the active profile, and synchronizes only Tome-owned repository content under explicit machine-local consent without silent deletion, overwrite, or Git-index mutation.

**Verified:** 2026-08-22T12:45:00Z  
**Status:** gaps_found  
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | **POOL-01** — a selected local profile, pool policy, and settings form one validated effective context; selection is explicit. | ✓ VERIFIED | `profiles::load_effective_context` requires `settings.profile`, validates `DirectoryName`, reads pool/profile/settings layers, expands and validates `Config`, and returns the selected profile. Normal CLI and desktop command paths call it. `cargo test -p tome --test cli_profiles`: 7 passed. |
| 2 | **POOL-02** — legacy layout migrates only through previewed/confirmed, recoverable `tome migrate profiles`. | ✓ VERIFIED | `migration_profiles.rs` writes atomically and reloads the new layers before legacy removal. `cli_profiles::migrate_profiles_happy_path` exercises the CLI confirmation and asserts pool/profile/settings creation and legacy-machine removal; focused suite passed. |
| 3 | **POOL-03** — a shared pool accumulates content across profiles and does not delete content merely because another profile lacks a source. | ✓ VERIFIED | `pool.rs` reconciles catalog observations before sync mutation; `cli_pool::cross_profile_pool_preserves_and_distributes` syncs profile A then source-less profile B and asserts library content and B's target symlink. `cli_pool`: 5 passed. |
| 4 | **POOL-04** — provenance has stable identities and identical observations merge deterministically. | ✓ VERIFIED | `pool::stable_identity` uses registry ID, normalized Git URL, or profile/source identity; reconciliation persists `LockEntry` observations in sorted maps. Covered by the pool integration suite and code path. |
| 5 | **POOL-05** — distinct same-name content produces deterministic read-only conflict instead of silently selecting a winner. | ✓ VERIFIED | `pool::reconcile` returns conflicts before apply; `cli_pool::conflicting_candidates_do_not_mutate_pool` asserts nonzero outcome with neither library entry nor lockfile written. Focused suite passed. |
| 6 | **POOL-06** — Git policy/consent governs pull and publication, including the Rust-authoritative desktop continuation. | ✓ VERIFIED | `desktop_git_consent_ipc` drives real Tauri mock-runtime IPC commands against throwaway Git remotes: a forged ID cannot consume the continuation; decline stays local-only; accept pulls then publishes; completed continuations clear managed state and `.tome-sync.lock`. |
| 7 | **POOL-07** — publishing only stages Tome-owned paths and leaves unrelated index/worktree state unchanged. | ✓ VERIFIED | `repo_sync.rs` derives owned paths and stages them explicitly; `cli_pool::git_owned_path_staging_commits_only_pool_content` compares unrelated staged/worktree state. Focused suite passed. |
| 8 | **POOL-08** — CLI/desktop status has profile/Git parity and layered configuration watcher events refresh desktop state. | ✓ VERIFIED | The shared builder exports all three watcher event types into regenerated bindings; status, skills, and doctor hooks refetch on each configuration-layer event. Focused UI tests cover all subscriptions. |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `crates/tome/src/profiles.rs` | Layered profile loader | ✓ VERIFIED | Substantive validated loader, used by CLI and desktop. |
| `crates/tome/src/migration_profiles.rs` | Atomic profile migration | ✓ VERIFIED | Used by `migrate profiles`; focused CLI suite passes. |
| `crates/tome/src/pool.rs` + `lockfile.rs` | Reconciliation/provenance catalog | ✓ VERIFIED | Reconcile-before-apply flow is invoked by sync; integration suite proves pool preservation/conflict barrier. |
| `crates/tome/src/repo_sync.rs` | Git policy, locking, consent, owned publication | ✓ VERIFIED | Explicit porcelain preflight, opaque consent continuation, and owned-path publisher are substantive and used. |
| `crates/tome/src/status.rs` | Profile/Git health text and JSON | ✓ VERIFIED | Gatherer is used by status command; dirty-state test confirms no repository mutation. |
| `crates/tome-desktop/src/commands.rs` | Rust effective-context IPC and consent boundary | ✓ VERIFIED | Calls `tome::profiles::load_effective_context`; command registration and generated TypeScript bindings include both consent commands. |
| `crates/tome-desktop/src/watcher.rs` | Layered artifact watcher events | ✓ VERIFIED | It recognizes and emits `PoolPolicy`, `Profiles`, and `LocalSettings`; the shared builder exports each typed event and desktop data hooks consume them. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| CLI dispatch / `lib.rs` | `profiles::load_effective_context` | settings policy before Git preflight; effective context after pull | ✓ WIRED | Direct calls in normal dispatch/sync path. |
| `migration_profiles.rs` | `profiles::load_effective_context` | reload written layers before removing legacy files | ✓ WIRED | Direct loader call after migration output validation. |
| `lib.rs` | `pool.rs` / `lockfile.rs` | reconcile barrier before library/distribution mutation | ✓ WIRED | Sync collects, reconciles, rejects conflicts, then applies/persists. |
| `lib.rs` | `repo_sync.rs` | Git preflight/pull before context and owned publication afterward | ✓ WIRED | CLI pool tests exercise policy and owned publication. |
| desktop commands | `profiles::load_effective_context` | `load_desktop_context` | ✓ WIRED | Same public Rust loader, not JavaScript or a desktop-local parser. |
| desktop command/UI | `GitConsentRequired` continuation | typed request ID and decision | ✓ WIRED | `desktop_git_consent_ipc` exercises the IPC boundary with both decisions, opaque-ID mismatch protection, Git state changes, lock cleanup, and managed state cleanup. |
| watcher | generated event bindings / UI refresh | layered configuration events | ✓ WIRED | `collect_events!` registers all three events; regenerated bindings expose them and status, skills, and doctor hooks refetch on each. |

### Data-Flow Trace

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| Effective context | `Config`, `MachinePrefs`, selected profile | pool TOML + `machines/<profile>.toml` + local settings | Yes | ✓ FLOWING |
| Status | profile/Git health | selected context + porcelain-v2 Git snapshot | Yes | ✓ FLOWING |
| Desktop Git consent | `GitConsentRequired` | Rust `RepoSync` continuation | Yes, through generated IPC and Tauri/Git fixture | ✓ FLOWING |
| Layered watcher refresh | emitted watcher event | filesystem writes | Generated typed event drives desktop-data refetch | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Profile selection and migration | `cargo test -p tome --test cli_profiles` | 7 passed | ✓ PASS |
| Cross-profile pool, conflicts, Git policy, owned staging | `cargo test -p tome --test cli_pool` | 5 passed | ✓ PASS |
| Profile/Git status and no-mutation status probe | `cargo test -p tome --test cli_status` | 17 passed | ✓ PASS |
| Layered watcher filesystem events | `cargo test -p tome-desktop --test watcher_smoke` | 3 passed | ✓ PASS |
| Desktop consent UI actions | `npm --prefix crates/tome-desktop/ui test -- --run useSync SyncView` | 5 files / 31 tests passed | ✓ PASS |
| Binding freshness | `cargo run -p tome-desktop --bin gen-bindings && git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts` | Exit 0; no binding diff | ✓ PASS |
| Desktop consent continuation lifecycle | `cargo test -p tome-desktop --test desktop_git_consent_ipc` | 1 passed; Tauri IPC plus real Git accept/decline fixture | ✓ PASS |
| Layered desktop refresh consumers | `npm --prefix crates/tome-desktop/ui test -- --run useStatus useLayeredConfigRefresh` | 2 files / 9 tests passed | ✓ PASS |
| Full quality gate | `make ci` | 990 tests passed; fmt and clippy passed | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Status | Evidence |
|---|---|---|---|
| POOL-01 | 01, 06 | ✓ SATISFIED | Shared Rust loader, explicit-selection CLI tests, desktop loader use. |
| POOL-02 | 02 | ✓ SATISFIED | CLI migration fixture and atomic migration implementation. |
| POOL-03 | 03 | ✓ SATISFIED | Cross-profile preservation/distribution fixture. |
| POOL-04 | 03 | ✓ SATISFIED | Stable identity/reconciliation implementation and pool fixture. |
| POOL-05 | 03 | ✓ SATISFIED | Conflict barrier fixture proves no mutation. |
| POOL-06 | 04, 06 | ✓ SATISFIED | Tauri IPC fixture exercises opaque continuation matching, decline, accept, Git effects, and state/lock cleanup. |
| POOL-07 | 04 | ✓ SATISFIED | Owned-path staging fixture preserves unrelated repository state. |
| POOL-08 | 05, 06 | ✓ SATISFIED | Typed watcher events are registered, generated, consumed by desktop data hooks, and covered by focused tests. |

All POOL-01 through POOL-08 IDs are declared by at least one phase plan; no orphaned requirements found.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---:|---|---|---|
| `crates/tome-desktop/ui/src/hooks/useSync.tsx` | 29, 112 | Historical “placeholder” comments | ℹ️ Info | Comments describe superseded earlier-phase UI states; no executable placeholder/stub was found. |
| `crates/tome-desktop/ui/src/views/SyncView.tsx` | 21, 32 | Historical “placeholder” comments | ℹ️ Info | Same; no unresolved `TBD`, `FIXME`, or `XXX` markers in phase implementation files. |

## Gaps Summary

Both verification gaps are closed. The layered configuration watcher events are registered in the shared Tauri/Specta builder, regenerated into TypeScript bindings, and consumed by all affected desktop data hooks. The Tauri IPC fixture now exercises opaque consent IDs against real Git state for both local-only and accepted outcomes, including lock and managed-state cleanup.

---

_Verified: 2026-08-23T00:00:00Z_  
_Verifier: the agent (gsd-verifier)_
