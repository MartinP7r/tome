---
phase: 27.1-multi-machine-shared-pool-configuration
verified: 2026-08-22T17:32:21Z
status: passed
score: 8/8 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 8/8
  gaps_closed:
    - "SyncView's bindings fixture now mocks and resets poolPolicyChanged, profilesChanged, and localSettingsChanged."
    - "SkillsView and Sidebar bindings fixtures now also mock and reset the layered watcher events."
  gaps_remaining: []
  regressions: []
---

# Phase 27.1: Multi-Machine Shared Pool Configuration Verification Report

**Phase Goal:** Make one Git-backed skill pool safely accumulate skills and provenance from machines with independent source and distribution topologies, while keeping runtime consent local and preventing silent cross-machine overwrites or removals.

**Verified:** 2026-08-22T17:32:21Z  
**Status:** passed  
**Re-verification:** Yes — after watcher-fixture commits `2ac0348` and `27034c2`

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | **POOL-01** — a selected pool policy, named profile, and local settings form one explicit validated effective context. | ✓ VERIFIED | `profiles::load_effective_context` is used by normal CLI and `commands::load_desktop_context`; fresh `cargo test -p tome --test cli_profiles` passed (7). |
| 2 | **POOL-02** — legacy configuration migrates only through previewed, recoverable migration. | ✓ VERIFIED | `migration_profiles` uses the layer writer and journal-aware recovery before legacy retirement; fresh `cli_profiles` suite passed, including migration cases. |
| 3 | **POOL-03** — pool content accumulates across selected profiles and remains distributable with no local discovery source. | ✓ VERIFIED | `pool::reconcile` retains catalog content across unavailable sources; fresh `cargo test -p tome --test cli_pool` passed (5), including cross-profile coverage. |
| 4 | **POOL-04** — provenance identity and repeated observations merge deterministically. | ✓ VERIFIED | Reconciliation merges catalog entries before persistence with stable identities and BTree ordering; fresh `cli_pool` suite passed. |
| 5 | **POOL-05** — independent same-name/different-content candidates stop with a read-only deterministic conflict. | ✓ VERIFIED | `pool::reconcile` returns conflicts when candidate hashes diverge before mutation; fresh `cli_pool` suite passed. |
| 6 | **POOL-06** — desktop requests Rust-authoritative consent and resumes only the matching Git continuation for accept or decline. | ✓ VERIFIED | Fresh `desktop_git_consent_ipc::tauri_ipc_resumes_only_matching_git_consent_and_releases_state` passed. It exercises a Tauri mock runtime against throwaway Git remotes: forged ID rejection, pre-pull decline, post-sync local-only decline, accept pull/publish, and lock/state cleanup. |
| 7 | **POOL-07** — publication stages only Tome-owned paths without changing unrelated index or worktree state. | ✓ VERIFIED | `repo_sync` sanitizes Git environment and stages explicit owned pathspecs; fresh `cli_pool` suite passed, including owned-path staging coverage. |
| 8 | **POOL-08** — selected profile/Git health is read-only and layered config writes refresh desktop data through generated typed events. | ✓ VERIFIED | Fresh `cli_status` (17), desktop watcher smoke (3), and expanded UI watcher suite (6 files / 41 tests) passed. The chain is watcher → Specta registration → generated bindings → status/skills/doctor React refetch. |

**Score:** 8/8 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `crates/tome/src/profiles.rs` | Layered profile loader | ✓ VERIFIED | Substantive validated loader; CLI and desktop both call it. |
| `crates/tome/src/migration_profiles.rs` | Previewed recoverable migration | ✓ VERIFIED | Journal/reload ordering keeps interrupted migration from being treated as a mixed layout. |
| `crates/tome/src/pool.rs` + `lockfile.rs` | Accumulating reconciliation and provenance catalog | ✓ VERIFIED | Conflict-free plan/reconcile/apply barrier persists authoritative catalog state. |
| `crates/tome/src/repo_sync.rs` | Consent-aware Git and owned publisher | ✓ VERIFIED | Exact path staging and continuation lifecycle are exercised by focused fixtures. |
| `crates/tome/src/status.rs` | Read-only profile/Git health | ✓ VERIFIED | Structured status derives a single porcelain snapshot; fresh 17-test CLI status suite passed. |
| `crates/tome-desktop/src/commands.rs` | Rust-authoritative desktop consent boundary | ✓ VERIFIED | Stores server-side continuation and verifies opaque request ID before resume. |
| `crates/tome-desktop/src/watcher.rs` | Typed layered configuration events | ✓ VERIFIED | Watches pool policy parent, profiles directory, and settings parent; smoke tests cover each emitted event. |
| `crates/tome-desktop/ui/src/bindings.ts` | Generated IPC/event contract | ✓ VERIFIED | Exposes consent command and all three layered watcher event handles; fresh generator produced no diff. |
| `crates/tome-desktop/ui/src/hooks/useStatus.ts`, `useSkills.ts`, `useDoctorReport.ts` | Refresh consumers | ✓ VERIFIED | Each subscribes to all layered event handles and refetches Rust-derived data. |
| `SyncView.test.tsx`, `SkillsView.test.tsx`, `Sidebar.test.tsx` | Compatible generated-event mocks | ✓ VERIFIED | Commits `2ac0348` and `27034c2` define and reset all three listener spies; six-file watcher suite passed 41/41. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| CLI and desktop commands | `profiles::load_effective_context` | Shared Rust loader | ✓ WIRED | `lib.rs` and desktop `commands.rs:86` use the same loader; no JavaScript or desktop-local parser. |
| Pool sync | reconciliation/catalog persistence | Plan/reconcile/apply barrier | ✓ WIRED | `pool::reconcile` detects conflicts before selected skills reach mutation paths. |
| Sync command | `RepoSync` / `PendingGitConsent` | Opaque ID then `resume(decision)` | ✓ WIRED | Exact-ID continuation test passes against real Git fixtures. |
| Watcher events | Specta builder → `bindings.ts` | Registered typed events and generator | ✓ WIRED | `PoolPolicyChanged`, `ProfilesChanged`, and `LocalSettingsChanged` are registered and present in bindings. |
| Generated events | React data hooks | `useTauriEvent(..., refetch)` | ✓ WIRED | Focused hook/view tests invoke the expanded event fixtures without mount failures. |

### Data-Flow Trace

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| Effective context | `EffectiveContext` | Pool policy + selected profile + local settings | Validated TOML layers | ✓ FLOWING |
| Desktop consent | Opaque request ID / continuation | Rust `RepoSync` state | Tauri command and Git fixture | ✓ FLOWING |
| Layered watcher | `PoolPolicyChanged`, `ProfilesChanged`, `LocalSettingsChanged` | Debounced filesystem paths | Temporary-directory writes in smoke fixture | ✓ FLOWING |
| React refresh | Hook state from status, skills, doctor commands | Generated event callbacks call `refetch` | Expanded UI suite invokes all listener registrations | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Profile selection and migration | `cargo test -p tome --test cli_profiles` | 7 passed | ✓ PASS |
| Pool reconciliation, conflicts, Git staging | `cargo test -p tome --test cli_pool` | 5 passed | ✓ PASS |
| Read-only profile/Git status | `cargo test -p tome --test cli_status` | 17 passed | ✓ PASS |
| Tauri/Git consent lifecycle | `cargo test -p tome-desktop --test desktop_git_consent_ipc tauri_ipc_resumes_only_matching_git_consent_and_releases_state -- --exact` | 1 passed | ✓ PASS |
| Layered watcher events | `cargo test -p tome-desktop --test watcher_smoke` | 3 passed | ✓ PASS |
| Binding freshness | `cargo run -p tome-desktop --bin gen-bindings && git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts` | No generated diff | ✓ PASS |
| Expanded UI watcher consumers | `npm --prefix crates/tome-desktop/ui test -- --run ...useStatus ...useLayeredConfigRefresh ...useSync ...SyncView ...SkillsView ...Sidebar` | 6 files / 41 tests passed | ✓ PASS |
| Full quality gate | `make ci` | Passed: format, clippy, and workspace tests | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Status | Evidence |
| --- | --- | --- | --- |
| POOL-01 | 01, 06 | ✓ SATISFIED | Shared effective-context loader and fresh CLI/desktop coverage. |
| POOL-02 | 02 | ✓ SATISFIED | Journal-backed migration with fresh profile suite evidence. |
| POOL-03 | 03 | ✓ SATISFIED | Fresh cross-profile pool fixture. |
| POOL-04 | 03 | ✓ SATISFIED | Deterministic catalog reconciliation path and fixture. |
| POOL-05 | 03 | ✓ SATISFIED | Read-only conflict barrier and fixture. |
| POOL-06 | 04, 06 | ✓ SATISFIED | Fresh real Tauri IPC/Git lifecycle proof. |
| POOL-07 | 04 | ✓ SATISFIED | Fresh owned-path staging fixture. |
| POOL-08 | 05, 06 | ✓ SATISFIED | Fresh status, watcher, generated-binding consumer, and expanded UI evidence. |

`REQUIREMENTS.md` does not map additional Phase 27.1 IDs; all POOL IDs claimed by plans are covered above. No probe is declared for this phase.

### Anti-Patterns Found

No blocker or warning anti-patterns found. Targeted scan of Phase 27.1 production files found no unreferenced `TBD`, `FIXME`, or `XXX` markers. The prior incomplete event mocks were corrected and the expanded UI suite now passes.

### Residual Risks

- The focused desktop consent test uses Tauri's mock runtime and throwaway Git remotes; it does not replace manual testing against a user's long-lived repository and OS credential/network configuration.
- Watcher smoke coverage proves typed events for filesystem writes, but event delivery timing on all supported desktop environments remains an integration risk.

### Gaps Summary

None. The earlier SyncView fixture failure is closed: `2ac0348` fixes SyncView, and `27034c2` covers the other affected view fixtures. Fresh focused suites, the expanded UI watcher suite, and `make ci` all pass.

---

_Verified: 2026-08-22T17:32:21Z_  
_Verifier: the agent (gsd-verifier)_
