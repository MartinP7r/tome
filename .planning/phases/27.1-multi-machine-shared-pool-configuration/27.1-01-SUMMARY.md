---
phase: 27.1-multi-machine-shared-pool-configuration
plan: 01
subsystem: configuration
tags: [rust, toml, profiles, cli, configuration]
requires:
  - phase: 27-sync-triage-ui
    provides: existing CLI and desktop configuration consumers
provides:
  - Explicit named profile lifecycle commands and atomic TOML persistence
  - Deterministic layered configuration projection with legacy override compatibility
affects: [27.1-02, 27.1-03, configuration-ui]
actuals:
  tokens: 3517
  tasks: 2
  commits: 2
tech-stack:
  added: []
  patterns: [checked TOML round-trip before atomic profile/settings writes, explicit legacy compatibility route]
key-files:
  created: [crates/tome/src/profiles.rs]
  modified: [crates/tome/src/cli.rs, crates/tome/src/lib.rs, crates/tome/tests/cli_profiles.rs]
key-decisions:
  - "Profile create, list, and select operate on committed machines/<name>.toml files; selection refuses a nonexistent profile."
  - "An explicit --machine path retains legacy directory_overrides loading and emits a deprecation warning."
patterns-established:
  - "Persisted profile/settings writers serialize, reparse, compare, then atomically rename."
requirements-completed: [POOL-01]
coverage:
  - id: D1
    description: Explicit profile create/list/select lifecycle with validation and atomic TOML persistence.
    requirement: POOL-01
    verification:
      - kind: integration
        ref: crates/tome/tests/cli_profiles.rs#profile_create_list_and_select_are_validated
        status: pass
    human_judgment: false
  - id: D2
    description: Legacy directory overrides remain readable and warn without being emitted by profile writers.
    requirement: POOL-01
    verification:
      - kind: integration
        ref: crates/tome/tests/cli_profiles.rs#legacy_directory_overrides_remain_compatible_with_a_warning
        status: pass
      - kind: unit
        ref: crates/tome/src/profiles.rs#profile_serialization_excludes_local_runtime_policy
        status: pass
    human_judgment: false
duration: 38min
completed: 2026-08-19
status: complete
---

# Phase 27.1 Plan 01: Layered Profile Configuration Summary

**Explicit, validated TOML machine-profile lifecycle with deterministic layered settings projection and deprecated legacy override support.**

## Performance

- **Duration:** 38 min
- **Completed:** 2026-08-19T15:29:38Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- Loaded selected pool/profile/local settings through a single effective context for normal CLI commands.
- Added atomic `profile create`, `profile list`, and validated `profile select` commands.
- Preserved explicit legacy `--machine` directory overrides with a one-time deprecation warning.

## Task Commits

1. **Task 1: Load one selected profile through the normal CLI path** — `efc20c5`
2. **Task 2: Complete profile lifecycle and legacy override compatibility** — `f7f7086`

## Files Created/Modified

- `crates/tome/src/profiles.rs` — layer schemas, effective context, profile persistence, and determinism tests.
- `crates/tome/src/cli.rs` — profile lifecycle subcommands.
- `crates/tome/src/lib.rs` — lifecycle routing and legacy override compatibility path.
- `crates/tome/tests/cli_profiles.rs` — end-to-end lifecycle and deprecation coverage.

## Decisions Made

- Profile selection validates that the named committed profile exists before local settings are changed.
- `--machine` remains the explicit legacy compatibility entry point; profile writers never emit `directory_overrides` or local runtime-policy fields.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Regression] Kept read-only `tome config` independent of profile selection**
- **Found during:** Task 2 verification
- **Issue:** Routing every normal command through the selected-profile loader broke the existing read-only config-path command.
- **Fix:** Kept `Config` in the non-profile bootstrap path, matching `Add`.
- **Files modified:** `crates/tome/src/lib.rs`
- **Verification:** `cargo test -p tome --test cli_profiles`; the affected `cli_add` integration test passed during `make ci`.
- **Committed in:** `f7f7086`

---

**Total deviations:** 1 auto-fixed (Rule 1 regression)
**Impact on plan:** Preserves a read-only command without weakening explicit profile selection for operational commands.

## Issues Encountered

- `make ci` was run but stopped in legacy `cli_backup` fixtures, which invoke `tome sync` without the newly required selected profile. Focused profile tests, profile unit tests, formatting, and clippy passed.

## Next Phase Readiness

- POOL-01's layer and lifecycle contract is available to later pool, migration, Git-policy, and desktop work.
- Existing integration fixtures that intentionally exercise normal sync without a selected profile need migration to the explicit-profile setup.

## Self-Check: PASSED

- `crates/tome/src/profiles.rs` and `crates/tome/tests/cli_profiles.rs` exist.
- Task commits `efc20c5` and `f7f7086` exist.
