---
phase: 27.1-multi-machine-shared-pool-configuration
plan: 03
subsystem: shared-pool-reconciliation
tags: [rust, pool, provenance, lockfile, conflict-resolution]
requires:
  - phase: 27.1-01
    provides: selected profile and shared pool policy layers
  - phase: 27.1-02
    provides: durable migration contracts for profile configuration
provides:
  - conflict-safe shared-pool candidate reconciliation before sync mutation
  - versioned JSON provenance observations and exclusion-first pool removal
affects: [27.1-04, configuration-ui, shared-pool-sync]
actuals:
  tokens: 17822
  tasks: 2
  commits: 4
tech-stack:
  added: []
  patterns: [plan-before-mutate pool reconciliation, authoritative observation catalog, exclusion-first recovery marker]
key-files:
  created: [crates/tome/src/pool.rs, crates/tome/tests/cli_pool.rs]
  modified: [crates/tome/src/lockfile.rs, crates/tome/src/lib.rs, crates/tome/src/cli.rs, crates/tome/src/profiles.rs]
key-decisions:
  - "Only selected-profile pool syncs apply the new conflict barrier; explicit legacy-machine compatibility retains its existing cleanup and duplicate behavior."
  - "Pool removal writes the shared exclusion before its journal marker and any derived-artifact cleanup."
patterns-established:
  - "Reconcile all hashed candidates into a pure catalog plan before library, manifest, catalog, or distribution mutation."
requirements-completed: [POOL-03, POOL-04, POOL-05]
coverage:
  - id: D1
    description: Cross-profile import preserves pool content and distributes it from a target-only profile.
    requirement: POOL-03
    verification:
      - kind: integration
        ref: crates/tome/tests/cli_pool.rs#cross_profile_pool_preserves_and_distributes
        status: pass
    human_judgment: false
  - id: D2
    description: Different same-name source content halts without pool or catalog mutation.
    requirement: POOL-05
    verification:
      - kind: integration
        ref: crates/tome/tests/cli_pool.rs#conflicting_candidates_do_not_mutate_pool
        status: pass
    human_judgment: false
  - id: D3
    description: Explicit removal persists an exclusion and restore permits a later re-import.
    requirement: POOL-03
    verification:
      - kind: integration
        ref: crates/tome/tests/cli_pool.rs#pool_remove_excludes_before_cleanup_and_restore_allows_import
        status: pass
    human_judgment: false
duration: 58min
completed: 2026-08-20
status: complete
---

# Phase 27.1 Plan 03: Shared Pool Reconciliation Summary

**Conflict-safe shared-pool reconciliation with versioned provenance observations, target-only distribution, and exclusion-first recoverable removal.**

## Performance

- **Duration:** 58 min
- **Completed:** 2026-08-20T14:36:25Z
- **Tasks:** 2/2
- **Files modified:** 12

## Accomplishments

- Collected and hashed every pool candidate before any shared-pool mutation; independent content differences now stop sync with sorted conflict details.
- Persisted stable source observations in `tome.lock` while retaining first facts and deduplicating repeated identical observations.
- Added explicit pool remove, restore, and source-pin resolution commands; removal persists the shared exclusion before a recovery marker and derived cleanup.
- Kept accumulated pool content distributable from profiles with only targets and retained explicit legacy compatibility behavior.

## Task Commits

1. **Task 1: Reconcile one profile observation into the tracked catalog without source-absence deletion** — `2167036`
2. **Task 2 RED: Add failing pool removal lifecycle coverage** — `74f3c2e`
3. **Task 2: Add explicit pool removal, restore, pins, and conflict resolution** — `0e67d2a`
4. **Task 2 fix: Retain legacy duplicate discovery behavior outside shared-pool mode** — `7d8f63a`

## Files Created/Modified

- `crates/tome/src/pool.rs` — candidate hashing, deterministic reconciliation, and removal recovery.
- `crates/tome/src/lockfile.rs` — authoritative source-observation catalog fields and merge semantics.
- `crates/tome/src/lib.rs` — pool barrier integration before consolidation and target-only distribution path.
- `crates/tome/src/cli.rs` / `crates/tome/src/profiles.rs` — pool removal, restore, resolution, exclusions, and pins.
- `crates/tome/tests/cli_pool.rs` — cross-profile, conflict-read-only, and removal/restore integration coverage.

## Decisions Made

- Selected-profile pool sync owns the new reconciliation policy; explicit legacy `--machine` paths retain their pre-migration compatibility behavior.
- Local observed paths are persisted as non-authoritative provenance metadata; stable identities and content hashes decide reconciliation.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Updated desktop test fixtures for the expanded lockfile entry schema**
- **Found during:** Task 2 full test verification
- **Issue:** Desktop test constructors could not compile after `LockEntry` gained authoritative observation history.
- **Fix:** Added empty observation histories to fixture entries.
- **Files modified:** `crates/tome-desktop/src/sync_types.rs`
- **Verification:** `make test` and `make lint` passed.
- **Committed in:** `0e67d2a`

**2. [Rule 1 - Regression] Preserved legacy sync behavior outside selected-profile pool mode**
- **Found during:** Task 2 full test verification
- **Issue:** Legacy fixture paths were incorrectly subject to shared-pool conflicts and source-absence semantics.
- **Fix:** Applied pool reconciliation only to selected-profile pool configurations; legacy explicit-machine paths keep their compatibility behavior.
- **Files modified:** `crates/tome/src/{discover,lib,profiles}.rs`
- **Verification:** `make test` and `make lint` passed.
- **Committed in:** `0e67d2a`, `7d8f63a`

**Total deviations:** 2 auto-fixed (Rule 1 regression, Rule 3 blocking issue).

## Verification

- `cargo test -p tome --test cli_pool` — passed.
- `cargo test -p tome pool::tests` — passed.
- `cargo test -p tome lockfile::tests` — passed.
- `make test` — passed (984 tests; existing `proc-macro-error2` future-incompatibility warning remains).
- `make lint` — passed.

## Next Phase Readiness

Pool sync now has a conflict-free plan/apply barrier, durable shared removal policy, and catalog history for subsequent Git policy and status work.

## Self-Check: PASSED

- `crates/tome/src/pool.rs` and `crates/tome/tests/cli_pool.rs` exist.
- Commits `2167036`, `74f3c2e`, `0e67d2a`, and `7d8f63a` exist.
