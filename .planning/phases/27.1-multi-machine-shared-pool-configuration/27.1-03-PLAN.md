---
phase: 27.1-multi-machine-shared-pool-configuration
plan: 03
type: execute
wave: 3
depends_on: [27.1-01, 27.1-02]
files_modified:
  - crates/tome/src/pool.rs
  - crates/tome/src/lockfile.rs
  - crates/tome/src/discover.rs
  - crates/tome/src/cleanup.rs
  - crates/tome/src/remove.rs
  - crates/tome/src/lib.rs
  - crates/tome/tests/cli_pool.rs
autonomous: true
requirements: [POOL-03, POOL-04, POOL-05]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Skills imported by one selected profile remain distributable by a profile with no discovery sources."
    - "Shared catalog observations retain stable origins and do not change for repeated identical syncs."
    - "Different content from independent same-name sources produces a deterministic read-only conflict until explicitly resolved."
  artifacts:
    - path: crates/tome/src/pool.rs
      provides: Candidate collection, reconciliation, removal/restore, and conflict resolution
    - path: crates/tome/src/lockfile.rs
      provides: Versioned authoritative provenance catalog
  key_links:
    - from: crates/tome/src/pool.rs
      to: crates/tome/src/lockfile.rs
      via: conflict-free reconciliation merges catalog observations before atomic catalog persistence
    - from: crates/tome/src/lib.rs
      to: crates/tome/src/pool.rs
      via: sync uses plan/reconcile/apply barrier before library or distribution mutation
---

<objective>
Replace first-wins discovery and source-absence cleanup with a conflict-safe accumulating shared pool and authoritative provenance catalog.

Purpose: Ensure independently configured machines can safely contribute to one shared library.
Output: Pool reconciliation service, catalog schema upgrade, explicit removal/restore/resolution commands, and cross-profile fixtures.
</objective>
<execution_context>@/Users/martin/.config/opencode/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/27.1-multi-machine-shared-pool-configuration/27.1-SPEC.md
@.planning/phases/27.1-multi-machine-shared-pool-configuration/27.1-RESEARCH.md
@crates/tome/src/discover.rs
@crates/tome/src/cleanup.rs
@crates/tome/src/lockfile.rs
@crates/tome/src/remove.rs
@crates/tome/src/lib.rs
</context>
<tasks>
<task type="tracer">
  <name>Task 1: Reconcile one profile observation into the tracked catalog without source-absence deletion</name>
  <files>crates/tome/src/pool.rs, crates/tome/src/lockfile.rs, crates/tome/src/discover.rs, crates/tome/src/lib.rs, crates/tome/tests/cli_pool.rs</files>
  <action>Create a pool candidate collector that retains every discovered same-name candidate and hashes each directory with `manifest::hash_directory`; replace `discover_all`'s first-wins selection only at pool-sync entry. Define deterministic stable identity for registry, normalized Git URL, and ordinary `<profile>/<directory>` sources. Evolve `tome.lock` to a versioned JSON catalog with initial/latest/deduplicated observations, legacy read support, atomic upgraded write after successful reconciliation, and BTree ordering. Wire a single conflict-free profile-A import path through normal sync and prove a profile-B target-only sync distributes preserved pool content. Do not use manifest ownership as provenance authority, per POOL-03/04.</action>
  <verify><automated>cargo test -p tome --test cli_pool cross_profile_pool_preserves_and_distributes && cargo test -p tome lockfile::tests</automated></verify>
  <done>One real candidate reaches library, derived manifest, authoritative catalog, and a later target-only profile without source absence removing it.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add explicit pool removal, restore, pins, and conflict resolution</name>
  <files>crates/tome/src/pool.rs, crates/tome/src/cleanup.rs, crates/tome/src/remove.rs, crates/tome/src/lib.rs, crates/tome/tests/cli_pool.rs</files>
  <behavior>
    - Missing, unavailable, and empty active sources preserve existing pool entries.
    - Explicit removal adds shared exclusion with deletion; restore clears exclusion before later import.
    - Equal-content candidates merge provenance; two and three differing candidates halt before any pool, manifest, catalog, exclusion, or distribution mutation.
    - Retain-current, accept-source, and exclude resolution persist a shared source pin or exclusion and make the next sync deterministic.
  </behavior>
  <action>Route cleanup through pool semantics so it reports source absence without deleting accumulated library content. Add plan/render/execute pool remove and restore commands; persist shared exclusions atomically with removal behavior and preserve cancellation safety. Add structured conflict output with sorted candidate identity/hash/location, plus explicit CLI resolution actions that write only the chosen pin or exclusion. Pinned-source updates may advance; unavailable pinned sources warn and preserve both pin and content. Exercise no-mutation byte checks, idempotency, source identity merges, and each resolution action for POOL-03, POOL-04, and POOL-05.</action>
  <verify><automated>cargo test -p tome --test cli_pool && cargo test -p tome pool::tests</automated></verify>
  <done>Pool changes are explicit and idempotent, provenance preserves origin history, and unresolved content differences cannot alter pool state.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| discovered source trees → pool reconciler | Untrusted/on-disk source content proposes library and catalog changes. |
| conflict command → shared policy | A chosen candidate determines globally shared future selection. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-27.1-05 | Tampering | pool reconciliation | high | mitigate | Hash all candidates and halt differing independent content before every mutation boundary. |
| T-27.1-06 | Tampering | pool remove/restore | high | mitigate | Use explicit plan/execute flow, shared exclusion tombstone, atomic catalog/config writes, and cancellation checks. |
| T-27.1-07 | Denial of service | catalog persistence | medium | mitigate | Retain deterministic bounded first/latest observations and atomic JSON replacement. |
| T-27.1-SC | Tampering | package installs | high | mitigate | No package installation is planned. |
</threat_model>
<verification>Run focused pool suites and `make test` after integration wiring.</verification>
<success_criteria>POOL-03 through POOL-05 deliver an accumulating, provenance-preserving pool with explicit conflict choices.</success_criteria>
<output>Create `.planning/phases/27.1-multi-machine-shared-pool-configuration/27.1-03-SUMMARY.md` when done</output>
