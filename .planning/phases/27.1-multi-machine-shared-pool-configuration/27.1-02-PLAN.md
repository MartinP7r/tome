---
phase: 27.1-multi-machine-shared-pool-configuration
plan: 02
type: execute
wave: 2
depends_on: [27.1-01]
files_modified:
  - crates/tome/src/migration_profiles.rs
  - crates/tome/src/cli.rs
  - crates/tome/src/lib.rs
  - crates/tome/tests/cli_profiles.rs
autonomous: true
requirements: [POOL-02]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Legacy layouts are refused by normal commands with migration guidance."
    - "Interactive migration previews, backs up, and safely writes every new layer without making old inputs unusable on interruption."
  artifacts:
    - path: crates/tome/src/migration_profiles.rs
      provides: Previewed legacy-to-profile migration transaction
  key_links:
    - from: crates/tome/src/migration_profiles.rs
      to: crates/tome/src/profiles.rs
      via: migration reloads the written layers through the effective-context loader before legacy removal
---

<objective>
Provide the explicit, interactive migration from portable config plus machine preferences to the three-layer profile model.

Purpose: Move existing installations without data loss or automatic configuration reinterpretation.
Output: `tome migrate profiles`, legacy-layout detection, backup/atomic-write transaction, and fault-injection coverage.
</objective>

<execution_context>@/Users/martin/.config/opencode/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/27.1-multi-machine-shared-pool-configuration/27.1-CONTEXT.md
@.planning/phases/27.1-multi-machine-shared-pool-configuration/27.1-SPEC.md
@crates/tome/src/migration_v010.rs
@crates/tome/src/config/mod.rs
@crates/tome/src/machine.rs
@crates/tome/src/profiles.rs
</context>
<tasks>
<task type="tracer">
  <name>Task 1: Migrate one legacy installation through preview and confirmation</name>
  <files>crates/tome/src/migration_profiles.rs, crates/tome/src/cli.rs, crates/tome/src/lib.rs, crates/tome/tests/cli_profiles.rs</files>
  <action>Add `tome migrate profiles` as an interactive-only nested migration command: reject non-TTY, `--no-input`, and bypass flags; ask for a validated profile name; build a pure plan mapping every legacy field once; render mapping plus file diffs to stderr; and require confirmation before writes. Back up original config and active machine files as collision-free timestamped sibling files, write pool policy/profile/settings atomically, reload via Plan 01's effective loader, then remove legacy machine TOML only at the final boundary. Bake legacy override effective paths into the new profile while retaining override syntax only in the backup, per POOL-02 and the locked migration decisions.</action>
  <verify><automated>cargo test -p tome --test cli_profiles migrate_profiles_happy_path</automated></verify>
  <done>A confirmed terminal migration produces validated shared policy, named profile, and local settings with readable sibling backups and only then retires active legacy machine preferences.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Make migration refusal, failure boundaries, and reruns deterministic</name>
  <files>crates/tome/src/migration_profiles.rs, crates/tome/src/cli.rs, crates/tome/src/lib.rs, crates/tome/tests/cli_profiles.rs</files>
  <behavior>
    - Normal commands identify an unmigrated legacy layout and direct the user to the migration command before mutation.
    - Existing profile names and malformed legacy input are refused without partial conversion.
    - Injected failure at each backup/write/remove boundary preserves usable legacy inputs; a completed rerun changes no bytes.
  </behavior>
  <action>Introduce testable write-boundary injection around the migration executor and fixtures for each legacy field, empty collection, configured order, profile collision, malformed input, interrupted writes, and repeat invocation. Wire legacy-layout detection into normal command setup without changing post-migration behavior. Keep rollback as documented manual restoration from the timestamped backups; do not add an automatic rollback command, per POOL-02.</action>
  <verify><automated>cargo test -p tome --test cli_profiles migrate_profiles && cargo test -p tome migration_profiles::tests</automated></verify>
  <done>Every migration boundary is covered, no normal command silently converts legacy state, and reruns are byte-idempotent.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| legacy files → migration plan | Existing persistent paths and TOML become replacement writes. |
| preview → confirmed execution | User approval separates a rendered plan from destructive legacy-file retirement. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-27.1-03 | Tampering | migration_profiles.rs | high | mitigate | Validate all source TOML, stage backups and new atomic files before final legacy removal, and reload new context before that removal. |
| T-27.1-04 | Repudiation | migration preview | medium | mitigate | Render field mapping and file diffs before explicit terminal confirmation. |
| T-27.1-SC | Tampering | package installs | high | mitigate | No package installation is planned. |
</threat_model>
<verification>Run migration unit/integration suites and `cargo clippy -p tome --all-targets -- -D warnings`.</verification>
<success_criteria>POOL-02 migration is explicit, previewed, backed up, ordered safely, and repeatable.</success_criteria>
<output>Create `.planning/phases/27.1-multi-machine-shared-pool-configuration/27.1-02-SUMMARY.md` when done</output>
