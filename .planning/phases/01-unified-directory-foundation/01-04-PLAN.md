---
phase: 01-unified-directory-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-01"]
files_modified:
  - crates/tome/src/wizard.rs
autonomous: true
requirements:
  - WIZ-01
  - WIZ-02
  - WIZ-03
  - WIZ-04
  - WIZ-05

must_haves:
  truths:
    - "Merged KNOWN_DIRECTORIES registry replaces separate KNOWN_SOURCES + KNOWN_TARGETS"
    - "Auto-discovery scans filesystem and assigns roles from registry"
    - "Summary table shows name, path, type, role with plain-english role descriptions"
    - "Custom directory addition includes role selection filtered by type"
    - "find_source_target_overlaps() is eliminated"
    - "ClaudePlugins directories always get Managed role (no role picker shown)"
  artifacts:
    - path: "crates/tome/src/wizard.rs"
      provides: "Unified wizard flow with KNOWN_DIRECTORIES"
      contains: "KNOWN_DIRECTORIES"
  key_links:
    - from: "wizard.rs KNOWN_DIRECTORIES"
      to: "config.rs DirectoryType"
      via: "KnownDirectory.directory_type field"
      pattern: "KnownDirectory"
    - from: "wizard.rs role picker"
      to: "config.rs DirectoryType::valid_roles()"
      via: "filtered role options"
      pattern: "valid_roles"
---

<objective>
Rewrite the wizard to use a merged KNOWN_DIRECTORIES registry, replacing KNOWN_SOURCES + KNOWN_TARGETS. Implement auto-discovery with role assignment, summary table with role descriptions, custom directory addition with role picker, and eliminate find_source_target_overlaps().

Purpose: The wizard is the primary onboarding experience. It must present the unified directory model clearly.
Output: Rewritten wizard.rs with KNOWN_DIRECTORIES, auto-role assignment, summary table, and role picker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-unified-directory-foundation/01-CONTEXT.md
@.planning/phases/01-unified-directory-foundation/01-RESEARCH.md
@.planning/phases/01-unified-directory-foundation/01-01-SUMMARY.md
@docs/v06-implementation-plan.md

<interfaces>
<!-- New types from Plan 01 (config.rs): -->
```rust
pub struct DirectoryName(String);
pub enum DirectoryType { ClaudePlugins, Directory, Git }
pub enum DirectoryRole { Managed, Synced, Source, Target }
pub struct DirectoryConfig { pub path: PathBuf, pub directory_type: DirectoryType, ... }

impl DirectoryType {
    pub fn default_role(&self) -> DirectoryRole;
    pub fn valid_roles(&self) -> Vec<DirectoryRole>;
}

impl DirectoryRole {
    pub fn description(&self) -> &'static str;
    // Returns e.g. "Managed (read-only, owned by package manager)"
}

pub struct Config {
    pub directories: BTreeMap<DirectoryName, DirectoryConfig>,
    // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite wizard with KNOWN_DIRECTORIES and unified flow</name>
  <files>crates/tome/src/wizard.rs</files>
  <read_first>
    - crates/tome/src/wizard.rs (full file -- understand KNOWN_SOURCES, KNOWN_TARGETS, find_source_target_overlaps, configure_sources, configure_targets, complete wizard flow)
    - crates/tome/src/config.rs (DirectoryName, DirectoryType, DirectoryRole, DirectoryConfig, Config from Plan 01)
    - docs/v06-implementation-plan.md (wizard rewrite section -- KnownDirectory struct, ~12-13 entries, new flow)
  </read_first>
  <action>
    Rewrite crates/tome/src/wizard.rs. This is a substantial rewrite (~400+ lines).

    **1. Replace KNOWN_SOURCES + KNOWN_TARGETS with KNOWN_DIRECTORIES:**

    Define `KnownDirectory` struct:
    ```rust
    struct KnownDirectory {
        name: &'static str,
        display: &'static str,
        default_path: &'static str,   // relative to $HOME
        directory_type: DirectoryType,
        default_role: DirectoryRole,
    }
    ```

    Create `KNOWN_DIRECTORIES: &[KnownDirectory]` with ~12-13 entries. Deduplicate from current KNOWN_SOURCES (10 entries) + KNOWN_TARGETS (9 entries). Key entries (read existing arrays from wizard.rs to get exact paths):
    - claude-plugins: ~/.claude/plugins, ClaudePlugins, Managed
    - claude-skills: ~/.claude/skills, Directory, Synced
    - antigravity: ~/.gemini/antigravity/skills, Directory, Synced
    - codex-agents: ~/.agents/skills, Directory, Synced (or Target if it's target-only)
    - codex-source: ~/.codex/skills, Directory, Source (if separate from agents)
    - openclaw: ~/.openclaw/skills, Directory, Synced
    - amp: ~/.amp/skills, Directory, Synced
    - aider: ~/.aider/skills, Directory, Synced
    - cline: ~/.cline/skills, Directory, Synced
    - roo: ~/.roo/skills, Directory, Synced
    - cursor: ~/.cursor/skills, Directory, Synced (or Target)
    - windsurf: ~/.windsurf/skills, Directory, Synced (or Target)

    Note: Some tools had separate source/target paths. Check the existing arrays carefully. Tools where the source path differs from the target path should have TWO entries (one Source, one Target or Synced).

    **2. REMOVE `find_source_target_overlaps()` entirely (WIZ-05).**
    REMOVE `KNOWN_SOURCES` and `KNOWN_TARGETS` arrays.

    **3. New wizard flow (replace `configure_sources` + `configure_targets`):**

    a. `find_known_directories_in(home: &Path) -> Vec<(KnownDirectory, PathBuf)>`:
       - For each entry in KNOWN_DIRECTORIES, check if `home.join(entry.default_path)` exists
       - Return found entries with their resolved paths

    b. Auto-discovery step (WIZ-02):
       - Call `find_known_directories_in()`
       - Present as MultiSelect: "Found these directories — select which to include"
       - Display format: `"{display} ({default_path}) [{role.description()}]"`

    c. Auto-assign roles from registry (WIZ-02):
       - Each selected directory gets its `default_role` from the registry
       - No role picker for ClaudePlugins (always Managed, per D-07)

    d. Summary table (WIZ-03):
       - Show tabled table: Name | Path | Type | Role
       - Role column shows `role.description()` (plain-english per D-04, D-05)
       - Ask: "Would you like to edit any directory's role?" (Confirm)
       - If yes, present Select for which directory to edit
       - Then present `dialoguer::Select` with valid roles for that type (D-06):
         - Call `directory_type.valid_roles()` to get filtered options
         - Display `role.description()` for each option
         - Skip picker for ClaudePlugins (D-07)
       - Loop until user is done editing

    e. Custom directory addition (WIZ-04):
       - "Add a custom directory?" loop
       - Input: name (validate as DirectoryName)
       - Input: path (expand tilde)
       - Select: type (ClaudePlugins, Directory)  -- Git not available in wizard since it needs URLs
       - Select: role (filtered by type via `directory_type.valid_roles()`, with descriptions)
       - Add to list, show updated summary

    f. Library location: unchanged from current wizard
    g. Exclusion picker: unchanged from current wizard
    h. Final summary + confirm + save: build Config with `directories: BTreeMap<DirectoryName, DirectoryConfig>`

    **4. Update `pub fn run(dry_run: bool) -> Result<()>` to use the new flow.**

    **5. Delete all unit tests referencing KNOWN_SOURCES/KNOWN_TARGETS.** Write new tests:
    - Test: KNOWN_DIRECTORIES has no duplicate names
    - Test: All KNOWN_DIRECTORIES entries have valid DirectoryName values
    - Test: find_known_directories_in returns entries where paths exist
  </action>
  <verify>
    <automated>cd /Users/martin/dev/opensource/tome && cargo test -p tome wizard::tests -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - wizard.rs contains `KNOWN_DIRECTORIES` (not KNOWN_SOURCES or KNOWN_TARGETS)
    - wizard.rs contains `struct KnownDirectory` with `directory_type` and `default_role` fields
    - wizard.rs does NOT contain `find_source_target_overlaps`
    - wizard.rs does NOT contain `KNOWN_SOURCES`
    - wizard.rs does NOT contain `KNOWN_TARGETS`
    - wizard.rs does NOT contain `configure_sources` or `configure_targets` (replaced with unified flow)
    - wizard.rs contains `valid_roles` call for role filtering (D-06)
    - wizard.rs contains `role.description()` or `description()` for plain-english display (D-04, D-05)
    - `cargo test -p tome wizard::tests` exits 0
  </acceptance_criteria>
  <done>Wizard uses merged KNOWN_DIRECTORIES registry, auto-discovers with role assignment, shows summary table with descriptions, supports custom directory addition with role picker, find_source_target_overlaps eliminated, all tests pass.</done>
</task>

</tasks>

<verification>
- `cargo test -p tome wizard::tests` passes
- `cargo clippy -p tome -- -D warnings` passes on wizard.rs
</verification>

<success_criteria>
- KNOWN_DIRECTORIES registry replaces separate arrays (WIZ-01)
- Auto-discovery assigns roles from registry (WIZ-02)
- Summary table shows role descriptions (WIZ-03)
- Custom directory addition includes filtered role picker (WIZ-04)
- find_source_target_overlaps eliminated (WIZ-05)
- All wizard unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-unified-directory-foundation/01-04-SUMMARY.md`
</output>
