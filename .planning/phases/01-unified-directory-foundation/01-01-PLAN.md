---
phase: 01-unified-directory-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/tome/src/config.rs
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-03
  - CFG-04
  - CFG-05
  - CFG-06

must_haves:
  truths:
    - "DirectoryName, DirectoryType, DirectoryRole, DirectoryConfig types exist and compile"
    - "Config struct has directories BTreeMap replacing sources Vec and targets BTreeMap"
    - "#[serde(deny_unknown_fields)] on Config rejects old-format TOML with migration hint"
    - "Config validation rejects invalid role/type combos"
    - "discovery_dirs() and distribution_dirs() convenience methods filter by role"
    - "Empty directories map is detectable for safety guard"
  artifacts:
    - path: "crates/tome/src/config.rs"
      provides: "DirectoryName, DirectoryType, DirectoryRole, DirectoryConfig, updated Config"
      exports: ["DirectoryName", "DirectoryType", "DirectoryRole", "DirectoryConfig", "Config"]
      contains: "deny_unknown_fields"
  key_links:
    - from: "config.rs DirectoryType"
      to: "config.rs DirectoryRole"
      via: "default_role() method"
      pattern: "fn default_role"
    - from: "config.rs Config"
      to: "config.rs DirectoryConfig"
      via: "BTreeMap<DirectoryName, DirectoryConfig>"
      pattern: "directories.*BTreeMap"
---

<objective>
Replace the entire type system in config.rs: remove Source, SourceType, TargetConfig, TargetMethod, TargetName and add DirectoryName, DirectoryType, DirectoryRole, DirectoryConfig. Update Config struct to use `directories: BTreeMap<DirectoryName, DirectoryConfig>` instead of separate `sources` and `targets`. Add `#[serde(deny_unknown_fields)]` with old-format detection and migration hint. Add convenience iterators and validation.

Purpose: This is the foundation every other module depends on. Nothing else can compile until these types exist.
Output: Fully rewritten config.rs with new type system, validation, serde support, and unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-unified-directory-foundation/01-CONTEXT.md
@.planning/phases/01-unified-directory-foundation/01-RESEARCH.md
@docs/v06-implementation-plan.md

<interfaces>
<!-- Current types being replaced (read from crates/tome/src/config.rs): -->

From crates/tome/src/config.rs (REMOVE these):
```rust
pub struct TargetName(String);  // Replace with DirectoryName
pub struct Source { name: String, path: PathBuf, source_type: SourceType }
pub enum SourceType { ClaudePlugins, Directory }
pub enum TargetMethod { Symlink { skills_dir: PathBuf } }
pub struct TargetConfig { pub enabled: bool, pub method: TargetMethod }
// Config.sources: Vec<Source>  -- REMOVE
// Config.targets: BTreeMap<TargetName, TargetConfig>  -- REMOVE
```

From crates/tome/src/validation.rs (REUSE):
```rust
pub fn validate_identifier(name: &str, kind: &str) -> Result<()>;
pub struct ContentHash(String);
```

From crates/tome/src/discover.rs (KEEP, reference only):
```rust
pub struct SkillName(String);  // Same newtype pattern to follow for DirectoryName
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define new directory types and rewrite Config struct</name>
  <files>crates/tome/src/config.rs</files>
  <read_first>
    - crates/tome/src/config.rs (full file -- understand all existing types, trait impls, serde layers, tests)
    - crates/tome/src/validation.rs (validate_identifier signature and ContentHash type)
    - crates/tome/src/discover.rs (SkillName newtype -- pattern to replicate for DirectoryName)
    - docs/v06-implementation-plan.md (type definitions, config format, validation rules)
  </read_first>
  <behavior>
    - Test: DirectoryName::new("valid-name") succeeds, DirectoryName::new("") fails, DirectoryName::new("a/b") fails
    - Test: DirectoryType default is Directory
    - Test: DirectoryType::default_role() returns Managed for ClaudePlugins, Synced for Directory, Source for Git
    - Test: DirectoryRole::description() returns plain-english parenthetical strings per D-04/D-05
    - Test: Minimal TOML `[directories.foo]\npath = "/tmp/foo"` parses with type=Directory, role=Synced (defaults chain correctly)
    - Test: Explicit TOML `[directories.foo]\npath = "/tmp"\ntype = "claude-plugins"\nrole = "managed"` parses correctly
    - Test: TOML with `[[sources]]` fails to parse AND error message contains the D-02 migration hint
    - Test: TOML with `[targets.foo]` fails to parse AND error message contains the D-02 migration hint
    - Test: TOML with unknown field `[directories.foo]\npath = "/tmp"\nbogus = true` fails (deny_unknown_fields)
    - Test: Config validation rejects Managed role with Directory type (only ClaudePlugins can be Managed)
    - Test: Config validation rejects Target role with Git type
    - Test: Config validation rejects git fields (branch/tag/rev) with non-Git type
    - Test: discovery_dirs() returns only Managed/Synced/Source entries
    - Test: distribution_dirs() returns only Synced/Target entries
    - Test: config.directories.is_empty() is true for empty config (for CFG-06 safety guard)
  </behavior>
  <action>
    Rewrite crates/tome/src/config.rs completely. The file is ~500 lines; replace the entire type system.

    **Types to ADD (per RESEARCH.md Type Hierarchy):**

    1. `DirectoryName` newtype -- identical trait impls to current `TargetName`:
       - `#[derive(Debug, Clone, PartialEq, Eq, Hash, PartialOrd, Ord, serde::Serialize)]`
       - `#[serde(transparent)]`
       - `new()` calls `crate::validation::validate_identifier(&name, "directory name")`
       - Impl: Display, AsRef<str>, AsRef<Path>, PartialEq<str>, PartialEq<&str>, Borrow<str>, TryFrom<String>
       - Custom `Deserialize` that calls `DirectoryName::new(s).map_err(serde::de::Error::custom)`

    2. `DirectoryType` enum:
       - Variants: `ClaudePlugins`, `Directory`, `Git`
       - `#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]`
       - `#[serde(rename_all = "kebab-case")]`
       - `impl Default` returns `Directory`
       - `impl Display` for each variant ("claude-plugins", "directory", "git")
       - `fn default_role(&self) -> DirectoryRole` -- ClaudePlugins->Managed, Directory->Synced, Git->Source
       - `fn valid_roles(&self) -> Vec<DirectoryRole>` -- ClaudePlugins->[Managed], Directory->[Synced,Source,Target], Git->[Source] (used by wizard D-06)

    3. `DirectoryRole` enum:
       - Variants: `Managed`, `Synced`, `Source`, `Target`
       - `#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]`
       - `#[serde(rename_all = "kebab-case")]`
       - `fn description(&self) -> &'static str` per D-04/D-05:
         - Managed: "Managed (read-only, owned by package manager)"
         - Synced: "Synced (skills discovered here AND distributed here)"
         - Source: "Source (skills discovered here, not distributed here)"
         - Target: "Target (skills distributed here, not discovered here)"

    4. `DirectoryConfig` struct:
       - `pub path: PathBuf`
       - `#[serde(rename = "type", default)] pub directory_type: DirectoryType`
       - `#[serde(default)] pub(crate) role: Option<DirectoryRole>` -- Option for post-deser defaulting
       - `#[serde(skip_serializing_if = "Option::is_none")] pub branch: Option<String>`
       - `#[serde(skip_serializing_if = "Option::is_none")] pub tag: Option<String>`
       - `#[serde(skip_serializing_if = "Option::is_none")] pub rev: Option<String>`
       - Add `#[serde(deny_unknown_fields)]` on DirectoryConfig too
       - `pub fn role(&self) -> DirectoryRole` -- returns self.role.unwrap_or_else(|| self.directory_type.default_role())
       - After deserialization, resolve defaults in a post-parse step

    5. Updated `Config` struct:
       - `#[serde(deny_unknown_fields)]` per D-01, D-03
       - `#[serde(default = "defaults::library_dir")] pub(crate) library_dir: PathBuf`
       - `#[serde(default)] pub(crate) exclude: BTreeSet<SkillName>`
       - `#[serde(default)] pub(crate) directories: BTreeMap<DirectoryName, DirectoryConfig>`
       - `#[serde(default)] pub(crate) backup: BackupConfig`
       - REMOVE `sources: Vec<Source>` and `targets: BTreeMap<TargetName, TargetConfig>`

    **Types to REMOVE entirely:**
    - `TargetName` (replaced by DirectoryName)
    - `Source` struct
    - `SourceType` enum
    - `TargetMethod` enum
    - `TargetConfig` struct
    - `RawTargetConfig` struct
    - `deserialize_targets()` function
    - All related Serialize/Deserialize impls

    **Methods to ADD on Config:**
    - `pub fn discovery_dirs(&self) -> impl Iterator<Item = (&DirectoryName, &DirectoryConfig)>` -- filters for Managed/Synced/Source roles
    - `pub fn distribution_dirs(&self) -> impl Iterator<Item = (&DirectoryName, &DirectoryConfig)>` -- filters for Synced/Target roles
    - `pub fn managed_dirs(&self) -> impl Iterator<Item = (&DirectoryName, &DirectoryConfig)>` -- filters for Managed only
    - `pub fn validate(&self) -> Result<()>` -- rejects invalid combos:
      - Managed role only valid with ClaudePlugins type
      - Target role invalid with Git type
      - branch/tag/rev fields only valid with Git type

    **Config::load() update per D-01, D-02:**
    Update the parse error path: when `toml::from_str::<Config>(&content)` fails, check if `content.contains("[[sources]]") || content.contains("[targets.")`. If so, wrap the error with:
    `"hint: tome v0.6 replaced [[sources]] and [targets.*] with [directories.*]. See CHANGELOG.md for migration instructions."`

    **Keep unchanged:** BackupConfig, defaults module, expand_tildes(), Config::save(), Config::load_or_default() (update to use new types but same logic).

    Write unit tests in the `#[cfg(test)] mod tests` block covering all behavior items above. Delete all existing tests that reference Source/TargetName/TargetConfig.
  </action>
  <verify>
    <automated>cd /Users/martin/dev/opensource/tome && cargo test -p tome config::tests -- --nocapture 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - config.rs contains `pub struct DirectoryName(String)`
    - config.rs contains `pub enum DirectoryType` with variants `ClaudePlugins`, `Directory`, `Git`
    - config.rs contains `pub enum DirectoryRole` with variants `Managed`, `Synced`, `Source`, `Target`
    - config.rs contains `pub struct DirectoryConfig`
    - config.rs contains `deny_unknown_fields` on Config struct
    - config.rs contains `fn discovery_dirs`
    - config.rs contains `fn distribution_dirs`
    - config.rs contains `fn validate`
    - config.rs contains the exact string `hint: tome v0.6 replaced [[sources]] and [targets.*] with [directories.*]. See CHANGELOG.md for migration instructions.`
    - config.rs does NOT contain `pub struct TargetName`
    - config.rs does NOT contain `pub struct Source`
    - config.rs does NOT contain `pub enum SourceType`
    - config.rs does NOT contain `pub enum TargetMethod`
    - config.rs does NOT contain `pub struct TargetConfig`
    - `cargo test -p tome config::tests` exits 0 with all tests passing
  </acceptance_criteria>
  <done>All new directory types compile, Config parses new TOML format, rejects old format with migration hint, validation catches invalid combos, convenience iterators work, all unit tests pass.</done>
</task>

</tasks>

<verification>
- `cargo test -p tome config::tests` passes
- `cargo clippy -p tome -- -D warnings` passes on config.rs (may have errors in other modules referencing old types -- that's expected and handled in subsequent plans)
</verification>

<success_criteria>
- DirectoryName, DirectoryType, DirectoryRole, DirectoryConfig types exist and are exported
- Config struct uses directories BTreeMap, not sources/targets
- Old-format TOML produces migration hint error
- Role/type combo validation works
- Convenience iterators filter correctly
- All config unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-unified-directory-foundation/01-01-SUMMARY.md`
</output>
