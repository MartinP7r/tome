---
phase: 25-rust-core-extraction-tauri-integration-spike
plan: 06
type: execute
wave: 5
depends_on: ["25-04", "25-05"]
files_modified:
  - crates/tome-desktop/ui-react/**
  - crates/tome-desktop/ui-solid/**
  - crates/tome-desktop/ui-svelte/**
  - crates/tome-desktop/tauri.conf.json
  - crates/tome-desktop/ui/**
  - .planning/research/v1.0-frontend-framework-decision.md
  - .planning/REQUIREMENTS.md
  - .planning/milestones/v1.0-REQUIREMENTS.md
autonomous: false
requirements: [CORE-02, CORE-03]

must_haves:
  truths:
    - "The same single-page StatusReport dashboard is built in React, Solid, and Svelte against the real bindings.ts"
    - "Each spike imports the ONE canonical bindings.ts via a Vite resolve.alias (no per-spike copy)"
    - "Each spike renders a real StatusReport from the user's actual tome_home (not fixtures)"
    - "Each candidate is scored 1-5 across all four criteria (bindings.ts ergonomics, bundle size + cold-start, dev-loop speed, ecosystem fit)"
    - "The decision + rationale + invalidation conditions are recorded in an ADR"
    - "D-GUI-04 in the canonical active .planning/REQUIREMENTS.md is updated with the chosen framework and the two losing spikes are deleted"
  artifacts:
    - path: ".planning/research/v1.0-frontend-framework-decision.md"
      provides: "Scoring table + ADR for the framework choice"
      contains: "Scoring"
    - path: "crates/tome-desktop/ui/**"
      provides: "The winning framework's scaffold (losers deleted)"
  key_links:
    - from: "spike frontends"
      to: "crates/tome-desktop/ui/src/bindings.ts"
      via: "Vite resolve.alias '@bindings' -> ../ui/src/bindings.ts; import { commands } from '@bindings'"
      pattern: "@bindings"
    - from: ".planning/REQUIREMENTS.md"
      to: "chosen framework"
      via: "D-GUI-04 update"
      pattern: "D-GUI-04"
---

<objective>
Build the same single-page `StatusReport` dashboard in React, Solid, and Svelte (D-01/D-02), each consuming the ONE committed `bindings.ts` (via a Vite `resolve.alias`, not a copy) and rendering a real `StatusReport` from the user's actual `tome_home`; score each 1-5 across the four criteria (D-03); record the winner + rationale + invalidation conditions in an ADR (D-04); update D-GUI-04 in the canonical active `.planning/REQUIREMENTS.md` (and the milestone archive copy for consistency); and delete the two losing spikes. This is the LAST plan — it picks the load-bearing frontend framework for all of v1.0.

Purpose: Settle the irreversible-from-Phase-26 framework decision with a built comparison against real data, not assumptions.
Output: Three spike frontends sharing one bindings.ts via alias, an ADR + scoring table, the winner collapsed into `ui/`, the losers deleted, REQUIREMENTS.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-rust-core-extraction-tauri-integration-spike/25-CONTEXT.md
@.planning/phases/25-rust-core-extraction-tauri-integration-spike/25-RESEARCH.md
@.planning/phases/25-rust-core-extraction-tauri-integration-spike/25-PATTERNS.md
@crates/tome-desktop/ui/src/bindings.ts

<interfaces>
<!-- From 25-04/25-05. The Rust backend + bindings are identical across all three frameworks. -->
Committed bindings (regenerated by 25-05 to contain TomeError/ErrorCode): `crates/tome-desktop/ui/src/bindings.ts` exports typed `commands.getStatus(): Promise<Result<StatusReport, TomeError>>` and `events.*` (tauri-specta generated). This is the SINGLE canonical file — spikes alias to it, they do NOT copy it.
Spike layout (RESEARCH Open Question Q-C / Project Structure note lines 254-255): ONE `crates/tome-desktop` Rust crate, THREE sibling `ui-react/` `ui-solid/` `ui-svelte/` frontends sharing the one `bindings.ts`. Switch active frontend via `tauri.conf.json` `beforeDevCommand`/`devUrl`/`frontendDist`. After the decision, collapse the winner into `ui/`, delete losers.
Shared-bindings wiring (BLOCKER-3 resolution): each spike's `vite.config.*` adds `resolve: { alias: { '@bindings': path.resolve(__dirname, '../ui/src/bindings.ts') } }` and every spike imports `from '@bindings'`. The alias resolves to the ONE canonical `crates/tome-desktop/ui/src/bindings.ts`; no per-spike `src/bindings.ts` copy is created. (Vite reads `__dirname` via `import { fileURLToPath }` in ESM configs — Claude's Discretion on the exact ESM `__dirname` shim.)
Four scoring criteria (D-03): (1) bindings.ts ergonomics in each framework's idioms, (2) production bundle size + cold-start TTI, (3) dev-loop speed (HMR latency, error quality, type-check speed), (4) ecosystem fit for v1.0 reqs (virtualized lists for VIEW-02/NF-01, keyboard-accessible widgets for NF-02, macOS-HIG-aligned components for NF-03).
Each spike renders ONLY the StatusReport dashboard — no list virtualization, no interactions (D-02).
Node + Tauri CLI may be missing (RESEARCH Environment Availability) — install via brew if absent (`cargo install tauri-cli --version "^2"`; Node via brew/volta).
Canonical REQUIREMENTS file: `.planning/REQUIREMENTS.md` is the active promoted copy (D-GUI-04 row at :146 — "TBD in Phase 25 spike"). `.planning/milestones/v1.0-REQUIREMENTS.md` (D-GUI-04 at :152) is the archive copy — update it too for consistency, but the active file is canonical.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the three StatusReport spike frontends sharing one bindings.ts via Vite alias</name>
  <read_first>
    - crates/tome-desktop/ui/src/bindings.ts (the ONE canonical generated typed commands the frontends alias-import)
    - crates/tome-desktop/tauri.conf.json (frontendDist/beforeDevCommand/devUrl to switch active frontend)
    - .planning/phases/25-rust-core-extraction-tauri-integration-spike/25-CONTEXT.md (D-01/D-02/D-03 + Specifics: render REAL tome_home data)
    - .planning/phases/25-rust-core-extraction-tauri-integration-spike/25-RESEARCH.md (Open Question Q-C one-crate-three-ui layout; Environment Availability)
    - .planning/phases/25-rust-core-extraction-tauri-integration-spike/25-PATTERNS.md (No Analog Found — ui/** is net-new)
  </read_first>
  <action>
    Ensure Node + Tauri CLI are installed (brew / `cargo install tauri-cli --version "^2"` if missing — Environment Availability). Create three sibling Vite frontends: `crates/tome-desktop/ui-react/`, `ui-solid/`, `ui-svelte/`.

    SHARED bindings.ts wiring (BLOCKER-3 — concrete mechanism, do NOT copy the file): in EACH spike's `vite.config.ts` (or `.js`/`.mts` per template) add a `resolve.alias` entry mapping `'@bindings'` to the single canonical file: `resolve: { alias: { '@bindings': path.resolve(__dirname, '../ui/src/bindings.ts') } }` (in an ESM config derive `__dirname` via `const __dirname = path.dirname(fileURLToPath(import.meta.url))`). EVERY spike imports the typed commands `from '@bindings'` (e.g. `import { commands } from '@bindings'`) so all three resolve to the ONE `crates/tome-desktop/ui/src/bindings.ts` — there must be NO `ui-*/src/bindings.ts` copy and NO per-spike `cargo run gen-bindings`. The Rust backend + bindings are identical across all three; regenerating per-framework is forbidden (it would defect from the single-source-of-truth contract — SC#3). If a spike's TS toolchain needs the path for type resolution, mirror the same alias in its `tsconfig.json` `compilerOptions.paths` (`"@bindings": ["../ui/src/bindings.ts"]`) — still pointing at the canonical file, never a copy.

    Each spike renders the SAME single-page dashboard from a real `StatusReport`: resolved `tome_home`, library dir, configured directories with role/type badges, skill counts, last sync, health summary — by calling `commands.getStatus()` and pattern-matching the `Result<StatusReport, TomeError>` discriminated union. NO list virtualization, NO interactions (D-02 — that is Phase 26). Wire `tauri.conf.json` so the active frontend is switchable (point `beforeDevCommand`/`devUrl`/`frontendDist` at one ui-* dir at a time). Verify each runs via `cargo tauri dev` against the dev machine's real `tome_home` and displays live data (not fixtures — Specifics + SC#2). While building, record per-framework observations for the four D-03 criteria (bindings.ts ergonomics, bundle size after `vite build`, HMR/dev-loop feel, ecosystem fit for future virtualized-list/a11y/HIG needs).
  </action>
  <verify>
    <automated>test -d crates/tome-desktop/ui-react && test -d crates/tome-desktop/ui-solid && test -d crates/tome-desktop/ui-svelte && rg -l "@bindings" crates/tome-desktop/ui-react crates/tome-desktop/ui-solid crates/tome-desktop/ui-svelte | wc -l && test -z "$(fd '^bindings\.ts$' crates/tome-desktop/ui-react/src crates/tome-desktop/ui-solid/src crates/tome-desktop/ui-svelte/src 2>/dev/null)" && echo "OK: no per-spike bindings.ts copy"</automated>
  </verify>
  <acceptance_criteria>
    - All three of `crates/tome-desktop/ui-react/`, `ui-solid/`, `ui-svelte/` exist as Vite projects.
    - Each `ui-*/vite.config.*` contains a `@bindings` `resolve.alias` pointing at `../ui/src/bindings.ts` (the canonical file): `rg -l "@bindings" crates/tome-desktop/ui-*/vite.config.*` lists all three, and each alias target is `../ui/src/bindings.ts` (not a local path).
    - Each spike imports the typed command via the alias: `rg -l "from ['\"]@bindings['\"]" crates/tome-desktop/ui-*/src` (or `import .* @bindings`) lists all three, AND each calls `getStatus` (`rg -l "getStatus" crates/tome-desktop/ui-*/src` lists ≥3 files).
    - NO per-spike copy of the bindings file exists: `fd '^bindings\.ts$' crates/tome-desktop/ui-react/src crates/tome-desktop/ui-solid/src crates/tome-desktop/ui-svelte/src` returns nothing (the only `bindings.ts` in the tree is the canonical `crates/tome-desktop/ui/src/bindings.ts`).
    - Each `vite build` succeeds (bundle-size data captured for criterion 2).
    - Manual confirmation (recorded in SUMMARY): each runs via `cargo tauri dev` and shows the real tome_home StatusReport.
  </acceptance_criteria>
  <done>Three StatusReport dashboards built, each importing the ONE canonical bindings.ts via a `@bindings` Vite alias (no per-spike copy), each rendering real tome_home data; per-criterion observations captured.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Which frontend framework (React / Solid / Svelte) does v1.0 commit to for Phases 26-31?</decision>
  <context>
    The choice is irreversible from Phase 26 onward (D-GUI-04; STATE.md "Frontend framework decision is load-bearing"). The three spikes have been built and scored. Present the completed scoring table (1-5 per criterion, four criteria) plus the agent's recommendation, with the measured bundle sizes, observed dev-loop feel, bindings.ts ergonomics notes, and ecosystem-fit assessment for the upcoming VIEW-02/NF-01 virtualized list, NF-02 keyboard/VoiceOver a11y, and NF-03 macOS-HIG needs. The user makes the final call.
  </context>
  <options>
    <option id="react">
      <name>React</name>
      <pros>Largest ecosystem (virtualization, a11y, component libs); most hiring/AI familiarity; mature Tauri examples.</pros>
      <cons>Largest bundle / slowest cold-start of the three; more runtime overhead for the 2000-skill 60fps NF-01 budget.</cons>
    </option>
    <option id="solid">
      <name>Solid</name>
      <pros>Best raw performance + small bundle (favorable for NF-01 60fps); JSX familiarity; fine-grained reactivity.</pros>
      <cons>Smaller ecosystem; fewer off-the-shelf a11y/HIG components; less Tauri-specta example coverage.</cons>
    </option>
    <option id="svelte">
      <name>Svelte</name>
      <pros>Smallest/most ergonomic authoring; small bundle; good dev-loop; compiles away runtime.</pros>
      <cons>Different mental model; ecosystem between React and Solid; virtualization libs less mature than React's.</cons>
    </option>
  </options>
  <resume-signal>Select: react, solid, or svelte (and note any overriding rationale).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record ADR + scoring table, collapse winner, delete losers, update D-GUI-04</name>
  <read_first>
    - .planning/REQUIREMENTS.md (canonical active copy — D-GUI-04 row at :146 — "TBD in Phase 25 spike")
    - .planning/milestones/v1.0-REQUIREMENTS.md (archive copy — D-GUI-04 row at :152 — update for consistency)
    - crates/tome-desktop/tauri.conf.json (point frontendDist/beforeDevCommand at the winner's ui/)
    - crates/tome-desktop/ui-<winner>/vite.config.* (the winner's @bindings alias — see note below on collapse)
    - .planning/phases/25-rust-core-extraction-tauri-integration-spike/25-CONTEXT.md (D-04 — ADR location + REQUIREMENTS update + delete losers)
  </read_first>
  <action>
    Create `.planning/research/v1.0-frontend-framework-decision.md` as an ADR: the four-criteria scoring table (1-5 per criterion for each of React/Solid/Svelte, with the measured bundle sizes and dev-loop/ergonomics/ecosystem notes from Task 1), the chosen framework (from the checkpoint decision), the rationale, and explicit "what would invalidate this choice" conditions.

    Collapse the winning `ui-<winner>/` into `crates/tome-desktop/ui/` so that `ui/src/bindings.ts` remains the committed canonical bindings location. NOTE on the alias after collapse: once the winner lives in `ui/`, its app source and `bindings.ts` are co-located, so update the winner's import to resolve `bindings.ts` relative to its now-sibling location (either keep the `@bindings` alias but repoint it to `./src/bindings.ts`, or switch the winner's imports to a plain relative `./bindings` import). Either way the canonical file stays at `crates/tome-desktop/ui/src/bindings.ts` and is NOT duplicated. Update `tauri.conf.json` to point `frontendDist`/`beforeDevCommand`/`devUrl` at `ui/` permanently. Delete the two losing `ui-<loser>/` directories (D-04).

    Update the D-GUI-04 row in the CANONICAL active `.planning/REQUIREMENTS.md` (line :146) to name the chosen framework (replace "Frontend framework: TBD in Phase 25 spike" with the committed choice + a pointer to the ADR). This active file is the canonical record. For consistency, also update the matching D-GUI-04 row in the archive `.planning/milestones/v1.0-REQUIREMENTS.md` (line :152) to name the same framework (and it can drop its stale "Phase 10 spike" wording). Ensure the committed `bindings.ts` still lives at `crates/tome-desktop/ui/src/bindings.ts` and the CI gen-bindings gate path still resolves.
  </action>
  <verify>
    <automated>test -f .planning/research/v1.0-frontend-framework-decision.md && test -d crates/tome-desktop/ui && test ! -d crates/tome-desktop/ui-react -o ! -d crates/tome-desktop/ui-solid -o ! -d crates/tome-desktop/ui-svelte && rg -c "Scoring" .planning/research/v1.0-frontend-framework-decision.md && rg "D-GUI-04" .planning/REQUIREMENTS.md | rg -v "TBD" && test -f crates/tome-desktop/ui/src/bindings.ts</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/research/v1.0-frontend-framework-decision.md` exists with a 4-criteria scoring table and an invalidation-conditions section.
    - Exactly one of the three spikes remains, collapsed into `crates/tome-desktop/ui/`; the other two `ui-*` dirs are deleted (`fd ui-react crates/tome-desktop` etc. return nothing for the losers).
    - `crates/tome-desktop/ui/src/bindings.ts` still exists (committed canonical bindings preserved at the canonical path; not duplicated elsewhere — `fd '^bindings\.ts$' crates/tome-desktop` returns exactly the one `ui/src/bindings.ts`).
    - The CANONICAL active `.planning/REQUIREMENTS.md` D-GUI-04 row names the chosen framework and no longer says "TBD in Phase 25 spike": `rg "D-GUI-04" .planning/REQUIREMENTS.md | rg -v "TBD"` returns the row.
    - The archive `.planning/milestones/v1.0-REQUIREMENTS.md` D-GUI-04 row also names the chosen framework (consistency): `rg "D-GUI-04" .planning/milestones/v1.0-REQUIREMENTS.md | rg -v "TBD"` returns the row.
    - `cargo run -p tome-desktop --bin gen-bindings && git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts` exits 0 (CI gate path still valid after collapse).
  </acceptance_criteria>
  <done>ADR + scoring table recorded; winner collapsed into ui/; losers deleted; D-GUI-04 updated in the canonical active REQUIREMENTS.md (and the archive copy for consistency); the single canonical bindings.ts path preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview (spike UI) → Rust | Spike calls only the `get_status` command exposed in capabilities/main.json |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-06-EoP | Elevation of Privilege | spike frontend command surface | mitigate | Spikes call only `get_status` (the sole command in the capability allowlist from 25-04); no new commands or plugins added. Throwaway spikes — losers deleted. |
| T-25-06-SC | Tampering | npm deps in the three Vite spikes | mitigate | Spike npm deps are framework scaffolds (vite + react/solid/svelte official templates); two of three trees are deleted after the decision. Only the winner's `node_modules` persists and it is gitignored (25-04). No npm deps cross into the shipped Rust artifact. |
| T-25-06-OOS | EoP | bundling/signing the spike app | accept (out of scope) | No production bundle, signing, or notarization this phase — DIST is Phase 31. Spikes run via `cargo tauri dev` only. |
</threat_model>

<verification>
- Three spikes build and render real StatusReport data, each importing the ONE canonical bindings.ts via the `@bindings` Vite alias (no per-spike copy).
- ADR + scoring table exist; decision recorded.
- Winner collapsed into ui/; losers deleted; D-GUI-04 updated in the canonical active REQUIREMENTS.md (and archive copy).
- gen-bindings gate path still resolves after the collapse.
</verification>

<success_criteria>
The frontend framework is decided via a built 3-way comparison scored across all four criteria, each spike sharing the single canonical bindings.ts via a Vite alias (no duplicate TS types — SC#3 intact), recorded in an ADR with invalidation conditions, the canonical active REQUIREMENTS.md D-GUI-04 names the winner, and only the winning scaffold remains.
</success_criteria>

<output>
Create `.planning/phases/25-rust-core-extraction-tauri-integration-spike/25-06-SUMMARY.md` when done. Record the chosen framework, the final scores, and the measured bundle sizes.
</output>
