---
phase: 25-rust-core-extraction-tauri-integration-spike
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/tome/src/progress.rs
  - crates/tome/src/lib.rs
autonomous: true
requirements: [CORE-04]

must_haves:
  truths:
    - "A ProgressSink trait exists in crates/tome that long-running ops can emit typed events into"
    - "ProgressEvent is a per-op typed enum the GUI can pattern-match (not string-match)"
    - "A CancelToken (Arc<AtomicBool>, no tokio) exists and reports is_cancelled() at stage boundaries"
    - "crates/tome gains no tokio dependency"
    - "A RecordingSink test double can assert the exact sequence of emitted events"
  artifacts:
    - path: "crates/tome/src/progress.rs"
      provides: "ProgressSink trait, ProgressEvent enum, SyncStage enum, CancelToken, NullSink, RecordingSink"
      contains: "pub trait ProgressSink"
  key_links:
    - from: "crates/tome/src/lib.rs"
      to: "crates/tome/src/progress.rs"
      via: "mod progress; re-export"
      pattern: "mod progress"
---

<objective>
Create the new `crates/tome/src/progress.rs` module: the `ProgressSink` trait, the typed `ProgressEvent` enum, the `SyncStage` enum, the hand-rolled `CancelToken`, and two in-core sinks (`NullSink` + a test `RecordingSink`). This is the domain-vocabulary half of CORE-04; the threading into `sync()` and the `IndicatifSink`/`TauriEventSink` live elsewhere (25-03 / 25-04).

Purpose: Establish the "structure at the edge" progress abstraction (D-09/D-17) without adding tokio to the domain and without touching the giant lib.rs dispatcher yet — this plan only adds a `mod progress;` declaration to lib.rs (a one-line header edit; the heavy lib.rs decomposition is 25-03, which depends on this plan).
Output: `progress.rs` with trait + enums + CancelToken + NullSink + RecordingSink, declared as a module.

NOTE: This plan does NOT modify `crates/tome/Cargo.toml`. `progress.rs` needs only `std` + `serde` (already a workspace dep); its gated `specta::Type` derives are enabled by the `bindings` feature that plan 25-01 adds to Cargo.toml. 25-01 and 25-02 run in the same wave with no file overlap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-rust-core-extraction-tauri-integration-spike/25-CONTEXT.md
@.planning/phases/25-rust-core-extraction-tauri-integration-spike/25-RESEARCH.md
@.planning/phases/25-rust-core-extraction-tauri-integration-spike/25-PATTERNS.md

<interfaces>
<!-- Target shapes from RESEARCH Code Examples. Exact enum members are Claude's Discretion. -->
RESEARCH Pattern 3 (lines 285-296): `trait ProgressSink: Send + Sync { fn emit(&self, event: ProgressEvent); }`; `enum ProgressEvent { SyncStageStarted { stage }, SyncStageProgress { stage, current, total }, SyncStageFinished { stage }, GitCloneProgress { directory, received }, BackupSnapshot { message } }`; `enum SyncStage { Reconcile, Discover, Consolidate, Distribute, Cleanup, Save }`.
RESEARCH Code Examples (lines 511-525): `CancelToken(Arc<AtomicBool>)` with `new()` / `cancel()` / `is_cancelled()`, `#[derive(Clone, Default)]`.
The current spinner smell to be re-homed later (lib.rs:148, 1672-1815) — NOT touched by this plan (that is 25-03).
Exhaustiveness-guard convention (remove.rs:79-116): new GUI-pattern-matched enums follow the repo's `ALL` array + `const _` drift-guard style.
The `bindings` feature + optional `specta` dep are added by 25-01 (same wave); the `#[cfg_attr(feature = "bindings", derive(specta::Type))]` derives in progress.rs compile once that feature exists — verify with `cargo build -p tome --features bindings` after both Wave 1 plans land.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProgressSink trait + ProgressEvent + SyncStage + CancelToken</name>
  <read_first>
    - crates/tome/src/lib.rs (lines 1-30 module declarations — add `mod progress;` here; 148-158 spinner helper + 1672-1815 sync stage call sites for vocabulary only, NOT to edit here)
    - crates/tome/src/lint.rs:21-32 (marker-type derive idiom the new enums follow)
    - crates/tome/src/remove.rs:79-116 (ALL array + const _ exhaustiveness guard convention)
    - .planning/phases/25-rust-core-extraction-tauri-integration-spike/25-CONTEXT.md (D-09..D-12)
    - .planning/phases/25-rust-core-extraction-tauri-integration-spike/25-RESEARCH.md (Pattern 3 + CancelToken Code Example + Pitfall "no tokio")
    - .planning/phases/25-rust-core-extraction-tauri-integration-spike/25-PATTERNS.md (progress.rs section)
  </read_first>
  <behavior>
    - A fresh `CancelToken::new()` reports `is_cancelled() == false`; after `.cancel()` it reports `true`; a clone observes the same cancelled state (shared Arc).
    - `ProgressEvent` and `SyncStage` derive the traits needed to be emitted, cloned, and (gated) specta-derived.
    - `SyncStage` covers Reconcile, Discover, Consolidate, Distribute, Cleanup, Save (mirrors the existing sync pipeline stages).
  </behavior>
  <action>
    Create `crates/tome/src/progress.rs`. Define `pub trait ProgressSink: Send + Sync { fn emit(&self, event: ProgressEvent); }`. Define `pub enum SyncStage { Reconcile, Discover, Consolidate, Distribute, Cleanup, Save }` with `#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize)]` + `#[cfg_attr(feature = "bindings", derive(specta::Type))]`, and add a `SyncStage::ALL` array + a `const _` exhaustiveness guard following the `remove.rs:79-116` convention. Define `pub enum ProgressEvent { SyncStageStarted { stage: SyncStage }, SyncStageProgress { stage: SyncStage, current: usize, total: usize }, SyncStageFinished { stage: SyncStage }, GitCloneProgress { directory: String, received: u64 }, BackupSnapshot { message: String } }` with the same derives + gated specta. Define `pub struct CancelToken(std::sync::Arc<std::sync::atomic::AtomicBool>)` with `#[derive(Clone, Default)]` and `new()` / `cancel()` (SeqCst store true) / `is_cancelled()` (SeqCst load) — exactly the ~12-line std-only newtype from RESEARCH; do NOT add tokio or tokio-util. Add `mod progress;` and the appropriate `pub use` to the `lib.rs` module list (top of file, alongside existing `mod` declarations — this single-line header addition is the ONLY lib.rs change in this plan; the lib.rs decomposition is 25-03, which depends on this plan). Do NOT edit `Cargo.toml` (the bindings feature comes from 25-01). Exact field tweaks are Claude's Discretion within the D-10 "typed, semantically rich" constraint.
  </action>
  <verify>
    <automated>cargo build -p tome 2>&1 | tail -3 && cargo tree -p tome -e normal 2>/dev/null | rg -c tokio || echo "no tokio (expected)"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "pub trait ProgressSink" crates/tome/src/progress.rs` returns 1.
    - `rg -c "struct CancelToken" crates/tome/src/progress.rs` returns 1; `rg -c "tokio" crates/tome/src/progress.rs` returns 0.
    - `rg -c "mod progress" crates/tome/src/lib.rs` returns 1.
    - `cargo tree -p tome -e normal 2>/dev/null | rg tokio` returns nothing (no tokio added to the domain).
    - `cargo build -p tome` succeeds (default features — the cfg_attr specta derive is inert without the feature).
    - `rg -c "SyncStage::ALL|const _" crates/tome/src/progress.rs` shows the exhaustiveness guard present.
  </acceptance_criteria>
  <done>progress.rs declares the trait, ProgressEvent, SyncStage (with exhaustiveness guard), and CancelToken; lib.rs gets the `mod progress;` line; no tokio in the tree; default build compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NullSink + RecordingSink test double</name>
  <read_first>
    - crates/tome/src/progress.rs (the trait from Task 1)
    - crates/tome/src/lint.rs:560-567 (lint_failed_downcast_through_anyhow — the test discipline to mirror)
    - .planning/phases/25-rust-core-extraction-tauri-integration-spike/25-RESEARCH.md (Wave 0 Gaps lines 611-619 — RecordingSink covers CORE-04)
  </read_first>
  <behavior>
    - `NullSink` implements `ProgressSink` and discards events (no panic, no output) — used by tests and `--quiet`.
    - `RecordingSink` implements `ProgressSink`, records every emitted event into an interior-mutable buffer, and exposes the recorded sequence for assertion.
    - A test emits SyncStageStarted{Discover} then SyncStageFinished{Discover} into a RecordingSink and asserts the recorded sequence matches exactly.
  </behavior>
  <action>
    In `progress.rs`, add `pub struct NullSink;` with `impl ProgressSink for NullSink { fn emit(&self, _: ProgressEvent) {} }`. Add a `pub struct RecordingSink` holding a `std::sync::Mutex<Vec<ProgressEvent>>` (interior mutability so `emit(&self, ...)` works through `&dyn ProgressSink`) with a method to snapshot the recorded events. Add a `#[cfg(test)] mod tests` covering: CancelToken new/cancel/clone behavior; RecordingSink captures an emitted event sequence in order; NullSink discards without panic. Mirror the assertion discipline of `lint_failed_downcast_through_anyhow`.
  </action>
  <verify>
    <automated>cargo test -p tome progress:: 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "struct NullSink|struct RecordingSink" crates/tome/src/progress.rs` returns 2.
    - `cargo test -p tome progress::` exits 0 with tests for CancelToken, RecordingSink ordering, and NullSink.
    - `cargo clippy --all-targets -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>NullSink + RecordingSink exist and are unit-tested; the event-sequence assertion harness is ready for 25-03's sync threading.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in this plan) | Pure in-process domain vocabulary; no IPC, no I/O, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02a | Tampering | ProgressEvent crossing IPC later | accept | This plan only defines the enum; serialization safety is enforced where TauriEventSink emits (25-04). No secrets in ProgressEvent fields (directory names + counts + messages only). |
| T-25-02b | Denial of Service | CancelToken / Send+Sync soundness | accept | AtomicBool newtype is trivially Send+Sync; checked at stage boundaries only (RESEARCH A4). No locking across emit. |
</threat_model>

<verification>
- `cargo build -p tome` compiles (default features; cfg_attr specta derive inert without `bindings`).
- After Wave 1 both plans land: `cargo build -p tome --features bindings` compiles progress.rs derives.
- `cargo tree` confirms no tokio in crates/tome.
- `cargo test -p tome progress::` green.
</verification>

<success_criteria>
ProgressSink trait + typed ProgressEvent + SyncStage + CancelToken + NullSink + RecordingSink exist in `progress.rs`, unit-tested, with no tokio dependency added to the domain.
</success_criteria>

<output>
Create `.planning/phases/25-rust-core-extraction-tauri-integration-spike/25-02-SUMMARY.md` when done. Record the final ProgressEvent variant set (Claude's Discretion choices).
</output>
