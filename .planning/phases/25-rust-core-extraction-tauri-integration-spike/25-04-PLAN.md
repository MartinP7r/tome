---
phase: 25-rust-core-extraction-tauri-integration-spike
plan: 04
type: execute
wave: 3
depends_on: ["25-01", "25-03"]
files_modified:
  - Cargo.toml
  - crates/tome-desktop/Cargo.toml
  - crates/tome-desktop/build.rs
  - crates/tome-desktop/tauri.conf.json
  - crates/tome-desktop/capabilities/main.json
  - crates/tome-desktop/src/main.rs
  - crates/tome-desktop/src/lib.rs
  - crates/tome-desktop/src/commands.rs
  - crates/tome-desktop/src/sink.rs
  - crates/tome-desktop/src/bin/gen-bindings.rs
  - crates/tome-desktop/ui/src/bindings.ts
  - .github/workflows/ci.yml
  - .gitignore
autonomous: false
requirements: [CORE-02, CORE-03, CORE-04]

must_haves:
  truths:
    - "crates/tome-desktop is a workspace member depending on crates/tome (path dep) with features=[bindings]"
    - "A get_status #[tauri::command] returns a real StatusReport from tome::status::gather against the user's actual tome_home"
    - "specta + tauri-specta generate a committed bindings.ts containing TS defs for every type crossing the IPC boundary AT THIS WAVE (StatusReport + SyncProgress; TomeError arrives in 25-05)"
    - "CI fails if bindings.ts is stale (gen-bindings + git diff --exit-code) and builds tome-desktop on macos-latest; the committed bindings.ts is an intentional Wave-3 partial snapshot regenerated by 25-05 (D-07)"
    - "cargo dist plan lists only the tome CLI artifact (tome-desktop excluded from release)"
    - "The cargo-dist-owned release.yml is NOT hand-edited"
  artifacts:
    - path: "crates/tome-desktop/src/bin/gen-bindings.rs"
      provides: "Bindings exporter sharing make_builder() with main.rs"
      contains: "make_builder"
    - path: "crates/tome-desktop/ui/src/bindings.ts"
      provides: "Generated + committed TS bindings (Wave-3 partial snapshot, regenerated by 25-05)"
      contains: "StatusReport"
    - path: "crates/tome-desktop/src/sink.rs"
      provides: "TauriEventSink impl ProgressSink over AppHandle::emit"
      contains: "impl ProgressSink for TauriEventSink"
  key_links:
    - from: "crates/tome-desktop/src/commands.rs"
      to: "tome::status::gather"
      via: "#[tauri::command] get_status"
      pattern: "status::gather"
    - from: ".github/workflows/ci.yml"
      to: "crates/tome-desktop/ui/src/bindings.ts"
      via: "gen-bindings + git diff --exit-code"
      pattern: "gen-bindings"
---

<objective>
Add the `crates/tome-desktop` Tauri 2 crate (CORE-02), wire `specta` + `tauri-specta` to generate a committed `bindings.ts` via a `gen-bindings` bin sharing `make_builder()` with `main.rs` (CORE-03, D-07 corrected — build.rs cannot see commands), implement a `get_status` command returning a real `StatusReport` and a `TauriEventSink` (CORE-04), add the CI freshness gate + macOS build job, and exclude `tome-desktop` from cargo-dist release artifacts. No frontend ships here — that is the spike (25-06).

This plan is Wave 3. The error boundary (`TomeError`/`ErrorCode`, CORE-05) lands in Wave 4 (plan 25-05), so the `bindings.ts` committed here is an INTENTIONAL Wave-3 partial snapshot: `get_status` returns `Result<StatusReport, String>`/stub now, and 25-05 Task 2 regenerates + re-commits `bindings.ts` once `TomeError` enters the boundary. The CI freshness gate is installed here and stays green on every committed state (each wave re-commits the regenerated file).

Purpose: Stand up the IPC shell + typed bindings contract the spike and all later GUI phases consume, without regressing the CLI release.
Output: tome-desktop crate, committed bindings.ts (Wave-3 snapshot), CI gates, cargo-dist opt-out.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-rust-core-extraction-tauri-integration-spike/25-CONTEXT.md
@.planning/phases/25-rust-core-extraction-tauri-integration-spike/25-RESEARCH.md
@.planning/phases/25-rust-core-extraction-tauri-integration-spike/25-PATTERNS.md
@crates/tome/src/progress.rs

<interfaces>
<!-- From 25-01/25-03 (tome domain) + RESEARCH verified tauri-specta v2 example. -->
From tome (gated bindings): `tome::status::{StatusReport, gather}`, `tome::progress::{ProgressSink, ProgressEvent, SyncStage, CancelToken}`. `gather(config, paths) -> Result<StatusReport>`.
ProgressEvent (from 25-02) variants/field types the sink bridges: `SyncStageStarted { stage: SyncStage }`, `SyncStageProgress { stage: SyncStage, current: usize, total: usize }`, `SyncStageFinished { stage: SyncStage }`, `GitCloneProgress { directory: String, received: u64 }`, `BackupSnapshot { message: String }`. `SyncStage` is a `Copy` enum already deriving gated `specta::Type` (25-02) — so the front-end event can carry `SyncStage` typed (no stringly conversion).
Pinned deps (RESEARCH Standard Stack lines 114-128 — re-verify with cargo search, bump trio together if newer rc):
  tauri = "2.11"; tauri-build = "2" (build-dep); tauri-specta = "=2.0.0-rc.25" features=["derive","typescript"]; specta = "=2.0.0-rc.25" features=["derive"]; specta-typescript = "0.0.12"; thiserror = "2".
v2 export shape (RESEARCH Code Examples lines 475-508 — VERIFIED against specta-rs/tauri-specta example): `Builder::<tauri::Wry>::new().commands(collect_commands![get_status]).events(collect_events![SyncProgress])`; under `#[cfg(debug_assertions)]` call `.export(Typescript::default(), "ui/src/bindings.ts")`; in tauri::Builder use `.invoke_handler(builder.invoke_handler()).setup(|app| { builder.mount_events(app); Ok(()) })`.
build.rs: `tauri_build::build()` ONLY — NOT the specta export (Pitfall 1).
Workspace dist opt-out: `crates/tome-desktop` sets `publish = false` + `[package.metadata.dist] dist = false` (verify exact key against cargo-dist 0.30.3; `cargo dist plan` is self-verifying — RESEARCH A1).
Existing CI (.github/workflows/ci.yml): single `check` job, matrix os=[ubuntu-latest, macos-latest], steps fmt/clippy/test/build.
TauriEventSink: AppHandle is Clone+Send+Sync; AppHandle::emit is cross-thread (RESEARCH Pitfall 5).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Pre-install verification of the three pre-1.0 specta-trio crates before adding them to the workspace.</what-built>
  <how-to-verify>
    The plan installs `specta = "=2.0.0-rc.25"`, `tauri-specta = "=2.0.0-rc.25"`, `specta-typescript = "0.0.12"` — all pre-1.0 (rc / 0.0.x), flagged in RESEARCH as Approved-but-pre-stable. Before the install task runs, confirm legitimacy:
    1. Visit https://crates.io/crates/specta — confirm owner is the `specta-rs` org and the latest version (re-run `cargo search specta tauri-specta specta-typescript` locally to confirm rc.25 is still the lockstep set; if a newer rc shipped, bump all three together and re-pin with `=`).
    2. Visit https://crates.io/crates/tauri-specta and https://crates.io/crates/specta-typescript — confirm specta-rs ownership + repo links to github.com/specta-rs.
    3. Confirm `tauri = "2.11"` resolves to a 2.11.x from tauri-apps.
    Type "approved" once the trio versions are confirmed in lockstep, or supply corrected versions.
  </how-to-verify>
  <resume-signal>Type "approved" or provide corrected version pins.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: tome-desktop crate scaffold + cargo-dist opt-out</name>
  <read_first>
    - Cargo.toml (workspace :1-3 members glob, :60-78 dist metadata)
    - crates/tome/Cargo.toml (the bindings feature added in 25-01)
    - CLAUDE.md (cargo-dist owns release.yml — run cargo dist init after metadata changes; do NOT hand-edit release.yml)
    - .planning/phases/25-rust-core-extraction-tauri-integration-spike/25-RESEARCH.md (Project Structure lines 222-253; Pitfall 4 dist opt-out; Installation lines 114-128)
    - .planning/phases/25-rust-core-extraction-tauri-integration-spike/25-PATTERNS.md ("No Analog Found" table — net-new scaffold)
  </read_first>
  <action>
    Create `crates/tome-desktop/Cargo.toml`: `[package] publish = false`; `[package.metadata.dist] dist = false` (verify the exact opt-out key against cargo-dist 0.30.3 docs — RESEARCH A1; `cargo dist plan` is the self-check); `[dependencies] tome = { path = "../tome", features = ["bindings"] }`, `tauri = { version = "2.11", features = [] }`, `tauri-specta = { version = "=2.0.0-rc.25", features = ["derive", "typescript"] }`, `specta = { version = "=2.0.0-rc.25", features = ["derive"] }`, `specta-typescript = "0.0.12"`, `serde`/`serde_json` (workspace), `thiserror = "2"`; `[build-dependencies] tauri-build = { version = "2", features = [] }`; declare the `gen-bindings` bin via `[[bin]] name = "gen-bindings" path = "src/bin/gen-bindings.rs"`. Create `build.rs` calling `tauri_build::build()` ONLY (NOT specta export — Pitfall 1). Create minimal `tauri.conf.json` (productName, identifier, `frontendDist` pointing at a ui dir, `devUrl`, a `beforeDevCommand` placeholder), `capabilities/main.json` exposing ONLY the `get_status` command to the main window (minimal allowlist — Security Domain), and placeholder `icons/`. The workspace `members = ["crates/*"]` glob already picks up the new crate — no membership edit needed. Run `cargo dist init` if (and only if) the dist metadata change requires regenerating workflow files; do NOT hand-edit `release.yml`. Add `.gitignore` entries for `crates/tome-desktop/ui/node_modules`, `crates/tome-desktop/ui/dist`, and `crates/tome-desktop/ui-*/{node_modules,dist}` — but NOT `bindings.ts` (it is committed).
  </action>
  <verify>
    <automated>cargo metadata --no-deps --format-version 1 2>/dev/null | rg -c tome-desktop && cargo dist plan 2>&1 | rg -c "tome-desktop" | xargs -I{} sh -c 'test {} -eq 0 && echo "OK: tome-desktop NOT a dist artifact" || echo "FAIL: tome-desktop in dist plan"'</automated>
  </verify>
  <acceptance_criteria>
    - `crates/tome-desktop/Cargo.toml` exists with `publish = false` and a cargo-dist opt-out key.
    - `cargo dist plan` output contains NO `tome-desktop` artifact (only `tome`).
    - `rg "specta = \{ version = \"=2.0.0-rc.25\"" crates/tome-desktop/Cargo.toml` returns 1 (exact pin).
    - `build.rs` calls `tauri_build::build()` and does NOT reference specta export.
    - `git diff .github/workflows/release.yml` shows no manual edits (only cargo-dist-init output if any).
    - `.gitignore` excludes ui node_modules/dist but not bindings.ts.
  </acceptance_criteria>
  <done>tome-desktop crate exists, pins the specta trio exactly, and is excluded from cargo-dist release artifacts; release.yml not hand-edited.</done>
</task>

<task type="auto">
  <name>Task 2: get_status command + TauriEventSink + make_builder + gen-bindings + committed bindings.ts</name>
  <read_first>
    - crates/tome/src/progress.rs (ProgressSink trait + ProgressEvent + SyncStage — TauriEventSink implements/bridges these)
    - crates/tome/src/status.rs (gather/StatusReport — the command returns this)
    - .planning/phases/25-rust-core-extraction-tauri-integration-spike/25-RESEARCH.md (Code Examples lines 475-508 verified v2 Builder/export/events; Open Question Q-A gen-bindings recipe; Pitfall 5 thread-safety; Pitfall 6 transparent-newtype check)
    - .planning/phases/25-rust-core-extraction-tauri-integration-spike/25-PATTERNS.md (No Analog Found — main.rs/commands.rs/sink.rs/gen-bindings.rs patterns)
  </read_first>
  <action>
    Create `crates/tome-desktop/src/lib.rs` with a `pub fn make_builder() -> tauri_specta::Builder<tauri::Wry>` that constructs the single source-of-truth command/event registry: `Builder::<Wry>::new().commands(collect_commands![commands::get_status]).events(collect_events![SyncProgress])`. Create `src/commands.rs`: a `#[tauri::command] #[specta::specta] fn get_status(app: tauri::AppHandle) -> Result<tome::status::StatusReport, TomeError>` (TomeError is added in 25-05; for THIS plan return `Result<StatusReport, String>` or a minimal local stub error and leave a `// TODO(25-05): TomeError` marker — keep the command compiling without the full error boundary). It resolves the real `tome_home` (load Config + TomePaths from the user's environment, mirroring how the CLI resolves them) and calls `tome::status::gather`.

    Create `src/sink.rs`: `pub struct TauriEventSink { app: tauri::AppHandle }` with `impl tome::progress::ProgressSink for TauriEventSink { fn emit(&self, event: ProgressEvent) { /* bridge to SyncProgress + app.emit(...) */ } }` (AppHandle is Send+Sync; emit is cross-thread — Pitfall 5).

    SyncProgress event + ProgressEvent → SyncProgress field mapping (WARNING 4 — use the TYPED option, no lossy stringly conversion): define the front-end event as `#[derive(serde::Serialize, Clone, specta::Type, tauri_specta::Event)] struct SyncProgress { stage: tome::progress::SyncStage, current: u32, total: u32 }`. `SyncStage` already derives gated `specta::Type` and is `Copy` (25-02), so carry it DIRECTLY (no `format!("{:?}", stage)` / `.as_str()` stringification — keep the front-end able to pattern-match the stage enum). Map the `usize`/`u64` numeric fields to the event's `u32` via saturating cast (`current as u32` is acceptable only after a `.min(u32::MAX as usize) as u32` saturating clamp; prefer `u32::try_from(x).unwrap_or(u32::MAX)`). In the sink's `emit`, bridge each `ProgressEvent` variant: `SyncStageStarted { stage }`/`SyncStageFinished { stage }` → `SyncProgress { stage, current: 0, total: 0 }` (or omit; Claude's Discretion on whether to emit a SyncProgress for started/finished vs only progress); `SyncStageProgress { stage, current, total }` → `SyncProgress { stage, current: saturating(current), total: saturating(total) }`; `GitCloneProgress { received, .. }` / `BackupSnapshot { .. }` → either a dedicated specta event or fold into SyncProgress (Claude's Discretion — but if a new event is added, register it in `collect_events![]` in `make_builder` and it WILL appear in bindings.ts).

    Create `src/main.rs`: call `make_builder()`, under `#[cfg(debug_assertions)]` call `.export(Typescript::default(), "ui/src/bindings.ts")`, then run `tauri::Builder::default().invoke_handler(builder.invoke_handler()).setup(move |app| { builder.mount_events(app); Ok(()) }).run(tauri::generate_context!())`. Create `src/bin/gen-bindings.rs`: import `make_builder()`, call `.export(Typescript::default(), "crates/tome-desktop/ui/src/bindings.ts")` (path relative to workspace root as CI runs it), exit. Run the bin once to generate and COMMIT `crates/tome-desktop/ui/src/bindings.ts`. Inspect the generated file to confirm transparent newtypes (`SkillName`, `DirectoryName`, `ContentHash`) emit as TS `string` (NOT a tuple/`unknown`) — if mis-rendered, add `#[specta(transparent)]` on the newtype back in `crates/tome` (Pitfall 6 / spike acceptance check) and regenerate.
  </action>
  <verify>
    <automated>cargo build -p tome-desktop 2>&1 | tail -5 && cargo run -p tome-desktop --bin gen-bindings 2>&1 | tail -3 && rg -c "StatusReport" crates/tome-desktop/ui/src/bindings.ts && rg -c "SyncStage" crates/tome-desktop/ui/src/bindings.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p tome-desktop` succeeds on the dev mac.
    - `cargo run -p tome-desktop --bin gen-bindings` writes `crates/tome-desktop/ui/src/bindings.ts`.
    - `rg -c "StatusReport" crates/tome-desktop/ui/src/bindings.ts` returns ≥1 (boundary type present).
    - `rg -c "SyncStage" crates/tome-desktop/ui/src/bindings.ts` returns ≥1 — SyncProgress carries the typed `SyncStage` enum (NOT a bare `stage: string`), so the front-end can discriminate stages (WARNING 4 typed option satisfied).
    - `rg "SkillName" crates/tome-desktop/ui/src/bindings.ts` shows `string` (not a tuple/array/unknown) — transparent-newtype acceptance check passes.
    - `rg -c "make_builder" crates/tome-desktop/src/lib.rs` returns 1 (single source of truth shared by main.rs + gen-bindings).
    - `rg -c "impl .*ProgressSink for TauriEventSink" crates/tome-desktop/src/sink.rs` returns 1.
    - The numeric `usize`/`u64` → `u32` mapping in `sink.rs` is saturating (`u32::try_from(..).unwrap_or(u32::MAX)` or an explicit clamp) — no silent wrap (`rg "try_from|min\(u32::MAX" crates/tome-desktop/src/sink.rs` returns ≥1).
  </acceptance_criteria>
  <done>tome-desktop builds; get_status returns a real StatusReport; bindings.ts is generated+committed with correct transparent-newtype rendering; SyncProgress carries the typed SyncStage (no stringly conversion) with saturating numeric casts; make_builder is shared by main.rs and gen-bindings.</done>
</task>

<task type="auto">
  <name>Task 3: CI bindings-freshness gate + tome-desktop macOS build job</name>
  <read_first>
    - .github/workflows/ci.yml (FULL — existing check job + matrix)
    - crates/tome-desktop/src/bin/gen-bindings.rs (the bin CI runs)
    - .planning/phases/25-rust-core-extraction-tauri-integration-spike/25-RESEARCH.md (Open Questions Q-A/Q-B — CI recipe; bindings gate is pure cargo, no npm)
  </read_first>
  <action>
    Add to `.github/workflows/ci.yml` (the hand-authored CI, NOT the cargo-dist-owned release.yml): a `bindings` job (ubuntu-latest is sufficient — pure cargo, no webview link needed for gen-bindings; but if gen-bindings transitively requires the tauri context, run on macos-latest) that runs `cargo run -p tome-desktop --bin gen-bindings` then `git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts` (fails if stale). Add a `desktop-build` job on macos-latest running `cargo build -p tome-desktop` (Xcode CLT + system WKWebView only; no frontend/npm build needed for the Rust compile — Q-B). Do NOT add a `cargo tauri build` / vite job (deferred to Phase 31). Confirm the existing `cargo build --release` step still builds only the CLI (it builds the workspace; verify it does not start pulling Tauri into the `tome` artifact — `tome` has no Tauri dep, so it won't). Do NOT touch `release.yml`.

    INTENTIONAL PARTIAL SNAPSHOT (intra-phase wave transition): the `bindings.ts` committed in Task 2 of THIS plan is a deliberate Wave-3 snapshot — at this point `get_status` returns `Result<StatusReport, String>` (or stub) because `TomeError`/`ErrorCode` do not enter the IPC boundary until Wave 4 (plan 25-05 Task 2). The committed `bindings.ts` will be REGENERATED + RE-COMMITTED by 25-05 Task 2 once `TomeError` is wired — that re-commit is an expected, legitimate change, NOT a freshness violation. The CI freshness gate (`gen-bindings` + `git diff --exit-code`) is INSTALLED here in 25-04 and is internally consistent against the Wave-3 snapshot (it passes because the committed file matches what gen-bindings emits at THIS wave), but it is first MEANINGFULLY validated against the final boundary shape at phase end, after 25-05 lands `TomeError`. The gate firing on the 25-04→25-05 transition is the gate working as designed: 25-05 regenerates and re-commits so the gate stays green on every committed state. Do NOT add `bindings.ts` to `.gitignore` and do NOT special-case the gate to ignore the Wave-3→Wave-4 change — the re-commit handles it.
  </action>
  <verify>
    <automated>cargo run -p tome-desktop --bin gen-bindings 2>&1 | tail -2 && git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts && echo "bindings fresh against Wave-3 snapshot (gate would pass)"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "gen-bindings" .github/workflows/ci.yml` returns ≥1 (freshness gate present).
    - `rg -c "git diff --exit-code" .github/workflows/ci.yml` returns ≥1.
    - `rg -c "cargo build -p tome-desktop" .github/workflows/ci.yml` returns ≥1 (macOS desktop build job).
    - `git diff .github/workflows/release.yml` shows no changes (cargo-dist file untouched).
    - Locally: `cargo run -p tome-desktop --bin gen-bindings && git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts` exits 0 (committed bindings are fresh AGAINST the Wave-3 snapshot — `get_status` still returns `Result<StatusReport, String>`/stub here; `TomeError` is NOT expected in `bindings.ts` yet, that is 25-05).
    - The committed `bindings.ts` is documented (in this plan's SUMMARY) as the INTENTIONAL Wave-3 partial snapshot that 25-05 Task 2 will regenerate + re-commit once `TomeError` enters the boundary; the CI gate is installed here but first meaningfully validated at phase end (after 25-05).
  </acceptance_criteria>
  <done>CI has a bindings-freshness gate and a macOS tome-desktop build job; release.yml is untouched; the committed `bindings.ts` is fresh as an INTENTIONAL Wave-3 partial snapshot (pre-`TomeError`) that 25-05 Task 2 regenerates + re-commits — the gate stays green on every committed state across the wave transition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview (JS) → Rust | `#[tauri::command]` surface callable from the webview |
| Rust → webview | `AppHandle::emit` event payloads streamed to JS |
| local on-disk state → app | Reads the user's real tome_home Config/manifest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04-EoP | Elevation of Privilege | webview→Rust command surface | mitigate | `capabilities/main.json` exposes ONLY `get_status`; no `tauri-plugin-shell`/`-fs` added this phase (V4 access control). Minimal allowlist keeps the surface auditable. |
| T-25-04-T | Tampering | remote content in webview | mitigate | `frontendDist` is local bundled assets only; no remote production `devUrl`; CSP set in tauri.conf.json. |
| T-25-04-I | Information Disclosure | SyncProgress / StatusReport event payloads | mitigate | Payloads carry directory names + counts + paths only — no secrets/tokens. No PII crosses the boundary (V5 input validation: domain newtypes already validated). |
| T-25-04-SC | Tampering | specta trio + tauri install | mitigate | Blocking human-verify checkpoint (Task above) confirms specta-rs/tauri-apps ownership + lockstep rc versions before install; exact `=` pins; cargo-deny CI covers registry policy. |
| T-25-04-OOS | EoP | hardened runtime / notarization / entitlements | accept (out of scope) | Explicitly DEFERRED to Phase 31 (DIST-04). Do NOT add entitlements speculatively this phase. |
</threat_model>

<verification>
- `cargo build -p tome-desktop` succeeds (macOS).
- `cargo run -p tome-desktop --bin gen-bindings` + `git diff --exit-code` is clean against the Wave-3 snapshot.
- `cargo dist plan` excludes tome-desktop.
- bindings.ts contains StatusReport + a typed SyncProgress carrying SyncStage, and renders transparent newtypes as `string`. (TomeError/ErrorCode are NOT yet present — they arrive in 25-05's regeneration.)
- CLI build/release path unaffected.
</verification>

<success_criteria>
tome-desktop builds a debug app surface, get_status returns a real StatusReport, bindings.ts is generated+committed via gen-bindings/make_builder as an intentional Wave-3 partial snapshot (regenerated by 25-05 once TomeError lands), SyncProgress carries the typed SyncStage with saturating numeric casts, CI gates the freshness + builds the crate on macOS, and cargo-dist still ships only the CLI.
</success_criteria>

<output>
Create `.planning/phases/25-rust-core-extraction-tauri-integration-spike/25-04-SUMMARY.md` when done. Record the confirmed specta-trio versions, the cargo-dist opt-out key used, whether any `#[specta(transparent)]` fixups were needed, and EXPLICITLY note that the committed `bindings.ts` is the intentional Wave-3 partial snapshot (pre-`TomeError`) that plan 25-05 Task 2 will regenerate + re-commit.
</output>
