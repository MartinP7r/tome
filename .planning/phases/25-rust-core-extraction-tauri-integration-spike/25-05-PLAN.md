---
phase: 25-rust-core-extraction-tauri-integration-spike
plan: 05
type: execute
wave: 4
depends_on: ["25-04"]
files_modified:
  - crates/tome-desktop/src/error.rs
  - crates/tome-desktop/src/commands.rs
  - crates/tome-desktop/src/lib.rs
  - crates/tome-desktop/Cargo.toml
  - crates/tome-desktop/ui/src/bindings.ts
  - crates/tome/src/errors.rs
  - crates/tome/src/lib.rs
  - crates/tome/Cargo.toml
autonomous: true
requirements: [CORE-05]

must_haves:
  truths:
    - "Errors crossing into the front-end are TomeError { code: ErrorCode, message: String, context: Vec<String> } — classified at the IPC boundary (D-13)"
    - "ErrorCode is a coarse ~6-variant enum (Validation, NotFound, Permission, Conflict, Git, Io, Internal) (D-15)"
    - "Classification uses typed DomainErrorKind sentinels via downcast (not message string-matching); unmatched falls to Internal (D-14)"
    - "context is the flattened anyhow .context() chain (no debugging info lost vs the CLI) (D-16)"
    - "The CLI keeps anyhow::Result internally with zero refactor and no CLI regression"
    - "bindings.ts is REGENERATED + RE-COMMITTED here so it now contains TomeError + ErrorCode (the Wave-3 partial snapshot from 25-04 is superseded by this final boundary shape)"
  artifacts:
    - path: "crates/tome-desktop/src/error.rs"
      provides: "TomeError, ErrorCode, From<anyhow::Error>"
      contains: "struct TomeError"
    - path: "crates/tome/src/errors.rs"
      provides: "DomainErrorKind thiserror sentinel enum"
      contains: "enum DomainErrorKind"
    - path: "crates/tome-desktop/ui/src/bindings.ts"
      provides: "Regenerated bindings now containing the TomeError/ErrorCode boundary types"
      contains: "TomeError"
  key_links:
    - from: "crates/tome-desktop/src/error.rs"
      to: "crates/tome::DomainErrorKind"
      via: "err.chain().find_map(downcast_ref::<DomainErrorKind>())"
      pattern: "downcast_ref::<.*DomainErrorKind>"
    - from: "crates/tome-desktop/src/commands.rs"
      to: "TomeError"
      via: "map_err(TomeError::from)"
      pattern: "TomeError::from|map_err"
---

<objective>
Add the `TomeError` IPC-boundary error classification (CORE-05): a coarse `ErrorCode` enum, a `thiserror` `DomainErrorKind` sentinel enum in `crates/tome` attached at GUI-relevant failure sites via anyhow `.context()`, and a `From<anyhow::Error> for TomeError` that downcasts the chain to pick a code and flattens the chain into `context`. The domain stays `anyhow::Result` (zero refactor). This generalizes the in-repo `LintFailed`/`MigrationPartialOrFailed` downcast pattern already proven in `main.rs`.

This is Wave 4. It is the plan that finalizes the IPC boundary shape: once `TomeError` is wired into `get_status`, this plan REGENERATES + RE-COMMITS `crates/tome-desktop/ui/src/bindings.ts` (superseding the intentional Wave-3 partial snapshot committed by 25-04), so the CI freshness gate installed in 25-04 is meaningfully satisfied against the final boundary types from here on.

Purpose: Let the front-end pattern-match on a stable `code` without inspecting messages, while preserving the full anyhow chain for a details view ("structure at the edge", D-17 — mirrors the ProgressSink symmetry).
Output: `error.rs` (boundary), `DomainErrorKind` (sentinels), `get_status` returning `Result<StatusReport, TomeError>`, and a regenerated/committed `bindings.ts` containing `TomeError` + `ErrorCode`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-rust-core-extraction-tauri-integration-spike/25-CONTEXT.md
@.planning/phases/25-rust-core-extraction-tauri-integration-spike/25-RESEARCH.md
@.planning/phases/25-rust-core-extraction-tauri-integration-spike/25-PATTERNS.md

<interfaces>
<!-- The proven in-repo downcast pattern (main.rs:31-46) D-13/D-14 generalize. -->
main.rs:36-43 already downcasts `LintFailed` + `MigrationPartialOrFailed` at the exit-code boundary — KEEP these as-is (CLI exit codes); they are NOT replaced by DomainErrorKind.
lint.rs:21-32 marker-type idiom (hand-rolled Display+Error); lint.rs:560-567 `lint_failed_downcast_through_anyhow` test — MIRROR this test discipline for TomeError.
RESEARCH Code Examples (lines 384-426 — VERIFIED): `enum DomainErrorKind` via thiserror; `From<anyhow::Error> for TomeError` walks `err.chain().find_map(|c| c.downcast_ref::<DomainErrorKind>())` → `ErrorCode`, else `Internal`; `context: err.chain().map(|c| c.to_string()).collect()`.
Subtlety: a sentinel may sit UNDER further `.context()` — iterate `err.chain()`, do not rely on a single top-level `downcast_ref`.
From 25-04: `get_status` currently returns `Result<StatusReport, String>` (or stub) with a `// TODO(25-05): TomeError` marker, and `crates/tome-desktop/ui/src/bindings.ts` is the intentional Wave-3 partial snapshot WITHOUT TomeError/ErrorCode. This plan supersedes that snapshot.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DomainErrorKind sentinels in crates/tome + attach at GUI-relevant sites</name>
  <read_first>
    - crates/tome/src/lint.rs:21-32,560-567,606-613 (marker idiom + downcast test + anyhow::bail! sentinel attach)
    - crates/tome/src/main.rs:31-46 (the proven downcast-at-boundary)
    - crates/tome/Cargo.toml (no thiserror dep yet — must add)
    - crates/tome/src/lib.rs (module declarations — add `mod errors;`)
    - .planning/phases/25-rust-core-extraction-tauri-integration-spike/25-CONTEXT.md (D-14/D-15 — coarse codes, typed sentinels, only GUI-relevant sites)
    - .planning/phases/25-rust-core-extraction-tauri-integration-spike/25-RESEARCH.md (Code Examples lines 384-426)
    - .planning/phases/25-rust-core-extraction-tauri-integration-spike/25-PATTERNS.md (error.rs section + "anyhow chain" shared pattern)
  </read_first>
  <behavior>
    - A `DomainErrorKind::NotFound` attached via `.context()` to an anyhow error is recoverable via `err.chain().find_map(downcast_ref::<DomainErrorKind>())` even when further `.context()` strings are layered on top.
    - Attaching a sentinel does NOT change the human-readable message the CLI prints (the `{e:#}` chain still reads the same) — so no CLI regression.
  </behavior>
  <action>
    Add `thiserror = "2"` to `crates/tome/Cargo.toml` `[dependencies]`. Create `crates/tome/src/errors.rs` defining `#[derive(Debug, thiserror::Error)] pub enum DomainErrorKind { #[error("validation failed")] Validation, #[error("not found")] NotFound, #[error("permission denied")] Permission, #[error("conflict")] Conflict, #[error("git operation failed")] Git, #[error("io failure")] Io }` (no `Internal` here — Internal is the boundary's fallback). Add `mod errors; pub use errors::DomainErrorKind;` to `lib.rs`. Attach sentinels at a SMALL set of GUI-relevant failure sites only (D-14 — not everywhere): e.g. `Config::validate` failures get `.context(DomainErrorKind::Validation)`, missing-directory/path-not-found sites get `NotFound`, permission-denied filesystem ops get `Permission`, content-hash/path-overlap collisions get `Conflict`, `git.rs` clone/update failures get `Git`. Use anyhow `.with_context(|| DomainErrorKind::X)` so the sentinel is attached but the existing human context still layers on top (Code Examples lines 401-406). Add a unit test in `errors.rs` mirroring `lint_failed_downcast_through_anyhow`: attach a sentinel, wrap with extra `.context("...")`, assert `err.chain().find_map(downcast_ref::<DomainErrorKind>())` recovers it. Do NOT remove/replace the existing `LintFailed`/`MigrationPartialOrFailed` markers (CLI exit-code mapping stays).
  </action>
  <verify>
    <automated>cargo test -p tome errors:: 2>&1 | tail -5 && cargo test --all 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "enum DomainErrorKind" crates/tome/src/errors.rs` returns 1.
    - `rg -c "thiserror" crates/tome/Cargo.toml` returns ≥1.
    - `cargo test -p tome errors::` exits 0 (sentinel survives layered .context() and downcasts).
    - `cargo test --all` exits 0 and `git diff --stat crates/tome/tests/snapshots/` shows no snapshot changes (sentinels did not alter CLI output — no regression).
    - `rg -c "LintFailed|MigrationPartialOrFailed" crates/tome/src/main.rs` still returns ≥2 (existing markers preserved).
  </acceptance_criteria>
  <done>DomainErrorKind sentinels exist and attach at GUI-relevant sites via .context() without changing CLI output; downcast-through-anyhow test passes; existing CLI markers preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TomeError + ErrorCode + From<anyhow::Error> boundary in tome-desktop + REGENERATE & RE-COMMIT bindings.ts</name>
  <read_first>
    - crates/tome-desktop/src/commands.rs (get_status with the // TODO(25-05) marker from 25-04)
    - crates/tome-desktop/src/lib.rs (make_builder + collect_commands)
    - crates/tome-desktop/Cargo.toml (specta/thiserror deps from 25-04)
    - crates/tome-desktop/ui/src/bindings.ts (the Wave-3 partial snapshot from 25-04 — this task supersedes it)
    - crates/tome-desktop/src/bin/gen-bindings.rs (the regeneration bin from 25-04)
    - crates/tome/src/errors.rs (DomainErrorKind from Task 1)
    - .planning/phases/25-rust-core-extraction-tauri-integration-spike/25-CONTEXT.md (D-15/D-16 — ErrorCode variants + TomeError payload)
    - .planning/phases/25-rust-core-extraction-tauri-integration-spike/25-RESEARCH.md (Code Examples lines 408-426 From impl)
    - .planning/phases/25-rust-core-extraction-tauri-integration-spike/25-PATTERNS.md (error.rs section + exhaustiveness-guard convention)
  </read_first>
  <behavior>
    - `TomeError::from(anyhow_err)` where the chain contains `DomainErrorKind::NotFound` produces `code == ErrorCode::NotFound`.
    - `TomeError::from(anyhow_err)` with no sentinel in the chain produces `code == ErrorCode::Internal`.
    - `context` equals the flattened `err.chain()` strings in order; `message` is the top-level error string.
    - `TomeError` derives `serde::Serialize` + (gated) `specta::Type` so it crosses the IPC boundary and appears in bindings.ts.
  </behavior>
  <action>
    Create `crates/tome-desktop/src/error.rs`: `#[derive(Debug, serde::Serialize, Clone, specta::Type)] pub enum ErrorCode { Validation, NotFound, Permission, Conflict, Git, Io, Internal }` with an `ALL` array + `const _` exhaustiveness guard (repo convention) and an `impl From<&tome::DomainErrorKind> for ErrorCode` mapping each sentinel variant to its code. `#[derive(Debug, serde::Serialize, Clone, specta::Type)] pub struct TomeError { pub code: ErrorCode, pub message: String, pub context: Vec<String> }`. Implement `From<anyhow::Error> for TomeError` exactly per RESEARCH Code Examples: `code = err.chain().find_map(|c| c.downcast_ref::<tome::DomainErrorKind>()).map(ErrorCode::from).unwrap_or(ErrorCode::Internal)`; `message = err.to_string()`; `context = err.chain().map(|c| c.to_string()).collect()`. Add `mod error;` to `tome-desktop/src/lib.rs`. Update `get_status` in `commands.rs` to return `Result<tome::status::StatusReport, TomeError>` and `.map_err(TomeError::from)` the `gather` result (remove the 25-04 stub + TODO marker). Add a unit test module in `error.rs` covering: sentinel-in-chain → correct code; no-sentinel → Internal; context flattening order.

    REQUIRED — regenerate + re-commit bindings.ts (closes the BLOCKER-2 wave-transition loop): now that `TomeError` is in the `get_status` signature, run `cargo run -p tome-desktop --bin gen-bindings` to REGENERATE `crates/tome-desktop/ui/src/bindings.ts`. This SUPERSEDES the intentional Wave-3 partial snapshot committed by 25-04 — the file MUST now contain `TomeError` and `ErrorCode`. Commit the regenerated `bindings.ts`. After this commit, the CI freshness gate installed in 25-04 (`gen-bindings` + `git diff --exit-code`) is meaningfully satisfied against the FINAL boundary shape (no diff). This step is non-optional: the Wave-3 snapshot is stale-by-design and is only made fresh by this regeneration.
  </action>
  <verify>
    <automated>cargo test -p tome-desktop error:: 2>&1 | tail -5 && cargo run -p tome-desktop --bin gen-bindings 2>&1 | tail -2 && rg -c "TomeError" crates/tome-desktop/ui/src/bindings.ts && rg -c "ErrorCode" crates/tome-desktop/ui/src/bindings.ts && git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts && echo "bindings re-committed + fresh against final boundary"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "struct TomeError" crates/tome-desktop/src/error.rs` returns 1; `rg -c "enum ErrorCode" crates/tome-desktop/src/error.rs` returns 1.
    - `rg -c "downcast_ref::<.*DomainErrorKind>" crates/tome-desktop/src/error.rs` returns 1 (typed-sentinel classification, not string-matching).
    - `cargo test -p tome-desktop error::` exits 0 (sentinel→code, no-sentinel→Internal, context-order tests pass).
    - `get_status` returns `Result<StatusReport, TomeError>` (no remaining `// TODO(25-05)` marker: `rg "TODO\(25-05\)" crates/tome-desktop/` returns nothing).
    - REQUIRED regeneration acceptance: after `cargo run -p tome-desktop --bin gen-bindings`, BOTH `rg -c "TomeError" crates/tome-desktop/ui/src/bindings.ts` AND `rg -c "ErrorCode" crates/tome-desktop/ui/src/bindings.ts` return ≥1 (the Wave-3 partial snapshot is superseded — `bindings.ts` now contains TomeError AND ErrorCode), and `git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts` is clean (regenerated file committed).
    - `cargo test --all` exits 0 (no CLI regression).
  </acceptance_criteria>
  <done>TomeError/ErrorCode classify anyhow errors via DomainErrorKind downcast at the boundary, flatten the chain into context, and `bindings.ts` is REGENERATED + RE-COMMITTED so it now contains TomeError + ErrorCode (superseding the 25-04 Wave-3 snapshot); the domain stays anyhow with no CLI regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust error → webview | `TomeError` payload returned to JS from `#[tauri::command]` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05-I | Information Disclosure | TomeError.message / context leaking internal paths | accept | The CLI already prints the same `{e:#}` chain to stderr; the GUI surfaces no MORE than the CLI does. Single-user local tool — no cross-user disclosure surface (Constraints: single-user). context is intended detail-view content (D-16). |
| T-25-05-S | Spoofing | mis-classified error code misleading the GUI | mitigate | Classification is typed-sentinel downcast (not fragile string-matching); unmatched safely falls to `Internal` (D-14). Unit tests pin sentinel→code mappings. |
| T-25-05-T | Tampering | exhaustiveness drift on ErrorCode | mitigate | `ErrorCode::ALL` + `const _` guard (repo convention) prevents a variant silently dropping out of the `From<&DomainErrorKind>` map. |
</threat_model>

<verification>
- `cargo test -p tome errors::` and `cargo test -p tome-desktop error::` green.
- `cargo test --all` green; snapshots unchanged (no CLI regression from sentinel attachment).
- bindings.ts is REGENERATED to contain TomeError + ErrorCode and is committed/fresh (`git diff --exit-code` clean) — the 25-04 Wave-3 snapshot is superseded.
- get_status returns Result<StatusReport, TomeError>.
</verification>

<success_criteria>
Errors crossing into the front-end are classified into a coarse `TomeError` via typed `DomainErrorKind` downcast at the boundary, preserving the full anyhow chain in `context`; the domain stays anyhow with zero CLI regression; the boundary types appear in the regenerated/re-committed bindings.ts (superseding the Wave-3 snapshot).
</success_criteria>

<output>
Create `.planning/phases/25-rust-core-extraction-tauri-integration-spike/25-05-SUMMARY.md` when done. List the GUI-relevant sites that received DomainErrorKind sentinels, and confirm that `bindings.ts` was regenerated + re-committed with TomeError + ErrorCode (superseding the 25-04 Wave-3 partial snapshot).
</output>
