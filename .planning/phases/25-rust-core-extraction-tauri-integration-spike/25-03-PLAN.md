---
phase: 25-rust-core-extraction-tauri-integration-spike
plan: 03
type: execute
wave: 2
depends_on: ["25-01", "25-02"]
files_modified:
  - crates/tome/src/lib.rs
  - crates/tome/src/git.rs
  - crates/tome/src/backup.rs
autonomous: true
requirements: [CORE-01, CORE-04]

must_haves:
  truths:
    - "Each top-level CLI command calls a domain function returning a structured Rust type, then a thin cmd_* presenter formats it"
    - "sync() takes an injected &dyn ProgressSink and a &CancelToken and checks is_cancelled() at stage boundaries"
    - "The CLI uses an IndicatifSink that re-homes the existing spinner presentation; --quiet / tests use NullSink"
    - "The full crates/tome/tests/cli*.rs integration suite passes byte-for-byte (no CLI regression)"
    - "insta snapshots are unchanged"
  artifacts:
    - path: "crates/tome/src/lib.rs"
      provides: "IndicatifSink, sync(... sink, cancel) signature, presenter decomposition"
      contains: "struct IndicatifSink"
  key_links:
    - from: "crates/tome/src/lib.rs::sync"
      to: "crates/tome/src/progress.rs::ProgressSink"
      via: "sink.emit(ProgressEvent::SyncStage...)"
      pattern: "sink\\.emit"
    - from: "crates/tome/src/lib.rs::sync"
      to: "CancelToken"
      via: "cancel.is_cancelled() at stage boundaries"
      pattern: "is_cancelled"
---

<objective>
Thread `ProgressSink` + `CancelToken` through `sync()` (and the `git`/`backup` long-ops), re-home the `indicatif` spinner presentation into an `IndicatifSink` (D-11), and finish the `lib.rs::run` presenter decomposition so every command calls a structured-type domain fn formatted by a thin `cmd_*` presenter (CORE-01 / D-GUI-08). This plan owns ALL `lib.rs` changes for the phase, avoiding wave conflicts. The library-canonical type shapes do NOT change (only `SkillOwnership`, done in 25-01).

Purpose: Make the domain callable from any front-end (the GUI calls the domain fns directly; the CLI keeps the presenters) and make long ops emit progress + observe cancellation.
Output: Decomposed presenters, `IndicatifSink`/`NullSink` wiring, extended `sync()` signature.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-rust-core-extraction-tauri-integration-spike/25-CONTEXT.md
@.planning/phases/25-rust-core-extraction-tauri-integration-spike/25-RESEARCH.md
@.planning/phases/25-rust-core-extraction-tauri-integration-spike/25-PATTERNS.md
@crates/tome/src/progress.rs

<interfaces>
<!-- From 25-02 (progress.rs) and current lib.rs shapes. -->
From 25-02: `trait ProgressSink: Send + Sync`, `ProgressEvent`, `SyncStage { Reconcile, Discover, Consolidate, Distribute, Cleanup, Save }`, `CancelToken`, `NullSink`, `RecordingSink`.
Current sync signature (lib.rs:1516): `fn sync(config: &Config, paths: &TomePaths, opts: SyncOptions<'_>) -> Result<()>`. `SyncOptions<'a>` struct at lib.rs:1278.
Current spinner helper (lib.rs:148-158) + the four stage spinner call sites (lib.rs:1672-1815: "Resolving git sources..." → Reconcile, "Discovering skills..." → Discover, "Consolidating to library..." → Consolidate, "Distributing to {name}..." → Distribute, each paired with finish_and_clear()).
Existing presenter shape that already works (lib.rs:570 cmd_status → status::show; lib.rs:539 cmd_sync). status::gather/show split (status.rs:95/216) is the CORE-01 template.
run() dispatcher: lib.rs:225 (25-command match cli.command).
CURRENT domain-fn names verified against the codebase (the GUI will call these; Task 2 makes/promotes them — do NOT assume aspirational names):
  - status: `pub fn gather(config, paths) -> Result<StatusReport>` (status.rs:95) — already `pub`. Template.
  - doctor: `pub fn diagnose(...)` (doctor.rs:451) — already `pub`.
  - list: NO domain fn yet — logic is inline in `cmd_list` (lib.rs:1203). Task 2 EXTRACTS it into `list.rs` as a `pub fn` returning a structured report (pick the name `list::collect` for symmetry with the template).
  - lint: domain fns are `lint::lint_library(library_dir) -> LintReport` (lint.rs:231) and `lint::lint_skill(...)` (lint.rs:80) — already `pub`. There is NO `lint::run`; do NOT invent one. The GUI calls `lint_library`/`lint_skill`.
  - remove/reassign/relocate/eject: `plan(...)` exists but is `pub(crate)` (remove.rs:278, reassign.rs:101, relocate.rs:46, eject.rs:26). Task 2 PROMOTES the GUI-relevant `plan` fns to `pub`.
  - fork: there is NO `fork.rs` / `fork::plan`. Fork shares `reassign::plan(..., is_fork: true)` (reassign.rs:101, `is_fork` field reassign.rs:85). The GUI uses `reassign::plan` for both reassign and fork. Do NOT expect a `fork::plan`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IndicatifSink + thread sink/cancel through sync()</name>
  <read_first>
    - crates/tome/src/lib.rs (FULL — run() at :225, cmd_sync :539, SyncOptions :1278, sync() :1516, spinner :148-158, stage call sites :1672-1815)
    - crates/tome/src/progress.rs (the trait + enums from 25-02)
    - crates/tome/src/git.rs (clone/update long-ops — for GitCloneProgress emit + cancel check)
    - crates/tome/src/backup.rs (snapshot/restore — for BackupSnapshot emit, adopt incrementally per D-11)
    - .planning/phases/25-rust-core-extraction-tauri-integration-spike/25-CONTEXT.md (D-09/D-11/D-12)
    - .planning/phases/25-rust-core-extraction-tauri-integration-spike/25-RESEARCH.md (Pattern 3)
    - .planning/phases/25-rust-core-extraction-tauri-integration-spike/25-PATTERNS.md (progress.rs + lib.rs sections)
  </read_first>
  <action>
    Add `struct IndicatifSink` in `lib.rs` (next to the presenters, per D-11) implementing `ProgressSink`: it owns the `spinner()`/`MultiProgress` machinery and translates `ProgressEvent::SyncStageStarted{stage}` → a spinner with the stage's existing message, `SyncStageFinished` → `finish_and_clear()`, `GitCloneProgress`/`BackupSnapshot` → matching spinner updates. Extend `fn sync(...)` to take `sink: &dyn ProgressSink, cancel: &CancelToken` (add to the signature; threading via separate args is cleaner than stuffing into `SyncOptions`). Replace the four inline `spinner(...)` + `finish_and_clear()` call sites (lib.rs:1672-1815) with `sink.emit(ProgressEvent::SyncStageStarted { stage: SyncStage::Reconcile })` / `...Discover` / `...Consolidate` / `...Distribute` (+ Cleanup, Save) and matching `SyncStageFinished`. Insert `if cancel.is_cancelled() { anyhow::bail!("sync cancelled"); }` checks at each stage boundary. Update `cmd_sync` to construct an `IndicatifSink` (or `NullSink` when `--quiet`) and a never-tripped `CancelToken::new()`, and pass them into `sync()`. Thread `sink`/`cancel` into `git::clone`/`update` and `backup::snapshot`/`restore` signatures so they emit `GitCloneProgress`/`BackupSnapshot` and observe cancellation (adopt these two long-op families now per D-11; other ops can adopt later). CRITICAL: stdout/stderr bytes MUST be identical to pre-change — the IndicatifSink must reproduce the exact spinner messages so `insta` snapshots and assert_cmd output do not drift.
  </action>
  <verify>
    <automated>cargo test --all 2>&1 | tail -8 && git diff --stat crates/tome/tests/snapshots/ 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "struct IndicatifSink" crates/tome/src/lib.rs` returns 1.
    - `rg -c "sink\.emit" crates/tome/src/lib.rs` returns ≥6 (one per stage emit minimum).
    - `rg -c "is_cancelled" crates/tome/src/lib.rs` returns ≥4 (stage-boundary cancel checks).
    - `cargo test --all` exits 0.
    - `git diff --stat crates/tome/tests/snapshots/` shows no modified snapshots (byte-for-byte preserved).
    - A unit test passes a `RecordingSink` into `sync()` (or a sync sub-path) and asserts ≥1 event per stage is emitted (covers CORE-04 / RESEARCH Test Map).
  </acceptance_criteria>
  <done>sync() takes sink+cancel, emits a typed event per stage, checks cancellation at boundaries; IndicatifSink reproduces existing CLI output exactly; snapshots unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Finish presenter decomposition for remaining commands</name>
  <read_first>
    - crates/tome/src/lib.rs (run() dispatcher :225, cmd_list :1203 inline list logic, cmd_lint :586, cmd_fork :969, and each cmd_* still holding inline domain logic)
    - crates/tome/src/status.rs:95,216 (gather/show split — the CORE-01 template)
    - crates/tome/src/list.rs (no top-level domain fn yet — extract one here)
    - crates/tome/src/lint.rs:80,231 (lint_skill / lint_library — already pub; the GUI uses these, there is NO lint::run)
    - crates/tome/src/remove.rs:278 / reassign.rs:101 / relocate.rs:46 / eject.rs:26 (plan fns — currently pub(crate), promote the GUI-relevant ones to pub)
    - crates/tome/src/doctor.rs:451 (diagnose — already pub)
    - .planning/phases/25-rust-core-extraction-tauri-integration-spike/25-RESEARCH.md (Pattern 1 — keep presenters inline, do NOT create presenters/ module)
    - .planning/phases/25-rust-core-extraction-tauri-integration-spike/25-PATTERNS.md (lib.rs section — Anti-pattern: do not change field shapes)
  </read_first>
  <action>
    FIRST verify the actual current fn names in the codebase before writing acceptance greps (the names below are verified as of this plan, but re-confirm — names may have shifted; correct any that differ and update this plan's acceptance list to match). For each top-level command whose domain logic is still inline in `run()`/`cmd_*`, extract the domain computation into its owning module as a `pub fn` returning a structured report/plan (mirroring `status::gather` and `remove::plan`), and leave a thin `cmd_*` presenter in `lib.rs` that calls it and formats output:
    - `list`: extract the inline logic in `cmd_list` (lib.rs:1203) into `list::collect(config, ...) -> Result<ListReport>` (net-new `pub fn` — there is no list domain fn today).
    - `lint`: domain logic already lives in `lint::lint_library` / `lint::lint_skill` (both `pub`) — leave as-is; `cmd_lint` already calls them. Do NOT rename to `lint::run`.
    - `remove`/`reassign`/`relocate`/`eject`: PROMOTE the GUI-relevant `plan(...)` fns from `pub(crate)` to `pub` so `tome-desktop` can call them in later phases.
    - `fork`: NO separate fn — fork uses `reassign::plan(..., is_fork: true)`; just ensure `reassign::plan` is `pub`.
    - `status`/`doctor`: already split + `pub`; confirm only.
    Keep presenters INLINE in `lib.rs` — do NOT create a `presenters/` module (RESEARCH Pattern 1 + CONTEXT Deferred Idea: minimizes diff, preserves snapshot bytes). Do NOT change any structured-type field shapes (the library-canonical types are the contract — only SkillOwnership changed, in 25-01). The regression gate is byte-for-byte CLI output.
  </action>
  <verify>
    <automated>cargo test --all 2>&1 | tail -8 && cargo clippy --all-targets -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `rg "mod presenters" crates/tome/src/lib.rs` returns nothing (no presenters module created).
    - The EXACT GUI-callable domain fns (names verified against the current codebase — correct any that differ before finalizing) are all `pub`. Verify each explicitly by FQN, do NOT rely on a loose alternation grep:
      - `rg -c "pub fn gather" crates/tome/src/status.rs` returns ≥1.
      - `rg -c "pub fn diagnose" crates/tome/src/doctor.rs` returns ≥1.
      - `rg -c "pub fn collect" crates/tome/src/list.rs` returns ≥1 (net-new extraction).
      - `rg -c "pub fn lint_library" crates/tome/src/lint.rs` returns ≥1 AND `rg -c "pub fn lint_skill" crates/tome/src/lint.rs` returns ≥1 (NO `lint::run` expected).
      - `rg -c "^pub fn plan" crates/tome/src/remove.rs crates/tome/src/reassign.rs crates/tome/src/relocate.rs crates/tome/src/eject.rs` returns ≥4 (promoted from `pub(crate)` to `pub`); correspondingly `rg -c "^pub\(crate\) fn plan" crates/tome/src/remove.rs crates/tome/src/reassign.rs crates/tome/src/relocate.rs crates/tome/src/eject.rs` returns 0 for those promoted.
      - Build-failure anchor: `cargo test --all` exits 0 — a missing or misnamed GUI-callable domain fn surfaces as a compile error here (and in 25-04 when `tome-desktop` calls `status::gather`), not as a silently-passing grep.
    - `git diff --stat crates/tome/tests/snapshots/` shows no modified snapshots.
    - `cargo clippy --all-targets -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>Every command's domain logic is a pub structured-type fn (verified by exact FQN against the real codebase names, not a loose alternation grep); presenters stay inline in lib.rs; CLI output byte-identical; the domain surface is callable from tome-desktop (build-failure anchored).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → domain fns | Already-validated via clap + newtypes; unchanged by decomposition. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03a | Tampering | sync() refactor altering write ordering | mitigate | Atomic temp+rename writes preserved; cancel checks only at stage boundaries (not mid-write) so a cancelled sync leaves no half-written manifest/lockfile (SYNC-04 invariant, RESEARCH A4). Full cli*.rs suite is the regression gate. |
| T-25-03b | Repudiation | CLI output drift hiding behavior change | mitigate | insta snapshot byte-for-byte gate + 130 assert_cmd tests guarantee observable behavior is unchanged. |
</threat_model>

<verification>
- `cargo test --all` green; snapshots unchanged.
- `cargo clippy --all-targets -- -D warnings` clean.
- A RecordingSink-based unit test proves ≥1 event/stage.
- The GUI-callable domain fns are `pub` (verified by exact FQN) and callable without CLI formatting; a missing/misnamed fn fails the build.
</verification>

<success_criteria>
sync() is sink+cancel aware with an IndicatifSink reproducing CLI output exactly; every command's domain logic is a pub structured-type fn behind a thin inline presenter; full CLI integration suite passes byte-for-byte.
</success_criteria>

<output>
Create `.planning/phases/25-rust-core-extraction-tauri-integration-spike/25-03-SUMMARY.md` when done. List the pub domain fns now available for the GUI (by exact FQN), noting any names that differed from this plan's assumptions and were corrected.
</output>
