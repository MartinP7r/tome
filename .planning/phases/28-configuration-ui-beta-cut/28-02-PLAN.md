---
phase: 28-configuration-ui-beta-cut
plan: 02
type: execute
wave: 4
depends_on: [28-01, 28-04, 28-05]
files_modified: [crates/tome-desktop/src/commands.rs, crates/tome-desktop/src/lib.rs, crates/tome-desktop/src/watcher.rs, crates/tome-desktop/tests/watcher_smoke.rs, crates/tome-desktop/ui/src/bindings.ts, crates/tome-desktop/ui/src/App.tsx, crates/tome-desktop/ui/src/stores/router.ts, crates/tome-desktop/src/menu.rs, crates/tome-desktop/ui/src/hooks/useMenuActions.ts, crates/tome-desktop/ui/src/hooks/useConfiguration.tsx, crates/tome-desktop/ui/src/views/__tests__/App.setup-gate.test.tsx]
autonomous: true
requirements: [CFG-01, CFG-02, CFG-05, NF-04]
estimate: { tokens: 34000, raw_tokens: 34000, tasks: 2, confidence: low }
must_haves:
  truths: [A Rust-backed setup gate prevents all normal desktop navigation until configuration saves or the user explicitly cancels; Configuration is a fifth persistent section and typed commands expose all Phase-28 IPC operations.]
  artifacts: [crates/tome-desktop/src/commands.rs, crates/tome-desktop/ui/src/bindings.ts]
  key_links: [make_builder registers every command and regenerated bindings match it.]
---
<objective>
Expose the configuration domain through the typed Tauri boundary and make Configuration the persistent fifth sidebar section.
Purpose: prove the first setup-state-to-screen path while preserving bindings freshness.
Output: registered commands, generated bindings, router/menu/sidebar integration.
</objective>
<execution_context>@/Users/martin/.config/opencode/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/28-configuration-ui-beta-cut/28-CONTEXT.md
@.planning/phases/27-sync-triage-ui/27-03-SUMMARY.md
@crates/tome-desktop/src/commands.rs
@crates/tome-desktop/src/lib.rs</context>
<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Centralize all Phase-28 command, watcher, registry, and bindings changes</name>
<files>crates/tome-desktop/src/commands.rs, crates/tome-desktop/src/lib.rs, crates/tome-desktop/src/watcher.rs, crates/tome-desktop/tests/watcher_smoke.rs, crates/tome-desktop/ui/src/bindings.ts</files>
<behavior>- Setup/config, Git verification, and complete preference draft commands return generated typed results and TomeError errors.
- Config apply reuses the Plan 28-01 projection and save_checked path; config/preference previews are read-only.
- ConfigChanged is a registered typed watcher event for atomic own-process and external tome.toml replacements.
</behavior>
<action>Add every Phase-28 command now so Plans 28-03 through 28-05 never modify commands.rs, make_builder, or bindings.ts: setup snapshot/actions; config draft load/validate/preview/apply; Git candidate verification; and complete preference get/validate/preview/apply. Resolve paths server-side and map anyhow only at the TomeError edge. Commands may call public domain seams created by Plans 28-01, 28-04 Task 1, and 28-05 Task 1; keep their implementations thin and do not duplicate TOML/validation. Extend WatcherPaths with resolved tome.toml, add ConfigChanged to WatcherEvent and typed event registration, watch the config parent with atomic-rename-safe exact-path filtering, and prove own/external atomic replacement emits one refresh event. Register every command/event in make_builder once, regenerate bindings.ts once, and add command/watcher tests. Commit RED/GREEN atomically as `test(28-02): cover configuration IPC and config watcher` and `feat(28-02): centralize configuration IPC boundary`.</action>
<verify><automated>cargo test -p tome-desktop --lib commands:: && cargo test -p tome-desktop --test watcher_smoke && cargo run -p tome-desktop --bin gen-bindings && git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts</automated></verify>
<done>All Phase-28 commands/events are typed, registered, binding-fresh, server-resolved, and config watcher coverage proves atomic own/external tome.toml refreshes.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Add Configuration navigation and enforce the app-level setup gate</name>
<files>crates/tome-desktop/ui/src/App.tsx, crates/tome-desktop/ui/src/stores/router.ts, crates/tome-desktop/src/menu.rs, crates/tome-desktop/ui/src/hooks/useMenuActions.ts, crates/tome-desktop/ui/src/hooks/useConfiguration.tsx, crates/tome-desktop/ui/src/views/__tests__/App.setup-gate.test.tsx</files>
<behavior>- Router, title, sidebar, menu events, menu ALL sentinel, and accelerators expose Status, Skills, Sync, Configuration, Health in that order after setup is unlocked.
- A Rust setup snapshot gates App before Sidebar, Titlebar, normal views, menu navigation handlers, or global keyboard handlers mount; only successful save or an explicit cancel action unlocks the shell for this run.
- Direct router calls, typed menu events, and ⌘ shortcuts cannot render Status/Skills/Sync/Health while the gate is locked.
</behavior>
<action>Add the fifth sidebar route per D-19, updating literal unions, title mapping, menu event enum/ALL/exhaustiveness sentinel, native View item and tests atomically. Preserve the Phase-27 sidebar contract and shift Health consistently to the sixth position/accelerator. Implement a root useConfiguration gate backed by the setup snapshot command: while locked, App renders only the dedicated setup host and does not mount Sidebar, normal views, or useMenuActions. Thread gate state into menu/keyboard handlers as defense in depth so typed MenuAction, direct setView calls, ⌘1..⌘6, ⌘R, and FocusSearch cannot escape before save or explicit cancellation; cancellation is an explicit setup-action result, never an implicit error fallback. The hook subscribes to ConfigChanged, refetches persisted config/setup state after atomic own/external updates, and preserves a dirty unresolved draft rather than overwriting it. Add direct-navigation tests for router, menu event, and keyboard attempts while locked plus unlock-after-save/cancel tests. Commit as `feat(28-02): gate desktop shell on setup state`.</action>
<verify><automated>cargo test -p tome-desktop --lib menu:: && cd crates/tome-desktop/ui && npm test -- --run src/views/__tests__/App.setup-gate.test.tsx && npm run build</automated></verify>
<done>D-02 is an app-level Rust-backed gate: no sidebar, menu/direct-navigation, keyboard navigation, or normal view can bypass setup before explicit save/cancel; Configuration remains durable after unlock.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| React→Tauri commands | UI input enters the local Rust authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-28-02-01 | Elevation | Tauri IPC | high | mitigate | Register narrow typed commands only; keep filesystem/shell permissions absent. |
| T-28-02-02 | Tampering | generated bindings | medium | mitigate | Regenerate bindings from make_builder and run freshness gate. |
| T-28-02-SC | Tampering | package installs | high | mitigate | No package installation in this plan. |
</threat_model>
<verification><automated>cargo clippy -p tome-desktop --all-targets -- -D warnings && cd crates/tome-desktop/ui && npm run build</automated></verification>
<success_criteria>Typed commands and all navigation surfaces expose the Configuration route with bindings freshness enforced.</success_criteria>
<output>Create `.planning/phases/28-configuration-ui-beta-cut/28-02-SUMMARY.md` when done</output>
