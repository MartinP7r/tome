---
phase: 28-configuration-ui-beta-cut
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified: [crates/tome-desktop/src/commands.rs, crates/tome-desktop/src/lib.rs, crates/tome-desktop/ui/src/bindings.ts, crates/tome-desktop/ui/src/App.tsx, crates/tome-desktop/ui/src/stores/router.ts, crates/tome-desktop/src/menu.rs]
autonomous: true
requirements: [CFG-01, CFG-02, CFG-05, NF-04]
estimate: { tokens: 34000, raw_tokens: 34000, tasks: 2, confidence: low }
must_haves:
  truths: [Configuration is a fifth persistent desktop section and typed commands expose setup/config draft operations.]
  artifacts: [crates/tome-desktop/src/commands.rs, crates/tome-desktop/ui/src/bindings.ts]
  key_links: [make_builder registers every command and regenerated bindings match it.]
---
<objective>
Expose the configuration domain through the typed Tauri boundary and make Configuration the persistent fifth sidebar section.
Purpose: prove the first setup-state-to-screen path while preserving bindings freshness.
Output: registered commands, generated bindings, router/menu/sidebar integration.
</objective>
<execution_context>@/Users/martin/.config/opencode/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/28-configuration-ui-beta-cut/28-CONTEXT.md
@.planning/phases/27-sync-triage-ui/27-03-SUMMARY.md
@crates/tome-desktop/src/commands.rs
@crates/tome-desktop/src/lib.rs</context>
<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Wire setup/config draft operations across the Tauri boundary</name>
<files>crates/tome-desktop/src/commands.rs, crates/tome-desktop/src/lib.rs, crates/tome-desktop/ui/src/bindings.ts</files>
<behavior>- Setup snapshot and config load/validate/preview/apply commands return generated typed results and TomeError errors.
- Apply reuses the Plan 28-01 domain projection and save_checked path; preview is read-only.
</behavior>
<action>Add thin commands for setup snapshot/actions and configuration draft load, validate, full TOML preview, and confirmed apply. Resolve all paths server-side, map anyhow only at the TomeError edge, and call Plan 28-01 helpers rather than serializing/validating TOML in commands.rs. Register each command in make_builder, regenerate bindings.ts, and add command-helper tests. Preserve watcher-driven freshness: own writes must use existing watched files and UI consumers refetch rather than cache the config. Commit RED/GREEN atomically as `test(28-02): cover configuration IPC commands` and `feat(28-02): expose configuration IPC boundary`.</action>
<verify><automated>cargo test -p tome-desktop --lib commands:: && cargo run -p tome-desktop --bin gen-bindings && git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts</automated></verify>
<done>All setup/config commands are typed, registered, binding-fresh, server-resolved, and apply uses the canonical Rust save path.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Add Configuration after Sync and before Health across shell navigation</name>
<files>crates/tome-desktop/ui/src/App.tsx, crates/tome-desktop/ui/src/stores/router.ts, crates/tome-desktop/src/menu.rs</files>
<behavior>- Router, title, sidebar, menu events, menu ALL sentinel, and accelerators expose Status, Skills, Sync, Configuration, Health in that order.
- The app can route to the Configuration shell without bypassing setup gating.
</behavior>
<action>Add the fifth sidebar route per D-19, updating literal unions, title mapping, menu event enum/ALL/exhaustiveness sentinel, native View item and tests atomically. Preserve the Phase-27 sidebar contract and shift Health consistently to the sixth position/accelerator. Render a ConfigurationView shell that receives the typed setup snapshot; D-02 gating remains authoritative in that view, not router heuristics. Commit as `feat(28-02): add configuration desktop section`.</action>
<verify><automated>cargo test -p tome-desktop --lib menu:: && cd crates/tome-desktop/ui && npm test -- --run && npm run build</automated></verify>
<done>Configuration is a durable fifth section in every navigation surface, and no stale hand-written command types exist.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| React→Tauri commands | UI input enters the local Rust authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-28-02-01 | Elevation | Tauri IPC | high | mitigate | Register narrow typed commands only; keep filesystem/shell permissions absent. |
| T-28-02-02 | Tampering | generated bindings | medium | mitigate | Regenerate bindings from make_builder and run freshness gate. |
| T-28-02-SC | Tampering | package installs | high | mitigate | No package installation in this plan. |
</threat_model>
<verification><automated>cargo clippy -p tome-desktop --all-targets -- -D warnings && cd crates/tome-desktop/ui && npm run build</automated></verification>
<success_criteria>Typed commands and all navigation surfaces expose the Configuration route with bindings freshness enforced.</success_criteria>
<output>Create `.planning/phases/28-configuration-ui-beta-cut/28-02-SUMMARY.md` when done</output>
