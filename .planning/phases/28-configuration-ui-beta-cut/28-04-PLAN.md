---
phase: 28-configuration-ui-beta-cut
plan: 04
type: execute
wave: 2
depends_on: [28-01]
files_modified: [crates/tome/src/add.rs, crates/tome/src/git.rs, crates/tome/src/lib.rs]
autonomous: true
requirements: [CFG-03, CFG-05]
estimate: { tokens: 43000, raw_tokens: 43000, tasks: 2, confidence: low }
must_haves:
  truths: [A Git source is cloned and layout-inspected before it can enter a configuration draft, and failures preserve the user draft for retry.]
  artifacts: [crates/tome/src/add.rs, crates/tome-desktop/ui/src/components/GitRepositoryForm.tsx]
  key_links: [Git verification uses clone_repo and TauriEventSink progress; verified candidate is inserted only into the normal config draft path.]
---
<objective>
Create the verified Git candidate domain seam that the central IPC plan and later form consume.
Purpose: prevent unverified repositories and guessed skill subdirectories from reaching tome.toml.
Output: clone-and-inspect domain helper with a draft-ready candidate, consumed by Plan 28-02 commands and Plan 28-05 UI.
</objective>
<execution_context>@/Users/martin/.config/opencode/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/28-configuration-ui-beta-cut/28-CONTEXT.md
@.planning/phases/28-configuration-ui-beta-cut/28-RESEARCH.md
@.planning/todos/pending/2026-06-26-tome-add-auto-detect-subdir.md
@crates/tome/src/add.rs
@crates/tome/src/git.rs
@.planning/phases/27-sync-triage-ui/27-05-SUMMARY.md</context>
<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Verify and inspect one Git candidate before draft insertion</name>
<files>crates/tome/src/add.rs, crates/tome/src/git.rs, crates/tome/src/lib.rs</files>
<behavior>- URL/name/ref input builds a candidate with default-branch behavior when no pin is chosen; branch, tag, and revision are mutually exclusive.
- Candidate clone uses existing Git environment clearing, cancellation, cache location, and ProgressSink.
- SKILL.md scanning returns no root, one recommended root, or explicit multiple candidates; an explicit subdir wins and every automatic selection requires review upstream.
- Clone/layout errors return an error without a config mutation.
</behavior>
<action>Factor reusable URL/name/ref candidate assembly out of add.rs without changing the CLI add command's config-only contract. Add a domain verification operation that clones into TomePaths::repos_dir through git::clone_repo, then performs a bounded deterministic SKILL.md-root scan. For D-12 and the folded todo, calculate a common ancestor only for related roots; return all unrelated candidates rather than silently choosing. Preserve D-13's default branch/no pin and exclusive branch/tag/rev semantics. The operation must return a draft-ready DirectoryConfig only after clone and reviewable layout data succeed; do not save it. Test success, explicit-subdir precedence, one-root recommendation, ambiguous roots, no roots, hostile Git env isolation, and failure/no-draft-inclusion before code. Commit `test(28-04): cover git candidate verification` then `feat(28-04): verify git sources before config draft`.</action>
<verify><automated>cargo test -p tome --lib add::tests && cargo test -p tome --lib git::tests</automated></verify>
<done>Git verification reuses canonical safeguards, reports deterministic review data, and cannot persist an unverified candidate.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Pin deterministic Git-layout recommendations and failure retention contracts</name>
<files>crates/tome/src/add.rs, crates/tome/src/git.rs</files>
<behavior>- Explicit subdir remains the reviewed selection even when scanning discovers a different root.
- Related skill roots yield one common-ancestor recommendation; unrelated roots remain separately selectable; no root returns a structured layout result.
- Clone failures leave no candidate result that any caller could insert into a draft.
</behavior>
<action>Add the edge-case unit fixtures and regression tests that make the Task 1 scanner and candidate result deterministic: nested SKILL.md roots, unrelated roots, repository-root layouts, explicit subdir precedence, and clone error propagation. Keep test fixtures local and use NullSink/cancellation controls; no configuration write or desktop command belongs in this plan. Commit `test(28-04): pin git layout recommendation edge cases`.</action>
<verify><automated>cargo test -p tome --lib add::tests && cargo test -p tome --lib git::tests</automated></verify>
<done>Layout recommendation behavior is deterministic and a failed clone cannot produce a draft-insertable candidate.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Git URL/ref→clone subprocess | User-controlled remote input reaches a local process/network operation. |
| clone→draft | Repository contents inform but do not directly persist configuration. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-28-04-01 | Tampering | Git subprocess | high | mitigate | Reuse clone_repo env clearing, cache hashing, ceiling protections, and typed Git errors. |
| T-28-04-02 | Tampering | layout scanner | medium | mitigate | Bounded deterministic SKILL.md scan returns reviewable candidates only. |
| T-28-04-03 | Repudiation | candidate insertion | medium | mitigate | Keep verified result separate from persistent draft apply and require later diff confirmation. |
| T-28-04-SC | Tampering | package installs | high | mitigate | No package installation in this plan. |
</threat_model>
<verification><automated>cargo test -p tome --lib add:: && cargo clippy -p tome --all-targets -- -D warnings</automated></verification>
<success_criteria>A typed verified candidate exists before the centralized IPC boundary is built; later UI can only insert it through the shared draft seam.</success_criteria>
<output>Create `.planning/phases/28-configuration-ui-beta-cut/28-04-SUMMARY.md` when done</output>
