---
phase: 28-configuration-ui-beta-cut
plan: 03
type: execute
wave: 5
depends_on: [28-02]
files_modified: [crates/tome-desktop/ui/src/views/ConfigurationView.tsx, crates/tome-desktop/ui/src/components/SetupFlow.tsx, crates/tome-desktop/ui/src/components/DirectoryEditor.tsx, crates/tome-desktop/ui/src/components/ConfigTomlDiff.tsx, crates/tome-desktop/ui/src/components/__tests__/SetupFlow.test.tsx, crates/tome-desktop/ui/src/components/__tests__/DirectoryEditor.test.tsx]
autonomous: true
requirements: [CFG-01, CFG-02, CFG-05, NF-04]
estimate: { tokens: 42000, raw_tokens: 42000, tasks: 2, confidence: low }
must_haves:
  truths: [No valid config opens a dedicated setup screen; users can edit/reorder a directory draft and explicitly apply a full validated tome.toml diff.]
  artifacts: [crates/tome-desktop/ui/src/views/ConfigurationView.tsx, crates/tome-desktop/ui/src/components/DirectoryEditor.tsx]
  key_links: [PreviewPopover renders Rust TOML preview and Apply invokes typed save only after confirmation.]
---
<objective>
Deliver the setup gate and list/detail directory editor on the proven configuration route.
Purpose: users can establish and change config safely without a hand-edited TOML file.
Output: setup screen, draft editor, order interactions, full diff confirmation, UI tests.
</objective>
<execution_context>@/Users/martin/.config/opencode/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/28-configuration-ui-beta-cut/28-CONTEXT.md
@.planning/phases/27-sync-triage-ui/27-03-SUMMARY.md
@crates/tome-desktop/ui/src/components/PreviewPopover.tsx
@crates/tome-desktop/ui/src/components/MachineTomlDiff.tsx</context>
<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Gate the application with the dedicated CLI-equivalent setup flow</name>
<files>crates/tome-desktop/ui/src/views/ConfigurationView.tsx, crates/tome-desktop/ui/src/components/SetupFlow.tsx, crates/tome-desktop/ui/src/components/__tests__/SetupFlow.test.tsx</files>
<behavior>- Greenfield blocks normal desktop sections until save or explicit cancel.
- Brownfield offers use, edit, reinitialize-with-backup, or cancel; legacy cleanup remains an explicit choice.
- Setup displays resolved folder and source, permits a folder change only for implicit default, offers XDG persistence for custom choice, and reviews preselected discovered/custom directories.
</behavior>
<action>Build the dedicated D-02 screen from the root gate hook and typed setup commands, rendering—not recreating—D-03/D-04/D-05 policy. Ensure use-existing/cancel leave the file untouched, reinitialize acknowledges its backup, and legacy cleanup cannot be implicit. Include inline custom-directory entry before the setup review. The root App gate in Plan 28-02, not this component, prevents other shell routes from mounting. Write tests before implementation and commit `test(28-03): cover setup gating choices` then `feat(28-03): add desktop setup flow`.</action>
<verify><automated>cd crates/tome-desktop/ui && npm test -- --run src/components/__tests__/SetupFlow.test.tsx</automated></verify>
<done>A missing or invalid configuration cannot reveal normal sections until the user saves or explicitly cancels setup, with CLI-equivalent choices represented.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Build list/detail directory editing with durable order and preview confirmation</name>
<files>crates/tome-desktop/ui/src/components/DirectoryEditor.tsx, crates/tome-desktop/ui/src/components/ConfigTomlDiff.tsx, crates/tome-desktop/ui/src/components/__tests__/DirectoryEditor.test.tsx</files>
<behavior>- Add is primary; list selection opens editable name/path/type/role/Git fields in an adjacent detail pane.
- Drag/drop plus keyboard-accessible Move up/down mutate the complete explicit ordered DirectoryName schema field; saved sequence round-trips through Rust's validated order representation.
- Invalid fields and affected-directory errors remain visible; preview/apply are disabled until Rust validation succeeds.
- Remove removes only the draft entry and final removal is represented in the full diff before Apply.
</behavior>
<action>Use React Aria GridList/useDragAndDrop (not ListBoxItem rows with nested buttons) for D-06/D-07 accessibility. Send the complete serializable draft to Rust after mutations; retain client inputs on validation failures and map returned field/affected-directory errors without JavaScript business validation per D-08 and CFG-05. Reuse PreviewPopover for D-10/NF-04 and render the server-generated full tome.toml line diff using a genericized diff renderer or a parallel config renderer; do not alter the triage-specific MachineTomlDiff contract. Apply only from the popover, call the typed command, and refresh live state after success. D-09 removal is configuration-only—do not invoke remove/cleanup APIs. Commit `test(28-03): cover directory draft editing` then `feat(28-03): add validated directory editor`.</action>
<verify><automated>cd crates/tome-desktop/ui && npm test -- --run src/components/__tests__/DirectoryEditor.test.tsx && npm run build</automated></verify>
<done>Users can add/edit/draft-remove/reorder directories accessibly, see Rust errors without losing edits, and save only through an explicit full TOML preview.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| user interaction→draft | Mutable UI values must not become config semantics in JavaScript. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-28-03-01 | Tampering | editor fields | high | mitigate | Rust validates every full draft; UI retains/errors only. |
| T-28-03-02 | Repudiation | removal/save | medium | mitigate | Full server diff and PreviewPopover Apply are mandatory. |
| T-28-03-03 | Denial | inaccessible reorder | medium | mitigate | Pair drag/drop with Move up/down keyboard controls. |
| T-28-03-SC | Tampering | package installs | high | mitigate | Reuse installed React Aria; no package installation. |
</threat_model>
<verification><automated>cd crates/tome-desktop/ui && npm test -- --run && npm run build</automated></verification>
<success_criteria>Setup and directory configuration are safe, accessible, draft-preserving, and confirm every persistent TOML change.</success_criteria>
<output>Create `.planning/phases/28-configuration-ui-beta-cut/28-03-SUMMARY.md` when done</output>
