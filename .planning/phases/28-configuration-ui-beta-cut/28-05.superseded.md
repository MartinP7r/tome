---
phase: 28-configuration-ui-beta-cut
plan: 05
type: execute
wave: 2
depends_on: [28-01]
files_modified: [crates/tome/src/machine.rs, crates/tome/src/lib.rs]
autonomous: true
requirements: [CFG-04, NF-04]
estimate: { tokens: 41000, raw_tokens: 41000, tasks: 2, confidence: low }
must_haves:
  truths: [All global and per-directory machine preferences can be edited, previewed as machine.toml, and explicitly applied.] 
  artifacts: [crates/tome/src/machine.rs, crates/tome-desktop/ui/src/components/PreferencesEditor.tsx]
  key_links: [Preferences preview/apply share a complete Rust projection and use PreviewPopover plus the existing diff renderer.]
---
<objective>
Provide the complete Rust machine-preference domain before central IPC wiring.
Purpose: replace hand-edited machine preferences while preserving existing rule semantics.
Output: preference domain projection and shared preview/apply helpers for central IPC wiring.
</objective>
<execution_context>@/Users/martin/.config/opencode/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/28-configuration-ui-beta-cut/28-CONTEXT.md
@.planning/phases/27-sync-triage-ui/27-03-SUMMARY.md
@crates/tome/src/machine.rs
@crates/tome-desktop/ui/src/components/MachineTomlDiff.tsx
@crates/tome-desktop/ui/src/components/PreviewPopover.tsx</context>
<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Project a complete validated machine-preference draft for the central IPC commands</name>
<files>crates/tome/src/machine.rs, crates/tome/src/lib.rs</files>
<behavior>- Read projection includes global disabled skills/directories and every configured directory's rule mode/entries, including unavailable saved names.
- Draft validation rejects simultaneous per-directory enabled and disabled sets and preserves unknown skill names rather than deleting them.
- A disabled directory retains its stored rules; the returned projection identifies them as inactive rather than removing them.
- Preview and apply share a projection, use machine::preview_save and save_machine_prefs, and apply requires the UI confirmation path.
</behavior>
<action>Introduce narrow serializable preference draft/projection types and accessors/mutators in machine.rs instead of exposing raw fields. Build the known-skill catalog from the library while retaining saved unavailable names with explanatory metadata per D-16. Encode D-15 global-plus-selectable-directory layout inputs and D-17 inactive retained rules in Rust projection results; enforce MachinePrefs::validate before preview/save. Plan 28-02 owns the typed get/validate/preview/apply commands, registry, and bindings; this task supplies their tested domain API and must not modify those shared files. Tests assert unknown preservation, mutual exclusion, disabled-directory retention, no preview write, and atomic apply. Do not route this comprehensive editor through TriageDecision. Commit `test(28-05): cover complete preferences projection` then `feat(28-05): add preference draft domain`.</action>
<verify><automated>cargo test -p tome --lib machine::tests && cargo clippy -p tome --all-targets -- -D warnings</automated></verify>
<done>Rust owns all preference semantics and exposes a complete tested domain API for the central typed commands.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Prove preference preview/apply helpers preserve all machine-local rules</name>
<files>crates/tome/src/machine.rs, crates/tome/src/lib.rs</files>
<behavior>- Shared preview/apply helpers produce the same proposed preferences from a complete draft.
- Atomic apply retains unknown selections and rules for disabled directories while rejecting invalid mixed rule modes.
</behavior>
<action>Finish the pure domain API consumed by Plan 28-02 commands: construct proposed MachinePrefs from a complete draft, validate it, delegate preview to machine::preview_save, and delegate confirmed persistence to save_machine_prefs. Keep all mutations/test seams inside tome and expose only narrow re-exports needed by the desktop command crate. Add red/green unit coverage for preview/apply equivalence, atomic output, retained unavailable names, and inactive retained rules; commit `test(28-05): cover preferences preview apply domain` then `feat(28-05): finalize preferences domain helpers`.</action>
<verify><automated>cargo test -p tome --lib machine::tests && cargo clippy -p tome --all-targets -- -D warnings</automated></verify>
<done>The central IPC plan can invoke one tested preference projection for read, validate, preview, and apply without exposing MachinePrefs internals.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| preferences form→machine projection | Untrusted values propose machine-local distribution rules. |
| projection→machine.toml | Local preference file is atomically replaced after confirmation. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-28-05-01 | Tampering | rule mode | high | mitigate | Rust validates enabled/disabled exclusivity before preview and save. |
| T-28-05-02 | Information disclosure | known skill picker | low | accept | Picker exposes only local library skill names already visible in the app. |
| T-28-05-03 | Repudiation | preference write | medium | mitigate | Server diff plus PreviewPopover explicit Apply and atomic machine save. |
| T-28-05-SC | Tampering | package installs | high | mitigate | No package installation in this plan. |
</threat_model>
<verification><automated>cargo clippy --workspace --all-targets -- -D warnings && cd crates/tome-desktop/ui && npm test -- --run && npm run build</automated></verification>
<success_criteria>The Rust preference domain retains all existing information and supplies the safe preview/apply behavior for the desktop boundary.</success_criteria>
<output>Create `.planning/phases/28-configuration-ui-beta-cut/28-05-SUMMARY.md` when done</output>
