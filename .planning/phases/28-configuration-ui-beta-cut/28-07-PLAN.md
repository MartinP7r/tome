---
phase: 28-configuration-ui-beta-cut
plan: 07
type: execute
wave: 8
depends_on: [28-03, 28-04, 28-05, 28-06]
files_modified: [crates/tome-desktop/ui/src/__mocks__/tauri-api-core.ts, crates/tome-desktop/tests/a11y/axe.spec.ts, crates/tome-desktop/ui/src/views/__tests__/ConfigurationView.test.tsx, .planning/REQUIREMENTS.md]
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03, CFG-04, CFG-05, NF-04]
estimate: { tokens: 27000, raw_tokens: 27000, tasks: 2, confidence: low }
must_haves:
  truths: [The beta configuration journey is accessible, binding-fresh, explicit about all writes, and covers every CFG requirement.]
  artifacts: [crates/tome-desktop/tests/a11y/axe.spec.ts, crates/tome-desktop/ui/src/views/__tests__/ConfigurationView.test.tsx]
  key_links: [Mocks mirror generated command results; ConfigChanged and preference watcher refresh preserve unresolved drafts while PreviewPopover gates writes.]
---
<objective>
Verify the integrated beta configuration journey and record requirement completion.
Purpose: catch cross-flow regressions after setup, directory, Git, and preference surfaces converge.
Output: integration/a11y coverage, bindings freshness proof, requirements traceability.
</objective>
<execution_context>@/Users/martin/.config/opencode/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/28-configuration-ui-beta-cut/28-CONTEXT.md
@.planning/phases/28-configuration-ui-beta-cut/28-RESEARCH.md
@.planning/phases/28-configuration-ui-beta-cut/28-02-SUMMARY.md
@crates/tome-desktop/tests/a11y/axe.spec.ts
@crates/tome-desktop/ui/src/__mocks__/tauri-api-core.ts</context>
<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Test the beta configuration journey and config-refresh safety states</name>
<files>crates/tome-desktop/ui/src/views/__tests__/ConfigurationView.test.tsx, crates/tome-desktop/ui/src/__mocks__/tauri-api-core.ts</files>
<behavior>- Greenfield setup through review/save unlocks the shell; direct navigation remains blocked before that result.
- Directory edits, reviewed Git insertion, and preferences call apply only after their PreviewPopover Apply action.
- ConfigChanged/MachinePrefsChanged refetch persisted state after atomic own/external edits without replacing dirty unresolved drafts.
</behavior>
<action>Add integration-oriented React tests and faithful generated-shape mocks for all command/result branches. Exercise production hooks/components rather than reproducing Rust validation. Assert ordering of preview then explicit Apply, retained draft state on validation/clone failure, direct-navigation block, and watcher-driven persisted-state refresh. Keep D-01 scope at the Phase-28 beta cut; do not add Phase 29 cleanup or Phases 29-31 desktop work. Commit `test(28-07): cover configuration beta journey`.</action>
<verify><automated>cd crates/tome-desktop/ui && npm test -- --run src/views/__tests__/ConfigurationView.test.tsx</automated></verify>
<done>Integrated UI tests prove setup gating, draft retention, confirmation order, Git verification, preferences, and config watcher freshness.</done>
</task>
<task type="auto">
<name>Task 2: Run configuration accessibility and beta-cut quality gates</name>
<files>crates/tome-desktop/tests/a11y/axe.spec.ts, .planning/REQUIREMENTS.md</files>
<action>Extend Playwright axe coverage for the locked setup screen, directory reorder controls, Git error/retry, searchable preferences, inactive retained rules, and both TOML preview dialogs. Verify keyboard labels for Move up/down and Apply controls. Regenerate bindings and run full Rust/UI gates. When all behavior is evidenced, mark CFG-01 through CFG-05 and Phase-28 applicable NF-04 traceability complete using a scoped edit; do not change future-phase rows. Commit `test(28-07): add configuration accessibility coverage` and `docs(28): record configuration beta requirements`.</action>
<verify><automated>cargo test --workspace && cargo clippy --workspace --all-targets -- -D warnings && cargo run -p tome-desktop --bin gen-bindings && git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts && cd crates/tome-desktop/ui && npm test -- --run && npm run build && npm run test:a11y</automated></verify>
<done>Configuration flows pass automated a11y, Rust, binding, UI test, and build gates with completed requirement traceability.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| mock/test harness→UI | Test doubles must preserve command shape, watcher behavior, and confirmation ordering. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-28-07-01 | Tampering | stale bindings/mocks | medium | mitigate | Regenerate bindings and update mocks alongside command/event shapes. |
| T-28-07-02 | Denial | inaccessible mutation UI | medium | mitigate | Axe scans cover setup, editor, Git, picker, and preview dialogs. |
| T-28-07-03 | Repudiation | silent write regression | high | mitigate | Integration tests assert preview then explicit Apply ordering. |
| T-28-07-SC | Tampering | package installs | high | mitigate | No package installation. |
</threat_model>
<verification><automated>make ci && cargo run -p tome-desktop --bin gen-bindings && git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts && cd crates/tome-desktop/ui && npm run build && npm run test:a11y</automated></verification>
<success_criteria>All Phase 28 configuration requirements have integrated automated evidence and beta scope ends without Phase 29-31 work.</success_criteria>
<output>Create `.planning/phases/28-configuration-ui-beta-cut/28-07-SUMMARY.md` when done</output>
