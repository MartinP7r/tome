---
phase: 28-configuration-ui-beta-cut
plan: 05
type: execute
wave: 3
depends_on: [28-02]
files_modified: [crates/tome/src/machine.rs, crates/tome/src/lib.rs, crates/tome-desktop/src/commands.rs, crates/tome-desktop/src/lib.rs, crates/tome-desktop/ui/src/bindings.ts, crates/tome-desktop/ui/src/components/PreferencesEditor.tsx, crates/tome-desktop/ui/src/hooks/usePreferences.tsx, crates/tome-desktop/ui/src/components/__tests__/PreferencesEditor.test.tsx]
autonomous: true
requirements: [CFG-04, NF-04]
estimate: { tokens: 41000, raw_tokens: 41000, tasks: 2, confidence: low }
must_haves:
  truths: [All global and per-directory machine preferences can be edited, previewed as machine.toml, and explicitly applied.] 
  artifacts: [crates/tome/src/machine.rs, crates/tome-desktop/ui/src/components/PreferencesEditor.tsx]
  key_links: [Preferences preview/apply share a complete Rust projection and use PreviewPopover plus the existing diff renderer.]
---
<objective>
Provide the complete machine-preference editor and confirmed machine.toml apply flow.
Purpose: replace hand-edited machine preferences while preserving existing rule semantics.
Output: preference draft projection/mutators, commands, searchable picker UI, server-side diff confirmation.
</objective>
<execution_context>@/Users/martin/.config/opencode/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/28-configuration-ui-beta-cut/28-CONTEXT.md
@.planning/phases/27-sync-triage-ui/27-03-SUMMARY.md
@crates/tome/src/machine.rs
@crates/tome-desktop/ui/src/components/MachineTomlDiff.tsx
@crates/tome-desktop/ui/src/components/PreviewPopover.tsx</context>
<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Project and persist a complete validated machine-preference draft</name>
<files>crates/tome/src/machine.rs, crates/tome/src/lib.rs, crates/tome-desktop/src/commands.rs, crates/tome-desktop/src/lib.rs, crates/tome-desktop/ui/src/bindings.ts</files>
<behavior>- Read projection includes global disabled skills/directories and every configured directory's rule mode/entries, including unavailable saved names.
- Draft validation rejects simultaneous per-directory enabled and disabled sets and preserves unknown skill names rather than deleting them.
- A disabled directory retains its stored rules; the returned projection identifies them as inactive rather than removing them.
- Preview and apply share a projection, use machine::preview_save and save_machine_prefs, and apply requires the UI confirmation path.
</behavior>
<action>Introduce narrow serializable preference draft/projection types and accessors/mutators in machine.rs instead of exposing raw fields. Build the known-skill catalog from the library while retaining saved unavailable names with explanatory metadata per D-16. Encode D-15 global-plus-selectable-directory layout inputs and D-17 inactive retained rules in Rust projection results; enforce MachinePrefs::validate before preview/save. Add typed get/validate/preview/apply commands, server-resolved paths, make_builder registration, bindings regeneration, and tests that assert unknown preservation, mutual exclusion, disabled-directory retention, no preview write, and atomic apply. Do not route this comprehensive editor through TriageDecision. Commit `test(28-05): cover complete preferences projection` then `feat(28-05): add preference draft IPC`.</action>
<verify><automated>cargo test -p tome --lib machine::tests && cargo test -p tome-desktop --lib commands:: && cargo run -p tome-desktop --bin gen-bindings && git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts</automated></verify>
<done>Rust owns all preference semantics, preserves unavailable/rule data, and exposes a complete typed preview/apply path.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Build global and per-directory preference editing with full diff Apply</name>
<files>crates/tome-desktop/ui/src/components/PreferencesEditor.tsx, crates/tome-desktop/ui/src/hooks/usePreferences.tsx, crates/tome-desktop/ui/src/components/__tests__/PreferencesEditor.test.tsx</files>
<behavior>- Users select global disabled skills/directories and select a directory to edit adjacent enabled or disabled rules.
- Search filters known skill choices without hiding unavailable selected entries; unavailable entries explain their status.
- Disabled-directory rules display but editing controls are inactive until re-enabled.
- Explicit Apply opens PreviewPopover containing MachineTomlDiff and only then writes machine.toml.
</behavior>
<action>Compose global controls plus adjacent directory detail per D-15 using the Rust draft projection as the only rule authority. Provide a searchable accessible picker of known skills and pinned unavailable selections per D-16. Render inactive—not erased—per-directory rules when the directory is disabled per D-17. Reuse PreviewPopover and MachineTomlDiff unchanged for D-18/NF-04; preview/apply errors follow the existing TomeError inline disclosure style and watcher refresh path. Add tests before implementation and commit `feat(28-05): add machine preferences editor`.</action>
<verify><automated>cd crates/tome-desktop/ui && npm test -- --run src/components/__tests__/PreferencesEditor.test.tsx && npm run build</automated></verify>
<done>Every machine preference is editable through an accessible UI and persists only after a complete machine.toml diff confirmation.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| preferences form→machine projection | Untrusted values propose machine-local distribution rules. |
| projection→machine.toml | Local preference file is atomically replaced after confirmation. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-28-05-01 | Tampering | rule mode | high | mitigate | Rust validates enabled/disabled exclusivity before preview and save. |
| T-28-05-02 | Information disclosure | known skill picker | low | accept | Picker exposes only local library skill names already visible in the app. |
| T-28-05-03 | Repudiation | preference write | medium | mitigate | Server diff plus PreviewPopover explicit Apply and atomic machine save. |
| T-28-05-SC | Tampering | package installs | high | mitigate | No package installation in this plan. |
</threat_model>
<verification><automated>cargo clippy --workspace --all-targets -- -D warnings && cd crates/tome-desktop/ui && npm test -- --run && npm run build</automated></verification>
<success_criteria>Machine preferences retain all existing information and can be safely reviewed and applied from the desktop.</success_criteria>
<output>Create `.planning/phases/28-configuration-ui-beta-cut/28-05-SUMMARY.md` when done</output>
