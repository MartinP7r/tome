---
phase: 28-configuration-ui-beta-cut
plan: 06
type: execute
wave: 4
depends_on: [28-03, 28-04, 28-05]
files_modified: [crates/tome-desktop/ui/src/__mocks__/tauri-api-core.ts, crates/tome-desktop/tests/a11y/axe.spec.ts, crates/tome-desktop/ui/src/views/__tests__/ConfigurationView.test.tsx, .planning/REQUIREMENTS.md]
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03, CFG-04, CFG-05, NF-04]
estimate: { tokens: 27000, raw_tokens: 27000, tasks: 2, confidence: low }
must_haves:
  truths: [The beta configuration journey is accessible, binding-fresh, explicit about all writes, and covers every CFG requirement.] 
  artifacts: [crates/tome-desktop/tests/a11y/axe.spec.ts, crates/tome-desktop/ui/src/views/__tests__/ConfigurationView.test.tsx]
  key_links: [Mocks mirror generated command results; watcher-triggered refresh and PreviewPopover Apply flows remain covered.]
---
<objective>
Verify the integrated beta configuration journey and record requirement completion.
Purpose: catch cross-flow regressions after setup, editor, Git, and preferences converge.
Output: integration/a11y coverage, binding freshness proof, requirements traceability.
</objective>
<execution_context>@/Users/martin/.config/opencode/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/28-configuration-ui-beta-cut/28-CONTEXT.md
@.planning/phases/28-configuration-ui-beta-cut/28-RESEARCH.md
@.planning/phases/27-sync-triage-ui/27-05-SUMMARY.md
@crates/tome-desktop/tests/a11y/axe.spec.ts
@crates/tome-desktop/ui/src/__mocks__/tauri-api-core.ts</context>
<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Test the end-to-end configuration happy path and safety failure states</name>
<files>crates/tome-desktop/ui/src/views/__tests__/ConfigurationView.test.tsx, crates/tome-desktop/ui/src/__mocks__/tauri-api-core.ts</files>
<behavior>- Greenfield setup through review/save unlocks Configuration and refetches live state.
- Directory add/reorder/remove, Git candidate review, and preference edits only call apply after their PreviewPopover Apply action.
- Rust validation and clone failure preserve draft inputs and block save; external watcher refresh replaces only successfully persisted live state, not an unresolved draft.
</behavior>
<action>Add integration-oriented React tests and faithful typed mocks for every new command/result branch. Exercise the production hook/component seams rather than reproducing Rust validation in tests. Explicitly assert D-01 stopping point by limiting completion scope to the Phase-28 beta configuration surfaces; do not add Phase 29 cleanup or Phases 29-31 desktop work. Include full TOML preview payloads and command-call ordering so no silent-write regression can hide behind a mock. Commit `test(28-06): cover configuration beta journey`.</action>
<verify><automated>cd crates/tome-desktop/ui && npm test -- --run src/views/__tests__/ConfigurationView.test.tsx</automated></verify>
<done>The integrated UI proves setup, draft retention, preview-before-apply, Git verification, preferences, and watcher freshness behavior.</done>
</task>
<task type="auto">
<name>Task 2: Run configuration accessibility and beta-cut quality gates</name>
<files>crates/tome-desktop/tests/a11y/axe.spec.ts, .planning/REQUIREMENTS.md</files>
<action>Extend the existing Playwright axe suite with configuration setup, directory reorder controls, Git error/retry, and both TOML preview dialogs. Verify keyboard labels for drag/drop alternatives, Move up/down, searchable picker, disabled rules, and explicit Apply actions. Regenerate bindings and run full Rust/UI gates. When all acceptance behavior is evidenced, mark CFG-01 through CFG-05 and Phase-28 applicable NF-04 traceability complete in REQUIREMENTS.md using a scoped edit; do not change unrelated future-phase rows. Commit `test(28-06): add configuration accessibility coverage` and `docs(28): record configuration beta requirements`.</action>
<verify><automated>cargo test --workspace && cargo clippy --workspace --all-targets -- -D warnings && cargo run -p tome-desktop --bin gen-bindings && git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts && cd crates/tome-desktop/ui && npm test -- --run && npm run build && npm run test:a11y</automated></verify>
<done>Configuration flows pass automated a11y, Rust, bindings, UI test, and build gates, with completed requirement traceability recorded.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| mock/test harness→UI | Test doubles must preserve command-shape and confirmation behavior. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-28-06-01 | Tampering | stale bindings/mocks | medium | mitigate | Regenerate bindings and update mock branches alongside each command shape. |
| T-28-06-02 | Denial | inaccessible mutation UI | medium | mitigate | Axe scans cover setup, editor, Git, picker, and preview dialogs. |
| T-28-06-03 | Repudiation | silent write regression | high | mitigate | Integration tests assert preview then explicit Apply command ordering. |
| T-28-06-SC | Tampering | package installs | high | mitigate | No package installation in this plan. |
</threat_model>
<verification><automated>make ci && cargo run -p tome-desktop --bin gen-bindings && git diff --exit-code -- crates/tome-desktop/ui/src/bindings.ts && cd crates/tome-desktop/ui && npm run build && npm run test:a11y</automated></verification>
<success_criteria>All Phase 28 configuration requirements have integrated automated evidence and beta scope ends without Phase 29-31 work.</success_criteria>
<output>Create `.planning/phases/28-configuration-ui-beta-cut/28-06-SUMMARY.md` when done</output>
