---
phase: 28-configuration-ui-beta-cut
plan: 06
type: execute
wave: 5
depends_on: [28-02, 28-03, 28-04, 28-05]
files_modified: [crates/tome-desktop/ui/src/components/GitRepositoryForm.tsx, crates/tome-desktop/ui/src/components/PreferencesEditor.tsx, crates/tome-desktop/ui/src/hooks/usePreferences.tsx, crates/tome-desktop/ui/src/components/__tests__/GitRepositoryForm.test.tsx, crates/tome-desktop/ui/src/components/__tests__/PreferencesEditor.test.tsx]
autonomous: true
requirements: [CFG-03, CFG-04, NF-04]
estimate: { tokens: 33000, raw_tokens: 33000, tasks: 2, confidence: low }
must_haves:
  truths: [Verified Git candidates enter the established directory draft only after review; every machine preference is editable and explicitly previewed before persistence.]
  artifacts: [crates/tome-desktop/ui/src/components/GitRepositoryForm.tsx, crates/tome-desktop/ui/src/components/PreferencesEditor.tsx]
  key_links: [Git form uses Plan 28-02 command and Plan 28-03 insertion callback; preferences use central typed commands, PreviewPopover, and MachineTomlDiff.]
---
<objective>
Compose the post-IPC Git and preferences UI surfaces through declared, already-landed seams.
Purpose: eliminate concurrent draft-hook and IPC ownership ambiguity.
Output: reviewed Git form and complete preferences editor.
</objective>
<execution_context>@/Users/martin/.config/opencode/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/28-configuration-ui-beta-cut/28-CONTEXT.md
@.planning/phases/28-configuration-ui-beta-cut/28-02-SUMMARY.md
@.planning/phases/28-configuration-ui-beta-cut/28-03-SUMMARY.md
@.planning/phases/27-sync-triage-ui/27-03-SUMMARY.md</context>
<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Insert a reviewed verified Git candidate through the established draft seam</name>
<files>crates/tome-desktop/ui/src/components/GitRepositoryForm.tsx, crates/tome-desktop/ui/src/components/__tests__/GitRepositoryForm.test.tsx</files>
<behavior>- Form uses the Plan 28-02 verification command and existing SyncProgress event channel, with Advanced exclusive branch/tag/revision controls.
- Reviewed recommendation remains editable; failure preserves every input and exposes retry; only explicit review success invokes the DirectoryEditor insertion callback.
</behavior>
<action>Build GitRepositoryForm as a child of the completed DirectoryEditor. Receive a typed insertVerifiedCandidate callback declared by Plan 28-03 rather than importing or mutating hook state. Use the central command/bindings produced by Plan 28-02, subscribe through the existing operation progress pattern, and implement D-11 through D-14 exactly. Tests prove no callback on clone/layout failure and one callback only after user accepts a reviewed candidate. Commit `test(28-06): cover reviewed git candidate insertion` then `feat(28-06): add reviewed git repository form`.</action>
<verify><automated>cd crates/tome-desktop/ui && npm test -- --run src/components/__tests__/GitRepositoryForm.test.tsx</automated></verify>
<done>Git UI has a real completed draft insertion seam and never bypasses verification or draft confirmation.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Build the global and per-directory preferences editor</name>
<files>crates/tome-desktop/ui/src/components/PreferencesEditor.tsx, crates/tome-desktop/ui/src/hooks/usePreferences.tsx, crates/tome-desktop/ui/src/components/__tests__/PreferencesEditor.test.tsx</files>
<behavior>- Users edit global disabled skills/directories and adjacent selected-directory enabled or disabled rules.
- Search retains selected unavailable names with explanation; disabled-directory rules remain visible and inactive.
- Apply opens PreviewPopover with MachineTomlDiff and calls central apply only after confirmation.
</behavior>
<action>Compose D-15 through D-18 from central typed commands and the Plan 28-05 domain projection. Keep no business validation in JavaScript. Use searchable accessible picker behavior from the returned catalog, keep unavailable selected entries visible, and make retained rules inactive under a disabled directory. Reuse PreviewPopover and MachineTomlDiff unchanged; preserve dirty preference state across ConfigChanged/MachinePrefsChanged until success/discard, then refetch persisted state. Commit `test(28-06): cover preferences editor` then `feat(28-06): add machine preferences editor`.</action>
<verify><automated>cd crates/tome-desktop/ui && npm test -- --run src/components/__tests__/PreferencesEditor.test.tsx && npm run build</automated></verify>
<done>All preference edits are accessible, draft-preserving, and explicitly confirmed through a complete machine.toml diff.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| UI→verified/draft results | UI must not promote unverified Git or invalid preference fields. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-28-06-01 | Tampering | Git insertion | high | mitigate | Only accepted typed verification result invokes declared draft callback. |
| T-28-06-02 | Tampering | preference editor | high | mitigate | Central Rust validation/commands own semantics. |
| T-28-06-03 | Repudiation | machine write | medium | mitigate | PreviewPopover and MachineTomlDiff precede Apply. |
| T-28-06-SC | Tampering | package installs | high | mitigate | No package installation. |
</threat_model>
<verification><automated>cd crates/tome-desktop/ui && npm test -- --run && npm run build</automated></verification>
<success_criteria>Git and preference UI integrate only against completed hooks/commands and keep explicit confirmation intact.</success_criteria>
<output>Create `.planning/phases/28-configuration-ui-beta-cut/28-06-SUMMARY.md` when done</output>
