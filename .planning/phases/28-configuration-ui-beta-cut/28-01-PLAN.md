---
phase: 28-configuration-ui-beta-cut
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [crates/tome/src/config/types.rs, crates/tome/src/config/mod.rs, crates/tome/src/wizard.rs, crates/tome/src/lib.rs]
autonomous: true
requirements: [CFG-01, CFG-02, CFG-05, NF-04]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Rust can project setup state and validate, preview, and save a portable configuration draft without dialoguer or JavaScript TOML logic.
    - Directory order selected in the editor is serialized and restored rather than being lost to Config.directories BTreeMap key ordering.
  artifacts: [crates/tome/src/config/types.rs, crates/tome/src/wizard.rs]
  key_links:
    - Config draft save delegates to Config::save_checked.
    - Setup projections reuse wizard machine-state and known-directory behavior.
---

<objective>
Create the Rust-owned configuration/setup domain used by the beta UI, including an explicit persisted directory order that complements the existing BTreeMap.

Purpose: establish the only validation, TOML, setup-state, and write authority before desktop wiring.
Output: serializable configuration/setup projections and tested draft preview/save helpers.
</objective>

<execution_context>
@/Users/martin/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/martin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-configuration-ui-beta-cut/28-CONTEXT.md
@.planning/phases/28-configuration-ui-beta-cut/28-RESEARCH.md
@.planning/phases/25-rust-core-extraction-tauri-integration-spike/25-04-SUMMARY.md
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Make a persisted configuration draft validate, diff, and save end-to-end</name>
  <files>crates/tome/src/config/types.rs, crates/tome/src/config/mod.rs, crates/tome/src/lib.rs</files>
  <behavior>
    - A draft persists an explicit ordered DirectoryName sequence across TOML serialization and reload while Config.directories remains a BTreeMap for lookup; legacy files receive a stable key-sorted migration default.
    - Invalid names, type/role combinations, overlap errors, and order references to missing/duplicate directories return structured Rust validation results without writing tome.toml.
    - Preview and apply derive bytes through the same projection; apply calls Config::save_checked and cannot write an invalid draft.
  </behavior>
  <action>Introduce a backward-compatible explicit Config schema field containing an ordered Vec of DirectoryName values adjacent to Config.directories, with serde default. On load of an existing file lacking the field, construct the stable migration default by taking the configured map keys in their BTreeMap key-sorted order; on save, emit the complete normalized order. Validate that the order has exactly every configured DirectoryName once: no duplicates, no unknown names, and no omitted configured names. This explicitly resolves the BTreeMap persistence constraint in D-07: React drag/keyboard changes update the schema field and saved TOML restores it; do not rely on BTreeMap iteration as user order. Add binding-gated serializable draft, validation-result, and tome.toml line-diff projection types plus domain helpers that parse validated DirectoryName/GitRef inputs, preserve invalid client fields in the returned error map, serialize only in Rust, and share one proposed-Config builder between preview and apply. Apply must revalidate and delegate to Config::save_checked per D-08, D-10, CFG-05, and NF-04; it must not delete library/distribution data per D-09. Add focused unit tests first, then implementation. Commit RED and GREEN atomically as `test(28-01): cover configuration draft projection` and `feat(28-01): add validated configuration draft domain`.</action>
  <verify><automated>cargo test -p tome --lib config:: && cargo test -p tome --lib -- wizard::tests</automated></verify>
  <done>A serializable draft has stable saved order, preview never writes, invalid drafts cannot reach save_checked, and valid drafts save through Config::save_checked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract non-dialoguer setup state and action helpers</name>
  <files>crates/tome/src/wizard.rs, crates/tome/src/lib.rs</files>
  <behavior>
    - Snapshot distinguishes greenfield, valid/malformed brownfield, legacy, and brownfield-with-legacy states.
    - Snapshot exposes resolved tome-home path and source; only Default permits a changed home, while custom Default selection may request XDG persistence.
    - Brownfield use/edit/reinitialize/cancel and explicit legacy leave/move/delete actions match CLI behavior; discovered candidates retain type, role, and path.
  </behavior>
  <action>Extract command-safe public/bindings-gated projections and noninteractive helpers from wizard.rs without invoking dialoguer. Reuse detect_machine_state, brownfield backup semantics, known-directory scanning, assemble_config, resolve_tome_home_with_source, and write_xdg_tome_home rather than duplicate their rules. Model D-02 setup gating inputs, D-03 brownfield/legacy actions, D-04 source attribution and editability/persistence policy, and D-05 preselected discovered/custom-directory review inputs. Keep legacy cleanup explicit and preserve invalid brownfield diagnostics. Add unit coverage for every state/action, source restriction, and custom-folder XDG write intent; commit as `feat(28-01): expose setup domain projections`.</action>
  <verify><automated>cargo test -p tome --lib wizard::tests && cargo clippy -p tome --all-targets -- -D warnings</automated></verify>
  <done>Desktop callers can obtain and act on CLI-equivalent setup state without dialoguer, and all configuration mutations remain behind the draft save helper.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| desktop draft→Rust domain | Untrusted serialized fields request config mutation. |
| Rust domain→tome.toml | Portable configuration is persisted locally. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-28-01-01 | Tampering | draft parser | high | mitigate | Parse validated newtypes and run Config::validate before preview and save. |
| T-28-01-02 | Repudiation | config save | medium | mitigate | Produce server-side full diff and require later explicit UI Apply; save only via save_checked. |
| T-28-01-03 | Tampering | directory ordering | medium | mitigate | Persist and validate explicit order separately from BTreeMap storage. |
| T-28-01-SC | Tampering | package installs | high | mitigate | No package installation in this plan. |
</threat_model>

<verification><automated>cargo test -p tome --lib && cargo clippy -p tome --all-targets -- -D warnings</automated></verification>
<success_criteria>Rust has one validated draft→preview→save path, one setup-state projection, and explicit durable directory ordering.</success_criteria>
<output>Create `.planning/phases/28-configuration-ui-beta-cut/28-01-SUMMARY.md` when done</output>
